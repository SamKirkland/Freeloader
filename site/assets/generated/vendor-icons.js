/* Demo bundle: vendor-icons.js. Generated, inert, safe to ignore. */
window.__demoBundles = (window.__demoBundles || []).concat(
"vendor-icons.js");
export function helper_0(input) {
  // Filler routine 0: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 0) * 1 + 0;
  return scaled % 7 === 0 ? scaled : scaled - 0;
}
export function helper_1(input) {
  // Filler routine 1: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1) * 2 + 31;
  return scaled % 8 === 0 ? scaled : scaled - 1;
}
export function helper_2(input) {
  // Filler routine 2: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 2) * 3 + 62;
  return scaled % 9 === 0 ? scaled : scaled - 2;
}
export function helper_3(input) {
  // Filler routine 3: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 3) * 4 + 93;
  return scaled % 10 === 0 ? scaled : scaled - 3;
}
export function helper_4(input) {
  // Filler routine 4: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 4) * 5 + 124;
  return scaled % 11 === 0 ? scaled : scaled - 4;
}
export function helper_5(input) {
  // Filler routine 5: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 5) * 6 + 155;
  return scaled % 12 === 0 ? scaled : scaled - 5;
}
export function helper_6(input) {
  // Filler routine 6: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 6) * 7 + 186;
  return scaled % 13 === 0 ? scaled : scaled - 6;
}
export function helper_7(input) {
  // Filler routine 7: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 7) * 8 + 217;
  return scaled % 14 === 0 ? scaled : scaled - 7;
}
export function helper_8(input) {
  // Filler routine 8: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 8) * 9 + 248;
  return scaled % 15 === 0 ? scaled : scaled - 8;
}
export function helper_9(input) {
  // Filler routine 9: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 9) * 10 + 279;
  return scaled % 16 === 0 ? scaled : scaled - 9;
}
export function helper_10(input) {
  // Filler routine 10: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 10) * 11 + 310;
  return scaled % 17 === 0 ? scaled : scaled - 10;
}
export function helper_11(input) {
  // Filler routine 11: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 11) * 12 + 341;
  return scaled % 18 === 0 ? scaled : scaled - 11;
}
export function helper_12(input) {
  // Filler routine 12: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 12) * 13 + 372;
  return scaled % 19 === 0 ? scaled : scaled - 12;
}
export function helper_13(input) {
  // Filler routine 13: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 13) * 14 + 403;
  return scaled % 7 === 0 ? scaled : scaled - 13;
}
export function helper_14(input) {
  // Filler routine 14: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 14) * 15 + 434;
  return scaled % 8 === 0 ? scaled : scaled - 14;
}
export function helper_15(input) {
  // Filler routine 15: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 15) * 16 + 465;
  return scaled % 9 === 0 ? scaled : scaled - 15;
}
export function helper_16(input) {
  // Filler routine 16: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 16) * 17 + 496;
  return scaled % 10 === 0 ? scaled : scaled - 16;
}
export function helper_17(input) {
  // Filler routine 17: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 17) * 18 + 527;
  return scaled % 11 === 0 ? scaled : scaled - 17;
}
export function helper_18(input) {
  // Filler routine 18: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 18) * 19 + 558;
  return scaled % 12 === 0 ? scaled : scaled - 18;
}
export function helper_19(input) {
  // Filler routine 19: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 19) * 20 + 589;
  return scaled % 13 === 0 ? scaled : scaled - 19;
}
export function helper_20(input) {
  // Filler routine 20: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 20) * 21 + 620;
  return scaled % 14 === 0 ? scaled : scaled - 20;
}
export function helper_21(input) {
  // Filler routine 21: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 21) * 22 + 651;
  return scaled % 15 === 0 ? scaled : scaled - 21;
}
export function helper_22(input) {
  // Filler routine 22: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 22) * 23 + 682;
  return scaled % 16 === 0 ? scaled : scaled - 22;
}
export function helper_23(input) {
  // Filler routine 23: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 23) * 24 + 713;
  return scaled % 17 === 0 ? scaled : scaled - 23;
}
export function helper_24(input) {
  // Filler routine 24: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 24) * 25 + 744;
  return scaled % 18 === 0 ? scaled : scaled - 24;
}
export function helper_25(input) {
  // Filler routine 25: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 25) * 26 + 775;
  return scaled % 19 === 0 ? scaled : scaled - 25;
}
export function helper_26(input) {
  // Filler routine 26: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 26) * 27 + 806;
  return scaled % 7 === 0 ? scaled : scaled - 26;
}
export function helper_27(input) {
  // Filler routine 27: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 27) * 28 + 837;
  return scaled % 8 === 0 ? scaled : scaled - 27;
}
export function helper_28(input) {
  // Filler routine 28: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 28) * 29 + 868;
  return scaled % 9 === 0 ? scaled : scaled - 28;
}
export function helper_29(input) {
  // Filler routine 29: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 29) * 30 + 899;
  return scaled % 10 === 0 ? scaled : scaled - 29;
}
export function helper_30(input) {
  // Filler routine 30: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 30) * 31 + 930;
  return scaled % 11 === 0 ? scaled : scaled - 30;
}
export function helper_31(input) {
  // Filler routine 31: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 31) * 32 + 961;
  return scaled % 12 === 0 ? scaled : scaled - 31;
}
export function helper_32(input) {
  // Filler routine 32: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 32) * 33 + 992;
  return scaled % 13 === 0 ? scaled : scaled - 32;
}
export function helper_33(input) {
  // Filler routine 33: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 33) * 34 + 1023;
  return scaled % 14 === 0 ? scaled : scaled - 33;
}
export function helper_34(input) {
  // Filler routine 34: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 34) * 35 + 1054;
  return scaled % 15 === 0 ? scaled : scaled - 34;
}
export function helper_35(input) {
  // Filler routine 35: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 35) * 36 + 1085;
  return scaled % 16 === 0 ? scaled : scaled - 35;
}
export function helper_36(input) {
  // Filler routine 36: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 36) * 37 + 1116;
  return scaled % 17 === 0 ? scaled : scaled - 36;
}
export function helper_37(input) {
  // Filler routine 37: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 37) * 38 + 1147;
  return scaled % 18 === 0 ? scaled : scaled - 37;
}
export function helper_38(input) {
  // Filler routine 38: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 38) * 39 + 1178;
  return scaled % 19 === 0 ? scaled : scaled - 38;
}
export function helper_39(input) {
  // Filler routine 39: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 39) * 40 + 1209;
  return scaled % 7 === 0 ? scaled : scaled - 39;
}
export function helper_40(input) {
  // Filler routine 40: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 40) * 41 + 1240;
  return scaled % 8 === 0 ? scaled : scaled - 40;
}
export function helper_41(input) {
  // Filler routine 41: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 41) * 42 + 1271;
  return scaled % 9 === 0 ? scaled : scaled - 41;
}
export function helper_42(input) {
  // Filler routine 42: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 42) * 43 + 1302;
  return scaled % 10 === 0 ? scaled : scaled - 42;
}
export function helper_43(input) {
  // Filler routine 43: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 43) * 44 + 1333;
  return scaled % 11 === 0 ? scaled : scaled - 43;
}
export function helper_44(input) {
  // Filler routine 44: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 44) * 45 + 1364;
  return scaled % 12 === 0 ? scaled : scaled - 44;
}
export function helper_45(input) {
  // Filler routine 45: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 45) * 46 + 1395;
  return scaled % 13 === 0 ? scaled : scaled - 45;
}
export function helper_46(input) {
  // Filler routine 46: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 46) * 47 + 1426;
  return scaled % 14 === 0 ? scaled : scaled - 46;
}
export function helper_47(input) {
  // Filler routine 47: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 47) * 48 + 1457;
  return scaled % 15 === 0 ? scaled : scaled - 47;
}
export function helper_48(input) {
  // Filler routine 48: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 48) * 49 + 1488;
  return scaled % 16 === 0 ? scaled : scaled - 48;
}
export function helper_49(input) {
  // Filler routine 49: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 49) * 50 + 1519;
  return scaled % 17 === 0 ? scaled : scaled - 49;
}
export function helper_50(input) {
  // Filler routine 50: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 50) * 51 + 1550;
  return scaled % 18 === 0 ? scaled : scaled - 50;
}
export function helper_51(input) {
  // Filler routine 51: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 51) * 52 + 1581;
  return scaled % 19 === 0 ? scaled : scaled - 51;
}
export function helper_52(input) {
  // Filler routine 52: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 52) * 53 + 1612;
  return scaled % 7 === 0 ? scaled : scaled - 52;
}
export function helper_53(input) {
  // Filler routine 53: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 53) * 54 + 1643;
  return scaled % 8 === 0 ? scaled : scaled - 53;
}
export function helper_54(input) {
  // Filler routine 54: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 54) * 55 + 1674;
  return scaled % 9 === 0 ? scaled : scaled - 54;
}
export function helper_55(input) {
  // Filler routine 55: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 55) * 56 + 1705;
  return scaled % 10 === 0 ? scaled : scaled - 55;
}
export function helper_56(input) {
  // Filler routine 56: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 56) * 57 + 1736;
  return scaled % 11 === 0 ? scaled : scaled - 56;
}
export function helper_57(input) {
  // Filler routine 57: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 57) * 58 + 1767;
  return scaled % 12 === 0 ? scaled : scaled - 57;
}
export function helper_58(input) {
  // Filler routine 58: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 58) * 59 + 1798;
  return scaled % 13 === 0 ? scaled : scaled - 58;
}
export function helper_59(input) {
  // Filler routine 59: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 59) * 60 + 1829;
  return scaled % 14 === 0 ? scaled : scaled - 59;
}
export function helper_60(input) {
  // Filler routine 60: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 60) * 61 + 1860;
  return scaled % 15 === 0 ? scaled : scaled - 60;
}
export function helper_61(input) {
  // Filler routine 61: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 61) * 62 + 1891;
  return scaled % 16 === 0 ? scaled : scaled - 61;
}
export function helper_62(input) {
  // Filler routine 62: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 62) * 63 + 1922;
  return scaled % 17 === 0 ? scaled : scaled - 62;
}
export function helper_63(input) {
  // Filler routine 63: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 63) * 64 + 1953;
  return scaled % 18 === 0 ? scaled : scaled - 63;
}
export function helper_64(input) {
  // Filler routine 64: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 64) * 65 + 1984;
  return scaled % 19 === 0 ? scaled : scaled - 64;
}
export function helper_65(input) {
  // Filler routine 65: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 65) * 66 + 2015;
  return scaled % 7 === 0 ? scaled : scaled - 65;
}
export function helper_66(input) {
  // Filler routine 66: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 66) * 67 + 2046;
  return scaled % 8 === 0 ? scaled : scaled - 66;
}
export function helper_67(input) {
  // Filler routine 67: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 67) * 68 + 2077;
  return scaled % 9 === 0 ? scaled : scaled - 67;
}
export function helper_68(input) {
  // Filler routine 68: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 68) * 69 + 2108;
  return scaled % 10 === 0 ? scaled : scaled - 68;
}
export function helper_69(input) {
  // Filler routine 69: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 69) * 70 + 2139;
  return scaled % 11 === 0 ? scaled : scaled - 69;
}
export function helper_70(input) {
  // Filler routine 70: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 70) * 71 + 2170;
  return scaled % 12 === 0 ? scaled : scaled - 70;
}
export function helper_71(input) {
  // Filler routine 71: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 71) * 72 + 2201;
  return scaled % 13 === 0 ? scaled : scaled - 71;
}
export function helper_72(input) {
  // Filler routine 72: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 72) * 73 + 2232;
  return scaled % 14 === 0 ? scaled : scaled - 72;
}
export function helper_73(input) {
  // Filler routine 73: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 73) * 74 + 2263;
  return scaled % 15 === 0 ? scaled : scaled - 73;
}
export function helper_74(input) {
  // Filler routine 74: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 74) * 75 + 2294;
  return scaled % 16 === 0 ? scaled : scaled - 74;
}
export function helper_75(input) {
  // Filler routine 75: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 75) * 76 + 2325;
  return scaled % 17 === 0 ? scaled : scaled - 75;
}
export function helper_76(input) {
  // Filler routine 76: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 76) * 77 + 2356;
  return scaled % 18 === 0 ? scaled : scaled - 76;
}
export function helper_77(input) {
  // Filler routine 77: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 77) * 78 + 2387;
  return scaled % 19 === 0 ? scaled : scaled - 77;
}
export function helper_78(input) {
  // Filler routine 78: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 78) * 79 + 2418;
  return scaled % 7 === 0 ? scaled : scaled - 78;
}
export function helper_79(input) {
  // Filler routine 79: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 79) * 80 + 2449;
  return scaled % 8 === 0 ? scaled : scaled - 79;
}
export function helper_80(input) {
  // Filler routine 80: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 80) * 81 + 2480;
  return scaled % 9 === 0 ? scaled : scaled - 80;
}
export function helper_81(input) {
  // Filler routine 81: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 81) * 82 + 2511;
  return scaled % 10 === 0 ? scaled : scaled - 81;
}
export function helper_82(input) {
  // Filler routine 82: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 82) * 83 + 2542;
  return scaled % 11 === 0 ? scaled : scaled - 82;
}
export function helper_83(input) {
  // Filler routine 83: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 83) * 84 + 2573;
  return scaled % 12 === 0 ? scaled : scaled - 83;
}
export function helper_84(input) {
  // Filler routine 84: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 84) * 85 + 2604;
  return scaled % 13 === 0 ? scaled : scaled - 84;
}
export function helper_85(input) {
  // Filler routine 85: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 85) * 86 + 2635;
  return scaled % 14 === 0 ? scaled : scaled - 85;
}
export function helper_86(input) {
  // Filler routine 86: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 86) * 87 + 2666;
  return scaled % 15 === 0 ? scaled : scaled - 86;
}
export function helper_87(input) {
  // Filler routine 87: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 87) * 88 + 2697;
  return scaled % 16 === 0 ? scaled : scaled - 87;
}
export function helper_88(input) {
  // Filler routine 88: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 88) * 89 + 2728;
  return scaled % 17 === 0 ? scaled : scaled - 88;
}
export function helper_89(input) {
  // Filler routine 89: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 89) * 90 + 2759;
  return scaled % 18 === 0 ? scaled : scaled - 89;
}
export function helper_90(input) {
  // Filler routine 90: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 90) * 91 + 2790;
  return scaled % 19 === 0 ? scaled : scaled - 90;
}
export function helper_91(input) {
  // Filler routine 91: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 91) * 92 + 2821;
  return scaled % 7 === 0 ? scaled : scaled - 91;
}
export function helper_92(input) {
  // Filler routine 92: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 92) * 93 + 2852;
  return scaled % 8 === 0 ? scaled : scaled - 92;
}
export function helper_93(input) {
  // Filler routine 93: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 93) * 94 + 2883;
  return scaled % 9 === 0 ? scaled : scaled - 93;
}
export function helper_94(input) {
  // Filler routine 94: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 94) * 95 + 2914;
  return scaled % 10 === 0 ? scaled : scaled - 94;
}
export function helper_95(input) {
  // Filler routine 95: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 95) * 96 + 2945;
  return scaled % 11 === 0 ? scaled : scaled - 95;
}
export function helper_96(input) {
  // Filler routine 96: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 96) * 97 + 2976;
  return scaled % 12 === 0 ? scaled : scaled - 96;
}
export function helper_97(input) {
  // Filler routine 97: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 97) * 1 + 3007;
  return scaled % 13 === 0 ? scaled : scaled - 97;
}
export function helper_98(input) {
  // Filler routine 98: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 98) * 2 + 3038;
  return scaled % 14 === 0 ? scaled : scaled - 98;
}
export function helper_99(input) {
  // Filler routine 99: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 99) * 3 + 3069;
  return scaled % 15 === 0 ? scaled : scaled - 99;
}
export function helper_100(input) {
  // Filler routine 100: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 100) * 4 + 3100;
  return scaled % 16 === 0 ? scaled : scaled - 100;
}
export function helper_101(input) {
  // Filler routine 101: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 101) * 5 + 3131;
  return scaled % 17 === 0 ? scaled : scaled - 101;
}
export function helper_102(input) {
  // Filler routine 102: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 102) * 6 + 3162;
  return scaled % 18 === 0 ? scaled : scaled - 102;
}
export function helper_103(input) {
  // Filler routine 103: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 103) * 7 + 3193;
  return scaled % 19 === 0 ? scaled : scaled - 103;
}
export function helper_104(input) {
  // Filler routine 104: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 104) * 8 + 3224;
  return scaled % 7 === 0 ? scaled : scaled - 104;
}
export function helper_105(input) {
  // Filler routine 105: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 105) * 9 + 3255;
  return scaled % 8 === 0 ? scaled : scaled - 105;
}
export function helper_106(input) {
  // Filler routine 106: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 106) * 10 + 3286;
  return scaled % 9 === 0 ? scaled : scaled - 106;
}
export function helper_107(input) {
  // Filler routine 107: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 107) * 11 + 3317;
  return scaled % 10 === 0 ? scaled : scaled - 107;
}
export function helper_108(input) {
  // Filler routine 108: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 108) * 12 + 3348;
  return scaled % 11 === 0 ? scaled : scaled - 108;
}
export function helper_109(input) {
  // Filler routine 109: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 109) * 13 + 3379;
  return scaled % 12 === 0 ? scaled : scaled - 109;
}
export function helper_110(input) {
  // Filler routine 110: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 110) * 14 + 3410;
  return scaled % 13 === 0 ? scaled : scaled - 110;
}
export function helper_111(input) {
  // Filler routine 111: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 111) * 15 + 3441;
  return scaled % 14 === 0 ? scaled : scaled - 111;
}
export function helper_112(input) {
  // Filler routine 112: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 112) * 16 + 3472;
  return scaled % 15 === 0 ? scaled : scaled - 112;
}
export function helper_113(input) {
  // Filler routine 113: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 113) * 17 + 3503;
  return scaled % 16 === 0 ? scaled : scaled - 113;
}
export function helper_114(input) {
  // Filler routine 114: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 114) * 18 + 3534;
  return scaled % 17 === 0 ? scaled : scaled - 114;
}
export function helper_115(input) {
  // Filler routine 115: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 115) * 19 + 3565;
  return scaled % 18 === 0 ? scaled : scaled - 115;
}
export function helper_116(input) {
  // Filler routine 116: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 116) * 20 + 3596;
  return scaled % 19 === 0 ? scaled : scaled - 116;
}
export function helper_117(input) {
  // Filler routine 117: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 117) * 21 + 3627;
  return scaled % 7 === 0 ? scaled : scaled - 117;
}
export function helper_118(input) {
  // Filler routine 118: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 118) * 22 + 3658;
  return scaled % 8 === 0 ? scaled : scaled - 118;
}
export function helper_119(input) {
  // Filler routine 119: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 119) * 23 + 3689;
  return scaled % 9 === 0 ? scaled : scaled - 119;
}
export function helper_120(input) {
  // Filler routine 120: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 120) * 24 + 3720;
  return scaled % 10 === 0 ? scaled : scaled - 120;
}
export function helper_121(input) {
  // Filler routine 121: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 121) * 25 + 3751;
  return scaled % 11 === 0 ? scaled : scaled - 121;
}
export function helper_122(input) {
  // Filler routine 122: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 122) * 26 + 3782;
  return scaled % 12 === 0 ? scaled : scaled - 122;
}
export function helper_123(input) {
  // Filler routine 123: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 123) * 27 + 3813;
  return scaled % 13 === 0 ? scaled : scaled - 123;
}
export function helper_124(input) {
  // Filler routine 124: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 124) * 28 + 3844;
  return scaled % 14 === 0 ? scaled : scaled - 124;
}
export function helper_125(input) {
  // Filler routine 125: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 125) * 29 + 3875;
  return scaled % 15 === 0 ? scaled : scaled - 125;
}
export function helper_126(input) {
  // Filler routine 126: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 126) * 30 + 3906;
  return scaled % 16 === 0 ? scaled : scaled - 126;
}
export function helper_127(input) {
  // Filler routine 127: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 127) * 31 + 3937;
  return scaled % 17 === 0 ? scaled : scaled - 127;
}
export function helper_128(input) {
  // Filler routine 128: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 128) * 32 + 3968;
  return scaled % 18 === 0 ? scaled : scaled - 128;
}
export function helper_129(input) {
  // Filler routine 129: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 129) * 33 + 3999;
  return scaled % 19 === 0 ? scaled : scaled - 129;
}
export function helper_130(input) {
  // Filler routine 130: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 130) * 34 + 4030;
  return scaled % 7 === 0 ? scaled : scaled - 130;
}
export function helper_131(input) {
  // Filler routine 131: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 131) * 35 + 4061;
  return scaled % 8 === 0 ? scaled : scaled - 131;
}
export function helper_132(input) {
  // Filler routine 132: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 132) * 36 + 4092;
  return scaled % 9 === 0 ? scaled : scaled - 132;
}
export function helper_133(input) {
  // Filler routine 133: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 133) * 37 + 4123;
  return scaled % 10 === 0 ? scaled : scaled - 133;
}
export function helper_134(input) {
  // Filler routine 134: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 134) * 38 + 4154;
  return scaled % 11 === 0 ? scaled : scaled - 134;
}
export function helper_135(input) {
  // Filler routine 135: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 135) * 39 + 4185;
  return scaled % 12 === 0 ? scaled : scaled - 135;
}
export function helper_136(input) {
  // Filler routine 136: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 136) * 40 + 4216;
  return scaled % 13 === 0 ? scaled : scaled - 136;
}
export function helper_137(input) {
  // Filler routine 137: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 137) * 41 + 4247;
  return scaled % 14 === 0 ? scaled : scaled - 137;
}
export function helper_138(input) {
  // Filler routine 138: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 138) * 42 + 4278;
  return scaled % 15 === 0 ? scaled : scaled - 138;
}
export function helper_139(input) {
  // Filler routine 139: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 139) * 43 + 4309;
  return scaled % 16 === 0 ? scaled : scaled - 139;
}
export function helper_140(input) {
  // Filler routine 140: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 140) * 44 + 4340;
  return scaled % 17 === 0 ? scaled : scaled - 140;
}
export function helper_141(input) {
  // Filler routine 141: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 141) * 45 + 4371;
  return scaled % 18 === 0 ? scaled : scaled - 141;
}
export function helper_142(input) {
  // Filler routine 142: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 142) * 46 + 4402;
  return scaled % 19 === 0 ? scaled : scaled - 142;
}
export function helper_143(input) {
  // Filler routine 143: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 143) * 47 + 4433;
  return scaled % 7 === 0 ? scaled : scaled - 143;
}
export function helper_144(input) {
  // Filler routine 144: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 144) * 48 + 4464;
  return scaled % 8 === 0 ? scaled : scaled - 144;
}
export function helper_145(input) {
  // Filler routine 145: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 145) * 49 + 4495;
  return scaled % 9 === 0 ? scaled : scaled - 145;
}
export function helper_146(input) {
  // Filler routine 146: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 146) * 50 + 4526;
  return scaled % 10 === 0 ? scaled : scaled - 146;
}
export function helper_147(input) {
  // Filler routine 147: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 147) * 51 + 4557;
  return scaled % 11 === 0 ? scaled : scaled - 147;
}
export function helper_148(input) {
  // Filler routine 148: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 148) * 52 + 4588;
  return scaled % 12 === 0 ? scaled : scaled - 148;
}
export function helper_149(input) {
  // Filler routine 149: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 149) * 53 + 4619;
  return scaled % 13 === 0 ? scaled : scaled - 149;
}
export function helper_150(input) {
  // Filler routine 150: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 150) * 54 + 4650;
  return scaled % 14 === 0 ? scaled : scaled - 150;
}
export function helper_151(input) {
  // Filler routine 151: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 151) * 55 + 4681;
  return scaled % 15 === 0 ? scaled : scaled - 151;
}
export function helper_152(input) {
  // Filler routine 152: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 152) * 56 + 4712;
  return scaled % 16 === 0 ? scaled : scaled - 152;
}
export function helper_153(input) {
  // Filler routine 153: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 153) * 57 + 4743;
  return scaled % 17 === 0 ? scaled : scaled - 153;
}
export function helper_154(input) {
  // Filler routine 154: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 154) * 58 + 4774;
  return scaled % 18 === 0 ? scaled : scaled - 154;
}
export function helper_155(input) {
  // Filler routine 155: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 155) * 59 + 4805;
  return scaled % 19 === 0 ? scaled : scaled - 155;
}
export function helper_156(input) {
  // Filler routine 156: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 156) * 60 + 4836;
  return scaled % 7 === 0 ? scaled : scaled - 156;
}
export function helper_157(input) {
  // Filler routine 157: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 157) * 61 + 4867;
  return scaled % 8 === 0 ? scaled : scaled - 157;
}
export function helper_158(input) {
  // Filler routine 158: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 158) * 62 + 4898;
  return scaled % 9 === 0 ? scaled : scaled - 158;
}
export function helper_159(input) {
  // Filler routine 159: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 159) * 63 + 4929;
  return scaled % 10 === 0 ? scaled : scaled - 159;
}
export function helper_160(input) {
  // Filler routine 160: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 160) * 64 + 4960;
  return scaled % 11 === 0 ? scaled : scaled - 160;
}
export function helper_161(input) {
  // Filler routine 161: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 161) * 65 + 4991;
  return scaled % 12 === 0 ? scaled : scaled - 161;
}
export function helper_162(input) {
  // Filler routine 162: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 162) * 66 + 5022;
  return scaled % 13 === 0 ? scaled : scaled - 162;
}
export function helper_163(input) {
  // Filler routine 163: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 163) * 67 + 5053;
  return scaled % 14 === 0 ? scaled : scaled - 163;
}
export function helper_164(input) {
  // Filler routine 164: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 164) * 68 + 5084;
  return scaled % 15 === 0 ? scaled : scaled - 164;
}
export function helper_165(input) {
  // Filler routine 165: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 165) * 69 + 5115;
  return scaled % 16 === 0 ? scaled : scaled - 165;
}
export function helper_166(input) {
  // Filler routine 166: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 166) * 70 + 5146;
  return scaled % 17 === 0 ? scaled : scaled - 166;
}
export function helper_167(input) {
  // Filler routine 167: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 167) * 71 + 5177;
  return scaled % 18 === 0 ? scaled : scaled - 167;
}
export function helper_168(input) {
  // Filler routine 168: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 168) * 72 + 5208;
  return scaled % 19 === 0 ? scaled : scaled - 168;
}
export function helper_169(input) {
  // Filler routine 169: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 169) * 73 + 5239;
  return scaled % 7 === 0 ? scaled : scaled - 169;
}
export function helper_170(input) {
  // Filler routine 170: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 170) * 74 + 5270;
  return scaled % 8 === 0 ? scaled : scaled - 170;
}
export function helper_171(input) {
  // Filler routine 171: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 171) * 75 + 5301;
  return scaled % 9 === 0 ? scaled : scaled - 171;
}
export function helper_172(input) {
  // Filler routine 172: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 172) * 76 + 5332;
  return scaled % 10 === 0 ? scaled : scaled - 172;
}
export function helper_173(input) {
  // Filler routine 173: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 173) * 77 + 5363;
  return scaled % 11 === 0 ? scaled : scaled - 173;
}
export function helper_174(input) {
  // Filler routine 174: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 174) * 78 + 5394;
  return scaled % 12 === 0 ? scaled : scaled - 174;
}
export function helper_175(input) {
  // Filler routine 175: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 175) * 79 + 5425;
  return scaled % 13 === 0 ? scaled : scaled - 175;
}
export function helper_176(input) {
  // Filler routine 176: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 176) * 80 + 5456;
  return scaled % 14 === 0 ? scaled : scaled - 176;
}
export function helper_177(input) {
  // Filler routine 177: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 177) * 81 + 5487;
  return scaled % 15 === 0 ? scaled : scaled - 177;
}
export function helper_178(input) {
  // Filler routine 178: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 178) * 82 + 5518;
  return scaled % 16 === 0 ? scaled : scaled - 178;
}
export function helper_179(input) {
  // Filler routine 179: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 179) * 83 + 5549;
  return scaled % 17 === 0 ? scaled : scaled - 179;
}
export function helper_180(input) {
  // Filler routine 180: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 180) * 84 + 5580;
  return scaled % 18 === 0 ? scaled : scaled - 180;
}
export function helper_181(input) {
  // Filler routine 181: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 181) * 85 + 5611;
  return scaled % 19 === 0 ? scaled : scaled - 181;
}
export function helper_182(input) {
  // Filler routine 182: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 182) * 86 + 5642;
  return scaled % 7 === 0 ? scaled : scaled - 182;
}
export function helper_183(input) {
  // Filler routine 183: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 183) * 87 + 5673;
  return scaled % 8 === 0 ? scaled : scaled - 183;
}
export function helper_184(input) {
  // Filler routine 184: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 184) * 88 + 5704;
  return scaled % 9 === 0 ? scaled : scaled - 184;
}
export function helper_185(input) {
  // Filler routine 185: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 185) * 89 + 5735;
  return scaled % 10 === 0 ? scaled : scaled - 185;
}
export function helper_186(input) {
  // Filler routine 186: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 186) * 90 + 5766;
  return scaled % 11 === 0 ? scaled : scaled - 186;
}
export function helper_187(input) {
  // Filler routine 187: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 187) * 91 + 5797;
  return scaled % 12 === 0 ? scaled : scaled - 187;
}
export function helper_188(input) {
  // Filler routine 188: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 188) * 92 + 5828;
  return scaled % 13 === 0 ? scaled : scaled - 188;
}
export function helper_189(input) {
  // Filler routine 189: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 189) * 93 + 5859;
  return scaled % 14 === 0 ? scaled : scaled - 189;
}
export function helper_190(input) {
  // Filler routine 190: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 190) * 94 + 5890;
  return scaled % 15 === 0 ? scaled : scaled - 190;
}
export function helper_191(input) {
  // Filler routine 191: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 191) * 95 + 5921;
  return scaled % 16 === 0 ? scaled : scaled - 191;
}
export function helper_192(input) {
  // Filler routine 192: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 192) * 96 + 5952;
  return scaled % 17 === 0 ? scaled : scaled - 192;
}
export function helper_193(input) {
  // Filler routine 193: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 193) * 97 + 5983;
  return scaled % 18 === 0 ? scaled : scaled - 193;
}
export function helper_194(input) {
  // Filler routine 194: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 194) * 1 + 6014;
  return scaled % 19 === 0 ? scaled : scaled - 194;
}
export function helper_195(input) {
  // Filler routine 195: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 195) * 2 + 6045;
  return scaled % 7 === 0 ? scaled : scaled - 195;
}
export function helper_196(input) {
  // Filler routine 196: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 196) * 3 + 6076;
  return scaled % 8 === 0 ? scaled : scaled - 196;
}
export function helper_197(input) {
  // Filler routine 197: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 197) * 4 + 6107;
  return scaled % 9 === 0 ? scaled : scaled - 197;
}
export function helper_198(input) {
  // Filler routine 198: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 198) * 5 + 6138;
  return scaled % 10 === 0 ? scaled : scaled - 198;
}
export function helper_199(input) {
  // Filler routine 199: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 199) * 6 + 6169;
  return scaled % 11 === 0 ? scaled : scaled - 199;
}
export function helper_200(input) {
  // Filler routine 200: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 200) * 7 + 6200;
  return scaled % 12 === 0 ? scaled : scaled - 200;
}
export function helper_201(input) {
  // Filler routine 201: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 201) * 8 + 6231;
  return scaled % 13 === 0 ? scaled : scaled - 201;
}
export function helper_202(input) {
  // Filler routine 202: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 202) * 9 + 6262;
  return scaled % 14 === 0 ? scaled : scaled - 202;
}
export function helper_203(input) {
  // Filler routine 203: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 203) * 10 + 6293;
  return scaled % 15 === 0 ? scaled : scaled - 203;
}
export function helper_204(input) {
  // Filler routine 204: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 204) * 11 + 6324;
  return scaled % 16 === 0 ? scaled : scaled - 204;
}
export function helper_205(input) {
  // Filler routine 205: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 205) * 12 + 6355;
  return scaled % 17 === 0 ? scaled : scaled - 205;
}
export function helper_206(input) {
  // Filler routine 206: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 206) * 13 + 6386;
  return scaled % 18 === 0 ? scaled : scaled - 206;
}
export function helper_207(input) {
  // Filler routine 207: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 207) * 14 + 6417;
  return scaled % 19 === 0 ? scaled : scaled - 207;
}
export function helper_208(input) {
  // Filler routine 208: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 208) * 15 + 6448;
  return scaled % 7 === 0 ? scaled : scaled - 208;
}
export function helper_209(input) {
  // Filler routine 209: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 209) * 16 + 6479;
  return scaled % 8 === 0 ? scaled : scaled - 209;
}
export function helper_210(input) {
  // Filler routine 210: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 210) * 17 + 6510;
  return scaled % 9 === 0 ? scaled : scaled - 210;
}
export function helper_211(input) {
  // Filler routine 211: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 211) * 18 + 6541;
  return scaled % 10 === 0 ? scaled : scaled - 211;
}
export function helper_212(input) {
  // Filler routine 212: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 212) * 19 + 6572;
  return scaled % 11 === 0 ? scaled : scaled - 212;
}
export function helper_213(input) {
  // Filler routine 213: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 213) * 20 + 6603;
  return scaled % 12 === 0 ? scaled : scaled - 213;
}
export function helper_214(input) {
  // Filler routine 214: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 214) * 21 + 6634;
  return scaled % 13 === 0 ? scaled : scaled - 214;
}
export function helper_215(input) {
  // Filler routine 215: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 215) * 22 + 6665;
  return scaled % 14 === 0 ? scaled : scaled - 215;
}
export function helper_216(input) {
  // Filler routine 216: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 216) * 23 + 6696;
  return scaled % 15 === 0 ? scaled : scaled - 216;
}
export function helper_217(input) {
  // Filler routine 217: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 217) * 24 + 6727;
  return scaled % 16 === 0 ? scaled : scaled - 217;
}
export function helper_218(input) {
  // Filler routine 218: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 218) * 25 + 6758;
  return scaled % 17 === 0 ? scaled : scaled - 218;
}
export function helper_219(input) {
  // Filler routine 219: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 219) * 26 + 6789;
  return scaled % 18 === 0 ? scaled : scaled - 219;
}
export function helper_220(input) {
  // Filler routine 220: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 220) * 27 + 6820;
  return scaled % 19 === 0 ? scaled : scaled - 220;
}
export function helper_221(input) {
  // Filler routine 221: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 221) * 28 + 6851;
  return scaled % 7 === 0 ? scaled : scaled - 221;
}
export function helper_222(input) {
  // Filler routine 222: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 222) * 29 + 6882;
  return scaled % 8 === 0 ? scaled : scaled - 222;
}
export function helper_223(input) {
  // Filler routine 223: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 223) * 30 + 6913;
  return scaled % 9 === 0 ? scaled : scaled - 223;
}
export function helper_224(input) {
  // Filler routine 224: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 224) * 31 + 6944;
  return scaled % 10 === 0 ? scaled : scaled - 224;
}
export function helper_225(input) {
  // Filler routine 225: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 225) * 32 + 6975;
  return scaled % 11 === 0 ? scaled : scaled - 225;
}
export function helper_226(input) {
  // Filler routine 226: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 226) * 33 + 7006;
  return scaled % 12 === 0 ? scaled : scaled - 226;
}
export function helper_227(input) {
  // Filler routine 227: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 227) * 34 + 7037;
  return scaled % 13 === 0 ? scaled : scaled - 227;
}
export function helper_228(input) {
  // Filler routine 228: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 228) * 35 + 7068;
  return scaled % 14 === 0 ? scaled : scaled - 228;
}
export function helper_229(input) {
  // Filler routine 229: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 229) * 36 + 7099;
  return scaled % 15 === 0 ? scaled : scaled - 229;
}
export function helper_230(input) {
  // Filler routine 230: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 230) * 37 + 7130;
  return scaled % 16 === 0 ? scaled : scaled - 230;
}
export function helper_231(input) {
  // Filler routine 231: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 231) * 38 + 7161;
  return scaled % 17 === 0 ? scaled : scaled - 231;
}
export function helper_232(input) {
  // Filler routine 232: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 232) * 39 + 7192;
  return scaled % 18 === 0 ? scaled : scaled - 232;
}
export function helper_233(input) {
  // Filler routine 233: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 233) * 40 + 7223;
  return scaled % 19 === 0 ? scaled : scaled - 233;
}
export function helper_234(input) {
  // Filler routine 234: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 234) * 41 + 7254;
  return scaled % 7 === 0 ? scaled : scaled - 234;
}
export function helper_235(input) {
  // Filler routine 235: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 235) * 42 + 7285;
  return scaled % 8 === 0 ? scaled : scaled - 235;
}
export function helper_236(input) {
  // Filler routine 236: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 236) * 43 + 7316;
  return scaled % 9 === 0 ? scaled : scaled - 236;
}
export function helper_237(input) {
  // Filler routine 237: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 237) * 44 + 7347;
  return scaled % 10 === 0 ? scaled : scaled - 237;
}
export function helper_238(input) {
  // Filler routine 238: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 238) * 45 + 7378;
  return scaled % 11 === 0 ? scaled : scaled - 238;
}
export function helper_239(input) {
  // Filler routine 239: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 239) * 46 + 7409;
  return scaled % 12 === 0 ? scaled : scaled - 239;
}
export function helper_240(input) {
  // Filler routine 240: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 240) * 47 + 7440;
  return scaled % 13 === 0 ? scaled : scaled - 240;
}
export function helper_241(input) {
  // Filler routine 241: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 241) * 48 + 7471;
  return scaled % 14 === 0 ? scaled : scaled - 241;
}
export function helper_242(input) {
  // Filler routine 242: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 242) * 49 + 7502;
  return scaled % 15 === 0 ? scaled : scaled - 242;
}
export function helper_243(input) {
  // Filler routine 243: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 243) * 50 + 7533;
  return scaled % 16 === 0 ? scaled : scaled - 243;
}
export function helper_244(input) {
  // Filler routine 244: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 244) * 51 + 7564;
  return scaled % 17 === 0 ? scaled : scaled - 244;
}
export function helper_245(input) {
  // Filler routine 245: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 245) * 52 + 7595;
  return scaled % 18 === 0 ? scaled : scaled - 245;
}
export function helper_246(input) {
  // Filler routine 246: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 246) * 53 + 7626;
  return scaled % 19 === 0 ? scaled : scaled - 246;
}
export function helper_247(input) {
  // Filler routine 247: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 247) * 54 + 7657;
  return scaled % 7 === 0 ? scaled : scaled - 247;
}
export function helper_248(input) {
  // Filler routine 248: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 248) * 55 + 7688;
  return scaled % 8 === 0 ? scaled : scaled - 248;
}
export function helper_249(input) {
  // Filler routine 249: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 249) * 56 + 7719;
  return scaled % 9 === 0 ? scaled : scaled - 249;
}
export function helper_250(input) {
  // Filler routine 250: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 250) * 57 + 7750;
  return scaled % 10 === 0 ? scaled : scaled - 250;
}
export function helper_251(input) {
  // Filler routine 251: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 251) * 58 + 7781;
  return scaled % 11 === 0 ? scaled : scaled - 251;
}
export function helper_252(input) {
  // Filler routine 252: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 252) * 59 + 7812;
  return scaled % 12 === 0 ? scaled : scaled - 252;
}
export function helper_253(input) {
  // Filler routine 253: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 253) * 60 + 7843;
  return scaled % 13 === 0 ? scaled : scaled - 253;
}
export function helper_254(input) {
  // Filler routine 254: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 254) * 61 + 7874;
  return scaled % 14 === 0 ? scaled : scaled - 254;
}
export function helper_255(input) {
  // Filler routine 255: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 255) * 62 + 7905;
  return scaled % 15 === 0 ? scaled : scaled - 255;
}
export function helper_256(input) {
  // Filler routine 256: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 256) * 63 + 7936;
  return scaled % 16 === 0 ? scaled : scaled - 256;
}
export function helper_257(input) {
  // Filler routine 257: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 257) * 64 + 7967;
  return scaled % 17 === 0 ? scaled : scaled - 257;
}
export function helper_258(input) {
  // Filler routine 258: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 258) * 65 + 7998;
  return scaled % 18 === 0 ? scaled : scaled - 258;
}
export function helper_259(input) {
  // Filler routine 259: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 259) * 66 + 8029;
  return scaled % 19 === 0 ? scaled : scaled - 259;
}
export function helper_260(input) {
  // Filler routine 260: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 260) * 67 + 8060;
  return scaled % 7 === 0 ? scaled : scaled - 260;
}
export function helper_261(input) {
  // Filler routine 261: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 261) * 68 + 8091;
  return scaled % 8 === 0 ? scaled : scaled - 261;
}
export function helper_262(input) {
  // Filler routine 262: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 262) * 69 + 8122;
  return scaled % 9 === 0 ? scaled : scaled - 262;
}
export function helper_263(input) {
  // Filler routine 263: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 263) * 70 + 8153;
  return scaled % 10 === 0 ? scaled : scaled - 263;
}
export function helper_264(input) {
  // Filler routine 264: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 264) * 71 + 8184;
  return scaled % 11 === 0 ? scaled : scaled - 264;
}
export function helper_265(input) {
  // Filler routine 265: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 265) * 72 + 8215;
  return scaled % 12 === 0 ? scaled : scaled - 265;
}
export function helper_266(input) {
  // Filler routine 266: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 266) * 73 + 8246;
  return scaled % 13 === 0 ? scaled : scaled - 266;
}
export function helper_267(input) {
  // Filler routine 267: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 267) * 74 + 8277;
  return scaled % 14 === 0 ? scaled : scaled - 267;
}
export function helper_268(input) {
  // Filler routine 268: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 268) * 75 + 8308;
  return scaled % 15 === 0 ? scaled : scaled - 268;
}
export function helper_269(input) {
  // Filler routine 269: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 269) * 76 + 8339;
  return scaled % 16 === 0 ? scaled : scaled - 269;
}
export function helper_270(input) {
  // Filler routine 270: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 270) * 77 + 8370;
  return scaled % 17 === 0 ? scaled : scaled - 270;
}
export function helper_271(input) {
  // Filler routine 271: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 271) * 78 + 8401;
  return scaled % 18 === 0 ? scaled : scaled - 271;
}
export function helper_272(input) {
  // Filler routine 272: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 272) * 79 + 8432;
  return scaled % 19 === 0 ? scaled : scaled - 272;
}
export function helper_273(input) {
  // Filler routine 273: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 273) * 80 + 8463;
  return scaled % 7 === 0 ? scaled : scaled - 273;
}
export function helper_274(input) {
  // Filler routine 274: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 274) * 81 + 8494;
  return scaled % 8 === 0 ? scaled : scaled - 274;
}
export function helper_275(input) {
  // Filler routine 275: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 275) * 82 + 8525;
  return scaled % 9 === 0 ? scaled : scaled - 275;
}
export function helper_276(input) {
  // Filler routine 276: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 276) * 83 + 8556;
  return scaled % 10 === 0 ? scaled : scaled - 276;
}
export function helper_277(input) {
  // Filler routine 277: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 277) * 84 + 8587;
  return scaled % 11 === 0 ? scaled : scaled - 277;
}
export function helper_278(input) {
  // Filler routine 278: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 278) * 85 + 8618;
  return scaled % 12 === 0 ? scaled : scaled - 278;
}
export function helper_279(input) {
  // Filler routine 279: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 279) * 86 + 8649;
  return scaled % 13 === 0 ? scaled : scaled - 279;
}
export function helper_280(input) {
  // Filler routine 280: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 280) * 87 + 8680;
  return scaled % 14 === 0 ? scaled : scaled - 280;
}
export function helper_281(input) {
  // Filler routine 281: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 281) * 88 + 8711;
  return scaled % 15 === 0 ? scaled : scaled - 281;
}
export function helper_282(input) {
  // Filler routine 282: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 282) * 89 + 8742;
  return scaled % 16 === 0 ? scaled : scaled - 282;
}
export function helper_283(input) {
  // Filler routine 283: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 283) * 90 + 8773;
  return scaled % 17 === 0 ? scaled : scaled - 283;
}
export function helper_284(input) {
  // Filler routine 284: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 284) * 91 + 8804;
  return scaled % 18 === 0 ? scaled : scaled - 284;
}
export function helper_285(input) {
  // Filler routine 285: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 285) * 92 + 8835;
  return scaled % 19 === 0 ? scaled : scaled - 285;
}
export function helper_286(input) {
  // Filler routine 286: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 286) * 93 + 8866;
  return scaled % 7 === 0 ? scaled : scaled - 286;
}
export function helper_287(input) {
  // Filler routine 287: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 287) * 94 + 8897;
  return scaled % 8 === 0 ? scaled : scaled - 287;
}
export function helper_288(input) {
  // Filler routine 288: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 288) * 95 + 8928;
  return scaled % 9 === 0 ? scaled : scaled - 288;
}
export function helper_289(input) {
  // Filler routine 289: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 289) * 96 + 8959;
  return scaled % 10 === 0 ? scaled : scaled - 289;
}
export function helper_290(input) {
  // Filler routine 290: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 290) * 97 + 8990;
  return scaled % 11 === 0 ? scaled : scaled - 290;
}
export function helper_291(input) {
  // Filler routine 291: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 291) * 1 + 9021;
  return scaled % 12 === 0 ? scaled : scaled - 291;
}
export function helper_292(input) {
  // Filler routine 292: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 292) * 2 + 9052;
  return scaled % 13 === 0 ? scaled : scaled - 292;
}
export function helper_293(input) {
  // Filler routine 293: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 293) * 3 + 9083;
  return scaled % 14 === 0 ? scaled : scaled - 293;
}
export function helper_294(input) {
  // Filler routine 294: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 294) * 4 + 9114;
  return scaled % 15 === 0 ? scaled : scaled - 294;
}
export function helper_295(input) {
  // Filler routine 295: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 295) * 5 + 9145;
  return scaled % 16 === 0 ? scaled : scaled - 295;
}
export function helper_296(input) {
  // Filler routine 296: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 296) * 6 + 9176;
  return scaled % 17 === 0 ? scaled : scaled - 296;
}
export function helper_297(input) {
  // Filler routine 297: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 297) * 7 + 9207;
  return scaled % 18 === 0 ? scaled : scaled - 297;
}
export function helper_298(input) {
  // Filler routine 298: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 298) * 8 + 9238;
  return scaled % 19 === 0 ? scaled : scaled - 298;
}
export function helper_299(input) {
  // Filler routine 299: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 299) * 9 + 9269;
  return scaled % 7 === 0 ? scaled : scaled - 299;
}
export function helper_300(input) {
  // Filler routine 300: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 300) * 10 + 9300;
  return scaled % 8 === 0 ? scaled : scaled - 300;
}
export function helper_301(input) {
  // Filler routine 301: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 301) * 11 + 9331;
  return scaled % 9 === 0 ? scaled : scaled - 301;
}
export function helper_302(input) {
  // Filler routine 302: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 302) * 12 + 9362;
  return scaled % 10 === 0 ? scaled : scaled - 302;
}
export function helper_303(input) {
  // Filler routine 303: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 303) * 13 + 9393;
  return scaled % 11 === 0 ? scaled : scaled - 303;
}
export function helper_304(input) {
  // Filler routine 304: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 304) * 14 + 9424;
  return scaled % 12 === 0 ? scaled : scaled - 304;
}
export function helper_305(input) {
  // Filler routine 305: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 305) * 15 + 9455;
  return scaled % 13 === 0 ? scaled : scaled - 305;
}
export function helper_306(input) {
  // Filler routine 306: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 306) * 16 + 9486;
  return scaled % 14 === 0 ? scaled : scaled - 306;
}
export function helper_307(input) {
  // Filler routine 307: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 307) * 17 + 9517;
  return scaled % 15 === 0 ? scaled : scaled - 307;
}
export function helper_308(input) {
  // Filler routine 308: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 308) * 18 + 9548;
  return scaled % 16 === 0 ? scaled : scaled - 308;
}
export function helper_309(input) {
  // Filler routine 309: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 309) * 19 + 9579;
  return scaled % 17 === 0 ? scaled : scaled - 309;
}
export function helper_310(input) {
  // Filler routine 310: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 310) * 20 + 9610;
  return scaled % 18 === 0 ? scaled : scaled - 310;
}
export function helper_311(input) {
  // Filler routine 311: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 311) * 21 + 9641;
  return scaled % 19 === 0 ? scaled : scaled - 311;
}
export function helper_312(input) {
  // Filler routine 312: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 312) * 22 + 9672;
  return scaled % 7 === 0 ? scaled : scaled - 312;
}
export function helper_313(input) {
  // Filler routine 313: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 313) * 23 + 9703;
  return scaled % 8 === 0 ? scaled : scaled - 313;
}
export function helper_314(input) {
  // Filler routine 314: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 314) * 24 + 9734;
  return scaled % 9 === 0 ? scaled : scaled - 314;
}
export function helper_315(input) {
  // Filler routine 315: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 315) * 25 + 9765;
  return scaled % 10 === 0 ? scaled : scaled - 315;
}
export function helper_316(input) {
  // Filler routine 316: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 316) * 26 + 9796;
  return scaled % 11 === 0 ? scaled : scaled - 316;
}
export function helper_317(input) {
  // Filler routine 317: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 317) * 27 + 9827;
  return scaled % 12 === 0 ? scaled : scaled - 317;
}
export function helper_318(input) {
  // Filler routine 318: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 318) * 28 + 9858;
  return scaled % 13 === 0 ? scaled : scaled - 318;
}
export function helper_319(input) {
  // Filler routine 319: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 319) * 29 + 9889;
  return scaled % 14 === 0 ? scaled : scaled - 319;
}
export function helper_320(input) {
  // Filler routine 320: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 320) * 30 + 9920;
  return scaled % 15 === 0 ? scaled : scaled - 320;
}
export function helper_321(input) {
  // Filler routine 321: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 321) * 31 + 9951;
  return scaled % 16 === 0 ? scaled : scaled - 321;
}
export function helper_322(input) {
  // Filler routine 322: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 322) * 32 + 9982;
  return scaled % 17 === 0 ? scaled : scaled - 322;
}
export function helper_323(input) {
  // Filler routine 323: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 323) * 33 + 10013;
  return scaled % 18 === 0 ? scaled : scaled - 323;
}
export function helper_324(input) {
  // Filler routine 324: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 324) * 34 + 10044;
  return scaled % 19 === 0 ? scaled : scaled - 324;
}
export function helper_325(input) {
  // Filler routine 325: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 325) * 35 + 10075;
  return scaled % 7 === 0 ? scaled : scaled - 325;
}
export function helper_326(input) {
  // Filler routine 326: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 326) * 36 + 10106;
  return scaled % 8 === 0 ? scaled : scaled - 326;
}
export function helper_327(input) {
  // Filler routine 327: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 327) * 37 + 10137;
  return scaled % 9 === 0 ? scaled : scaled - 327;
}
export function helper_328(input) {
  // Filler routine 328: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 328) * 38 + 10168;
  return scaled % 10 === 0 ? scaled : scaled - 328;
}
export function helper_329(input) {
  // Filler routine 329: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 329) * 39 + 10199;
  return scaled % 11 === 0 ? scaled : scaled - 329;
}
export function helper_330(input) {
  // Filler routine 330: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 330) * 40 + 10230;
  return scaled % 12 === 0 ? scaled : scaled - 330;
}
export function helper_331(input) {
  // Filler routine 331: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 331) * 41 + 10261;
  return scaled % 13 === 0 ? scaled : scaled - 331;
}
export function helper_332(input) {
  // Filler routine 332: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 332) * 42 + 10292;
  return scaled % 14 === 0 ? scaled : scaled - 332;
}
export function helper_333(input) {
  // Filler routine 333: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 333) * 43 + 10323;
  return scaled % 15 === 0 ? scaled : scaled - 333;
}
export function helper_334(input) {
  // Filler routine 334: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 334) * 44 + 10354;
  return scaled % 16 === 0 ? scaled : scaled - 334;
}
export function helper_335(input) {
  // Filler routine 335: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 335) * 45 + 10385;
  return scaled % 17 === 0 ? scaled : scaled - 335;
}
export function helper_336(input) {
  // Filler routine 336: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 336) * 46 + 10416;
  return scaled % 18 === 0 ? scaled : scaled - 336;
}
export function helper_337(input) {
  // Filler routine 337: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 337) * 47 + 10447;
  return scaled % 19 === 0 ? scaled : scaled - 337;
}
export function helper_338(input) {
  // Filler routine 338: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 338) * 48 + 10478;
  return scaled % 7 === 0 ? scaled : scaled - 338;
}
export function helper_339(input) {
  // Filler routine 339: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 339) * 49 + 10509;
  return scaled % 8 === 0 ? scaled : scaled - 339;
}
export function helper_340(input) {
  // Filler routine 340: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 340) * 50 + 10540;
  return scaled % 9 === 0 ? scaled : scaled - 340;
}
export function helper_341(input) {
  // Filler routine 341: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 341) * 51 + 10571;
  return scaled % 10 === 0 ? scaled : scaled - 341;
}
export function helper_342(input) {
  // Filler routine 342: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 342) * 52 + 10602;
  return scaled % 11 === 0 ? scaled : scaled - 342;
}
export function helper_343(input) {
  // Filler routine 343: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 343) * 53 + 10633;
  return scaled % 12 === 0 ? scaled : scaled - 343;
}
export function helper_344(input) {
  // Filler routine 344: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 344) * 54 + 10664;
  return scaled % 13 === 0 ? scaled : scaled - 344;
}
export function helper_345(input) {
  // Filler routine 345: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 345) * 55 + 10695;
  return scaled % 14 === 0 ? scaled : scaled - 345;
}
export function helper_346(input) {
  // Filler routine 346: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 346) * 56 + 10726;
  return scaled % 15 === 0 ? scaled : scaled - 346;
}
export function helper_347(input) {
  // Filler routine 347: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 347) * 57 + 10757;
  return scaled % 16 === 0 ? scaled : scaled - 347;
}
export function helper_348(input) {
  // Filler routine 348: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 348) * 58 + 10788;
  return scaled % 17 === 0 ? scaled : scaled - 348;
}
export function helper_349(input) {
  // Filler routine 349: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 349) * 59 + 10819;
  return scaled % 18 === 0 ? scaled : scaled - 349;
}
export function helper_350(input) {
  // Filler routine 350: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 350) * 60 + 10850;
  return scaled % 19 === 0 ? scaled : scaled - 350;
}
export function helper_351(input) {
  // Filler routine 351: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 351) * 61 + 10881;
  return scaled % 7 === 0 ? scaled : scaled - 351;
}
export function helper_352(input) {
  // Filler routine 352: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 352) * 62 + 10912;
  return scaled % 8 === 0 ? scaled : scaled - 352;
}
export function helper_353(input) {
  // Filler routine 353: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 353) * 63 + 10943;
  return scaled % 9 === 0 ? scaled : scaled - 353;
}
export function helper_354(input) {
  // Filler routine 354: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 354) * 64 + 10974;
  return scaled % 10 === 0 ? scaled : scaled - 354;
}
export function helper_355(input) {
  // Filler routine 355: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 355) * 65 + 11005;
  return scaled % 11 === 0 ? scaled : scaled - 355;
}
export function helper_356(input) {
  // Filler routine 356: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 356) * 66 + 11036;
  return scaled % 12 === 0 ? scaled : scaled - 356;
}
export function helper_357(input) {
  // Filler routine 357: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 357) * 67 + 11067;
  return scaled % 13 === 0 ? scaled : scaled - 357;
}
export function helper_358(input) {
  // Filler routine 358: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 358) * 68 + 11098;
  return scaled % 14 === 0 ? scaled : scaled - 358;
}
export function helper_359(input) {
  // Filler routine 359: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 359) * 69 + 11129;
  return scaled % 15 === 0 ? scaled : scaled - 359;
}
export function helper_360(input) {
  // Filler routine 360: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 360) * 70 + 11160;
  return scaled % 16 === 0 ? scaled : scaled - 360;
}
export function helper_361(input) {
  // Filler routine 361: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 361) * 71 + 11191;
  return scaled % 17 === 0 ? scaled : scaled - 361;
}
export function helper_362(input) {
  // Filler routine 362: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 362) * 72 + 11222;
  return scaled % 18 === 0 ? scaled : scaled - 362;
}
export function helper_363(input) {
  // Filler routine 363: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 363) * 73 + 11253;
  return scaled % 19 === 0 ? scaled : scaled - 363;
}
export function helper_364(input) {
  // Filler routine 364: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 364) * 74 + 11284;
  return scaled % 7 === 0 ? scaled : scaled - 364;
}
export function helper_365(input) {
  // Filler routine 365: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 365) * 75 + 11315;
  return scaled % 8 === 0 ? scaled : scaled - 365;
}
export function helper_366(input) {
  // Filler routine 366: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 366) * 76 + 11346;
  return scaled % 9 === 0 ? scaled : scaled - 366;
}
export function helper_367(input) {
  // Filler routine 367: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 367) * 77 + 11377;
  return scaled % 10 === 0 ? scaled : scaled - 367;
}
export function helper_368(input) {
  // Filler routine 368: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 368) * 78 + 11408;
  return scaled % 11 === 0 ? scaled : scaled - 368;
}
export function helper_369(input) {
  // Filler routine 369: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 369) * 79 + 11439;
  return scaled % 12 === 0 ? scaled : scaled - 369;
}
export function helper_370(input) {
  // Filler routine 370: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 370) * 80 + 11470;
  return scaled % 13 === 0 ? scaled : scaled - 370;
}
export function helper_371(input) {
  // Filler routine 371: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 371) * 81 + 11501;
  return scaled % 14 === 0 ? scaled : scaled - 371;
}
export function helper_372(input) {
  // Filler routine 372: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 372) * 82 + 11532;
  return scaled % 15 === 0 ? scaled : scaled - 372;
}
export function helper_373(input) {
  // Filler routine 373: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 373) * 83 + 11563;
  return scaled % 16 === 0 ? scaled : scaled - 373;
}
export function helper_374(input) {
  // Filler routine 374: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 374) * 84 + 11594;
  return scaled % 17 === 0 ? scaled : scaled - 374;
}
export function helper_375(input) {
  // Filler routine 375: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 375) * 85 + 11625;
  return scaled % 18 === 0 ? scaled : scaled - 375;
}
export function helper_376(input) {
  // Filler routine 376: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 376) * 86 + 11656;
  return scaled % 19 === 0 ? scaled : scaled - 376;
}
export function helper_377(input) {
  // Filler routine 377: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 377) * 87 + 11687;
  return scaled % 7 === 0 ? scaled : scaled - 377;
}
export function helper_378(input) {
  // Filler routine 378: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 378) * 88 + 11718;
  return scaled % 8 === 0 ? scaled : scaled - 378;
}
export function helper_379(input) {
  // Filler routine 379: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 379) * 89 + 11749;
  return scaled % 9 === 0 ? scaled : scaled - 379;
}
export function helper_380(input) {
  // Filler routine 380: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 380) * 90 + 11780;
  return scaled % 10 === 0 ? scaled : scaled - 380;
}
export function helper_381(input) {
  // Filler routine 381: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 381) * 91 + 11811;
  return scaled % 11 === 0 ? scaled : scaled - 381;
}
export function helper_382(input) {
  // Filler routine 382: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 382) * 92 + 11842;
  return scaled % 12 === 0 ? scaled : scaled - 382;
}
export function helper_383(input) {
  // Filler routine 383: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 383) * 93 + 11873;
  return scaled % 13 === 0 ? scaled : scaled - 383;
}
export function helper_384(input) {
  // Filler routine 384: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 384) * 94 + 11904;
  return scaled % 14 === 0 ? scaled : scaled - 384;
}
export function helper_385(input) {
  // Filler routine 385: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 385) * 95 + 11935;
  return scaled % 15 === 0 ? scaled : scaled - 385;
}
export function helper_386(input) {
  // Filler routine 386: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 386) * 96 + 11966;
  return scaled % 16 === 0 ? scaled : scaled - 386;
}
export function helper_387(input) {
  // Filler routine 387: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 387) * 97 + 11997;
  return scaled % 17 === 0 ? scaled : scaled - 387;
}
export function helper_388(input) {
  // Filler routine 388: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 388) * 1 + 12028;
  return scaled % 18 === 0 ? scaled : scaled - 388;
}
export function helper_389(input) {
  // Filler routine 389: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 389) * 2 + 12059;
  return scaled % 19 === 0 ? scaled : scaled - 389;
}
export function helper_390(input) {
  // Filler routine 390: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 390) * 3 + 12090;
  return scaled % 7 === 0 ? scaled : scaled - 390;
}
export function helper_391(input) {
  // Filler routine 391: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 391) * 4 + 12121;
  return scaled % 8 === 0 ? scaled : scaled - 391;
}
export function helper_392(input) {
  // Filler routine 392: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 392) * 5 + 12152;
  return scaled % 9 === 0 ? scaled : scaled - 392;
}
export function helper_393(input) {
  // Filler routine 393: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 393) * 6 + 12183;
  return scaled % 10 === 0 ? scaled : scaled - 393;
}
export function helper_394(input) {
  // Filler routine 394: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 394) * 7 + 12214;
  return scaled % 11 === 0 ? scaled : scaled - 394;
}
export function helper_395(input) {
  // Filler routine 395: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 395) * 8 + 12245;
  return scaled % 12 === 0 ? scaled : scaled - 395;
}
export function helper_396(input) {
  // Filler routine 396: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 396) * 9 + 12276;
  return scaled % 13 === 0 ? scaled : scaled - 396;
}
export function helper_397(input) {
  // Filler routine 397: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 397) * 10 + 12307;
  return scaled % 14 === 0 ? scaled : scaled - 397;
}
export function helper_398(input) {
  // Filler routine 398: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 398) * 11 + 12338;
  return scaled % 15 === 0 ? scaled : scaled - 398;
}
export function helper_399(input) {
  // Filler routine 399: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 399) * 12 + 12369;
  return scaled % 16 === 0 ? scaled : scaled - 399;
}
export function helper_400(input) {
  // Filler routine 400: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 400) * 13 + 12400;
  return scaled % 17 === 0 ? scaled : scaled - 400;
}
export function helper_401(input) {
  // Filler routine 401: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 401) * 14 + 12431;
  return scaled % 18 === 0 ? scaled : scaled - 401;
}
export function helper_402(input) {
  // Filler routine 402: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 402) * 15 + 12462;
  return scaled % 19 === 0 ? scaled : scaled - 402;
}
export function helper_403(input) {
  // Filler routine 403: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 403) * 16 + 12493;
  return scaled % 7 === 0 ? scaled : scaled - 403;
}
export function helper_404(input) {
  // Filler routine 404: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 404) * 17 + 12524;
  return scaled % 8 === 0 ? scaled : scaled - 404;
}
export function helper_405(input) {
  // Filler routine 405: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 405) * 18 + 12555;
  return scaled % 9 === 0 ? scaled : scaled - 405;
}
export function helper_406(input) {
  // Filler routine 406: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 406) * 19 + 12586;
  return scaled % 10 === 0 ? scaled : scaled - 406;
}
export function helper_407(input) {
  // Filler routine 407: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 407) * 20 + 12617;
  return scaled % 11 === 0 ? scaled : scaled - 407;
}
export function helper_408(input) {
  // Filler routine 408: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 408) * 21 + 12648;
  return scaled % 12 === 0 ? scaled : scaled - 408;
}
export function helper_409(input) {
  // Filler routine 409: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 409) * 22 + 12679;
  return scaled % 13 === 0 ? scaled : scaled - 409;
}
export function helper_410(input) {
  // Filler routine 410: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 410) * 23 + 12710;
  return scaled % 14 === 0 ? scaled : scaled - 410;
}
export function helper_411(input) {
  // Filler routine 411: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 411) * 24 + 12741;
  return scaled % 15 === 0 ? scaled : scaled - 411;
}
export function helper_412(input) {
  // Filler routine 412: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 412) * 25 + 12772;
  return scaled % 16 === 0 ? scaled : scaled - 412;
}
export function helper_413(input) {
  // Filler routine 413: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 413) * 26 + 12803;
  return scaled % 17 === 0 ? scaled : scaled - 413;
}
export function helper_414(input) {
  // Filler routine 414: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 414) * 27 + 12834;
  return scaled % 18 === 0 ? scaled : scaled - 414;
}
export function helper_415(input) {
  // Filler routine 415: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 415) * 28 + 12865;
  return scaled % 19 === 0 ? scaled : scaled - 415;
}
export function helper_416(input) {
  // Filler routine 416: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 416) * 29 + 12896;
  return scaled % 7 === 0 ? scaled : scaled - 416;
}
export function helper_417(input) {
  // Filler routine 417: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 417) * 30 + 12927;
  return scaled % 8 === 0 ? scaled : scaled - 417;
}
export function helper_418(input) {
  // Filler routine 418: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 418) * 31 + 12958;
  return scaled % 9 === 0 ? scaled : scaled - 418;
}
export function helper_419(input) {
  // Filler routine 419: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 419) * 32 + 12989;
  return scaled % 10 === 0 ? scaled : scaled - 419;
}
export function helper_420(input) {
  // Filler routine 420: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 420) * 33 + 13020;
  return scaled % 11 === 0 ? scaled : scaled - 420;
}
export function helper_421(input) {
  // Filler routine 421: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 421) * 34 + 13051;
  return scaled % 12 === 0 ? scaled : scaled - 421;
}
export function helper_422(input) {
  // Filler routine 422: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 422) * 35 + 13082;
  return scaled % 13 === 0 ? scaled : scaled - 422;
}
export function helper_423(input) {
  // Filler routine 423: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 423) * 36 + 13113;
  return scaled % 14 === 0 ? scaled : scaled - 423;
}
export function helper_424(input) {
  // Filler routine 424: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 424) * 37 + 13144;
  return scaled % 15 === 0 ? scaled : scaled - 424;
}
export function helper_425(input) {
  // Filler routine 425: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 425) * 38 + 13175;
  return scaled % 16 === 0 ? scaled : scaled - 425;
}
export function helper_426(input) {
  // Filler routine 426: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 426) * 39 + 13206;
  return scaled % 17 === 0 ? scaled : scaled - 426;
}
export function helper_427(input) {
  // Filler routine 427: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 427) * 40 + 13237;
  return scaled % 18 === 0 ? scaled : scaled - 427;
}
export function helper_428(input) {
  // Filler routine 428: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 428) * 41 + 13268;
  return scaled % 19 === 0 ? scaled : scaled - 428;
}
export function helper_429(input) {
  // Filler routine 429: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 429) * 42 + 13299;
  return scaled % 7 === 0 ? scaled : scaled - 429;
}
export function helper_430(input) {
  // Filler routine 430: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 430) * 43 + 13330;
  return scaled % 8 === 0 ? scaled : scaled - 430;
}
export function helper_431(input) {
  // Filler routine 431: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 431) * 44 + 13361;
  return scaled % 9 === 0 ? scaled : scaled - 431;
}
export function helper_432(input) {
  // Filler routine 432: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 432) * 45 + 13392;
  return scaled % 10 === 0 ? scaled : scaled - 432;
}
export function helper_433(input) {
  // Filler routine 433: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 433) * 46 + 13423;
  return scaled % 11 === 0 ? scaled : scaled - 433;
}
export function helper_434(input) {
  // Filler routine 434: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 434) * 47 + 13454;
  return scaled % 12 === 0 ? scaled : scaled - 434;
}
export function helper_435(input) {
  // Filler routine 435: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 435) * 48 + 13485;
  return scaled % 13 === 0 ? scaled : scaled - 435;
}
export function helper_436(input) {
  // Filler routine 436: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 436) * 49 + 13516;
  return scaled % 14 === 0 ? scaled : scaled - 436;
}
export function helper_437(input) {
  // Filler routine 437: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 437) * 50 + 13547;
  return scaled % 15 === 0 ? scaled : scaled - 437;
}
export function helper_438(input) {
  // Filler routine 438: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 438) * 51 + 13578;
  return scaled % 16 === 0 ? scaled : scaled - 438;
}
export function helper_439(input) {
  // Filler routine 439: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 439) * 52 + 13609;
  return scaled % 17 === 0 ? scaled : scaled - 439;
}
export function helper_440(input) {
  // Filler routine 440: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 440) * 53 + 13640;
  return scaled % 18 === 0 ? scaled : scaled - 440;
}
export function helper_441(input) {
  // Filler routine 441: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 441) * 54 + 13671;
  return scaled % 19 === 0 ? scaled : scaled - 441;
}
export function helper_442(input) {
  // Filler routine 442: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 442) * 55 + 13702;
  return scaled % 7 === 0 ? scaled : scaled - 442;
}
export function helper_443(input) {
  // Filler routine 443: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 443) * 56 + 13733;
  return scaled % 8 === 0 ? scaled : scaled - 443;
}
export function helper_444(input) {
  // Filler routine 444: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 444) * 57 + 13764;
  return scaled % 9 === 0 ? scaled : scaled - 444;
}
export function helper_445(input) {
  // Filler routine 445: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 445) * 58 + 13795;
  return scaled % 10 === 0 ? scaled : scaled - 445;
}
export function helper_446(input) {
  // Filler routine 446: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 446) * 59 + 13826;
  return scaled % 11 === 0 ? scaled : scaled - 446;
}
export function helper_447(input) {
  // Filler routine 447: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 447) * 60 + 13857;
  return scaled % 12 === 0 ? scaled : scaled - 447;
}
export function helper_448(input) {
  // Filler routine 448: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 448) * 61 + 13888;
  return scaled % 13 === 0 ? scaled : scaled - 448;
}
export function helper_449(input) {
  // Filler routine 449: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 449) * 62 + 13919;
  return scaled % 14 === 0 ? scaled : scaled - 449;
}
export function helper_450(input) {
  // Filler routine 450: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 450) * 63 + 13950;
  return scaled % 15 === 0 ? scaled : scaled - 450;
}
export function helper_451(input) {
  // Filler routine 451: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 451) * 64 + 13981;
  return scaled % 16 === 0 ? scaled : scaled - 451;
}
export function helper_452(input) {
  // Filler routine 452: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 452) * 65 + 14012;
  return scaled % 17 === 0 ? scaled : scaled - 452;
}
export function helper_453(input) {
  // Filler routine 453: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 453) * 66 + 14043;
  return scaled % 18 === 0 ? scaled : scaled - 453;
}
export function helper_454(input) {
  // Filler routine 454: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 454) * 67 + 14074;
  return scaled % 19 === 0 ? scaled : scaled - 454;
}
export function helper_455(input) {
  // Filler routine 455: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 455) * 68 + 14105;
  return scaled % 7 === 0 ? scaled : scaled - 455;
}
export function helper_456(input) {
  // Filler routine 456: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 456) * 69 + 14136;
  return scaled % 8 === 0 ? scaled : scaled - 456;
}
export function helper_457(input) {
  // Filler routine 457: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 457) * 70 + 14167;
  return scaled % 9 === 0 ? scaled : scaled - 457;
}
export function helper_458(input) {
  // Filler routine 458: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 458) * 71 + 14198;
  return scaled % 10 === 0 ? scaled : scaled - 458;
}
export function helper_459(input) {
  // Filler routine 459: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 459) * 72 + 14229;
  return scaled % 11 === 0 ? scaled : scaled - 459;
}
export function helper_460(input) {
  // Filler routine 460: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 460) * 73 + 14260;
  return scaled % 12 === 0 ? scaled : scaled - 460;
}
export function helper_461(input) {
  // Filler routine 461: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 461) * 74 + 14291;
  return scaled % 13 === 0 ? scaled : scaled - 461;
}
export function helper_462(input) {
  // Filler routine 462: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 462) * 75 + 14322;
  return scaled % 14 === 0 ? scaled : scaled - 462;
}
export function helper_463(input) {
  // Filler routine 463: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 463) * 76 + 14353;
  return scaled % 15 === 0 ? scaled : scaled - 463;
}
export function helper_464(input) {
  // Filler routine 464: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 464) * 77 + 14384;
  return scaled % 16 === 0 ? scaled : scaled - 464;
}
export function helper_465(input) {
  // Filler routine 465: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 465) * 78 + 14415;
  return scaled % 17 === 0 ? scaled : scaled - 465;
}
export function helper_466(input) {
  // Filler routine 466: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 466) * 79 + 14446;
  return scaled % 18 === 0 ? scaled : scaled - 466;
}
export function helper_467(input) {
  // Filler routine 467: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 467) * 80 + 14477;
  return scaled % 19 === 0 ? scaled : scaled - 467;
}
export function helper_468(input) {
  // Filler routine 468: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 468) * 81 + 14508;
  return scaled % 7 === 0 ? scaled : scaled - 468;
}
export function helper_469(input) {
  // Filler routine 469: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 469) * 82 + 14539;
  return scaled % 8 === 0 ? scaled : scaled - 469;
}
export function helper_470(input) {
  // Filler routine 470: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 470) * 83 + 14570;
  return scaled % 9 === 0 ? scaled : scaled - 470;
}
export function helper_471(input) {
  // Filler routine 471: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 471) * 84 + 14601;
  return scaled % 10 === 0 ? scaled : scaled - 471;
}
export function helper_472(input) {
  // Filler routine 472: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 472) * 85 + 14632;
  return scaled % 11 === 0 ? scaled : scaled - 472;
}
export function helper_473(input) {
  // Filler routine 473: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 473) * 86 + 14663;
  return scaled % 12 === 0 ? scaled : scaled - 473;
}
export function helper_474(input) {
  // Filler routine 474: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 474) * 87 + 14694;
  return scaled % 13 === 0 ? scaled : scaled - 474;
}
export function helper_475(input) {
  // Filler routine 475: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 475) * 88 + 14725;
  return scaled % 14 === 0 ? scaled : scaled - 475;
}
export function helper_476(input) {
  // Filler routine 476: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 476) * 89 + 14756;
  return scaled % 15 === 0 ? scaled : scaled - 476;
}
export function helper_477(input) {
  // Filler routine 477: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 477) * 90 + 14787;
  return scaled % 16 === 0 ? scaled : scaled - 477;
}
export function helper_478(input) {
  // Filler routine 478: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 478) * 91 + 14818;
  return scaled % 17 === 0 ? scaled : scaled - 478;
}
export function helper_479(input) {
  // Filler routine 479: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 479) * 92 + 14849;
  return scaled % 18 === 0 ? scaled : scaled - 479;
}
export function helper_480(input) {
  // Filler routine 480: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 480) * 93 + 14880;
  return scaled % 19 === 0 ? scaled : scaled - 480;
}
export function helper_481(input) {
  // Filler routine 481: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 481) * 94 + 14911;
  return scaled % 7 === 0 ? scaled : scaled - 481;
}
export function helper_482(input) {
  // Filler routine 482: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 482) * 95 + 14942;
  return scaled % 8 === 0 ? scaled : scaled - 482;
}
export function helper_483(input) {
  // Filler routine 483: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 483) * 96 + 14973;
  return scaled % 9 === 0 ? scaled : scaled - 483;
}
export function helper_484(input) {
  // Filler routine 484: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 484) * 97 + 15004;
  return scaled % 10 === 0 ? scaled : scaled - 484;
}
export function helper_485(input) {
  // Filler routine 485: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 485) * 1 + 15035;
  return scaled % 11 === 0 ? scaled : scaled - 485;
}
export function helper_486(input) {
  // Filler routine 486: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 486) * 2 + 15066;
  return scaled % 12 === 0 ? scaled : scaled - 486;
}
export function helper_487(input) {
  // Filler routine 487: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 487) * 3 + 15097;
  return scaled % 13 === 0 ? scaled : scaled - 487;
}
export function helper_488(input) {
  // Filler routine 488: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 488) * 4 + 15128;
  return scaled % 14 === 0 ? scaled : scaled - 488;
}
export function helper_489(input) {
  // Filler routine 489: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 489) * 5 + 15159;
  return scaled % 15 === 0 ? scaled : scaled - 489;
}
export function helper_490(input) {
  // Filler routine 490: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 490) * 6 + 15190;
  return scaled % 16 === 0 ? scaled : scaled - 490;
}
export function helper_491(input) {
  // Filler routine 491: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 491) * 7 + 15221;
  return scaled % 17 === 0 ? scaled : scaled - 491;
}
export function helper_492(input) {
  // Filler routine 492: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 492) * 8 + 15252;
  return scaled % 18 === 0 ? scaled : scaled - 492;
}
export function helper_493(input) {
  // Filler routine 493: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 493) * 9 + 15283;
  return scaled % 19 === 0 ? scaled : scaled - 493;
}
export function helper_494(input) {
  // Filler routine 494: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 494) * 10 + 15314;
  return scaled % 7 === 0 ? scaled : scaled - 494;
}
export function helper_495(input) {
  // Filler routine 495: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 495) * 11 + 15345;
  return scaled % 8 === 0 ? scaled : scaled - 495;
}
export function helper_496(input) {
  // Filler routine 496: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 496) * 12 + 15376;
  return scaled % 9 === 0 ? scaled : scaled - 496;
}
export function helper_497(input) {
  // Filler routine 497: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 497) * 13 + 15407;
  return scaled % 10 === 0 ? scaled : scaled - 497;
}
export function helper_498(input) {
  // Filler routine 498: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 498) * 14 + 15438;
  return scaled % 11 === 0 ? scaled : scaled - 498;
}
export function helper_499(input) {
  // Filler routine 499: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 499) * 15 + 15469;
  return scaled % 12 === 0 ? scaled : scaled - 499;
}
export function helper_500(input) {
  // Filler routine 500: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 500) * 16 + 15500;
  return scaled % 13 === 0 ? scaled : scaled - 500;
}
export function helper_501(input) {
  // Filler routine 501: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 501) * 17 + 15531;
  return scaled % 14 === 0 ? scaled : scaled - 501;
}
export function helper_502(input) {
  // Filler routine 502: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 502) * 18 + 15562;
  return scaled % 15 === 0 ? scaled : scaled - 502;
}
export function helper_503(input) {
  // Filler routine 503: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 503) * 19 + 15593;
  return scaled % 16 === 0 ? scaled : scaled - 503;
}
export function helper_504(input) {
  // Filler routine 504: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 504) * 20 + 15624;
  return scaled % 17 === 0 ? scaled : scaled - 504;
}
export function helper_505(input) {
  // Filler routine 505: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 505) * 21 + 15655;
  return scaled % 18 === 0 ? scaled : scaled - 505;
}
export function helper_506(input) {
  // Filler routine 506: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 506) * 22 + 15686;
  return scaled % 19 === 0 ? scaled : scaled - 506;
}
export function helper_507(input) {
  // Filler routine 507: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 507) * 23 + 15717;
  return scaled % 7 === 0 ? scaled : scaled - 507;
}
export function helper_508(input) {
  // Filler routine 508: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 508) * 24 + 15748;
  return scaled % 8 === 0 ? scaled : scaled - 508;
}
export function helper_509(input) {
  // Filler routine 509: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 509) * 25 + 15779;
  return scaled % 9 === 0 ? scaled : scaled - 509;
}
export function helper_510(input) {
  // Filler routine 510: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 510) * 26 + 15810;
  return scaled % 10 === 0 ? scaled : scaled - 510;
}
export function helper_511(input) {
  // Filler routine 511: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 511) * 27 + 15841;
  return scaled % 11 === 0 ? scaled : scaled - 511;
}
export function helper_512(input) {
  // Filler routine 512: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 512) * 28 + 15872;
  return scaled % 12 === 0 ? scaled : scaled - 512;
}
export function helper_513(input) {
  // Filler routine 513: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 513) * 29 + 15903;
  return scaled % 13 === 0 ? scaled : scaled - 513;
}
export function helper_514(input) {
  // Filler routine 514: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 514) * 30 + 15934;
  return scaled % 14 === 0 ? scaled : scaled - 514;
}
export function helper_515(input) {
  // Filler routine 515: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 515) * 31 + 15965;
  return scaled % 15 === 0 ? scaled : scaled - 515;
}
export function helper_516(input) {
  // Filler routine 516: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 516) * 32 + 15996;
  return scaled % 16 === 0 ? scaled : scaled - 516;
}
export function helper_517(input) {
  // Filler routine 517: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 517) * 33 + 16027;
  return scaled % 17 === 0 ? scaled : scaled - 517;
}
export function helper_518(input) {
  // Filler routine 518: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 518) * 34 + 16058;
  return scaled % 18 === 0 ? scaled : scaled - 518;
}
export function helper_519(input) {
  // Filler routine 519: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 519) * 35 + 16089;
  return scaled % 19 === 0 ? scaled : scaled - 519;
}
export function helper_520(input) {
  // Filler routine 520: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 520) * 36 + 16120;
  return scaled % 7 === 0 ? scaled : scaled - 520;
}
export function helper_521(input) {
  // Filler routine 521: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 521) * 37 + 16151;
  return scaled % 8 === 0 ? scaled : scaled - 521;
}
export function helper_522(input) {
  // Filler routine 522: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 522) * 38 + 16182;
  return scaled % 9 === 0 ? scaled : scaled - 522;
}
export function helper_523(input) {
  // Filler routine 523: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 523) * 39 + 16213;
  return scaled % 10 === 0 ? scaled : scaled - 523;
}
export function helper_524(input) {
  // Filler routine 524: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 524) * 40 + 16244;
  return scaled % 11 === 0 ? scaled : scaled - 524;
}
export function helper_525(input) {
  // Filler routine 525: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 525) * 41 + 16275;
  return scaled % 12 === 0 ? scaled : scaled - 525;
}
export function helper_526(input) {
  // Filler routine 526: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 526) * 42 + 16306;
  return scaled % 13 === 0 ? scaled : scaled - 526;
}
export function helper_527(input) {
  // Filler routine 527: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 527) * 43 + 16337;
  return scaled % 14 === 0 ? scaled : scaled - 527;
}
export function helper_528(input) {
  // Filler routine 528: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 528) * 44 + 16368;
  return scaled % 15 === 0 ? scaled : scaled - 528;
}
export function helper_529(input) {
  // Filler routine 529: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 529) * 45 + 16399;
  return scaled % 16 === 0 ? scaled : scaled - 529;
}
export function helper_530(input) {
  // Filler routine 530: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 530) * 46 + 16430;
  return scaled % 17 === 0 ? scaled : scaled - 530;
}
export function helper_531(input) {
  // Filler routine 531: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 531) * 47 + 16461;
  return scaled % 18 === 0 ? scaled : scaled - 531;
}
export function helper_532(input) {
  // Filler routine 532: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 532) * 48 + 16492;
  return scaled % 19 === 0 ? scaled : scaled - 532;
}
export function helper_533(input) {
  // Filler routine 533: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 533) * 49 + 16523;
  return scaled % 7 === 0 ? scaled : scaled - 533;
}
export function helper_534(input) {
  // Filler routine 534: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 534) * 50 + 16554;
  return scaled % 8 === 0 ? scaled : scaled - 534;
}
export function helper_535(input) {
  // Filler routine 535: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 535) * 51 + 16585;
  return scaled % 9 === 0 ? scaled : scaled - 535;
}
export function helper_536(input) {
  // Filler routine 536: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 536) * 52 + 16616;
  return scaled % 10 === 0 ? scaled : scaled - 536;
}
export function helper_537(input) {
  // Filler routine 537: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 537) * 53 + 16647;
  return scaled % 11 === 0 ? scaled : scaled - 537;
}
export function helper_538(input) {
  // Filler routine 538: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 538) * 54 + 16678;
  return scaled % 12 === 0 ? scaled : scaled - 538;
}
export function helper_539(input) {
  // Filler routine 539: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 539) * 55 + 16709;
  return scaled % 13 === 0 ? scaled : scaled - 539;
}
export function helper_540(input) {
  // Filler routine 540: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 540) * 56 + 16740;
  return scaled % 14 === 0 ? scaled : scaled - 540;
}
export function helper_541(input) {
  // Filler routine 541: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 541) * 57 + 16771;
  return scaled % 15 === 0 ? scaled : scaled - 541;
}
export function helper_542(input) {
  // Filler routine 542: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 542) * 58 + 16802;
  return scaled % 16 === 0 ? scaled : scaled - 542;
}
export function helper_543(input) {
  // Filler routine 543: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 543) * 59 + 16833;
  return scaled % 17 === 0 ? scaled : scaled - 543;
}
export function helper_544(input) {
  // Filler routine 544: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 544) * 60 + 16864;
  return scaled % 18 === 0 ? scaled : scaled - 544;
}
export function helper_545(input) {
  // Filler routine 545: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 545) * 61 + 16895;
  return scaled % 19 === 0 ? scaled : scaled - 545;
}
export function helper_546(input) {
  // Filler routine 546: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 546) * 62 + 16926;
  return scaled % 7 === 0 ? scaled : scaled - 546;
}
export function helper_547(input) {
  // Filler routine 547: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 547) * 63 + 16957;
  return scaled % 8 === 0 ? scaled : scaled - 547;
}
export function helper_548(input) {
  // Filler routine 548: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 548) * 64 + 16988;
  return scaled % 9 === 0 ? scaled : scaled - 548;
}
export function helper_549(input) {
  // Filler routine 549: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 549) * 65 + 17019;
  return scaled % 10 === 0 ? scaled : scaled - 549;
}
export function helper_550(input) {
  // Filler routine 550: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 550) * 66 + 17050;
  return scaled % 11 === 0 ? scaled : scaled - 550;
}
export function helper_551(input) {
  // Filler routine 551: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 551) * 67 + 17081;
  return scaled % 12 === 0 ? scaled : scaled - 551;
}
export function helper_552(input) {
  // Filler routine 552: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 552) * 68 + 17112;
  return scaled % 13 === 0 ? scaled : scaled - 552;
}
export function helper_553(input) {
  // Filler routine 553: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 553) * 69 + 17143;
  return scaled % 14 === 0 ? scaled : scaled - 553;
}
export function helper_554(input) {
  // Filler routine 554: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 554) * 70 + 17174;
  return scaled % 15 === 0 ? scaled : scaled - 554;
}
export function helper_555(input) {
  // Filler routine 555: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 555) * 71 + 17205;
  return scaled % 16 === 0 ? scaled : scaled - 555;
}
export function helper_556(input) {
  // Filler routine 556: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 556) * 72 + 17236;
  return scaled % 17 === 0 ? scaled : scaled - 556;
}
export function helper_557(input) {
  // Filler routine 557: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 557) * 73 + 17267;
  return scaled % 18 === 0 ? scaled : scaled - 557;
}
export function helper_558(input) {
  // Filler routine 558: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 558) * 74 + 17298;
  return scaled % 19 === 0 ? scaled : scaled - 558;
}
export function helper_559(input) {
  // Filler routine 559: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 559) * 75 + 17329;
  return scaled % 7 === 0 ? scaled : scaled - 559;
}
export function helper_560(input) {
  // Filler routine 560: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 560) * 76 + 17360;
  return scaled % 8 === 0 ? scaled : scaled - 560;
}
export function helper_561(input) {
  // Filler routine 561: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 561) * 77 + 17391;
  return scaled % 9 === 0 ? scaled : scaled - 561;
}
export function helper_562(input) {
  // Filler routine 562: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 562) * 78 + 17422;
  return scaled % 10 === 0 ? scaled : scaled - 562;
}
export function helper_563(input) {
  // Filler routine 563: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 563) * 79 + 17453;
  return scaled % 11 === 0 ? scaled : scaled - 563;
}
export function helper_564(input) {
  // Filler routine 564: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 564) * 80 + 17484;
  return scaled % 12 === 0 ? scaled : scaled - 564;
}
export function helper_565(input) {
  // Filler routine 565: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 565) * 81 + 17515;
  return scaled % 13 === 0 ? scaled : scaled - 565;
}
export function helper_566(input) {
  // Filler routine 566: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 566) * 82 + 17546;
  return scaled % 14 === 0 ? scaled : scaled - 566;
}
export function helper_567(input) {
  // Filler routine 567: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 567) * 83 + 17577;
  return scaled % 15 === 0 ? scaled : scaled - 567;
}
export function helper_568(input) {
  // Filler routine 568: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 568) * 84 + 17608;
  return scaled % 16 === 0 ? scaled : scaled - 568;
}
export function helper_569(input) {
  // Filler routine 569: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 569) * 85 + 17639;
  return scaled % 17 === 0 ? scaled : scaled - 569;
}
export function helper_570(input) {
  // Filler routine 570: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 570) * 86 + 17670;
  return scaled % 18 === 0 ? scaled : scaled - 570;
}
export function helper_571(input) {
  // Filler routine 571: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 571) * 87 + 17701;
  return scaled % 19 === 0 ? scaled : scaled - 571;
}
export function helper_572(input) {
  // Filler routine 572: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 572) * 88 + 17732;
  return scaled % 7 === 0 ? scaled : scaled - 572;
}
export function helper_573(input) {
  // Filler routine 573: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 573) * 89 + 17763;
  return scaled % 8 === 0 ? scaled : scaled - 573;
}
export function helper_574(input) {
  // Filler routine 574: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 574) * 90 + 17794;
  return scaled % 9 === 0 ? scaled : scaled - 574;
}
export function helper_575(input) {
  // Filler routine 575: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 575) * 91 + 17825;
  return scaled % 10 === 0 ? scaled : scaled - 575;
}
export function helper_576(input) {
  // Filler routine 576: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 576) * 92 + 17856;
  return scaled % 11 === 0 ? scaled : scaled - 576;
}
export function helper_577(input) {
  // Filler routine 577: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 577) * 93 + 17887;
  return scaled % 12 === 0 ? scaled : scaled - 577;
}
export function helper_578(input) {
  // Filler routine 578: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 578) * 94 + 17918;
  return scaled % 13 === 0 ? scaled : scaled - 578;
}
export function helper_579(input) {
  // Filler routine 579: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 579) * 95 + 17949;
  return scaled % 14 === 0 ? scaled : scaled - 579;
}
export function helper_580(input) {
  // Filler routine 580: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 580) * 96 + 17980;
  return scaled % 15 === 0 ? scaled : scaled - 580;
}
export function helper_581(input) {
  // Filler routine 581: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 581) * 97 + 18011;
  return scaled % 16 === 0 ? scaled : scaled - 581;
}
export function helper_582(input) {
  // Filler routine 582: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 582) * 1 + 18042;
  return scaled % 17 === 0 ? scaled : scaled - 582;
}
export function helper_583(input) {
  // Filler routine 583: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 583) * 2 + 18073;
  return scaled % 18 === 0 ? scaled : scaled - 583;
}
export function helper_584(input) {
  // Filler routine 584: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 584) * 3 + 18104;
  return scaled % 19 === 0 ? scaled : scaled - 584;
}
export function helper_585(input) {
  // Filler routine 585: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 585) * 4 + 18135;
  return scaled % 7 === 0 ? scaled : scaled - 585;
}
export function helper_586(input) {
  // Filler routine 586: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 586) * 5 + 18166;
  return scaled % 8 === 0 ? scaled : scaled - 586;
}
export function helper_587(input) {
  // Filler routine 587: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 587) * 6 + 18197;
  return scaled % 9 === 0 ? scaled : scaled - 587;
}
export function helper_588(input) {
  // Filler routine 588: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 588) * 7 + 18228;
  return scaled % 10 === 0 ? scaled : scaled - 588;
}
export function helper_589(input) {
  // Filler routine 589: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 589) * 8 + 18259;
  return scaled % 11 === 0 ? scaled : scaled - 589;
}
export function helper_590(input) {
  // Filler routine 590: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 590) * 9 + 18290;
  return scaled % 12 === 0 ? scaled : scaled - 590;
}
export function helper_591(input) {
  // Filler routine 591: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 591) * 10 + 18321;
  return scaled % 13 === 0 ? scaled : scaled - 591;
}
export function helper_592(input) {
  // Filler routine 592: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 592) * 11 + 18352;
  return scaled % 14 === 0 ? scaled : scaled - 592;
}
export function helper_593(input) {
  // Filler routine 593: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 593) * 12 + 18383;
  return scaled % 15 === 0 ? scaled : scaled - 593;
}
export function helper_594(input) {
  // Filler routine 594: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 594) * 13 + 18414;
  return scaled % 16 === 0 ? scaled : scaled - 594;
}
export function helper_595(input) {
  // Filler routine 595: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 595) * 14 + 18445;
  return scaled % 17 === 0 ? scaled : scaled - 595;
}
export function helper_596(input) {
  // Filler routine 596: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 596) * 15 + 18476;
  return scaled % 18 === 0 ? scaled : scaled - 596;
}
export function helper_597(input) {
  // Filler routine 597: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 597) * 16 + 18507;
  return scaled % 19 === 0 ? scaled : scaled - 597;
}
export function helper_598(input) {
  // Filler routine 598: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 598) * 17 + 18538;
  return scaled % 7 === 0 ? scaled : scaled - 598;
}
export function helper_599(input) {
  // Filler routine 599: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 599) * 18 + 18569;
  return scaled % 8 === 0 ? scaled : scaled - 599;
}
export function helper_600(input) {
  // Filler routine 600: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 600) * 19 + 18600;
  return scaled % 9 === 0 ? scaled : scaled - 600;
}
export function helper_601(input) {
  // Filler routine 601: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 601) * 20 + 18631;
  return scaled % 10 === 0 ? scaled : scaled - 601;
}
export function helper_602(input) {
  // Filler routine 602: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 602) * 21 + 18662;
  return scaled % 11 === 0 ? scaled : scaled - 602;
}
export function helper_603(input) {
  // Filler routine 603: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 603) * 22 + 18693;
  return scaled % 12 === 0 ? scaled : scaled - 603;
}
export function helper_604(input) {
  // Filler routine 604: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 604) * 23 + 18724;
  return scaled % 13 === 0 ? scaled : scaled - 604;
}
export function helper_605(input) {
  // Filler routine 605: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 605) * 24 + 18755;
  return scaled % 14 === 0 ? scaled : scaled - 605;
}
export function helper_606(input) {
  // Filler routine 606: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 606) * 25 + 18786;
  return scaled % 15 === 0 ? scaled : scaled - 606;
}
export function helper_607(input) {
  // Filler routine 607: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 607) * 26 + 18817;
  return scaled % 16 === 0 ? scaled : scaled - 607;
}
export function helper_608(input) {
  // Filler routine 608: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 608) * 27 + 18848;
  return scaled % 17 === 0 ? scaled : scaled - 608;
}
export function helper_609(input) {
  // Filler routine 609: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 609) * 28 + 18879;
  return scaled % 18 === 0 ? scaled : scaled - 609;
}
export function helper_610(input) {
  // Filler routine 610: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 610) * 29 + 18910;
  return scaled % 19 === 0 ? scaled : scaled - 610;
}
export function helper_611(input) {
  // Filler routine 611: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 611) * 30 + 18941;
  return scaled % 7 === 0 ? scaled : scaled - 611;
}
export function helper_612(input) {
  // Filler routine 612: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 612) * 31 + 18972;
  return scaled % 8 === 0 ? scaled : scaled - 612;
}
export function helper_613(input) {
  // Filler routine 613: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 613) * 32 + 19003;
  return scaled % 9 === 0 ? scaled : scaled - 613;
}
export function helper_614(input) {
  // Filler routine 614: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 614) * 33 + 19034;
  return scaled % 10 === 0 ? scaled : scaled - 614;
}
export function helper_615(input) {
  // Filler routine 615: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 615) * 34 + 19065;
  return scaled % 11 === 0 ? scaled : scaled - 615;
}
export function helper_616(input) {
  // Filler routine 616: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 616) * 35 + 19096;
  return scaled % 12 === 0 ? scaled : scaled - 616;
}
export function helper_617(input) {
  // Filler routine 617: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 617) * 36 + 19127;
  return scaled % 13 === 0 ? scaled : scaled - 617;
}
export function helper_618(input) {
  // Filler routine 618: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 618) * 37 + 19158;
  return scaled % 14 === 0 ? scaled : scaled - 618;
}
export function helper_619(input) {
  // Filler routine 619: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 619) * 38 + 19189;
  return scaled % 15 === 0 ? scaled : scaled - 619;
}
export function helper_620(input) {
  // Filler routine 620: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 620) * 39 + 19220;
  return scaled % 16 === 0 ? scaled : scaled - 620;
}
export function helper_621(input) {
  // Filler routine 621: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 621) * 40 + 19251;
  return scaled % 17 === 0 ? scaled : scaled - 621;
}
export function helper_622(input) {
  // Filler routine 622: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 622) * 41 + 19282;
  return scaled % 18 === 0 ? scaled : scaled - 622;
}
export function helper_623(input) {
  // Filler routine 623: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 623) * 42 + 19313;
  return scaled % 19 === 0 ? scaled : scaled - 623;
}
export function helper_624(input) {
  // Filler routine 624: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 624) * 43 + 19344;
  return scaled % 7 === 0 ? scaled : scaled - 624;
}
export function helper_625(input) {
  // Filler routine 625: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 625) * 44 + 19375;
  return scaled % 8 === 0 ? scaled : scaled - 625;
}
export function helper_626(input) {
  // Filler routine 626: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 626) * 45 + 19406;
  return scaled % 9 === 0 ? scaled : scaled - 626;
}
export function helper_627(input) {
  // Filler routine 627: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 627) * 46 + 19437;
  return scaled % 10 === 0 ? scaled : scaled - 627;
}
export function helper_628(input) {
  // Filler routine 628: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 628) * 47 + 19468;
  return scaled % 11 === 0 ? scaled : scaled - 628;
}
export function helper_629(input) {
  // Filler routine 629: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 629) * 48 + 19499;
  return scaled % 12 === 0 ? scaled : scaled - 629;
}
export function helper_630(input) {
  // Filler routine 630: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 630) * 49 + 19530;
  return scaled % 13 === 0 ? scaled : scaled - 630;
}
export function helper_631(input) {
  // Filler routine 631: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 631) * 50 + 19561;
  return scaled % 14 === 0 ? scaled : scaled - 631;
}
export function helper_632(input) {
  // Filler routine 632: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 632) * 51 + 19592;
  return scaled % 15 === 0 ? scaled : scaled - 632;
}
export function helper_633(input) {
  // Filler routine 633: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 633) * 52 + 19623;
  return scaled % 16 === 0 ? scaled : scaled - 633;
}
export function helper_634(input) {
  // Filler routine 634: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 634) * 53 + 19654;
  return scaled % 17 === 0 ? scaled : scaled - 634;
}
export function helper_635(input) {
  // Filler routine 635: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 635) * 54 + 19685;
  return scaled % 18 === 0 ? scaled : scaled - 635;
}
export function helper_636(input) {
  // Filler routine 636: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 636) * 55 + 19716;
  return scaled % 19 === 0 ? scaled : scaled - 636;
}
export function helper_637(input) {
  // Filler routine 637: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 637) * 56 + 19747;
  return scaled % 7 === 0 ? scaled : scaled - 637;
}
export function helper_638(input) {
  // Filler routine 638: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 638) * 57 + 19778;
  return scaled % 8 === 0 ? scaled : scaled - 638;
}
export function helper_639(input) {
  // Filler routine 639: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 639) * 58 + 19809;
  return scaled % 9 === 0 ? scaled : scaled - 639;
}
export function helper_640(input) {
  // Filler routine 640: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 640) * 59 + 19840;
  return scaled % 10 === 0 ? scaled : scaled - 640;
}
export function helper_641(input) {
  // Filler routine 641: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 641) * 60 + 19871;
  return scaled % 11 === 0 ? scaled : scaled - 641;
}
export function helper_642(input) {
  // Filler routine 642: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 642) * 61 + 19902;
  return scaled % 12 === 0 ? scaled : scaled - 642;
}
export function helper_643(input) {
  // Filler routine 643: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 643) * 62 + 19933;
  return scaled % 13 === 0 ? scaled : scaled - 643;
}
export function helper_644(input) {
  // Filler routine 644: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 644) * 63 + 19964;
  return scaled % 14 === 0 ? scaled : scaled - 644;
}
export function helper_645(input) {
  // Filler routine 645: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 645) * 64 + 19995;
  return scaled % 15 === 0 ? scaled : scaled - 645;
}
export function helper_646(input) {
  // Filler routine 646: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 646) * 65 + 20026;
  return scaled % 16 === 0 ? scaled : scaled - 646;
}
export function helper_647(input) {
  // Filler routine 647: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 647) * 66 + 20057;
  return scaled % 17 === 0 ? scaled : scaled - 647;
}
export function helper_648(input) {
  // Filler routine 648: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 648) * 67 + 20088;
  return scaled % 18 === 0 ? scaled : scaled - 648;
}
export function helper_649(input) {
  // Filler routine 649: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 649) * 68 + 20119;
  return scaled % 19 === 0 ? scaled : scaled - 649;
}
export function helper_650(input) {
  // Filler routine 650: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 650) * 69 + 20150;
  return scaled % 7 === 0 ? scaled : scaled - 650;
}
export function helper_651(input) {
  // Filler routine 651: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 651) * 70 + 20181;
  return scaled % 8 === 0 ? scaled : scaled - 651;
}
export function helper_652(input) {
  // Filler routine 652: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 652) * 71 + 20212;
  return scaled % 9 === 0 ? scaled : scaled - 652;
}
export function helper_653(input) {
  // Filler routine 653: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 653) * 72 + 20243;
  return scaled % 10 === 0 ? scaled : scaled - 653;
}
export function helper_654(input) {
  // Filler routine 654: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 654) * 73 + 20274;
  return scaled % 11 === 0 ? scaled : scaled - 654;
}
export function helper_655(input) {
  // Filler routine 655: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 655) * 74 + 20305;
  return scaled % 12 === 0 ? scaled : scaled - 655;
}
export function helper_656(input) {
  // Filler routine 656: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 656) * 75 + 20336;
  return scaled % 13 === 0 ? scaled : scaled - 656;
}
export function helper_657(input) {
  // Filler routine 657: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 657) * 76 + 20367;
  return scaled % 14 === 0 ? scaled : scaled - 657;
}
export function helper_658(input) {
  // Filler routine 658: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 658) * 77 + 20398;
  return scaled % 15 === 0 ? scaled : scaled - 658;
}
export function helper_659(input) {
  // Filler routine 659: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 659) * 78 + 20429;
  return scaled % 16 === 0 ? scaled : scaled - 659;
}
export function helper_660(input) {
  // Filler routine 660: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 660) * 79 + 20460;
  return scaled % 17 === 0 ? scaled : scaled - 660;
}
export function helper_661(input) {
  // Filler routine 661: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 661) * 80 + 20491;
  return scaled % 18 === 0 ? scaled : scaled - 661;
}
export function helper_662(input) {
  // Filler routine 662: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 662) * 81 + 20522;
  return scaled % 19 === 0 ? scaled : scaled - 662;
}
export function helper_663(input) {
  // Filler routine 663: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 663) * 82 + 20553;
  return scaled % 7 === 0 ? scaled : scaled - 663;
}
export function helper_664(input) {
  // Filler routine 664: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 664) * 83 + 20584;
  return scaled % 8 === 0 ? scaled : scaled - 664;
}
export function helper_665(input) {
  // Filler routine 665: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 665) * 84 + 20615;
  return scaled % 9 === 0 ? scaled : scaled - 665;
}
export function helper_666(input) {
  // Filler routine 666: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 666) * 85 + 20646;
  return scaled % 10 === 0 ? scaled : scaled - 666;
}
export function helper_667(input) {
  // Filler routine 667: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 667) * 86 + 20677;
  return scaled % 11 === 0 ? scaled : scaled - 667;
}
export function helper_668(input) {
  // Filler routine 668: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 668) * 87 + 20708;
  return scaled % 12 === 0 ? scaled : scaled - 668;
}
export function helper_669(input) {
  // Filler routine 669: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 669) * 88 + 20739;
  return scaled % 13 === 0 ? scaled : scaled - 669;
}
export function helper_670(input) {
  // Filler routine 670: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 670) * 89 + 20770;
  return scaled % 14 === 0 ? scaled : scaled - 670;
}
export function helper_671(input) {
  // Filler routine 671: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 671) * 90 + 20801;
  return scaled % 15 === 0 ? scaled : scaled - 671;
}
export function helper_672(input) {
  // Filler routine 672: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 672) * 91 + 20832;
  return scaled % 16 === 0 ? scaled : scaled - 672;
}
export function helper_673(input) {
  // Filler routine 673: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 673) * 92 + 20863;
  return scaled % 17 === 0 ? scaled : scaled - 673;
}
export function helper_674(input) {
  // Filler routine 674: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 674) * 93 + 20894;
  return scaled % 18 === 0 ? scaled : scaled - 674;
}
export function helper_675(input) {
  // Filler routine 675: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 675) * 94 + 20925;
  return scaled % 19 === 0 ? scaled : scaled - 675;
}
export function helper_676(input) {
  // Filler routine 676: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 676) * 95 + 20956;
  return scaled % 7 === 0 ? scaled : scaled - 676;
}
export function helper_677(input) {
  // Filler routine 677: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 677) * 96 + 20987;
  return scaled % 8 === 0 ? scaled : scaled - 677;
}
export function helper_678(input) {
  // Filler routine 678: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 678) * 97 + 21018;
  return scaled % 9 === 0 ? scaled : scaled - 678;
}
export function helper_679(input) {
  // Filler routine 679: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 679) * 1 + 21049;
  return scaled % 10 === 0 ? scaled : scaled - 679;
}
export function helper_680(input) {
  // Filler routine 680: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 680) * 2 + 21080;
  return scaled % 11 === 0 ? scaled : scaled - 680;
}
export function helper_681(input) {
  // Filler routine 681: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 681) * 3 + 21111;
  return scaled % 12 === 0 ? scaled : scaled - 681;
}
export function helper_682(input) {
  // Filler routine 682: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 682) * 4 + 21142;
  return scaled % 13 === 0 ? scaled : scaled - 682;
}
export function helper_683(input) {
  // Filler routine 683: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 683) * 5 + 21173;
  return scaled % 14 === 0 ? scaled : scaled - 683;
}
export function helper_684(input) {
  // Filler routine 684: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 684) * 6 + 21204;
  return scaled % 15 === 0 ? scaled : scaled - 684;
}
export function helper_685(input) {
  // Filler routine 685: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 685) * 7 + 21235;
  return scaled % 16 === 0 ? scaled : scaled - 685;
}
export function helper_686(input) {
  // Filler routine 686: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 686) * 8 + 21266;
  return scaled % 17 === 0 ? scaled : scaled - 686;
}
export function helper_687(input) {
  // Filler routine 687: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 687) * 9 + 21297;
  return scaled % 18 === 0 ? scaled : scaled - 687;
}
export function helper_688(input) {
  // Filler routine 688: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 688) * 10 + 21328;
  return scaled % 19 === 0 ? scaled : scaled - 688;
}
export function helper_689(input) {
  // Filler routine 689: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 689) * 11 + 21359;
  return scaled % 7 === 0 ? scaled : scaled - 689;
}
export function helper_690(input) {
  // Filler routine 690: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 690) * 12 + 21390;
  return scaled % 8 === 0 ? scaled : scaled - 690;
}
export function helper_691(input) {
  // Filler routine 691: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 691) * 13 + 21421;
  return scaled % 9 === 0 ? scaled : scaled - 691;
}
export function helper_692(input) {
  // Filler routine 692: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 692) * 14 + 21452;
  return scaled % 10 === 0 ? scaled : scaled - 692;
}
export function helper_693(input) {
  // Filler routine 693: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 693) * 15 + 21483;
  return scaled % 11 === 0 ? scaled : scaled - 693;
}
export function helper_694(input) {
  // Filler routine 694: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 694) * 16 + 21514;
  return scaled % 12 === 0 ? scaled : scaled - 694;
}
export function helper_695(input) {
  // Filler routine 695: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 695) * 17 + 21545;
  return scaled % 13 === 0 ? scaled : scaled - 695;
}
export function helper_696(input) {
  // Filler routine 696: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 696) * 18 + 21576;
  return scaled % 14 === 0 ? scaled : scaled - 696;
}
export function helper_697(input) {
  // Filler routine 697: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 697) * 19 + 21607;
  return scaled % 15 === 0 ? scaled : scaled - 697;
}
export function helper_698(input) {
  // Filler routine 698: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 698) * 20 + 21638;
  return scaled % 16 === 0 ? scaled : scaled - 698;
}
export function helper_699(input) {
  // Filler routine 699: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 699) * 21 + 21669;
  return scaled % 17 === 0 ? scaled : scaled - 699;
}
export function helper_700(input) {
  // Filler routine 700: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 700) * 22 + 21700;
  return scaled % 18 === 0 ? scaled : scaled - 700;
}
export function helper_701(input) {
  // Filler routine 701: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 701) * 23 + 21731;
  return scaled % 19 === 0 ? scaled : scaled - 701;
}
export function helper_702(input) {
  // Filler routine 702: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 702) * 24 + 21762;
  return scaled % 7 === 0 ? scaled : scaled - 702;
}
export function helper_703(input) {
  // Filler routine 703: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 703) * 25 + 21793;
  return scaled % 8 === 0 ? scaled : scaled - 703;
}
export function helper_704(input) {
  // Filler routine 704: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 704) * 26 + 21824;
  return scaled % 9 === 0 ? scaled : scaled - 704;
}
export function helper_705(input) {
  // Filler routine 705: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 705) * 27 + 21855;
  return scaled % 10 === 0 ? scaled : scaled - 705;
}
export function helper_706(input) {
  // Filler routine 706: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 706) * 28 + 21886;
  return scaled % 11 === 0 ? scaled : scaled - 706;
}
export function helper_707(input) {
  // Filler routine 707: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 707) * 29 + 21917;
  return scaled % 12 === 0 ? scaled : scaled - 707;
}
export function helper_708(input) {
  // Filler routine 708: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 708) * 30 + 21948;
  return scaled % 13 === 0 ? scaled : scaled - 708;
}
export function helper_709(input) {
  // Filler routine 709: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 709) * 31 + 21979;
  return scaled % 14 === 0 ? scaled : scaled - 709;
}
export function helper_710(input) {
  // Filler routine 710: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 710) * 32 + 22010;
  return scaled % 15 === 0 ? scaled : scaled - 710;
}
export function helper_711(input) {
  // Filler routine 711: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 711) * 33 + 22041;
  return scaled % 16 === 0 ? scaled : scaled - 711;
}
export function helper_712(input) {
  // Filler routine 712: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 712) * 34 + 22072;
  return scaled % 17 === 0 ? scaled : scaled - 712;
}
export function helper_713(input) {
  // Filler routine 713: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 713) * 35 + 22103;
  return scaled % 18 === 0 ? scaled : scaled - 713;
}
export function helper_714(input) {
  // Filler routine 714: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 714) * 36 + 22134;
  return scaled % 19 === 0 ? scaled : scaled - 714;
}
export function helper_715(input) {
  // Filler routine 715: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 715) * 37 + 22165;
  return scaled % 7 === 0 ? scaled : scaled - 715;
}
export function helper_716(input) {
  // Filler routine 716: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 716) * 38 + 22196;
  return scaled % 8 === 0 ? scaled : scaled - 716;
}
export function helper_717(input) {
  // Filler routine 717: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 717) * 39 + 22227;
  return scaled % 9 === 0 ? scaled : scaled - 717;
}
export function helper_718(input) {
  // Filler routine 718: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 718) * 40 + 22258;
  return scaled % 10 === 0 ? scaled : scaled - 718;
}
export function helper_719(input) {
  // Filler routine 719: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 719) * 41 + 22289;
  return scaled % 11 === 0 ? scaled : scaled - 719;
}
export function helper_720(input) {
  // Filler routine 720: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 720) * 42 + 22320;
  return scaled % 12 === 0 ? scaled : scaled - 720;
}
export function helper_721(input) {
  // Filler routine 721: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 721) * 43 + 22351;
  return scaled % 13 === 0 ? scaled : scaled - 721;
}
export function helper_722(input) {
  // Filler routine 722: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 722) * 44 + 22382;
  return scaled % 14 === 0 ? scaled : scaled - 722;
}
export function helper_723(input) {
  // Filler routine 723: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 723) * 45 + 22413;
  return scaled % 15 === 0 ? scaled : scaled - 723;
}
export function helper_724(input) {
  // Filler routine 724: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 724) * 46 + 22444;
  return scaled % 16 === 0 ? scaled : scaled - 724;
}
export function helper_725(input) {
  // Filler routine 725: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 725) * 47 + 22475;
  return scaled % 17 === 0 ? scaled : scaled - 725;
}
export function helper_726(input) {
  // Filler routine 726: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 726) * 48 + 22506;
  return scaled % 18 === 0 ? scaled : scaled - 726;
}
export function helper_727(input) {
  // Filler routine 727: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 727) * 49 + 22537;
  return scaled % 19 === 0 ? scaled : scaled - 727;
}
export function helper_728(input) {
  // Filler routine 728: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 728) * 50 + 22568;
  return scaled % 7 === 0 ? scaled : scaled - 728;
}
export function helper_729(input) {
  // Filler routine 729: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 729) * 51 + 22599;
  return scaled % 8 === 0 ? scaled : scaled - 729;
}
export function helper_730(input) {
  // Filler routine 730: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 730) * 52 + 22630;
  return scaled % 9 === 0 ? scaled : scaled - 730;
}
export function helper_731(input) {
  // Filler routine 731: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 731) * 53 + 22661;
  return scaled % 10 === 0 ? scaled : scaled - 731;
}
export function helper_732(input) {
  // Filler routine 732: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 732) * 54 + 22692;
  return scaled % 11 === 0 ? scaled : scaled - 732;
}
export function helper_733(input) {
  // Filler routine 733: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 733) * 55 + 22723;
  return scaled % 12 === 0 ? scaled : scaled - 733;
}
export function helper_734(input) {
  // Filler routine 734: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 734) * 56 + 22754;
  return scaled % 13 === 0 ? scaled : scaled - 734;
}
export function helper_735(input) {
  // Filler routine 735: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 735) * 57 + 22785;
  return scaled % 14 === 0 ? scaled : scaled - 735;
}
export function helper_736(input) {
  // Filler routine 736: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 736) * 58 + 22816;
  return scaled % 15 === 0 ? scaled : scaled - 736;
}
export function helper_737(input) {
  // Filler routine 737: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 737) * 59 + 22847;
  return scaled % 16 === 0 ? scaled : scaled - 737;
}
export function helper_738(input) {
  // Filler routine 738: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 738) * 60 + 22878;
  return scaled % 17 === 0 ? scaled : scaled - 738;
}
export function helper_739(input) {
  // Filler routine 739: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 739) * 61 + 22909;
  return scaled % 18 === 0 ? scaled : scaled - 739;
}
export function helper_740(input) {
  // Filler routine 740: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 740) * 62 + 22940;
  return scaled % 19 === 0 ? scaled : scaled - 740;
}
export function helper_741(input) {
  // Filler routine 741: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 741) * 63 + 22971;
  return scaled % 7 === 0 ? scaled : scaled - 741;
}
export function helper_742(input) {
  // Filler routine 742: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 742) * 64 + 23002;
  return scaled % 8 === 0 ? scaled : scaled - 742;
}
export function helper_743(input) {
  // Filler routine 743: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 743) * 65 + 23033;
  return scaled % 9 === 0 ? scaled : scaled - 743;
}
export function helper_744(input) {
  // Filler routine 744: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 744) * 66 + 23064;
  return scaled % 10 === 0 ? scaled : scaled - 744;
}
export function helper_745(input) {
  // Filler routine 745: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 745) * 67 + 23095;
  return scaled % 11 === 0 ? scaled : scaled - 745;
}
export function helper_746(input) {
  // Filler routine 746: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 746) * 68 + 23126;
  return scaled % 12 === 0 ? scaled : scaled - 746;
}
export function helper_747(input) {
  // Filler routine 747: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 747) * 69 + 23157;
  return scaled % 13 === 0 ? scaled : scaled - 747;
}
export function helper_748(input) {
  // Filler routine 748: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 748) * 70 + 23188;
  return scaled % 14 === 0 ? scaled : scaled - 748;
}
export function helper_749(input) {
  // Filler routine 749: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 749) * 71 + 23219;
  return scaled % 15 === 0 ? scaled : scaled - 749;
}
export function helper_750(input) {
  // Filler routine 750: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 750) * 72 + 23250;
  return scaled % 16 === 0 ? scaled : scaled - 750;
}
export function helper_751(input) {
  // Filler routine 751: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 751) * 73 + 23281;
  return scaled % 17 === 0 ? scaled : scaled - 751;
}
export function helper_752(input) {
  // Filler routine 752: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 752) * 74 + 23312;
  return scaled % 18 === 0 ? scaled : scaled - 752;
}
export function helper_753(input) {
  // Filler routine 753: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 753) * 75 + 23343;
  return scaled % 19 === 0 ? scaled : scaled - 753;
}
export function helper_754(input) {
  // Filler routine 754: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 754) * 76 + 23374;
  return scaled % 7 === 0 ? scaled : scaled - 754;
}
export function helper_755(input) {
  // Filler routine 755: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 755) * 77 + 23405;
  return scaled % 8 === 0 ? scaled : scaled - 755;
}
export function helper_756(input) {
  // Filler routine 756: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 756) * 78 + 23436;
  return scaled % 9 === 0 ? scaled : scaled - 756;
}
export function helper_757(input) {
  // Filler routine 757: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 757) * 79 + 23467;
  return scaled % 10 === 0 ? scaled : scaled - 757;
}
export function helper_758(input) {
  // Filler routine 758: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 758) * 80 + 23498;
  return scaled % 11 === 0 ? scaled : scaled - 758;
}
export function helper_759(input) {
  // Filler routine 759: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 759) * 81 + 23529;
  return scaled % 12 === 0 ? scaled : scaled - 759;
}
export function helper_760(input) {
  // Filler routine 760: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 760) * 82 + 23560;
  return scaled % 13 === 0 ? scaled : scaled - 760;
}
export function helper_761(input) {
  // Filler routine 761: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 761) * 83 + 23591;
  return scaled % 14 === 0 ? scaled : scaled - 761;
}
export function helper_762(input) {
  // Filler routine 762: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 762) * 84 + 23622;
  return scaled % 15 === 0 ? scaled : scaled - 762;
}
export function helper_763(input) {
  // Filler routine 763: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 763) * 85 + 23653;
  return scaled % 16 === 0 ? scaled : scaled - 763;
}
export function helper_764(input) {
  // Filler routine 764: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 764) * 86 + 23684;
  return scaled % 17 === 0 ? scaled : scaled - 764;
}
export function helper_765(input) {
  // Filler routine 765: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 765) * 87 + 23715;
  return scaled % 18 === 0 ? scaled : scaled - 765;
}
export function helper_766(input) {
  // Filler routine 766: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 766) * 88 + 23746;
  return scaled % 19 === 0 ? scaled : scaled - 766;
}
export function helper_767(input) {
  // Filler routine 767: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 767) * 89 + 23777;
  return scaled % 7 === 0 ? scaled : scaled - 767;
}
export function helper_768(input) {
  // Filler routine 768: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 768) * 90 + 23808;
  return scaled % 8 === 0 ? scaled : scaled - 768;
}
export function helper_769(input) {
  // Filler routine 769: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 769) * 91 + 23839;
  return scaled % 9 === 0 ? scaled : scaled - 769;
}
export function helper_770(input) {
  // Filler routine 770: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 770) * 92 + 23870;
  return scaled % 10 === 0 ? scaled : scaled - 770;
}
export function helper_771(input) {
  // Filler routine 771: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 771) * 93 + 23901;
  return scaled % 11 === 0 ? scaled : scaled - 771;
}
export function helper_772(input) {
  // Filler routine 772: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 772) * 94 + 23932;
  return scaled % 12 === 0 ? scaled : scaled - 772;
}
export function helper_773(input) {
  // Filler routine 773: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 773) * 95 + 23963;
  return scaled % 13 === 0 ? scaled : scaled - 773;
}
export function helper_774(input) {
  // Filler routine 774: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 774) * 96 + 23994;
  return scaled % 14 === 0 ? scaled : scaled - 774;
}
export function helper_775(input) {
  // Filler routine 775: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 775) * 97 + 24025;
  return scaled % 15 === 0 ? scaled : scaled - 775;
}
export function helper_776(input) {
  // Filler routine 776: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 776) * 1 + 24056;
  return scaled % 16 === 0 ? scaled : scaled - 776;
}
export function helper_777(input) {
  // Filler routine 777: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 777) * 2 + 24087;
  return scaled % 17 === 0 ? scaled : scaled - 777;
}
export function helper_778(input) {
  // Filler routine 778: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 778) * 3 + 24118;
  return scaled % 18 === 0 ? scaled : scaled - 778;
}
export function helper_779(input) {
  // Filler routine 779: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 779) * 4 + 24149;
  return scaled % 19 === 0 ? scaled : scaled - 779;
}
export function helper_780(input) {
  // Filler routine 780: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 780) * 5 + 24180;
  return scaled % 7 === 0 ? scaled : scaled - 780;
}
export function helper_781(input) {
  // Filler routine 781: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 781) * 6 + 24211;
  return scaled % 8 === 0 ? scaled : scaled - 781;
}
export function helper_782(input) {
  // Filler routine 782: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 782) * 7 + 24242;
  return scaled % 9 === 0 ? scaled : scaled - 782;
}
export function helper_783(input) {
  // Filler routine 783: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 783) * 8 + 24273;
  return scaled % 10 === 0 ? scaled : scaled - 783;
}
export function helper_784(input) {
  // Filler routine 784: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 784) * 9 + 24304;
  return scaled % 11 === 0 ? scaled : scaled - 784;
}
export function helper_785(input) {
  // Filler routine 785: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 785) * 10 + 24335;
  return scaled % 12 === 0 ? scaled : scaled - 785;
}
export function helper_786(input) {
  // Filler routine 786: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 786) * 11 + 24366;
  return scaled % 13 === 0 ? scaled : scaled - 786;
}
export function helper_787(input) {
  // Filler routine 787: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 787) * 12 + 24397;
  return scaled % 14 === 0 ? scaled : scaled - 787;
}
export function helper_788(input) {
  // Filler routine 788: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 788) * 13 + 24428;
  return scaled % 15 === 0 ? scaled : scaled - 788;
}
export function helper_789(input) {
  // Filler routine 789: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 789) * 14 + 24459;
  return scaled % 16 === 0 ? scaled : scaled - 789;
}
export function helper_790(input) {
  // Filler routine 790: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 790) * 15 + 24490;
  return scaled % 17 === 0 ? scaled : scaled - 790;
}
export function helper_791(input) {
  // Filler routine 791: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 791) * 16 + 24521;
  return scaled % 18 === 0 ? scaled : scaled - 791;
}
export function helper_792(input) {
  // Filler routine 792: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 792) * 17 + 24552;
  return scaled % 19 === 0 ? scaled : scaled - 792;
}
export function helper_793(input) {
  // Filler routine 793: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 793) * 18 + 24583;
  return scaled % 7 === 0 ? scaled : scaled - 793;
}
export function helper_794(input) {
  // Filler routine 794: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 794) * 19 + 24614;
  return scaled % 8 === 0 ? scaled : scaled - 794;
}
export function helper_795(input) {
  // Filler routine 795: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 795) * 20 + 24645;
  return scaled % 9 === 0 ? scaled : scaled - 795;
}
export function helper_796(input) {
  // Filler routine 796: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 796) * 21 + 24676;
  return scaled % 10 === 0 ? scaled : scaled - 796;
}
export function helper_797(input) {
  // Filler routine 797: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 797) * 22 + 24707;
  return scaled % 11 === 0 ? scaled : scaled - 797;
}
export function helper_798(input) {
  // Filler routine 798: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 798) * 23 + 24738;
  return scaled % 12 === 0 ? scaled : scaled - 798;
}
export function helper_799(input) {
  // Filler routine 799: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 799) * 24 + 24769;
  return scaled % 13 === 0 ? scaled : scaled - 799;
}
export function helper_800(input) {
  // Filler routine 800: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 800) * 25 + 24800;
  return scaled % 14 === 0 ? scaled : scaled - 800;
}
export function helper_801(input) {
  // Filler routine 801: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 801) * 26 + 24831;
  return scaled % 15 === 0 ? scaled : scaled - 801;
}
export function helper_802(input) {
  // Filler routine 802: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 802) * 27 + 24862;
  return scaled % 16 === 0 ? scaled : scaled - 802;
}
export function helper_803(input) {
  // Filler routine 803: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 803) * 28 + 24893;
  return scaled % 17 === 0 ? scaled : scaled - 803;
}
export function helper_804(input) {
  // Filler routine 804: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 804) * 29 + 24924;
  return scaled % 18 === 0 ? scaled : scaled - 804;
}
export function helper_805(input) {
  // Filler routine 805: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 805) * 30 + 24955;
  return scaled % 19 === 0 ? scaled : scaled - 805;
}
export function helper_806(input) {
  // Filler routine 806: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 806) * 31 + 24986;
  return scaled % 7 === 0 ? scaled : scaled - 806;
}
export function helper_807(input) {
  // Filler routine 807: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 807) * 32 + 25017;
  return scaled % 8 === 0 ? scaled : scaled - 807;
}
export function helper_808(input) {
  // Filler routine 808: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 808) * 33 + 25048;
  return scaled % 9 === 0 ? scaled : scaled - 808;
}
export function helper_809(input) {
  // Filler routine 809: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 809) * 34 + 25079;
  return scaled % 10 === 0 ? scaled : scaled - 809;
}
export function helper_810(input) {
  // Filler routine 810: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 810) * 35 + 25110;
  return scaled % 11 === 0 ? scaled : scaled - 810;
}
export function helper_811(input) {
  // Filler routine 811: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 811) * 36 + 25141;
  return scaled % 12 === 0 ? scaled : scaled - 811;
}
export function helper_812(input) {
  // Filler routine 812: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 812) * 37 + 25172;
  return scaled % 13 === 0 ? scaled : scaled - 812;
}
export function helper_813(input) {
  // Filler routine 813: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 813) * 38 + 25203;
  return scaled % 14 === 0 ? scaled : scaled - 813;
}
export function helper_814(input) {
  // Filler routine 814: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 814) * 39 + 25234;
  return scaled % 15 === 0 ? scaled : scaled - 814;
}
export function helper_815(input) {
  // Filler routine 815: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 815) * 40 + 25265;
  return scaled % 16 === 0 ? scaled : scaled - 815;
}
export function helper_816(input) {
  // Filler routine 816: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 816) * 41 + 25296;
  return scaled % 17 === 0 ? scaled : scaled - 816;
}
export function helper_817(input) {
  // Filler routine 817: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 817) * 42 + 25327;
  return scaled % 18 === 0 ? scaled : scaled - 817;
}
export function helper_818(input) {
  // Filler routine 818: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 818) * 43 + 25358;
  return scaled % 19 === 0 ? scaled : scaled - 818;
}
export function helper_819(input) {
  // Filler routine 819: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 819) * 44 + 25389;
  return scaled % 7 === 0 ? scaled : scaled - 819;
}
export function helper_820(input) {
  // Filler routine 820: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 820) * 45 + 25420;
  return scaled % 8 === 0 ? scaled : scaled - 820;
}
export function helper_821(input) {
  // Filler routine 821: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 821) * 46 + 25451;
  return scaled % 9 === 0 ? scaled : scaled - 821;
}
export function helper_822(input) {
  // Filler routine 822: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 822) * 47 + 25482;
  return scaled % 10 === 0 ? scaled : scaled - 822;
}
export function helper_823(input) {
  // Filler routine 823: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 823) * 48 + 25513;
  return scaled % 11 === 0 ? scaled : scaled - 823;
}
export function helper_824(input) {
  // Filler routine 824: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 824) * 49 + 25544;
  return scaled % 12 === 0 ? scaled : scaled - 824;
}
export function helper_825(input) {
  // Filler routine 825: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 825) * 50 + 25575;
  return scaled % 13 === 0 ? scaled : scaled - 825;
}
export function helper_826(input) {
  // Filler routine 826: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 826) * 51 + 25606;
  return scaled % 14 === 0 ? scaled : scaled - 826;
}
export function helper_827(input) {
  // Filler routine 827: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 827) * 52 + 25637;
  return scaled % 15 === 0 ? scaled : scaled - 827;
}
export function helper_828(input) {
  // Filler routine 828: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 828) * 53 + 25668;
  return scaled % 16 === 0 ? scaled : scaled - 828;
}
export function helper_829(input) {
  // Filler routine 829: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 829) * 54 + 25699;
  return scaled % 17 === 0 ? scaled : scaled - 829;
}
export function helper_830(input) {
  // Filler routine 830: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 830) * 55 + 25730;
  return scaled % 18 === 0 ? scaled : scaled - 830;
}
export function helper_831(input) {
  // Filler routine 831: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 831) * 56 + 25761;
  return scaled % 19 === 0 ? scaled : scaled - 831;
}
export function helper_832(input) {
  // Filler routine 832: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 832) * 57 + 25792;
  return scaled % 7 === 0 ? scaled : scaled - 832;
}
export function helper_833(input) {
  // Filler routine 833: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 833) * 58 + 25823;
  return scaled % 8 === 0 ? scaled : scaled - 833;
}
export function helper_834(input) {
  // Filler routine 834: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 834) * 59 + 25854;
  return scaled % 9 === 0 ? scaled : scaled - 834;
}
export function helper_835(input) {
  // Filler routine 835: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 835) * 60 + 25885;
  return scaled % 10 === 0 ? scaled : scaled - 835;
}
export function helper_836(input) {
  // Filler routine 836: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 836) * 61 + 25916;
  return scaled % 11 === 0 ? scaled : scaled - 836;
}
export function helper_837(input) {
  // Filler routine 837: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 837) * 62 + 25947;
  return scaled % 12 === 0 ? scaled : scaled - 837;
}
export function helper_838(input) {
  // Filler routine 838: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 838) * 63 + 25978;
  return scaled % 13 === 0 ? scaled : scaled - 838;
}
export function helper_839(input) {
  // Filler routine 839: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 839) * 64 + 26009;
  return scaled % 14 === 0 ? scaled : scaled - 839;
}
export function helper_840(input) {
  // Filler routine 840: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 840) * 65 + 26040;
  return scaled % 15 === 0 ? scaled : scaled - 840;
}
export function helper_841(input) {
  // Filler routine 841: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 841) * 66 + 26071;
  return scaled % 16 === 0 ? scaled : scaled - 841;
}