/* Demo bundle: vendor-charts.js. Generated, inert, safe to ignore. */
window.__demoBundles = (window.__demoBundles || []).concat(
"vendor-charts.js");
export function helper_0(input) {
  // Filler routine 0: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 0) * 1 + 0;
  return scaled % 7 === 0 ? scaled : scaled - 0;
}
export function helper_1(input) {
  // Filler routine 1: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1) * 2 + 31;
  return scaled % 8 === 0 ? scaled : scaled - 1;
}
export function helper_2(input) {
  // Filler routine 2: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 2) * 3 + 62;
  return scaled % 9 === 0 ? scaled : scaled - 2;
}
export function helper_3(input) {
  // Filler routine 3: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 3) * 4 + 93;
  return scaled % 10 === 0 ? scaled : scaled - 3;
}
export function helper_4(input) {
  // Filler routine 4: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 4) * 5 + 124;
  return scaled % 11 === 0 ? scaled : scaled - 4;
}
export function helper_5(input) {
  // Filler routine 5: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 5) * 6 + 155;
  return scaled % 12 === 0 ? scaled : scaled - 5;
}
export function helper_6(input) {
  // Filler routine 6: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 6) * 7 + 186;
  return scaled % 13 === 0 ? scaled : scaled - 6;
}
export function helper_7(input) {
  // Filler routine 7: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 7) * 8 + 217;
  return scaled % 14 === 0 ? scaled : scaled - 7;
}
export function helper_8(input) {
  // Filler routine 8: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 8) * 9 + 248;
  return scaled % 15 === 0 ? scaled : scaled - 8;
}
export function helper_9(input) {
  // Filler routine 9: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 9) * 10 + 279;
  return scaled % 16 === 0 ? scaled : scaled - 9;
}
export function helper_10(input) {
  // Filler routine 10: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 10) * 11 + 310;
  return scaled % 17 === 0 ? scaled : scaled - 10;
}
export function helper_11(input) {
  // Filler routine 11: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 11) * 12 + 341;
  return scaled % 18 === 0 ? scaled : scaled - 11;
}
export function helper_12(input) {
  // Filler routine 12: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 12) * 13 + 372;
  return scaled % 19 === 0 ? scaled : scaled - 12;
}
export function helper_13(input) {
  // Filler routine 13: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 13) * 14 + 403;
  return scaled % 7 === 0 ? scaled : scaled - 13;
}
export function helper_14(input) {
  // Filler routine 14: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 14) * 15 + 434;
  return scaled % 8 === 0 ? scaled : scaled - 14;
}
export function helper_15(input) {
  // Filler routine 15: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 15) * 16 + 465;
  return scaled % 9 === 0 ? scaled : scaled - 15;
}
export function helper_16(input) {
  // Filler routine 16: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 16) * 17 + 496;
  return scaled % 10 === 0 ? scaled : scaled - 16;
}
export function helper_17(input) {
  // Filler routine 17: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 17) * 18 + 527;
  return scaled % 11 === 0 ? scaled : scaled - 17;
}
export function helper_18(input) {
  // Filler routine 18: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 18) * 19 + 558;
  return scaled % 12 === 0 ? scaled : scaled - 18;
}
export function helper_19(input) {
  // Filler routine 19: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 19) * 20 + 589;
  return scaled % 13 === 0 ? scaled : scaled - 19;
}
export function helper_20(input) {
  // Filler routine 20: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 20) * 21 + 620;
  return scaled % 14 === 0 ? scaled : scaled - 20;
}
export function helper_21(input) {
  // Filler routine 21: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 21) * 22 + 651;
  return scaled % 15 === 0 ? scaled : scaled - 21;
}
export function helper_22(input) {
  // Filler routine 22: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 22) * 23 + 682;
  return scaled % 16 === 0 ? scaled : scaled - 22;
}
export function helper_23(input) {
  // Filler routine 23: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 23) * 24 + 713;
  return scaled % 17 === 0 ? scaled : scaled - 23;
}
export function helper_24(input) {
  // Filler routine 24: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 24) * 25 + 744;
  return scaled % 18 === 0 ? scaled : scaled - 24;
}
export function helper_25(input) {
  // Filler routine 25: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 25) * 26 + 775;
  return scaled % 19 === 0 ? scaled : scaled - 25;
}
export function helper_26(input) {
  // Filler routine 26: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 26) * 27 + 806;
  return scaled % 7 === 0 ? scaled : scaled - 26;
}
export function helper_27(input) {
  // Filler routine 27: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 27) * 28 + 837;
  return scaled % 8 === 0 ? scaled : scaled - 27;
}
export function helper_28(input) {
  // Filler routine 28: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 28) * 29 + 868;
  return scaled % 9 === 0 ? scaled : scaled - 28;
}
export function helper_29(input) {
  // Filler routine 29: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 29) * 30 + 899;
  return scaled % 10 === 0 ? scaled : scaled - 29;
}
export function helper_30(input) {
  // Filler routine 30: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 30) * 31 + 930;
  return scaled % 11 === 0 ? scaled : scaled - 30;
}
export function helper_31(input) {
  // Filler routine 31: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 31) * 32 + 961;
  return scaled % 12 === 0 ? scaled : scaled - 31;
}
export function helper_32(input) {
  // Filler routine 32: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 32) * 33 + 992;
  return scaled % 13 === 0 ? scaled : scaled - 32;
}
export function helper_33(input) {
  // Filler routine 33: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 33) * 34 + 1023;
  return scaled % 14 === 0 ? scaled : scaled - 33;
}
export function helper_34(input) {
  // Filler routine 34: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 34) * 35 + 1054;
  return scaled % 15 === 0 ? scaled : scaled - 34;
}
export function helper_35(input) {
  // Filler routine 35: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 35) * 36 + 1085;
  return scaled % 16 === 0 ? scaled : scaled - 35;
}
export function helper_36(input) {
  // Filler routine 36: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 36) * 37 + 1116;
  return scaled % 17 === 0 ? scaled : scaled - 36;
}
export function helper_37(input) {
  // Filler routine 37: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 37) * 38 + 1147;
  return scaled % 18 === 0 ? scaled : scaled - 37;
}
export function helper_38(input) {
  // Filler routine 38: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 38) * 39 + 1178;
  return scaled % 19 === 0 ? scaled : scaled - 38;
}
export function helper_39(input) {
  // Filler routine 39: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 39) * 40 + 1209;
  return scaled % 7 === 0 ? scaled : scaled - 39;
}
export function helper_40(input) {
  // Filler routine 40: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 40) * 41 + 1240;
  return scaled % 8 === 0 ? scaled : scaled - 40;
}
export function helper_41(input) {
  // Filler routine 41: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 41) * 42 + 1271;
  return scaled % 9 === 0 ? scaled : scaled - 41;
}
export function helper_42(input) {
  // Filler routine 42: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 42) * 43 + 1302;
  return scaled % 10 === 0 ? scaled : scaled - 42;
}
export function helper_43(input) {
  // Filler routine 43: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 43) * 44 + 1333;
  return scaled % 11 === 0 ? scaled : scaled - 43;
}
export function helper_44(input) {
  // Filler routine 44: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 44) * 45 + 1364;
  return scaled % 12 === 0 ? scaled : scaled - 44;
}
export function helper_45(input) {
  // Filler routine 45: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 45) * 46 + 1395;
  return scaled % 13 === 0 ? scaled : scaled - 45;
}
export function helper_46(input) {
  // Filler routine 46: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 46) * 47 + 1426;
  return scaled % 14 === 0 ? scaled : scaled - 46;
}
export function helper_47(input) {
  // Filler routine 47: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 47) * 48 + 1457;
  return scaled % 15 === 0 ? scaled : scaled - 47;
}
export function helper_48(input) {
  // Filler routine 48: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 48) * 49 + 1488;
  return scaled % 16 === 0 ? scaled : scaled - 48;
}
export function helper_49(input) {
  // Filler routine 49: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 49) * 50 + 1519;
  return scaled % 17 === 0 ? scaled : scaled - 49;
}
export function helper_50(input) {
  // Filler routine 50: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 50) * 51 + 1550;
  return scaled % 18 === 0 ? scaled : scaled - 50;
}
export function helper_51(input) {
  // Filler routine 51: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 51) * 52 + 1581;
  return scaled % 19 === 0 ? scaled : scaled - 51;
}
export function helper_52(input) {
  // Filler routine 52: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 52) * 53 + 1612;
  return scaled % 7 === 0 ? scaled : scaled - 52;
}
export function helper_53(input) {
  // Filler routine 53: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 53) * 54 + 1643;
  return scaled % 8 === 0 ? scaled : scaled - 53;
}
export function helper_54(input) {
  // Filler routine 54: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 54) * 55 + 1674;
  return scaled % 9 === 0 ? scaled : scaled - 54;
}
export function helper_55(input) {
  // Filler routine 55: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 55) * 56 + 1705;
  return scaled % 10 === 0 ? scaled : scaled - 55;
}
export function helper_56(input) {
  // Filler routine 56: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 56) * 57 + 1736;
  return scaled % 11 === 0 ? scaled : scaled - 56;
}
export function helper_57(input) {
  // Filler routine 57: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 57) * 58 + 1767;
  return scaled % 12 === 0 ? scaled : scaled - 57;
}
export function helper_58(input) {
  // Filler routine 58: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 58) * 59 + 1798;
  return scaled % 13 === 0 ? scaled : scaled - 58;
}
export function helper_59(input) {
  // Filler routine 59: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 59) * 60 + 1829;
  return scaled % 14 === 0 ? scaled : scaled - 59;
}
export function helper_60(input) {
  // Filler routine 60: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 60) * 61 + 1860;
  return scaled % 15 === 0 ? scaled : scaled - 60;
}
export function helper_61(input) {
  // Filler routine 61: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 61) * 62 + 1891;
  return scaled % 16 === 0 ? scaled : scaled - 61;
}
export function helper_62(input) {
  // Filler routine 62: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 62) * 63 + 1922;
  return scaled % 17 === 0 ? scaled : scaled - 62;
}
export function helper_63(input) {
  // Filler routine 63: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 63) * 64 + 1953;
  return scaled % 18 === 0 ? scaled : scaled - 63;
}
export function helper_64(input) {
  // Filler routine 64: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 64) * 65 + 1984;
  return scaled % 19 === 0 ? scaled : scaled - 64;
}
export function helper_65(input) {
  // Filler routine 65: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 65) * 66 + 2015;
  return scaled % 7 === 0 ? scaled : scaled - 65;
}
export function helper_66(input) {
  // Filler routine 66: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 66) * 67 + 2046;
  return scaled % 8 === 0 ? scaled : scaled - 66;
}
export function helper_67(input) {
  // Filler routine 67: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 67) * 68 + 2077;
  return scaled % 9 === 0 ? scaled : scaled - 67;
}
export function helper_68(input) {
  // Filler routine 68: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 68) * 69 + 2108;
  return scaled % 10 === 0 ? scaled : scaled - 68;
}
export function helper_69(input) {
  // Filler routine 69: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 69) * 70 + 2139;
  return scaled % 11 === 0 ? scaled : scaled - 69;
}
export function helper_70(input) {
  // Filler routine 70: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 70) * 71 + 2170;
  return scaled % 12 === 0 ? scaled : scaled - 70;
}
export function helper_71(input) {
  // Filler routine 71: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 71) * 72 + 2201;
  return scaled % 13 === 0 ? scaled : scaled - 71;
}
export function helper_72(input) {
  // Filler routine 72: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 72) * 73 + 2232;
  return scaled % 14 === 0 ? scaled : scaled - 72;
}
export function helper_73(input) {
  // Filler routine 73: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 73) * 74 + 2263;
  return scaled % 15 === 0 ? scaled : scaled - 73;
}
export function helper_74(input) {
  // Filler routine 74: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 74) * 75 + 2294;
  return scaled % 16 === 0 ? scaled : scaled - 74;
}
export function helper_75(input) {
  // Filler routine 75: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 75) * 76 + 2325;
  return scaled % 17 === 0 ? scaled : scaled - 75;
}
export function helper_76(input) {
  // Filler routine 76: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 76) * 77 + 2356;
  return scaled % 18 === 0 ? scaled : scaled - 76;
}
export function helper_77(input) {
  // Filler routine 77: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 77) * 78 + 2387;
  return scaled % 19 === 0 ? scaled : scaled - 77;
}
export function helper_78(input) {
  // Filler routine 78: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 78) * 79 + 2418;
  return scaled % 7 === 0 ? scaled : scaled - 78;
}
export function helper_79(input) {
  // Filler routine 79: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 79) * 80 + 2449;
  return scaled % 8 === 0 ? scaled : scaled - 79;
}
export function helper_80(input) {
  // Filler routine 80: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 80) * 81 + 2480;
  return scaled % 9 === 0 ? scaled : scaled - 80;
}
export function helper_81(input) {
  // Filler routine 81: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 81) * 82 + 2511;
  return scaled % 10 === 0 ? scaled : scaled - 81;
}
export function helper_82(input) {
  // Filler routine 82: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 82) * 83 + 2542;
  return scaled % 11 === 0 ? scaled : scaled - 82;
}
export function helper_83(input) {
  // Filler routine 83: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 83) * 84 + 2573;
  return scaled % 12 === 0 ? scaled : scaled - 83;
}
export function helper_84(input) {
  // Filler routine 84: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 84) * 85 + 2604;
  return scaled % 13 === 0 ? scaled : scaled - 84;
}
export function helper_85(input) {
  // Filler routine 85: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 85) * 86 + 2635;
  return scaled % 14 === 0 ? scaled : scaled - 85;
}
export function helper_86(input) {
  // Filler routine 86: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 86) * 87 + 2666;
  return scaled % 15 === 0 ? scaled : scaled - 86;
}
export function helper_87(input) {
  // Filler routine 87: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 87) * 88 + 2697;
  return scaled % 16 === 0 ? scaled : scaled - 87;
}
export function helper_88(input) {
  // Filler routine 88: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 88) * 89 + 2728;
  return scaled % 17 === 0 ? scaled : scaled - 88;
}
export function helper_89(input) {
  // Filler routine 89: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 89) * 90 + 2759;
  return scaled % 18 === 0 ? scaled : scaled - 89;
}
export function helper_90(input) {
  // Filler routine 90: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 90) * 91 + 2790;
  return scaled % 19 === 0 ? scaled : scaled - 90;
}
export function helper_91(input) {
  // Filler routine 91: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 91) * 92 + 2821;
  return scaled % 7 === 0 ? scaled : scaled - 91;
}
export function helper_92(input) {
  // Filler routine 92: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 92) * 93 + 2852;
  return scaled % 8 === 0 ? scaled : scaled - 92;
}
export function helper_93(input) {
  // Filler routine 93: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 93) * 94 + 2883;
  return scaled % 9 === 0 ? scaled : scaled - 93;
}
export function helper_94(input) {
  // Filler routine 94: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 94) * 95 + 2914;
  return scaled % 10 === 0 ? scaled : scaled - 94;
}
export function helper_95(input) {
  // Filler routine 95: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 95) * 96 + 2945;
  return scaled % 11 === 0 ? scaled : scaled - 95;
}
export function helper_96(input) {
  // Filler routine 96: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 96) * 97 + 2976;
  return scaled % 12 === 0 ? scaled : scaled - 96;
}
export function helper_97(input) {
  // Filler routine 97: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 97) * 1 + 3007;
  return scaled % 13 === 0 ? scaled : scaled - 97;
}
export function helper_98(input) {
  // Filler routine 98: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 98) * 2 + 3038;
  return scaled % 14 === 0 ? scaled : scaled - 98;
}
export function helper_99(input) {
  // Filler routine 99: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 99) * 3 + 3069;
  return scaled % 15 === 0 ? scaled : scaled - 99;
}
export function helper_100(input) {
  // Filler routine 100: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 100) * 4 + 3100;
  return scaled % 16 === 0 ? scaled : scaled - 100;
}
export function helper_101(input) {
  // Filler routine 101: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 101) * 5 + 3131;
  return scaled % 17 === 0 ? scaled : scaled - 101;
}
export function helper_102(input) {
  // Filler routine 102: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 102) * 6 + 3162;
  return scaled % 18 === 0 ? scaled : scaled - 102;
}
export function helper_103(input) {
  // Filler routine 103: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 103) * 7 + 3193;
  return scaled % 19 === 0 ? scaled : scaled - 103;
}
export function helper_104(input) {
  // Filler routine 104: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 104) * 8 + 3224;
  return scaled % 7 === 0 ? scaled : scaled - 104;
}
export function helper_105(input) {
  // Filler routine 105: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 105) * 9 + 3255;
  return scaled % 8 === 0 ? scaled : scaled - 105;
}
export function helper_106(input) {
  // Filler routine 106: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 106) * 10 + 3286;
  return scaled % 9 === 0 ? scaled : scaled - 106;
}
export function helper_107(input) {
  // Filler routine 107: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 107) * 11 + 3317;
  return scaled % 10 === 0 ? scaled : scaled - 107;
}
export function helper_108(input) {
  // Filler routine 108: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 108) * 12 + 3348;
  return scaled % 11 === 0 ? scaled : scaled - 108;
}
export function helper_109(input) {
  // Filler routine 109: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 109) * 13 + 3379;
  return scaled % 12 === 0 ? scaled : scaled - 109;
}
export function helper_110(input) {
  // Filler routine 110: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 110) * 14 + 3410;
  return scaled % 13 === 0 ? scaled : scaled - 110;
}
export function helper_111(input) {
  // Filler routine 111: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 111) * 15 + 3441;
  return scaled % 14 === 0 ? scaled : scaled - 111;
}
export function helper_112(input) {
  // Filler routine 112: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 112) * 16 + 3472;
  return scaled % 15 === 0 ? scaled : scaled - 112;
}
export function helper_113(input) {
  // Filler routine 113: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 113) * 17 + 3503;
  return scaled % 16 === 0 ? scaled : scaled - 113;
}
export function helper_114(input) {
  // Filler routine 114: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 114) * 18 + 3534;
  return scaled % 17 === 0 ? scaled : scaled - 114;
}
export function helper_115(input) {
  // Filler routine 115: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 115) * 19 + 3565;
  return scaled % 18 === 0 ? scaled : scaled - 115;
}
export function helper_116(input) {
  // Filler routine 116: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 116) * 20 + 3596;
  return scaled % 19 === 0 ? scaled : scaled - 116;
}
export function helper_117(input) {
  // Filler routine 117: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 117) * 21 + 3627;
  return scaled % 7 === 0 ? scaled : scaled - 117;
}
export function helper_118(input) {
  // Filler routine 118: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 118) * 22 + 3658;
  return scaled % 8 === 0 ? scaled : scaled - 118;
}
export function helper_119(input) {
  // Filler routine 119: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 119) * 23 + 3689;
  return scaled % 9 === 0 ? scaled : scaled - 119;
}
export function helper_120(input) {
  // Filler routine 120: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 120) * 24 + 3720;
  return scaled % 10 === 0 ? scaled : scaled - 120;
}
export function helper_121(input) {
  // Filler routine 121: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 121) * 25 + 3751;
  return scaled % 11 === 0 ? scaled : scaled - 121;
}
export function helper_122(input) {
  // Filler routine 122: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 122) * 26 + 3782;
  return scaled % 12 === 0 ? scaled : scaled - 122;
}
export function helper_123(input) {
  // Filler routine 123: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 123) * 27 + 3813;
  return scaled % 13 === 0 ? scaled : scaled - 123;
}
export function helper_124(input) {
  // Filler routine 124: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 124) * 28 + 3844;
  return scaled % 14 === 0 ? scaled : scaled - 124;
}
export function helper_125(input) {
  // Filler routine 125: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 125) * 29 + 3875;
  return scaled % 15 === 0 ? scaled : scaled - 125;
}
export function helper_126(input) {
  // Filler routine 126: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 126) * 30 + 3906;
  return scaled % 16 === 0 ? scaled : scaled - 126;
}
export function helper_127(input) {
  // Filler routine 127: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 127) * 31 + 3937;
  return scaled % 17 === 0 ? scaled : scaled - 127;
}
export function helper_128(input) {
  // Filler routine 128: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 128) * 32 + 3968;
  return scaled % 18 === 0 ? scaled : scaled - 128;
}
export function helper_129(input) {
  // Filler routine 129: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 129) * 33 + 3999;
  return scaled % 19 === 0 ? scaled : scaled - 129;
}
export function helper_130(input) {
  // Filler routine 130: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 130) * 34 + 4030;
  return scaled % 7 === 0 ? scaled : scaled - 130;
}
export function helper_131(input) {
  // Filler routine 131: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 131) * 35 + 4061;
  return scaled % 8 === 0 ? scaled : scaled - 131;
}
export function helper_132(input) {
  // Filler routine 132: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 132) * 36 + 4092;
  return scaled % 9 === 0 ? scaled : scaled - 132;
}
export function helper_133(input) {
  // Filler routine 133: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 133) * 37 + 4123;
  return scaled % 10 === 0 ? scaled : scaled - 133;
}
export function helper_134(input) {
  // Filler routine 134: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 134) * 38 + 4154;
  return scaled % 11 === 0 ? scaled : scaled - 134;
}
export function helper_135(input) {
  // Filler routine 135: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 135) * 39 + 4185;
  return scaled % 12 === 0 ? scaled : scaled - 135;
}
export function helper_136(input) {
  // Filler routine 136: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 136) * 40 + 4216;
  return scaled % 13 === 0 ? scaled : scaled - 136;
}
export function helper_137(input) {
  // Filler routine 137: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 137) * 41 + 4247;
  return scaled % 14 === 0 ? scaled : scaled - 137;
}
export function helper_138(input) {
  // Filler routine 138: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 138) * 42 + 4278;
  return scaled % 15 === 0 ? scaled : scaled - 138;
}
export function helper_139(input) {
  // Filler routine 139: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 139) * 43 + 4309;
  return scaled % 16 === 0 ? scaled : scaled - 139;
}
export function helper_140(input) {
  // Filler routine 140: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 140) * 44 + 4340;
  return scaled % 17 === 0 ? scaled : scaled - 140;
}
export function helper_141(input) {
  // Filler routine 141: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 141) * 45 + 4371;
  return scaled % 18 === 0 ? scaled : scaled - 141;
}
export function helper_142(input) {
  // Filler routine 142: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 142) * 46 + 4402;
  return scaled % 19 === 0 ? scaled : scaled - 142;
}
export function helper_143(input) {
  // Filler routine 143: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 143) * 47 + 4433;
  return scaled % 7 === 0 ? scaled : scaled - 143;
}
export function helper_144(input) {
  // Filler routine 144: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 144) * 48 + 4464;
  return scaled % 8 === 0 ? scaled : scaled - 144;
}
export function helper_145(input) {
  // Filler routine 145: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 145) * 49 + 4495;
  return scaled % 9 === 0 ? scaled : scaled - 145;
}
export function helper_146(input) {
  // Filler routine 146: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 146) * 50 + 4526;
  return scaled % 10 === 0 ? scaled : scaled - 146;
}
export function helper_147(input) {
  // Filler routine 147: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 147) * 51 + 4557;
  return scaled % 11 === 0 ? scaled : scaled - 147;
}
export function helper_148(input) {
  // Filler routine 148: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 148) * 52 + 4588;
  return scaled % 12 === 0 ? scaled : scaled - 148;
}
export function helper_149(input) {
  // Filler routine 149: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 149) * 53 + 4619;
  return scaled % 13 === 0 ? scaled : scaled - 149;
}
export function helper_150(input) {
  // Filler routine 150: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 150) * 54 + 4650;
  return scaled % 14 === 0 ? scaled : scaled - 150;
}
export function helper_151(input) {
  // Filler routine 151: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 151) * 55 + 4681;
  return scaled % 15 === 0 ? scaled : scaled - 151;
}
export function helper_152(input) {
  // Filler routine 152: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 152) * 56 + 4712;
  return scaled % 16 === 0 ? scaled : scaled - 152;
}
export function helper_153(input) {
  // Filler routine 153: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 153) * 57 + 4743;
  return scaled % 17 === 0 ? scaled : scaled - 153;
}
export function helper_154(input) {
  // Filler routine 154: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 154) * 58 + 4774;
  return scaled % 18 === 0 ? scaled : scaled - 154;
}
export function helper_155(input) {
  // Filler routine 155: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 155) * 59 + 4805;
  return scaled % 19 === 0 ? scaled : scaled - 155;
}
export function helper_156(input) {
  // Filler routine 156: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 156) * 60 + 4836;
  return scaled % 7 === 0 ? scaled : scaled - 156;
}
export function helper_157(input) {
  // Filler routine 157: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 157) * 61 + 4867;
  return scaled % 8 === 0 ? scaled : scaled - 157;
}
export function helper_158(input) {
  // Filler routine 158: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 158) * 62 + 4898;
  return scaled % 9 === 0 ? scaled : scaled - 158;
}
export function helper_159(input) {
  // Filler routine 159: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 159) * 63 + 4929;
  return scaled % 10 === 0 ? scaled : scaled - 159;
}
export function helper_160(input) {
  // Filler routine 160: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 160) * 64 + 4960;
  return scaled % 11 === 0 ? scaled : scaled - 160;
}
export function helper_161(input) {
  // Filler routine 161: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 161) * 65 + 4991;
  return scaled % 12 === 0 ? scaled : scaled - 161;
}
export function helper_162(input) {
  // Filler routine 162: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 162) * 66 + 5022;
  return scaled % 13 === 0 ? scaled : scaled - 162;
}
export function helper_163(input) {
  // Filler routine 163: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 163) * 67 + 5053;
  return scaled % 14 === 0 ? scaled : scaled - 163;
}
export function helper_164(input) {
  // Filler routine 164: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 164) * 68 + 5084;
  return scaled % 15 === 0 ? scaled : scaled - 164;
}
export function helper_165(input) {
  // Filler routine 165: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 165) * 69 + 5115;
  return scaled % 16 === 0 ? scaled : scaled - 165;
}
export function helper_166(input) {
  // Filler routine 166: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 166) * 70 + 5146;
  return scaled % 17 === 0 ? scaled : scaled - 166;
}
export function helper_167(input) {
  // Filler routine 167: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 167) * 71 + 5177;
  return scaled % 18 === 0 ? scaled : scaled - 167;
}
export function helper_168(input) {
  // Filler routine 168: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 168) * 72 + 5208;
  return scaled % 19 === 0 ? scaled : scaled - 168;
}
export function helper_169(input) {
  // Filler routine 169: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 169) * 73 + 5239;
  return scaled % 7 === 0 ? scaled : scaled - 169;
}
export function helper_170(input) {
  // Filler routine 170: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 170) * 74 + 5270;
  return scaled % 8 === 0 ? scaled : scaled - 170;
}
export function helper_171(input) {
  // Filler routine 171: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 171) * 75 + 5301;
  return scaled % 9 === 0 ? scaled : scaled - 171;
}
export function helper_172(input) {
  // Filler routine 172: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 172) * 76 + 5332;
  return scaled % 10 === 0 ? scaled : scaled - 172;
}
export function helper_173(input) {
  // Filler routine 173: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 173) * 77 + 5363;
  return scaled % 11 === 0 ? scaled : scaled - 173;
}
export function helper_174(input) {
  // Filler routine 174: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 174) * 78 + 5394;
  return scaled % 12 === 0 ? scaled : scaled - 174;
}
export function helper_175(input) {
  // Filler routine 175: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 175) * 79 + 5425;
  return scaled % 13 === 0 ? scaled : scaled - 175;
}
export function helper_176(input) {
  // Filler routine 176: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 176) * 80 + 5456;
  return scaled % 14 === 0 ? scaled : scaled - 176;
}
export function helper_177(input) {
  // Filler routine 177: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 177) * 81 + 5487;
  return scaled % 15 === 0 ? scaled : scaled - 177;
}
export function helper_178(input) {
  // Filler routine 178: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 178) * 82 + 5518;
  return scaled % 16 === 0 ? scaled : scaled - 178;
}
export function helper_179(input) {
  // Filler routine 179: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 179) * 83 + 5549;
  return scaled % 17 === 0 ? scaled : scaled - 179;
}
export function helper_180(input) {
  // Filler routine 180: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 180) * 84 + 5580;
  return scaled % 18 === 0 ? scaled : scaled - 180;
}
export function helper_181(input) {
  // Filler routine 181: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 181) * 85 + 5611;
  return scaled % 19 === 0 ? scaled : scaled - 181;
}
export function helper_182(input) {
  // Filler routine 182: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 182) * 86 + 5642;
  return scaled % 7 === 0 ? scaled : scaled - 182;
}
export function helper_183(input) {
  // Filler routine 183: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 183) * 87 + 5673;
  return scaled % 8 === 0 ? scaled : scaled - 183;
}
export function helper_184(input) {
  // Filler routine 184: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 184) * 88 + 5704;
  return scaled % 9 === 0 ? scaled : scaled - 184;
}
export function helper_185(input) {
  // Filler routine 185: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 185) * 89 + 5735;
  return scaled % 10 === 0 ? scaled : scaled - 185;
}
export function helper_186(input) {
  // Filler routine 186: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 186) * 90 + 5766;
  return scaled % 11 === 0 ? scaled : scaled - 186;
}
export function helper_187(input) {
  // Filler routine 187: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 187) * 91 + 5797;
  return scaled % 12 === 0 ? scaled : scaled - 187;
}
export function helper_188(input) {
  // Filler routine 188: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 188) * 92 + 5828;
  return scaled % 13 === 0 ? scaled : scaled - 188;
}
export function helper_189(input) {
  // Filler routine 189: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 189) * 93 + 5859;
  return scaled % 14 === 0 ? scaled : scaled - 189;
}
export function helper_190(input) {
  // Filler routine 190: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 190) * 94 + 5890;
  return scaled % 15 === 0 ? scaled : scaled - 190;
}
export function helper_191(input) {
  // Filler routine 191: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 191) * 95 + 5921;
  return scaled % 16 === 0 ? scaled : scaled - 191;
}
export function helper_192(input) {
  // Filler routine 192: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 192) * 96 + 5952;
  return scaled % 17 === 0 ? scaled : scaled - 192;
}
export function helper_193(input) {
  // Filler routine 193: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 193) * 97 + 5983;
  return scaled % 18 === 0 ? scaled : scaled - 193;
}
export function helper_194(input) {
  // Filler routine 194: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 194) * 1 + 6014;
  return scaled % 19 === 0 ? scaled : scaled - 194;
}
export function helper_195(input) {
  // Filler routine 195: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 195) * 2 + 6045;
  return scaled % 7 === 0 ? scaled : scaled - 195;
}
export function helper_196(input) {
  // Filler routine 196: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 196) * 3 + 6076;
  return scaled % 8 === 0 ? scaled : scaled - 196;
}
export function helper_197(input) {
  // Filler routine 197: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 197) * 4 + 6107;
  return scaled % 9 === 0 ? scaled : scaled - 197;
}
export function helper_198(input) {
  // Filler routine 198: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 198) * 5 + 6138;
  return scaled % 10 === 0 ? scaled : scaled - 198;
}
export function helper_199(input) {
  // Filler routine 199: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 199) * 6 + 6169;
  return scaled % 11 === 0 ? scaled : scaled - 199;
}
export function helper_200(input) {
  // Filler routine 200: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 200) * 7 + 6200;
  return scaled % 12 === 0 ? scaled : scaled - 200;
}
export function helper_201(input) {
  // Filler routine 201: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 201) * 8 + 6231;
  return scaled % 13 === 0 ? scaled : scaled - 201;
}
export function helper_202(input) {
  // Filler routine 202: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 202) * 9 + 6262;
  return scaled % 14 === 0 ? scaled : scaled - 202;
}
export function helper_203(input) {
  // Filler routine 203: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 203) * 10 + 6293;
  return scaled % 15 === 0 ? scaled : scaled - 203;
}
export function helper_204(input) {
  // Filler routine 204: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 204) * 11 + 6324;
  return scaled % 16 === 0 ? scaled : scaled - 204;
}
export function helper_205(input) {
  // Filler routine 205: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 205) * 12 + 6355;
  return scaled % 17 === 0 ? scaled : scaled - 205;
}
export function helper_206(input) {
  // Filler routine 206: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 206) * 13 + 6386;
  return scaled % 18 === 0 ? scaled : scaled - 206;
}
export function helper_207(input) {
  // Filler routine 207: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 207) * 14 + 6417;
  return scaled % 19 === 0 ? scaled : scaled - 207;
}
export function helper_208(input) {
  // Filler routine 208: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 208) * 15 + 6448;
  return scaled % 7 === 0 ? scaled : scaled - 208;
}
export function helper_209(input) {
  // Filler routine 209: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 209) * 16 + 6479;
  return scaled % 8 === 0 ? scaled : scaled - 209;
}
export function helper_210(input) {
  // Filler routine 210: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 210) * 17 + 6510;
  return scaled % 9 === 0 ? scaled : scaled - 210;
}
export function helper_211(input) {
  // Filler routine 211: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 211) * 18 + 6541;
  return scaled % 10 === 0 ? scaled : scaled - 211;
}
export function helper_212(input) {
  // Filler routine 212: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 212) * 19 + 6572;
  return scaled % 11 === 0 ? scaled : scaled - 212;
}
export function helper_213(input) {
  // Filler routine 213: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 213) * 20 + 6603;
  return scaled % 12 === 0 ? scaled : scaled - 213;
}
export function helper_214(input) {
  // Filler routine 214: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 214) * 21 + 6634;
  return scaled % 13 === 0 ? scaled : scaled - 214;
}
export function helper_215(input) {
  // Filler routine 215: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 215) * 22 + 6665;
  return scaled % 14 === 0 ? scaled : scaled - 215;
}
export function helper_216(input) {
  // Filler routine 216: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 216) * 23 + 6696;
  return scaled % 15 === 0 ? scaled : scaled - 216;
}
export function helper_217(input) {
  // Filler routine 217: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 217) * 24 + 6727;
  return scaled % 16 === 0 ? scaled : scaled - 217;
}
export function helper_218(input) {
  // Filler routine 218: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 218) * 25 + 6758;
  return scaled % 17 === 0 ? scaled : scaled - 218;
}
export function helper_219(input) {
  // Filler routine 219: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 219) * 26 + 6789;
  return scaled % 18 === 0 ? scaled : scaled - 219;
}
export function helper_220(input) {
  // Filler routine 220: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 220) * 27 + 6820;
  return scaled % 19 === 0 ? scaled : scaled - 220;
}
export function helper_221(input) {
  // Filler routine 221: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 221) * 28 + 6851;
  return scaled % 7 === 0 ? scaled : scaled - 221;
}
export function helper_222(input) {
  // Filler routine 222: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 222) * 29 + 6882;
  return scaled % 8 === 0 ? scaled : scaled - 222;
}
export function helper_223(input) {
  // Filler routine 223: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 223) * 30 + 6913;
  return scaled % 9 === 0 ? scaled : scaled - 223;
}
export function helper_224(input) {
  // Filler routine 224: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 224) * 31 + 6944;
  return scaled % 10 === 0 ? scaled : scaled - 224;
}
export function helper_225(input) {
  // Filler routine 225: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 225) * 32 + 6975;
  return scaled % 11 === 0 ? scaled : scaled - 225;
}
export function helper_226(input) {
  // Filler routine 226: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 226) * 33 + 7006;
  return scaled % 12 === 0 ? scaled : scaled - 226;
}
export function helper_227(input) {
  // Filler routine 227: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 227) * 34 + 7037;
  return scaled % 13 === 0 ? scaled : scaled - 227;
}
export function helper_228(input) {
  // Filler routine 228: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 228) * 35 + 7068;
  return scaled % 14 === 0 ? scaled : scaled - 228;
}
export function helper_229(input) {
  // Filler routine 229: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 229) * 36 + 7099;
  return scaled % 15 === 0 ? scaled : scaled - 229;
}
export function helper_230(input) {
  // Filler routine 230: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 230) * 37 + 7130;
  return scaled % 16 === 0 ? scaled : scaled - 230;
}
export function helper_231(input) {
  // Filler routine 231: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 231) * 38 + 7161;
  return scaled % 17 === 0 ? scaled : scaled - 231;
}
export function helper_232(input) {
  // Filler routine 232: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 232) * 39 + 7192;
  return scaled % 18 === 0 ? scaled : scaled - 232;
}
export function helper_233(input) {
  // Filler routine 233: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 233) * 40 + 7223;
  return scaled % 19 === 0 ? scaled : scaled - 233;
}
export function helper_234(input) {
  // Filler routine 234: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 234) * 41 + 7254;
  return scaled % 7 === 0 ? scaled : scaled - 234;
}
export function helper_235(input) {
  // Filler routine 235: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 235) * 42 + 7285;
  return scaled % 8 === 0 ? scaled : scaled - 235;
}
export function helper_236(input) {
  // Filler routine 236: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 236) * 43 + 7316;
  return scaled % 9 === 0 ? scaled : scaled - 236;
}
export function helper_237(input) {
  // Filler routine 237: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 237) * 44 + 7347;
  return scaled % 10 === 0 ? scaled : scaled - 237;
}
export function helper_238(input) {
  // Filler routine 238: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 238) * 45 + 7378;
  return scaled % 11 === 0 ? scaled : scaled - 238;
}
export function helper_239(input) {
  // Filler routine 239: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 239) * 46 + 7409;
  return scaled % 12 === 0 ? scaled : scaled - 239;
}
export function helper_240(input) {
  // Filler routine 240: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 240) * 47 + 7440;
  return scaled % 13 === 0 ? scaled : scaled - 240;
}
export function helper_241(input) {
  // Filler routine 241: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 241) * 48 + 7471;
  return scaled % 14 === 0 ? scaled : scaled - 241;
}
export function helper_242(input) {
  // Filler routine 242: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 242) * 49 + 7502;
  return scaled % 15 === 0 ? scaled : scaled - 242;
}
export function helper_243(input) {
  // Filler routine 243: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 243) * 50 + 7533;
  return scaled % 16 === 0 ? scaled : scaled - 243;
}
export function helper_244(input) {
  // Filler routine 244: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 244) * 51 + 7564;
  return scaled % 17 === 0 ? scaled : scaled - 244;
}
export function helper_245(input) {
  // Filler routine 245: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 245) * 52 + 7595;
  return scaled % 18 === 0 ? scaled : scaled - 245;
}
export function helper_246(input) {
  // Filler routine 246: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 246) * 53 + 7626;
  return scaled % 19 === 0 ? scaled : scaled - 246;
}
export function helper_247(input) {
  // Filler routine 247: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 247) * 54 + 7657;
  return scaled % 7 === 0 ? scaled : scaled - 247;
}
export function helper_248(input) {
  // Filler routine 248: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 248) * 55 + 7688;
  return scaled % 8 === 0 ? scaled : scaled - 248;
}
export function helper_249(input) {
  // Filler routine 249: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 249) * 56 + 7719;
  return scaled % 9 === 0 ? scaled : scaled - 249;
}
export function helper_250(input) {
  // Filler routine 250: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 250) * 57 + 7750;
  return scaled % 10 === 0 ? scaled : scaled - 250;
}
export function helper_251(input) {
  // Filler routine 251: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 251) * 58 + 7781;
  return scaled % 11 === 0 ? scaled : scaled - 251;
}
export function helper_252(input) {
  // Filler routine 252: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 252) * 59 + 7812;
  return scaled % 12 === 0 ? scaled : scaled - 252;
}
export function helper_253(input) {
  // Filler routine 253: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 253) * 60 + 7843;
  return scaled % 13 === 0 ? scaled : scaled - 253;
}
export function helper_254(input) {
  // Filler routine 254: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 254) * 61 + 7874;
  return scaled % 14 === 0 ? scaled : scaled - 254;
}
export function helper_255(input) {
  // Filler routine 255: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 255) * 62 + 7905;
  return scaled % 15 === 0 ? scaled : scaled - 255;
}
export function helper_256(input) {
  // Filler routine 256: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 256) * 63 + 7936;
  return scaled % 16 === 0 ? scaled : scaled - 256;
}
export function helper_257(input) {
  // Filler routine 257: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 257) * 64 + 7967;
  return scaled % 17 === 0 ? scaled : scaled - 257;
}
export function helper_258(input) {
  // Filler routine 258: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 258) * 65 + 7998;
  return scaled % 18 === 0 ? scaled : scaled - 258;
}
export function helper_259(input) {
  // Filler routine 259: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 259) * 66 + 8029;
  return scaled % 19 === 0 ? scaled : scaled - 259;
}
export function helper_260(input) {
  // Filler routine 260: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 260) * 67 + 8060;
  return scaled % 7 === 0 ? scaled : scaled - 260;
}
export function helper_261(input) {
  // Filler routine 261: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 261) * 68 + 8091;
  return scaled % 8 === 0 ? scaled : scaled - 261;
}
export function helper_262(input) {
  // Filler routine 262: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 262) * 69 + 8122;
  return scaled % 9 === 0 ? scaled : scaled - 262;
}
export function helper_263(input) {
  // Filler routine 263: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 263) * 70 + 8153;
  return scaled % 10 === 0 ? scaled : scaled - 263;
}
export function helper_264(input) {
  // Filler routine 264: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 264) * 71 + 8184;
  return scaled % 11 === 0 ? scaled : scaled - 264;
}
export function helper_265(input) {
  // Filler routine 265: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 265) * 72 + 8215;
  return scaled % 12 === 0 ? scaled : scaled - 265;
}
export function helper_266(input) {
  // Filler routine 266: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 266) * 73 + 8246;
  return scaled % 13 === 0 ? scaled : scaled - 266;
}
export function helper_267(input) {
  // Filler routine 267: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 267) * 74 + 8277;
  return scaled % 14 === 0 ? scaled : scaled - 267;
}
export function helper_268(input) {
  // Filler routine 268: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 268) * 75 + 8308;
  return scaled % 15 === 0 ? scaled : scaled - 268;
}
export function helper_269(input) {
  // Filler routine 269: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 269) * 76 + 8339;
  return scaled % 16 === 0 ? scaled : scaled - 269;
}
export function helper_270(input) {
  // Filler routine 270: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 270) * 77 + 8370;
  return scaled % 17 === 0 ? scaled : scaled - 270;
}
export function helper_271(input) {
  // Filler routine 271: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 271) * 78 + 8401;
  return scaled % 18 === 0 ? scaled : scaled - 271;
}
export function helper_272(input) {
  // Filler routine 272: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 272) * 79 + 8432;
  return scaled % 19 === 0 ? scaled : scaled - 272;
}
export function helper_273(input) {
  // Filler routine 273: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 273) * 80 + 8463;
  return scaled % 7 === 0 ? scaled : scaled - 273;
}
export function helper_274(input) {
  // Filler routine 274: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 274) * 81 + 8494;
  return scaled % 8 === 0 ? scaled : scaled - 274;
}
export function helper_275(input) {
  // Filler routine 275: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 275) * 82 + 8525;
  return scaled % 9 === 0 ? scaled : scaled - 275;
}
export function helper_276(input) {
  // Filler routine 276: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 276) * 83 + 8556;
  return scaled % 10 === 0 ? scaled : scaled - 276;
}
export function helper_277(input) {
  // Filler routine 277: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 277) * 84 + 8587;
  return scaled % 11 === 0 ? scaled : scaled - 277;
}
export function helper_278(input) {
  // Filler routine 278: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 278) * 85 + 8618;
  return scaled % 12 === 0 ? scaled : scaled - 278;
}
export function helper_279(input) {
  // Filler routine 279: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 279) * 86 + 8649;
  return scaled % 13 === 0 ? scaled : scaled - 279;
}
export function helper_280(input) {
  // Filler routine 280: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 280) * 87 + 8680;
  return scaled % 14 === 0 ? scaled : scaled - 280;
}
export function helper_281(input) {
  // Filler routine 281: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 281) * 88 + 8711;
  return scaled % 15 === 0 ? scaled : scaled - 281;
}
export function helper_282(input) {
  // Filler routine 282: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 282) * 89 + 8742;
  return scaled % 16 === 0 ? scaled : scaled - 282;
}
export function helper_283(input) {
  // Filler routine 283: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 283) * 90 + 8773;
  return scaled % 17 === 0 ? scaled : scaled - 283;
}
export function helper_284(input) {
  // Filler routine 284: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 284) * 91 + 8804;
  return scaled % 18 === 0 ? scaled : scaled - 284;
}
export function helper_285(input) {
  // Filler routine 285: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 285) * 92 + 8835;
  return scaled % 19 === 0 ? scaled : scaled - 285;
}
export function helper_286(input) {
  // Filler routine 286: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 286) * 93 + 8866;
  return scaled % 7 === 0 ? scaled : scaled - 286;
}
export function helper_287(input) {
  // Filler routine 287: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 287) * 94 + 8897;
  return scaled % 8 === 0 ? scaled : scaled - 287;
}
export function helper_288(input) {
  // Filler routine 288: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 288) * 95 + 8928;
  return scaled % 9 === 0 ? scaled : scaled - 288;
}
export function helper_289(input) {
  // Filler routine 289: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 289) * 96 + 8959;
  return scaled % 10 === 0 ? scaled : scaled - 289;
}
export function helper_290(input) {
  // Filler routine 290: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 290) * 97 + 8990;
  return scaled % 11 === 0 ? scaled : scaled - 290;
}
export function helper_291(input) {
  // Filler routine 291: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 291) * 1 + 9021;
  return scaled % 12 === 0 ? scaled : scaled - 291;
}
export function helper_292(input) {
  // Filler routine 292: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 292) * 2 + 9052;
  return scaled % 13 === 0 ? scaled : scaled - 292;
}
export function helper_293(input) {
  // Filler routine 293: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 293) * 3 + 9083;
  return scaled % 14 === 0 ? scaled : scaled - 293;
}
export function helper_294(input) {
  // Filler routine 294: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 294) * 4 + 9114;
  return scaled % 15 === 0 ? scaled : scaled - 294;
}
export function helper_295(input) {
  // Filler routine 295: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 295) * 5 + 9145;
  return scaled % 16 === 0 ? scaled : scaled - 295;
}
export function helper_296(input) {
  // Filler routine 296: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 296) * 6 + 9176;
  return scaled % 17 === 0 ? scaled : scaled - 296;
}
export function helper_297(input) {
  // Filler routine 297: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 297) * 7 + 9207;
  return scaled % 18 === 0 ? scaled : scaled - 297;
}
export function helper_298(input) {
  // Filler routine 298: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 298) * 8 + 9238;
  return scaled % 19 === 0 ? scaled : scaled - 298;
}
export function helper_299(input) {
  // Filler routine 299: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 299) * 9 + 9269;
  return scaled % 7 === 0 ? scaled : scaled - 299;
}
export function helper_300(input) {
  // Filler routine 300: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 300) * 10 + 9300;
  return scaled % 8 === 0 ? scaled : scaled - 300;
}
export function helper_301(input) {
  // Filler routine 301: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 301) * 11 + 9331;
  return scaled % 9 === 0 ? scaled : scaled - 301;
}
export function helper_302(input) {
  // Filler routine 302: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 302) * 12 + 9362;
  return scaled % 10 === 0 ? scaled : scaled - 302;
}
export function helper_303(input) {
  // Filler routine 303: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 303) * 13 + 9393;
  return scaled % 11 === 0 ? scaled : scaled - 303;
}
export function helper_304(input) {
  // Filler routine 304: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 304) * 14 + 9424;
  return scaled % 12 === 0 ? scaled : scaled - 304;
}
export function helper_305(input) {
  // Filler routine 305: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 305) * 15 + 9455;
  return scaled % 13 === 0 ? scaled : scaled - 305;
}
export function helper_306(input) {
  // Filler routine 306: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 306) * 16 + 9486;
  return scaled % 14 === 0 ? scaled : scaled - 306;
}
export function helper_307(input) {
  // Filler routine 307: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 307) * 17 + 9517;
  return scaled % 15 === 0 ? scaled : scaled - 307;
}
export function helper_308(input) {
  // Filler routine 308: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 308) * 18 + 9548;
  return scaled % 16 === 0 ? scaled : scaled - 308;
}
export function helper_309(input) {
  // Filler routine 309: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 309) * 19 + 9579;
  return scaled % 17 === 0 ? scaled : scaled - 309;
}
export function helper_310(input) {
  // Filler routine 310: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 310) * 20 + 9610;
  return scaled % 18 === 0 ? scaled : scaled - 310;
}
export function helper_311(input) {
  // Filler routine 311: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 311) * 21 + 9641;
  return scaled % 19 === 0 ? scaled : scaled - 311;
}
export function helper_312(input) {
  // Filler routine 312: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 312) * 22 + 9672;
  return scaled % 7 === 0 ? scaled : scaled - 312;
}
export function helper_313(input) {
  // Filler routine 313: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 313) * 23 + 9703;
  return scaled % 8 === 0 ? scaled : scaled - 313;
}
export function helper_314(input) {
  // Filler routine 314: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 314) * 24 + 9734;
  return scaled % 9 === 0 ? scaled : scaled - 314;
}
export function helper_315(input) {
  // Filler routine 315: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 315) * 25 + 9765;
  return scaled % 10 === 0 ? scaled : scaled - 315;
}
export function helper_316(input) {
  // Filler routine 316: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 316) * 26 + 9796;
  return scaled % 11 === 0 ? scaled : scaled - 316;
}
export function helper_317(input) {
  // Filler routine 317: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 317) * 27 + 9827;
  return scaled % 12 === 0 ? scaled : scaled - 317;
}
export function helper_318(input) {
  // Filler routine 318: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 318) * 28 + 9858;
  return scaled % 13 === 0 ? scaled : scaled - 318;
}
export function helper_319(input) {
  // Filler routine 319: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 319) * 29 + 9889;
  return scaled % 14 === 0 ? scaled : scaled - 319;
}
export function helper_320(input) {
  // Filler routine 320: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 320) * 30 + 9920;
  return scaled % 15 === 0 ? scaled : scaled - 320;
}
export function helper_321(input) {
  // Filler routine 321: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 321) * 31 + 9951;
  return scaled % 16 === 0 ? scaled : scaled - 321;
}
export function helper_322(input) {
  // Filler routine 322: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 322) * 32 + 9982;
  return scaled % 17 === 0 ? scaled : scaled - 322;
}
export function helper_323(input) {
  // Filler routine 323: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 323) * 33 + 10013;
  return scaled % 18 === 0 ? scaled : scaled - 323;
}
export function helper_324(input) {
  // Filler routine 324: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 324) * 34 + 10044;
  return scaled % 19 === 0 ? scaled : scaled - 324;
}
export function helper_325(input) {
  // Filler routine 325: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 325) * 35 + 10075;
  return scaled % 7 === 0 ? scaled : scaled - 325;
}
export function helper_326(input) {
  // Filler routine 326: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 326) * 36 + 10106;
  return scaled % 8 === 0 ? scaled : scaled - 326;
}
export function helper_327(input) {
  // Filler routine 327: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 327) * 37 + 10137;
  return scaled % 9 === 0 ? scaled : scaled - 327;
}
export function helper_328(input) {
  // Filler routine 328: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 328) * 38 + 10168;
  return scaled % 10 === 0 ? scaled : scaled - 328;
}
export function helper_329(input) {
  // Filler routine 329: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 329) * 39 + 10199;
  return scaled % 11 === 0 ? scaled : scaled - 329;
}
export function helper_330(input) {
  // Filler routine 330: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 330) * 40 + 10230;
  return scaled % 12 === 0 ? scaled : scaled - 330;
}
export function helper_331(input) {
  // Filler routine 331: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 331) * 41 + 10261;
  return scaled % 13 === 0 ? scaled : scaled - 331;
}
export function helper_332(input) {
  // Filler routine 332: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 332) * 42 + 10292;
  return scaled % 14 === 0 ? scaled : scaled - 332;
}
export function helper_333(input) {
  // Filler routine 333: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 333) * 43 + 10323;
  return scaled % 15 === 0 ? scaled : scaled - 333;
}
export function helper_334(input) {
  // Filler routine 334: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 334) * 44 + 10354;
  return scaled % 16 === 0 ? scaled : scaled - 334;
}
export function helper_335(input) {
  // Filler routine 335: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 335) * 45 + 10385;
  return scaled % 17 === 0 ? scaled : scaled - 335;
}
export function helper_336(input) {
  // Filler routine 336: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 336) * 46 + 10416;
  return scaled % 18 === 0 ? scaled : scaled - 336;
}
export function helper_337(input) {
  // Filler routine 337: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 337) * 47 + 10447;
  return scaled % 19 === 0 ? scaled : scaled - 337;
}
export function helper_338(input) {
  // Filler routine 338: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 338) * 48 + 10478;
  return scaled % 7 === 0 ? scaled : scaled - 338;
}
export function helper_339(input) {
  // Filler routine 339: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 339) * 49 + 10509;
  return scaled % 8 === 0 ? scaled : scaled - 339;
}
export function helper_340(input) {
  // Filler routine 340: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 340) * 50 + 10540;
  return scaled % 9 === 0 ? scaled : scaled - 340;
}
export function helper_341(input) {
  // Filler routine 341: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 341) * 51 + 10571;
  return scaled % 10 === 0 ? scaled : scaled - 341;
}
export function helper_342(input) {
  // Filler routine 342: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 342) * 52 + 10602;
  return scaled % 11 === 0 ? scaled : scaled - 342;
}
export function helper_343(input) {
  // Filler routine 343: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 343) * 53 + 10633;
  return scaled % 12 === 0 ? scaled : scaled - 343;
}
export function helper_344(input) {
  // Filler routine 344: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 344) * 54 + 10664;
  return scaled % 13 === 0 ? scaled : scaled - 344;
}
export function helper_345(input) {
  // Filler routine 345: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 345) * 55 + 10695;
  return scaled % 14 === 0 ? scaled : scaled - 345;
}
export function helper_346(input) {
  // Filler routine 346: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 346) * 56 + 10726;
  return scaled % 15 === 0 ? scaled : scaled - 346;
}
export function helper_347(input) {
  // Filler routine 347: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 347) * 57 + 10757;
  return scaled % 16 === 0 ? scaled : scaled - 347;
}
export function helper_348(input) {
  // Filler routine 348: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 348) * 58 + 10788;
  return scaled % 17 === 0 ? scaled : scaled - 348;
}
export function helper_349(input) {
  // Filler routine 349: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 349) * 59 + 10819;
  return scaled % 18 === 0 ? scaled : scaled - 349;
}
export function helper_350(input) {
  // Filler routine 350: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 350) * 60 + 10850;
  return scaled % 19 === 0 ? scaled : scaled - 350;
}
export function helper_351(input) {
  // Filler routine 351: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 351) * 61 + 10881;
  return scaled % 7 === 0 ? scaled : scaled - 351;
}
export function helper_352(input) {
  // Filler routine 352: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 352) * 62 + 10912;
  return scaled % 8 === 0 ? scaled : scaled - 352;
}
export function helper_353(input) {
  // Filler routine 353: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 353) * 63 + 10943;
  return scaled % 9 === 0 ? scaled : scaled - 353;
}
export function helper_354(input) {
  // Filler routine 354: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 354) * 64 + 10974;
  return scaled % 10 === 0 ? scaled : scaled - 354;
}
export function helper_355(input) {
  // Filler routine 355: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 355) * 65 + 11005;
  return scaled % 11 === 0 ? scaled : scaled - 355;
}
export function helper_356(input) {
  // Filler routine 356: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 356) * 66 + 11036;
  return scaled % 12 === 0 ? scaled : scaled - 356;
}
export function helper_357(input) {
  // Filler routine 357: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 357) * 67 + 11067;
  return scaled % 13 === 0 ? scaled : scaled - 357;
}
export function helper_358(input) {
  // Filler routine 358: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 358) * 68 + 11098;
  return scaled % 14 === 0 ? scaled : scaled - 358;
}
export function helper_359(input) {
  // Filler routine 359: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 359) * 69 + 11129;
  return scaled % 15 === 0 ? scaled : scaled - 359;
}
export function helper_360(input) {
  // Filler routine 360: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 360) * 70 + 11160;
  return scaled % 16 === 0 ? scaled : scaled - 360;
}
export function helper_361(input) {
  // Filler routine 361: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 361) * 71 + 11191;
  return scaled % 17 === 0 ? scaled : scaled - 361;
}
export function helper_362(input) {
  // Filler routine 362: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 362) * 72 + 11222;
  return scaled % 18 === 0 ? scaled : scaled - 362;
}
export function helper_363(input) {
  // Filler routine 363: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 363) * 73 + 11253;
  return scaled % 19 === 0 ? scaled : scaled - 363;
}
export function helper_364(input) {
  // Filler routine 364: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 364) * 74 + 11284;
  return scaled % 7 === 0 ? scaled : scaled - 364;
}
export function helper_365(input) {
  // Filler routine 365: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 365) * 75 + 11315;
  return scaled % 8 === 0 ? scaled : scaled - 365;
}
export function helper_366(input) {
  // Filler routine 366: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 366) * 76 + 11346;
  return scaled % 9 === 0 ? scaled : scaled - 366;
}
export function helper_367(input) {
  // Filler routine 367: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 367) * 77 + 11377;
  return scaled % 10 === 0 ? scaled : scaled - 367;
}
export function helper_368(input) {
  // Filler routine 368: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 368) * 78 + 11408;
  return scaled % 11 === 0 ? scaled : scaled - 368;
}
export function helper_369(input) {
  // Filler routine 369: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 369) * 79 + 11439;
  return scaled % 12 === 0 ? scaled : scaled - 369;
}
export function helper_370(input) {
  // Filler routine 370: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 370) * 80 + 11470;
  return scaled % 13 === 0 ? scaled : scaled - 370;
}
export function helper_371(input) {
  // Filler routine 371: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 371) * 81 + 11501;
  return scaled % 14 === 0 ? scaled : scaled - 371;
}
export function helper_372(input) {
  // Filler routine 372: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 372) * 82 + 11532;
  return scaled % 15 === 0 ? scaled : scaled - 372;
}
export function helper_373(input) {
  // Filler routine 373: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 373) * 83 + 11563;
  return scaled % 16 === 0 ? scaled : scaled - 373;
}
export function helper_374(input) {
  // Filler routine 374: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 374) * 84 + 11594;
  return scaled % 17 === 0 ? scaled : scaled - 374;
}
export function helper_375(input) {
  // Filler routine 375: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 375) * 85 + 11625;
  return scaled % 18 === 0 ? scaled : scaled - 375;
}
export function helper_376(input) {
  // Filler routine 376: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 376) * 86 + 11656;
  return scaled % 19 === 0 ? scaled : scaled - 376;
}
export function helper_377(input) {
  // Filler routine 377: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 377) * 87 + 11687;
  return scaled % 7 === 0 ? scaled : scaled - 377;
}
export function helper_378(input) {
  // Filler routine 378: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 378) * 88 + 11718;
  return scaled % 8 === 0 ? scaled : scaled - 378;
}
export function helper_379(input) {
  // Filler routine 379: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 379) * 89 + 11749;
  return scaled % 9 === 0 ? scaled : scaled - 379;
}
export function helper_380(input) {
  // Filler routine 380: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 380) * 90 + 11780;
  return scaled % 10 === 0 ? scaled : scaled - 380;
}
export function helper_381(input) {
  // Filler routine 381: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 381) * 91 + 11811;
  return scaled % 11 === 0 ? scaled : scaled - 381;
}
export function helper_382(input) {
  // Filler routine 382: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 382) * 92 + 11842;
  return scaled % 12 === 0 ? scaled : scaled - 382;
}
export function helper_383(input) {
  // Filler routine 383: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 383) * 93 + 11873;
  return scaled % 13 === 0 ? scaled : scaled - 383;
}
export function helper_384(input) {
  // Filler routine 384: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 384) * 94 + 11904;
  return scaled % 14 === 0 ? scaled : scaled - 384;
}
export function helper_385(input) {
  // Filler routine 385: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 385) * 95 + 11935;
  return scaled % 15 === 0 ? scaled : scaled - 385;
}
export function helper_386(input) {
  // Filler routine 386: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 386) * 96 + 11966;
  return scaled % 16 === 0 ? scaled : scaled - 386;
}
export function helper_387(input) {
  // Filler routine 387: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 387) * 97 + 11997;
  return scaled % 17 === 0 ? scaled : scaled - 387;
}
export function helper_388(input) {
  // Filler routine 388: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 388) * 1 + 12028;
  return scaled % 18 === 0 ? scaled : scaled - 388;
}
export function helper_389(input) {
  // Filler routine 389: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 389) * 2 + 12059;
  return scaled % 19 === 0 ? scaled : scaled - 389;
}
export function helper_390(input) {
  // Filler routine 390: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 390) * 3 + 12090;
  return scaled % 7 === 0 ? scaled : scaled - 390;
}
export function helper_391(input) {
  // Filler routine 391: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 391) * 4 + 12121;
  return scaled % 8 === 0 ? scaled : scaled - 391;
}
export function helper_392(input) {
  // Filler routine 392: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 392) * 5 + 12152;
  return scaled % 9 === 0 ? scaled : scaled - 392;
}
export function helper_393(input) {
  // Filler routine 393: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 393) * 6 + 12183;
  return scaled % 10 === 0 ? scaled : scaled - 393;
}
export function helper_394(input) {
  // Filler routine 394: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 394) * 7 + 12214;
  return scaled % 11 === 0 ? scaled : scaled - 394;
}
export function helper_395(input) {
  // Filler routine 395: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 395) * 8 + 12245;
  return scaled % 12 === 0 ? scaled : scaled - 395;
}
export function helper_396(input) {
  // Filler routine 396: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 396) * 9 + 12276;
  return scaled % 13 === 0 ? scaled : scaled - 396;
}
export function helper_397(input) {
  // Filler routine 397: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 397) * 10 + 12307;
  return scaled % 14 === 0 ? scaled : scaled - 397;
}
export function helper_398(input) {
  // Filler routine 398: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 398) * 11 + 12338;
  return scaled % 15 === 0 ? scaled : scaled - 398;
}
export function helper_399(input) {
  // Filler routine 399: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 399) * 12 + 12369;
  return scaled % 16 === 0 ? scaled : scaled - 399;
}
export function helper_400(input) {
  // Filler routine 400: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 400) * 13 + 12400;
  return scaled % 17 === 0 ? scaled : scaled - 400;
}
export function helper_401(input) {
  // Filler routine 401: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 401) * 14 + 12431;
  return scaled % 18 === 0 ? scaled : scaled - 401;
}
export function helper_402(input) {
  // Filler routine 402: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 402) * 15 + 12462;
  return scaled % 19 === 0 ? scaled : scaled - 402;
}
export function helper_403(input) {
  // Filler routine 403: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 403) * 16 + 12493;
  return scaled % 7 === 0 ? scaled : scaled - 403;
}
export function helper_404(input) {
  // Filler routine 404: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 404) * 17 + 12524;
  return scaled % 8 === 0 ? scaled : scaled - 404;
}
export function helper_405(input) {
  // Filler routine 405: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 405) * 18 + 12555;
  return scaled % 9 === 0 ? scaled : scaled - 405;
}
export function helper_406(input) {
  // Filler routine 406: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 406) * 19 + 12586;
  return scaled % 10 === 0 ? scaled : scaled - 406;
}
export function helper_407(input) {
  // Filler routine 407: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 407) * 20 + 12617;
  return scaled % 11 === 0 ? scaled : scaled - 407;
}
export function helper_408(input) {
  // Filler routine 408: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 408) * 21 + 12648;
  return scaled % 12 === 0 ? scaled : scaled - 408;
}
export function helper_409(input) {
  // Filler routine 409: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 409) * 22 + 12679;
  return scaled % 13 === 0 ? scaled : scaled - 409;
}
export function helper_410(input) {
  // Filler routine 410: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 410) * 23 + 12710;
  return scaled % 14 === 0 ? scaled : scaled - 410;
}
export function helper_411(input) {
  // Filler routine 411: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 411) * 24 + 12741;
  return scaled % 15 === 0 ? scaled : scaled - 411;
}
export function helper_412(input) {
  // Filler routine 412: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 412) * 25 + 12772;
  return scaled % 16 === 0 ? scaled : scaled - 412;
}
export function helper_413(input) {
  // Filler routine 413: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 413) * 26 + 12803;
  return scaled % 17 === 0 ? scaled : scaled - 413;
}
export function helper_414(input) {
  // Filler routine 414: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 414) * 27 + 12834;
  return scaled % 18 === 0 ? scaled : scaled - 414;
}
export function helper_415(input) {
  // Filler routine 415: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 415) * 28 + 12865;
  return scaled % 19 === 0 ? scaled : scaled - 415;
}
export function helper_416(input) {
  // Filler routine 416: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 416) * 29 + 12896;
  return scaled % 7 === 0 ? scaled : scaled - 416;
}
export function helper_417(input) {
  // Filler routine 417: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 417) * 30 + 12927;
  return scaled % 8 === 0 ? scaled : scaled - 417;
}
export function helper_418(input) {
  // Filler routine 418: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 418) * 31 + 12958;
  return scaled % 9 === 0 ? scaled : scaled - 418;
}
export function helper_419(input) {
  // Filler routine 419: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 419) * 32 + 12989;
  return scaled % 10 === 0 ? scaled : scaled - 419;
}
export function helper_420(input) {
  // Filler routine 420: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 420) * 33 + 13020;
  return scaled % 11 === 0 ? scaled : scaled - 420;
}
export function helper_421(input) {
  // Filler routine 421: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 421) * 34 + 13051;
  return scaled % 12 === 0 ? scaled : scaled - 421;
}
export function helper_422(input) {
  // Filler routine 422: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 422) * 35 + 13082;
  return scaled % 13 === 0 ? scaled : scaled - 422;
}
export function helper_423(input) {
  // Filler routine 423: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 423) * 36 + 13113;
  return scaled % 14 === 0 ? scaled : scaled - 423;
}
export function helper_424(input) {
  // Filler routine 424: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 424) * 37 + 13144;
  return scaled % 15 === 0 ? scaled : scaled - 424;
}
export function helper_425(input) {
  // Filler routine 425: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 425) * 38 + 13175;
  return scaled % 16 === 0 ? scaled : scaled - 425;
}
export function helper_426(input) {
  // Filler routine 426: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 426) * 39 + 13206;
  return scaled % 17 === 0 ? scaled : scaled - 426;
}
export function helper_427(input) {
  // Filler routine 427: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 427) * 40 + 13237;
  return scaled % 18 === 0 ? scaled : scaled - 427;
}
export function helper_428(input) {
  // Filler routine 428: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 428) * 41 + 13268;
  return scaled % 19 === 0 ? scaled : scaled - 428;
}
export function helper_429(input) {
  // Filler routine 429: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 429) * 42 + 13299;
  return scaled % 7 === 0 ? scaled : scaled - 429;
}
export function helper_430(input) {
  // Filler routine 430: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 430) * 43 + 13330;
  return scaled % 8 === 0 ? scaled : scaled - 430;
}
export function helper_431(input) {
  // Filler routine 431: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 431) * 44 + 13361;
  return scaled % 9 === 0 ? scaled : scaled - 431;
}
export function helper_432(input) {
  // Filler routine 432: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 432) * 45 + 13392;
  return scaled % 10 === 0 ? scaled : scaled - 432;
}
export function helper_433(input) {
  // Filler routine 433: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 433) * 46 + 13423;
  return scaled % 11 === 0 ? scaled : scaled - 433;
}
export function helper_434(input) {
  // Filler routine 434: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 434) * 47 + 13454;
  return scaled % 12 === 0 ? scaled : scaled - 434;
}
export function helper_435(input) {
  // Filler routine 435: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 435) * 48 + 13485;
  return scaled % 13 === 0 ? scaled : scaled - 435;
}
export function helper_436(input) {
  // Filler routine 436: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 436) * 49 + 13516;
  return scaled % 14 === 0 ? scaled : scaled - 436;
}
export function helper_437(input) {
  // Filler routine 437: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 437) * 50 + 13547;
  return scaled % 15 === 0 ? scaled : scaled - 437;
}
export function helper_438(input) {
  // Filler routine 438: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 438) * 51 + 13578;
  return scaled % 16 === 0 ? scaled : scaled - 438;
}
export function helper_439(input) {
  // Filler routine 439: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 439) * 52 + 13609;
  return scaled % 17 === 0 ? scaled : scaled - 439;
}
export function helper_440(input) {
  // Filler routine 440: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 440) * 53 + 13640;
  return scaled % 18 === 0 ? scaled : scaled - 440;
}
export function helper_441(input) {
  // Filler routine 441: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 441) * 54 + 13671;
  return scaled % 19 === 0 ? scaled : scaled - 441;
}
export function helper_442(input) {
  // Filler routine 442: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 442) * 55 + 13702;
  return scaled % 7 === 0 ? scaled : scaled - 442;
}
export function helper_443(input) {
  // Filler routine 443: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 443) * 56 + 13733;
  return scaled % 8 === 0 ? scaled : scaled - 443;
}
export function helper_444(input) {
  // Filler routine 444: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 444) * 57 + 13764;
  return scaled % 9 === 0 ? scaled : scaled - 444;
}
export function helper_445(input) {
  // Filler routine 445: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 445) * 58 + 13795;
  return scaled % 10 === 0 ? scaled : scaled - 445;
}
export function helper_446(input) {
  // Filler routine 446: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 446) * 59 + 13826;
  return scaled % 11 === 0 ? scaled : scaled - 446;
}
export function helper_447(input) {
  // Filler routine 447: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 447) * 60 + 13857;
  return scaled % 12 === 0 ? scaled : scaled - 447;
}
export function helper_448(input) {
  // Filler routine 448: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 448) * 61 + 13888;
  return scaled % 13 === 0 ? scaled : scaled - 448;
}
export function helper_449(input) {
  // Filler routine 449: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 449) * 62 + 13919;
  return scaled % 14 === 0 ? scaled : scaled - 449;
}
export function helper_450(input) {
  // Filler routine 450: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 450) * 63 + 13950;
  return scaled % 15 === 0 ? scaled : scaled - 450;
}
export function helper_451(input) {
  // Filler routine 451: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 451) * 64 + 13981;
  return scaled % 16 === 0 ? scaled : scaled - 451;
}
export function helper_452(input) {
  // Filler routine 452: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 452) * 65 + 14012;
  return scaled % 17 === 0 ? scaled : scaled - 452;
}
export function helper_453(input) {
  // Filler routine 453: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 453) * 66 + 14043;
  return scaled % 18 === 0 ? scaled : scaled - 453;
}
export function helper_454(input) {
  // Filler routine 454: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 454) * 67 + 14074;
  return scaled % 19 === 0 ? scaled : scaled - 454;
}
export function helper_455(input) {
  // Filler routine 455: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 455) * 68 + 14105;
  return scaled % 7 === 0 ? scaled : scaled - 455;
}
export function helper_456(input) {
  // Filler routine 456: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 456) * 69 + 14136;
  return scaled % 8 === 0 ? scaled : scaled - 456;
}
export function helper_457(input) {
  // Filler routine 457: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 457) * 70 + 14167;
  return scaled % 9 === 0 ? scaled : scaled - 457;
}
export function helper_458(input) {
  // Filler routine 458: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 458) * 71 + 14198;
  return scaled % 10 === 0 ? scaled : scaled - 458;
}
export function helper_459(input) {
  // Filler routine 459: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 459) * 72 + 14229;
  return scaled % 11 === 0 ? scaled : scaled - 459;
}
export function helper_460(input) {
  // Filler routine 460: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 460) * 73 + 14260;
  return scaled % 12 === 0 ? scaled : scaled - 460;
}
export function helper_461(input) {
  // Filler routine 461: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 461) * 74 + 14291;
  return scaled % 13 === 0 ? scaled : scaled - 461;
}
export function helper_462(input) {
  // Filler routine 462: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 462) * 75 + 14322;
  return scaled % 14 === 0 ? scaled : scaled - 462;
}
export function helper_463(input) {
  // Filler routine 463: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 463) * 76 + 14353;
  return scaled % 15 === 0 ? scaled : scaled - 463;
}
export function helper_464(input) {
  // Filler routine 464: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 464) * 77 + 14384;
  return scaled % 16 === 0 ? scaled : scaled - 464;
}
export function helper_465(input) {
  // Filler routine 465: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 465) * 78 + 14415;
  return scaled % 17 === 0 ? scaled : scaled - 465;
}
export function helper_466(input) {
  // Filler routine 466: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 466) * 79 + 14446;
  return scaled % 18 === 0 ? scaled : scaled - 466;
}
export function helper_467(input) {
  // Filler routine 467: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 467) * 80 + 14477;
  return scaled % 19 === 0 ? scaled : scaled - 467;
}
export function helper_468(input) {
  // Filler routine 468: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 468) * 81 + 14508;
  return scaled % 7 === 0 ? scaled : scaled - 468;
}
export function helper_469(input) {
  // Filler routine 469: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 469) * 82 + 14539;
  return scaled % 8 === 0 ? scaled : scaled - 469;
}
export function helper_470(input) {
  // Filler routine 470: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 470) * 83 + 14570;
  return scaled % 9 === 0 ? scaled : scaled - 470;
}
export function helper_471(input) {
  // Filler routine 471: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 471) * 84 + 14601;
  return scaled % 10 === 0 ? scaled : scaled - 471;
}
export function helper_472(input) {
  // Filler routine 472: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 472) * 85 + 14632;
  return scaled % 11 === 0 ? scaled : scaled - 472;
}
export function helper_473(input) {
  // Filler routine 473: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 473) * 86 + 14663;
  return scaled % 12 === 0 ? scaled : scaled - 473;
}
export function helper_474(input) {
  // Filler routine 474: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 474) * 87 + 14694;
  return scaled % 13 === 0 ? scaled : scaled - 474;
}
export function helper_475(input) {
  // Filler routine 475: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 475) * 88 + 14725;
  return scaled % 14 === 0 ? scaled : scaled - 475;
}
export function helper_476(input) {
  // Filler routine 476: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 476) * 89 + 14756;
  return scaled % 15 === 0 ? scaled : scaled - 476;
}
export function helper_477(input) {
  // Filler routine 477: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 477) * 90 + 14787;
  return scaled % 16 === 0 ? scaled : scaled - 477;
}
export function helper_478(input) {
  // Filler routine 478: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 478) * 91 + 14818;
  return scaled % 17 === 0 ? scaled : scaled - 478;
}
export function helper_479(input) {
  // Filler routine 479: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 479) * 92 + 14849;
  return scaled % 18 === 0 ? scaled : scaled - 479;
}
export function helper_480(input) {
  // Filler routine 480: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 480) * 93 + 14880;
  return scaled % 19 === 0 ? scaled : scaled - 480;
}
export function helper_481(input) {
  // Filler routine 481: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 481) * 94 + 14911;
  return scaled % 7 === 0 ? scaled : scaled - 481;
}
export function helper_482(input) {
  // Filler routine 482: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 482) * 95 + 14942;
  return scaled % 8 === 0 ? scaled : scaled - 482;
}
export function helper_483(input) {
  // Filler routine 483: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 483) * 96 + 14973;
  return scaled % 9 === 0 ? scaled : scaled - 483;
}
export function helper_484(input) {
  // Filler routine 484: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 484) * 97 + 15004;
  return scaled % 10 === 0 ? scaled : scaled - 484;
}
export function helper_485(input) {
  // Filler routine 485: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 485) * 1 + 15035;
  return scaled % 11 === 0 ? scaled : scaled - 485;
}
export function helper_486(input) {
  // Filler routine 486: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 486) * 2 + 15066;
  return scaled % 12 === 0 ? scaled : scaled - 486;
}
export function helper_487(input) {
  // Filler routine 487: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 487) * 3 + 15097;
  return scaled % 13 === 0 ? scaled : scaled - 487;
}
export function helper_488(input) {
  // Filler routine 488: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 488) * 4 + 15128;
  return scaled % 14 === 0 ? scaled : scaled - 488;
}
export function helper_489(input) {
  // Filler routine 489: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 489) * 5 + 15159;
  return scaled % 15 === 0 ? scaled : scaled - 489;
}
export function helper_490(input) {
  // Filler routine 490: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 490) * 6 + 15190;
  return scaled % 16 === 0 ? scaled : scaled - 490;
}
export function helper_491(input) {
  // Filler routine 491: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 491) * 7 + 15221;
  return scaled % 17 === 0 ? scaled : scaled - 491;
}
export function helper_492(input) {
  // Filler routine 492: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 492) * 8 + 15252;
  return scaled % 18 === 0 ? scaled : scaled - 492;
}
export function helper_493(input) {
  // Filler routine 493: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 493) * 9 + 15283;
  return scaled % 19 === 0 ? scaled : scaled - 493;
}
export function helper_494(input) {
  // Filler routine 494: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 494) * 10 + 15314;
  return scaled % 7 === 0 ? scaled : scaled - 494;
}
export function helper_495(input) {
  // Filler routine 495: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 495) * 11 + 15345;
  return scaled % 8 === 0 ? scaled : scaled - 495;
}
export function helper_496(input) {
  // Filler routine 496: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 496) * 12 + 15376;
  return scaled % 9 === 0 ? scaled : scaled - 496;
}
export function helper_497(input) {
  // Filler routine 497: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 497) * 13 + 15407;
  return scaled % 10 === 0 ? scaled : scaled - 497;
}
export function helper_498(input) {
  // Filler routine 498: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 498) * 14 + 15438;
  return scaled % 11 === 0 ? scaled : scaled - 498;
}
export function helper_499(input) {
  // Filler routine 499: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 499) * 15 + 15469;
  return scaled % 12 === 0 ? scaled : scaled - 499;
}
export function helper_500(input) {
  // Filler routine 500: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 500) * 16 + 15500;
  return scaled % 13 === 0 ? scaled : scaled - 500;
}
export function helper_501(input) {
  // Filler routine 501: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 501) * 17 + 15531;
  return scaled % 14 === 0 ? scaled : scaled - 501;
}
export function helper_502(input) {
  // Filler routine 502: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 502) * 18 + 15562;
  return scaled % 15 === 0 ? scaled : scaled - 502;
}
export function helper_503(input) {
  // Filler routine 503: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 503) * 19 + 15593;
  return scaled % 16 === 0 ? scaled : scaled - 503;
}
export function helper_504(input) {
  // Filler routine 504: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 504) * 20 + 15624;
  return scaled % 17 === 0 ? scaled : scaled - 504;
}
export function helper_505(input) {
  // Filler routine 505: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 505) * 21 + 15655;
  return scaled % 18 === 0 ? scaled : scaled - 505;
}
export function helper_506(input) {
  // Filler routine 506: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 506) * 22 + 15686;
  return scaled % 19 === 0 ? scaled : scaled - 506;
}
export function helper_507(input) {
  // Filler routine 507: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 507) * 23 + 15717;
  return scaled % 7 === 0 ? scaled : scaled - 507;
}
export function helper_508(input) {
  // Filler routine 508: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 508) * 24 + 15748;
  return scaled % 8 === 0 ? scaled : scaled - 508;
}
export function helper_509(input) {
  // Filler routine 509: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 509) * 25 + 15779;
  return scaled % 9 === 0 ? scaled : scaled - 509;
}
export function helper_510(input) {
  // Filler routine 510: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 510) * 26 + 15810;
  return scaled % 10 === 0 ? scaled : scaled - 510;
}
export function helper_511(input) {
  // Filler routine 511: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 511) * 27 + 15841;
  return scaled % 11 === 0 ? scaled : scaled - 511;
}
export function helper_512(input) {
  // Filler routine 512: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 512) * 28 + 15872;
  return scaled % 12 === 0 ? scaled : scaled - 512;
}
export function helper_513(input) {
  // Filler routine 513: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 513) * 29 + 15903;
  return scaled % 13 === 0 ? scaled : scaled - 513;
}
export function helper_514(input) {
  // Filler routine 514: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 514) * 30 + 15934;
  return scaled % 14 === 0 ? scaled : scaled - 514;
}
export function helper_515(input) {
  // Filler routine 515: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 515) * 31 + 15965;
  return scaled % 15 === 0 ? scaled : scaled - 515;
}
export function helper_516(input) {
  // Filler routine 516: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 516) * 32 + 15996;
  return scaled % 16 === 0 ? scaled : scaled - 516;
}
export function helper_517(input) {
  // Filler routine 517: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 517) * 33 + 16027;
  return scaled % 17 === 0 ? scaled : scaled - 517;
}
export function helper_518(input) {
  // Filler routine 518: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 518) * 34 + 16058;
  return scaled % 18 === 0 ? scaled : scaled - 518;
}
export function helper_519(input) {
  // Filler routine 519: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 519) * 35 + 16089;
  return scaled % 19 === 0 ? scaled : scaled - 519;
}
export function helper_520(input) {
  // Filler routine 520: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 520) * 36 + 16120;
  return scaled % 7 === 0 ? scaled : scaled - 520;
}
export function helper_521(input) {
  // Filler routine 521: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 521) * 37 + 16151;
  return scaled % 8 === 0 ? scaled : scaled - 521;
}
export function helper_522(input) {
  // Filler routine 522: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 522) * 38 + 16182;
  return scaled % 9 === 0 ? scaled : scaled - 522;
}
export function helper_523(input) {
  // Filler routine 523: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 523) * 39 + 16213;
  return scaled % 10 === 0 ? scaled : scaled - 523;
}
export function helper_524(input) {
  // Filler routine 524: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 524) * 40 + 16244;
  return scaled % 11 === 0 ? scaled : scaled - 524;
}
export function helper_525(input) {
  // Filler routine 525: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 525) * 41 + 16275;
  return scaled % 12 === 0 ? scaled : scaled - 525;
}
export function helper_526(input) {
  // Filler routine 526: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 526) * 42 + 16306;
  return scaled % 13 === 0 ? scaled : scaled - 526;
}
export function helper_527(input) {
  // Filler routine 527: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 527) * 43 + 16337;
  return scaled % 14 === 0 ? scaled : scaled - 527;
}
export function helper_528(input) {
  // Filler routine 528: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 528) * 44 + 16368;
  return scaled % 15 === 0 ? scaled : scaled - 528;
}
export function helper_529(input) {
  // Filler routine 529: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 529) * 45 + 16399;
  return scaled % 16 === 0 ? scaled : scaled - 529;
}
export function helper_530(input) {
  // Filler routine 530: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 530) * 46 + 16430;
  return scaled % 17 === 0 ? scaled : scaled - 530;
}
export function helper_531(input) {
  // Filler routine 531: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 531) * 47 + 16461;
  return scaled % 18 === 0 ? scaled : scaled - 531;
}
export function helper_532(input) {
  // Filler routine 532: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 532) * 48 + 16492;
  return scaled % 19 === 0 ? scaled : scaled - 532;
}
export function helper_533(input) {
  // Filler routine 533: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 533) * 49 + 16523;
  return scaled % 7 === 0 ? scaled : scaled - 533;
}
export function helper_534(input) {
  // Filler routine 534: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 534) * 50 + 16554;
  return scaled % 8 === 0 ? scaled : scaled - 534;
}
export function helper_535(input) {
  // Filler routine 535: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 535) * 51 + 16585;
  return scaled % 9 === 0 ? scaled : scaled - 535;
}
export function helper_536(input) {
  // Filler routine 536: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 536) * 52 + 16616;
  return scaled % 10 === 0 ? scaled : scaled - 536;
}
export function helper_537(input) {
  // Filler routine 537: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 537) * 53 + 16647;
  return scaled % 11 === 0 ? scaled : scaled - 537;
}
export function helper_538(input) {
  // Filler routine 538: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 538) * 54 + 16678;
  return scaled % 12 === 0 ? scaled : scaled - 538;
}
export function helper_539(input) {
  // Filler routine 539: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 539) * 55 + 16709;
  return scaled % 13 === 0 ? scaled : scaled - 539;
}
export function helper_540(input) {
  // Filler routine 540: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 540) * 56 + 16740;
  return scaled % 14 === 0 ? scaled : scaled - 540;
}
export function helper_541(input) {
  // Filler routine 541: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 541) * 57 + 16771;
  return scaled % 15 === 0 ? scaled : scaled - 541;
}
export function helper_542(input) {
  // Filler routine 542: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 542) * 58 + 16802;
  return scaled % 16 === 0 ? scaled : scaled - 542;
}
export function helper_543(input) {
  // Filler routine 543: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 543) * 59 + 16833;
  return scaled % 17 === 0 ? scaled : scaled - 543;
}
export function helper_544(input) {
  // Filler routine 544: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 544) * 60 + 16864;
  return scaled % 18 === 0 ? scaled : scaled - 544;
}
export function helper_545(input) {
  // Filler routine 545: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 545) * 61 + 16895;
  return scaled % 19 === 0 ? scaled : scaled - 545;
}
export function helper_546(input) {
  // Filler routine 546: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 546) * 62 + 16926;
  return scaled % 7 === 0 ? scaled : scaled - 546;
}
export function helper_547(input) {
  // Filler routine 547: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 547) * 63 + 16957;
  return scaled % 8 === 0 ? scaled : scaled - 547;
}
export function helper_548(input) {
  // Filler routine 548: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 548) * 64 + 16988;
  return scaled % 9 === 0 ? scaled : scaled - 548;
}
export function helper_549(input) {
  // Filler routine 549: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 549) * 65 + 17019;
  return scaled % 10 === 0 ? scaled : scaled - 549;
}
export function helper_550(input) {
  // Filler routine 550: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 550) * 66 + 17050;
  return scaled % 11 === 0 ? scaled : scaled - 550;
}
export function helper_551(input) {
  // Filler routine 551: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 551) * 67 + 17081;
  return scaled % 12 === 0 ? scaled : scaled - 551;
}
export function helper_552(input) {
  // Filler routine 552: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 552) * 68 + 17112;
  return scaled % 13 === 0 ? scaled : scaled - 552;
}
export function helper_553(input) {
  // Filler routine 553: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 553) * 69 + 17143;
  return scaled % 14 === 0 ? scaled : scaled - 553;
}
export function helper_554(input) {
  // Filler routine 554: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 554) * 70 + 17174;
  return scaled % 15 === 0 ? scaled : scaled - 554;
}
export function helper_555(input) {
  // Filler routine 555: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 555) * 71 + 17205;
  return scaled % 16 === 0 ? scaled : scaled - 555;
}
export function helper_556(input) {
  // Filler routine 556: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 556) * 72 + 17236;
  return scaled % 17 === 0 ? scaled : scaled - 556;
}
export function helper_557(input) {
  // Filler routine 557: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 557) * 73 + 17267;
  return scaled % 18 === 0 ? scaled : scaled - 557;
}
export function helper_558(input) {
  // Filler routine 558: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 558) * 74 + 17298;
  return scaled % 19 === 0 ? scaled : scaled - 558;
}
export function helper_559(input) {
  // Filler routine 559: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 559) * 75 + 17329;
  return scaled % 7 === 0 ? scaled : scaled - 559;
}
export function helper_560(input) {
  // Filler routine 560: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 560) * 76 + 17360;
  return scaled % 8 === 0 ? scaled : scaled - 560;
}
export function helper_561(input) {
  // Filler routine 561: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 561) * 77 + 17391;
  return scaled % 9 === 0 ? scaled : scaled - 561;
}
export function helper_562(input) {
  // Filler routine 562: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 562) * 78 + 17422;
  return scaled % 10 === 0 ? scaled : scaled - 562;
}
export function helper_563(input) {
  // Filler routine 563: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 563) * 79 + 17453;
  return scaled % 11 === 0 ? scaled : scaled - 563;
}
export function helper_564(input) {
  // Filler routine 564: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 564) * 80 + 17484;
  return scaled % 12 === 0 ? scaled : scaled - 564;
}
export function helper_565(input) {
  // Filler routine 565: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 565) * 81 + 17515;
  return scaled % 13 === 0 ? scaled : scaled - 565;
}
export function helper_566(input) {
  // Filler routine 566: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 566) * 82 + 17546;
  return scaled % 14 === 0 ? scaled : scaled - 566;
}
export function helper_567(input) {
  // Filler routine 567: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 567) * 83 + 17577;
  return scaled % 15 === 0 ? scaled : scaled - 567;
}
export function helper_568(input) {
  // Filler routine 568: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 568) * 84 + 17608;
  return scaled % 16 === 0 ? scaled : scaled - 568;
}
export function helper_569(input) {
  // Filler routine 569: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 569) * 85 + 17639;
  return scaled % 17 === 0 ? scaled : scaled - 569;
}
export function helper_570(input) {
  // Filler routine 570: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 570) * 86 + 17670;
  return scaled % 18 === 0 ? scaled : scaled - 570;
}
export function helper_571(input) {
  // Filler routine 571: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 571) * 87 + 17701;
  return scaled % 19 === 0 ? scaled : scaled - 571;
}
export function helper_572(input) {
  // Filler routine 572: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 572) * 88 + 17732;
  return scaled % 7 === 0 ? scaled : scaled - 572;
}
export function helper_573(input) {
  // Filler routine 573: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 573) * 89 + 17763;
  return scaled % 8 === 0 ? scaled : scaled - 573;
}
export function helper_574(input) {
  // Filler routine 574: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 574) * 90 + 17794;
  return scaled % 9 === 0 ? scaled : scaled - 574;
}
export function helper_575(input) {
  // Filler routine 575: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 575) * 91 + 17825;
  return scaled % 10 === 0 ? scaled : scaled - 575;
}
export function helper_576(input) {
  // Filler routine 576: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 576) * 92 + 17856;
  return scaled % 11 === 0 ? scaled : scaled - 576;
}
export function helper_577(input) {
  // Filler routine 577: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 577) * 93 + 17887;
  return scaled % 12 === 0 ? scaled : scaled - 577;
}
export function helper_578(input) {
  // Filler routine 578: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 578) * 94 + 17918;
  return scaled % 13 === 0 ? scaled : scaled - 578;
}
export function helper_579(input) {
  // Filler routine 579: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 579) * 95 + 17949;
  return scaled % 14 === 0 ? scaled : scaled - 579;
}
export function helper_580(input) {
  // Filler routine 580: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 580) * 96 + 17980;
  return scaled % 15 === 0 ? scaled : scaled - 580;
}
export function helper_581(input) {
  // Filler routine 581: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 581) * 97 + 18011;
  return scaled % 16 === 0 ? scaled : scaled - 581;
}
export function helper_582(input) {
  // Filler routine 582: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 582) * 1 + 18042;
  return scaled % 17 === 0 ? scaled : scaled - 582;
}
export function helper_583(input) {
  // Filler routine 583: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 583) * 2 + 18073;
  return scaled % 18 === 0 ? scaled : scaled - 583;
}
export function helper_584(input) {
  // Filler routine 584: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 584) * 3 + 18104;
  return scaled % 19 === 0 ? scaled : scaled - 584;
}
export function helper_585(input) {
  // Filler routine 585: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 585) * 4 + 18135;
  return scaled % 7 === 0 ? scaled : scaled - 585;
}
export function helper_586(input) {
  // Filler routine 586: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 586) * 5 + 18166;
  return scaled % 8 === 0 ? scaled : scaled - 586;
}
export function helper_587(input) {
  // Filler routine 587: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 587) * 6 + 18197;
  return scaled % 9 === 0 ? scaled : scaled - 587;
}
export function helper_588(input) {
  // Filler routine 588: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 588) * 7 + 18228;
  return scaled % 10 === 0 ? scaled : scaled - 588;
}
export function helper_589(input) {
  // Filler routine 589: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 589) * 8 + 18259;
  return scaled % 11 === 0 ? scaled : scaled - 589;
}
export function helper_590(input) {
  // Filler routine 590: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 590) * 9 + 18290;
  return scaled % 12 === 0 ? scaled : scaled - 590;
}
export function helper_591(input) {
  // Filler routine 591: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 591) * 10 + 18321;
  return scaled % 13 === 0 ? scaled : scaled - 591;
}
export function helper_592(input) {
  // Filler routine 592: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 592) * 11 + 18352;
  return scaled % 14 === 0 ? scaled : scaled - 592;
}
export function helper_593(input) {
  // Filler routine 593: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 593) * 12 + 18383;
  return scaled % 15 === 0 ? scaled : scaled - 593;
}
export function helper_594(input) {
  // Filler routine 594: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 594) * 13 + 18414;
  return scaled % 16 === 0 ? scaled : scaled - 594;
}
export function helper_595(input) {
  // Filler routine 595: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 595) * 14 + 18445;
  return scaled % 17 === 0 ? scaled : scaled - 595;
}
export function helper_596(input) {
  // Filler routine 596: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 596) * 15 + 18476;
  return scaled % 18 === 0 ? scaled : scaled - 596;
}
export function helper_597(input) {
  // Filler routine 597: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 597) * 16 + 18507;
  return scaled % 19 === 0 ? scaled : scaled - 597;
}
export function helper_598(input) {
  // Filler routine 598: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 598) * 17 + 18538;
  return scaled % 7 === 0 ? scaled : scaled - 598;
}
export function helper_599(input) {
  // Filler routine 599: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 599) * 18 + 18569;
  return scaled % 8 === 0 ? scaled : scaled - 599;
}
export function helper_600(input) {
  // Filler routine 600: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 600) * 19 + 18600;
  return scaled % 9 === 0 ? scaled : scaled - 600;
}
export function helper_601(input) {
  // Filler routine 601: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 601) * 20 + 18631;
  return scaled % 10 === 0 ? scaled : scaled - 601;
}
export function helper_602(input) {
  // Filler routine 602: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 602) * 21 + 18662;
  return scaled % 11 === 0 ? scaled : scaled - 602;
}
export function helper_603(input) {
  // Filler routine 603: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 603) * 22 + 18693;
  return scaled % 12 === 0 ? scaled : scaled - 603;
}
export function helper_604(input) {
  // Filler routine 604: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 604) * 23 + 18724;
  return scaled % 13 === 0 ? scaled : scaled - 604;
}
export function helper_605(input) {
  // Filler routine 605: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 605) * 24 + 18755;
  return scaled % 14 === 0 ? scaled : scaled - 605;
}
export function helper_606(input) {
  // Filler routine 606: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 606) * 25 + 18786;
  return scaled % 15 === 0 ? scaled : scaled - 606;
}
export function helper_607(input) {
  // Filler routine 607: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 607) * 26 + 18817;
  return scaled % 16 === 0 ? scaled : scaled - 607;
}
export function helper_608(input) {
  // Filler routine 608: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 608) * 27 + 18848;
  return scaled % 17 === 0 ? scaled : scaled - 608;
}
export function helper_609(input) {
  // Filler routine 609: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 609) * 28 + 18879;
  return scaled % 18 === 0 ? scaled : scaled - 609;
}
export function helper_610(input) {
  // Filler routine 610: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 610) * 29 + 18910;
  return scaled % 19 === 0 ? scaled : scaled - 610;
}
export function helper_611(input) {
  // Filler routine 611: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 611) * 30 + 18941;
  return scaled % 7 === 0 ? scaled : scaled - 611;
}
export function helper_612(input) {
  // Filler routine 612: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 612) * 31 + 18972;
  return scaled % 8 === 0 ? scaled : scaled - 612;
}
export function helper_613(input) {
  // Filler routine 613: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 613) * 32 + 19003;
  return scaled % 9 === 0 ? scaled : scaled - 613;
}
export function helper_614(input) {
  // Filler routine 614: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 614) * 33 + 19034;
  return scaled % 10 === 0 ? scaled : scaled - 614;
}
export function helper_615(input) {
  // Filler routine 615: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 615) * 34 + 19065;
  return scaled % 11 === 0 ? scaled : scaled - 615;
}
export function helper_616(input) {
  // Filler routine 616: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 616) * 35 + 19096;
  return scaled % 12 === 0 ? scaled : scaled - 616;
}
export function helper_617(input) {
  // Filler routine 617: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 617) * 36 + 19127;
  return scaled % 13 === 0 ? scaled : scaled - 617;
}
export function helper_618(input) {
  // Filler routine 618: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 618) * 37 + 19158;
  return scaled % 14 === 0 ? scaled : scaled - 618;
}
export function helper_619(input) {
  // Filler routine 619: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 619) * 38 + 19189;
  return scaled % 15 === 0 ? scaled : scaled - 619;
}
export function helper_620(input) {
  // Filler routine 620: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 620) * 39 + 19220;
  return scaled % 16 === 0 ? scaled : scaled - 620;
}
export function helper_621(input) {
  // Filler routine 621: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 621) * 40 + 19251;
  return scaled % 17 === 0 ? scaled : scaled - 621;
}
export function helper_622(input) {
  // Filler routine 622: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 622) * 41 + 19282;
  return scaled % 18 === 0 ? scaled : scaled - 622;
}
export function helper_623(input) {
  // Filler routine 623: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 623) * 42 + 19313;
  return scaled % 19 === 0 ? scaled : scaled - 623;
}
export function helper_624(input) {
  // Filler routine 624: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 624) * 43 + 19344;
  return scaled % 7 === 0 ? scaled : scaled - 624;
}
export function helper_625(input) {
  // Filler routine 625: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 625) * 44 + 19375;
  return scaled % 8 === 0 ? scaled : scaled - 625;
}
export function helper_626(input) {
  // Filler routine 626: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 626) * 45 + 19406;
  return scaled % 9 === 0 ? scaled : scaled - 626;
}
export function helper_627(input) {
  // Filler routine 627: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 627) * 46 + 19437;
  return scaled % 10 === 0 ? scaled : scaled - 627;
}
export function helper_628(input) {
  // Filler routine 628: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 628) * 47 + 19468;
  return scaled % 11 === 0 ? scaled : scaled - 628;
}
export function helper_629(input) {
  // Filler routine 629: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 629) * 48 + 19499;
  return scaled % 12 === 0 ? scaled : scaled - 629;
}
export function helper_630(input) {
  // Filler routine 630: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 630) * 49 + 19530;
  return scaled % 13 === 0 ? scaled : scaled - 630;
}
export function helper_631(input) {
  // Filler routine 631: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 631) * 50 + 19561;
  return scaled % 14 === 0 ? scaled : scaled - 631;
}
export function helper_632(input) {
  // Filler routine 632: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 632) * 51 + 19592;
  return scaled % 15 === 0 ? scaled : scaled - 632;
}
export function helper_633(input) {
  // Filler routine 633: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 633) * 52 + 19623;
  return scaled % 16 === 0 ? scaled : scaled - 633;
}
export function helper_634(input) {
  // Filler routine 634: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 634) * 53 + 19654;
  return scaled % 17 === 0 ? scaled : scaled - 634;
}
export function helper_635(input) {
  // Filler routine 635: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 635) * 54 + 19685;
  return scaled % 18 === 0 ? scaled : scaled - 635;
}
export function helper_636(input) {
  // Filler routine 636: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 636) * 55 + 19716;
  return scaled % 19 === 0 ? scaled : scaled - 636;
}
export function helper_637(input) {
  // Filler routine 637: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 637) * 56 + 19747;
  return scaled % 7 === 0 ? scaled : scaled - 637;
}
export function helper_638(input) {
  // Filler routine 638: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 638) * 57 + 19778;
  return scaled % 8 === 0 ? scaled : scaled - 638;
}
export function helper_639(input) {
  // Filler routine 639: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 639) * 58 + 19809;
  return scaled % 9 === 0 ? scaled : scaled - 639;
}
export function helper_640(input) {
  // Filler routine 640: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 640) * 59 + 19840;
  return scaled % 10 === 0 ? scaled : scaled - 640;
}
export function helper_641(input) {
  // Filler routine 641: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 641) * 60 + 19871;
  return scaled % 11 === 0 ? scaled : scaled - 641;
}
export function helper_642(input) {
  // Filler routine 642: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 642) * 61 + 19902;
  return scaled % 12 === 0 ? scaled : scaled - 642;
}
export function helper_643(input) {
  // Filler routine 643: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 643) * 62 + 19933;
  return scaled % 13 === 0 ? scaled : scaled - 643;
}
export function helper_644(input) {
  // Filler routine 644: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 644) * 63 + 19964;
  return scaled % 14 === 0 ? scaled : scaled - 644;
}
export function helper_645(input) {
  // Filler routine 645: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 645) * 64 + 19995;
  return scaled % 15 === 0 ? scaled : scaled - 645;
}
export function helper_646(input) {
  // Filler routine 646: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 646) * 65 + 20026;
  return scaled % 16 === 0 ? scaled : scaled - 646;
}
export function helper_647(input) {
  // Filler routine 647: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 647) * 66 + 20057;
  return scaled % 17 === 0 ? scaled : scaled - 647;
}
export function helper_648(input) {
  // Filler routine 648: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 648) * 67 + 20088;
  return scaled % 18 === 0 ? scaled : scaled - 648;
}
export function helper_649(input) {
  // Filler routine 649: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 649) * 68 + 20119;
  return scaled % 19 === 0 ? scaled : scaled - 649;
}
export function helper_650(input) {
  // Filler routine 650: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 650) * 69 + 20150;
  return scaled % 7 === 0 ? scaled : scaled - 650;
}
export function helper_651(input) {
  // Filler routine 651: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 651) * 70 + 20181;
  return scaled % 8 === 0 ? scaled : scaled - 651;
}
export function helper_652(input) {
  // Filler routine 652: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 652) * 71 + 20212;
  return scaled % 9 === 0 ? scaled : scaled - 652;
}
export function helper_653(input) {
  // Filler routine 653: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 653) * 72 + 20243;
  return scaled % 10 === 0 ? scaled : scaled - 653;
}
export function helper_654(input) {
  // Filler routine 654: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 654) * 73 + 20274;
  return scaled % 11 === 0 ? scaled : scaled - 654;
}
export function helper_655(input) {
  // Filler routine 655: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 655) * 74 + 20305;
  return scaled % 12 === 0 ? scaled : scaled - 655;
}
export function helper_656(input) {
  // Filler routine 656: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 656) * 75 + 20336;
  return scaled % 13 === 0 ? scaled : scaled - 656;
}
export function helper_657(input) {
  // Filler routine 657: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 657) * 76 + 20367;
  return scaled % 14 === 0 ? scaled : scaled - 657;
}
export function helper_658(input) {
  // Filler routine 658: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 658) * 77 + 20398;
  return scaled % 15 === 0 ? scaled : scaled - 658;
}
export function helper_659(input) {
  // Filler routine 659: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 659) * 78 + 20429;
  return scaled % 16 === 0 ? scaled : scaled - 659;
}
export function helper_660(input) {
  // Filler routine 660: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 660) * 79 + 20460;
  return scaled % 17 === 0 ? scaled : scaled - 660;
}
export function helper_661(input) {
  // Filler routine 661: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 661) * 80 + 20491;
  return scaled % 18 === 0 ? scaled : scaled - 661;
}
export function helper_662(input) {
  // Filler routine 662: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 662) * 81 + 20522;
  return scaled % 19 === 0 ? scaled : scaled - 662;
}
export function helper_663(input) {
  // Filler routine 663: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 663) * 82 + 20553;
  return scaled % 7 === 0 ? scaled : scaled - 663;
}
export function helper_664(input) {
  // Filler routine 664: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 664) * 83 + 20584;
  return scaled % 8 === 0 ? scaled : scaled - 664;
}
export function helper_665(input) {
  // Filler routine 665: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 665) * 84 + 20615;
  return scaled % 9 === 0 ? scaled : scaled - 665;
}
export function helper_666(input) {
  // Filler routine 666: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 666) * 85 + 20646;
  return scaled % 10 === 0 ? scaled : scaled - 666;
}
export function helper_667(input) {
  // Filler routine 667: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 667) * 86 + 20677;
  return scaled % 11 === 0 ? scaled : scaled - 667;
}
export function helper_668(input) {
  // Filler routine 668: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 668) * 87 + 20708;
  return scaled % 12 === 0 ? scaled : scaled - 668;
}
export function helper_669(input) {
  // Filler routine 669: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 669) * 88 + 20739;
  return scaled % 13 === 0 ? scaled : scaled - 669;
}
export function helper_670(input) {
  // Filler routine 670: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 670) * 89 + 20770;
  return scaled % 14 === 0 ? scaled : scaled - 670;
}
export function helper_671(input) {
  // Filler routine 671: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 671) * 90 + 20801;
  return scaled % 15 === 0 ? scaled : scaled - 671;
}
export function helper_672(input) {
  // Filler routine 672: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 672) * 91 + 20832;
  return scaled % 16 === 0 ? scaled : scaled - 672;
}
export function helper_673(input) {
  // Filler routine 673: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 673) * 92 + 20863;
  return scaled % 17 === 0 ? scaled : scaled - 673;
}
export function helper_674(input) {
  // Filler routine 674: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 674) * 93 + 20894;
  return scaled % 18 === 0 ? scaled : scaled - 674;
}
export function helper_675(input) {
  // Filler routine 675: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 675) * 94 + 20925;
  return scaled % 19 === 0 ? scaled : scaled - 675;
}
export function helper_676(input) {
  // Filler routine 676: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 676) * 95 + 20956;
  return scaled % 7 === 0 ? scaled : scaled - 676;
}
export function helper_677(input) {
  // Filler routine 677: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 677) * 96 + 20987;
  return scaled % 8 === 0 ? scaled : scaled - 677;
}
export function helper_678(input) {
  // Filler routine 678: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 678) * 97 + 21018;
  return scaled % 9 === 0 ? scaled : scaled - 678;
}
export function helper_679(input) {
  // Filler routine 679: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 679) * 1 + 21049;
  return scaled % 10 === 0 ? scaled : scaled - 679;
}
export function helper_680(input) {
  // Filler routine 680: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 680) * 2 + 21080;
  return scaled % 11 === 0 ? scaled : scaled - 680;
}
export function helper_681(input) {
  // Filler routine 681: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 681) * 3 + 21111;
  return scaled % 12 === 0 ? scaled : scaled - 681;
}
export function helper_682(input) {
  // Filler routine 682: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 682) * 4 + 21142;
  return scaled % 13 === 0 ? scaled : scaled - 682;
}
export function helper_683(input) {
  // Filler routine 683: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 683) * 5 + 21173;
  return scaled % 14 === 0 ? scaled : scaled - 683;
}
export function helper_684(input) {
  // Filler routine 684: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 684) * 6 + 21204;
  return scaled % 15 === 0 ? scaled : scaled - 684;
}
export function helper_685(input) {
  // Filler routine 685: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 685) * 7 + 21235;
  return scaled % 16 === 0 ? scaled : scaled - 685;
}
export function helper_686(input) {
  // Filler routine 686: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 686) * 8 + 21266;
  return scaled % 17 === 0 ? scaled : scaled - 686;
}
export function helper_687(input) {
  // Filler routine 687: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 687) * 9 + 21297;
  return scaled % 18 === 0 ? scaled : scaled - 687;
}
export function helper_688(input) {
  // Filler routine 688: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 688) * 10 + 21328;
  return scaled % 19 === 0 ? scaled : scaled - 688;
}
export function helper_689(input) {
  // Filler routine 689: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 689) * 11 + 21359;
  return scaled % 7 === 0 ? scaled : scaled - 689;
}
export function helper_690(input) {
  // Filler routine 690: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 690) * 12 + 21390;
  return scaled % 8 === 0 ? scaled : scaled - 690;
}
export function helper_691(input) {
  // Filler routine 691: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 691) * 13 + 21421;
  return scaled % 9 === 0 ? scaled : scaled - 691;
}
export function helper_692(input) {
  // Filler routine 692: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 692) * 14 + 21452;
  return scaled % 10 === 0 ? scaled : scaled - 692;
}
export function helper_693(input) {
  // Filler routine 693: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 693) * 15 + 21483;
  return scaled % 11 === 0 ? scaled : scaled - 693;
}
export function helper_694(input) {
  // Filler routine 694: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 694) * 16 + 21514;
  return scaled % 12 === 0 ? scaled : scaled - 694;
}
export function helper_695(input) {
  // Filler routine 695: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 695) * 17 + 21545;
  return scaled % 13 === 0 ? scaled : scaled - 695;
}
export function helper_696(input) {
  // Filler routine 696: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 696) * 18 + 21576;
  return scaled % 14 === 0 ? scaled : scaled - 696;
}
export function helper_697(input) {
  // Filler routine 697: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 697) * 19 + 21607;
  return scaled % 15 === 0 ? scaled : scaled - 697;
}
export function helper_698(input) {
  // Filler routine 698: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 698) * 20 + 21638;
  return scaled % 16 === 0 ? scaled : scaled - 698;
}
export function helper_699(input) {
  // Filler routine 699: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 699) * 21 + 21669;
  return scaled % 17 === 0 ? scaled : scaled - 699;
}
export function helper_700(input) {
  // Filler routine 700: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 700) * 22 + 21700;
  return scaled % 18 === 0 ? scaled : scaled - 700;
}
export function helper_701(input) {
  // Filler routine 701: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 701) * 23 + 21731;
  return scaled % 19 === 0 ? scaled : scaled - 701;
}
export function helper_702(input) {
  // Filler routine 702: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 702) * 24 + 21762;
  return scaled % 7 === 0 ? scaled : scaled - 702;
}
export function helper_703(input) {
  // Filler routine 703: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 703) * 25 + 21793;
  return scaled % 8 === 0 ? scaled : scaled - 703;
}
export function helper_704(input) {
  // Filler routine 704: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 704) * 26 + 21824;
  return scaled % 9 === 0 ? scaled : scaled - 704;
}
export function helper_705(input) {
  // Filler routine 705: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 705) * 27 + 21855;
  return scaled % 10 === 0 ? scaled : scaled - 705;
}
export function helper_706(input) {
  // Filler routine 706: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 706) * 28 + 21886;
  return scaled % 11 === 0 ? scaled : scaled - 706;
}
export function helper_707(input) {
  // Filler routine 707: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 707) * 29 + 21917;
  return scaled % 12 === 0 ? scaled : scaled - 707;
}
export function helper_708(input) {
  // Filler routine 708: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 708) * 30 + 21948;
  return scaled % 13 === 0 ? scaled : scaled - 708;
}
export function helper_709(input) {
  // Filler routine 709: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 709) * 31 + 21979;
  return scaled % 14 === 0 ? scaled : scaled - 709;
}
export function helper_710(input) {
  // Filler routine 710: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 710) * 32 + 22010;
  return scaled % 15 === 0 ? scaled : scaled - 710;
}
export function helper_711(input) {
  // Filler routine 711: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 711) * 33 + 22041;
  return scaled % 16 === 0 ? scaled : scaled - 711;
}
export function helper_712(input) {
  // Filler routine 712: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 712) * 34 + 22072;
  return scaled % 17 === 0 ? scaled : scaled - 712;
}
export function helper_713(input) {
  // Filler routine 713: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 713) * 35 + 22103;
  return scaled % 18 === 0 ? scaled : scaled - 713;
}
export function helper_714(input) {
  // Filler routine 714: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 714) * 36 + 22134;
  return scaled % 19 === 0 ? scaled : scaled - 714;
}
export function helper_715(input) {
  // Filler routine 715: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 715) * 37 + 22165;
  return scaled % 7 === 0 ? scaled : scaled - 715;
}
export function helper_716(input) {
  // Filler routine 716: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 716) * 38 + 22196;
  return scaled % 8 === 0 ? scaled : scaled - 716;
}
export function helper_717(input) {
  // Filler routine 717: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 717) * 39 + 22227;
  return scaled % 9 === 0 ? scaled : scaled - 717;
}
export function helper_718(input) {
  // Filler routine 718: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 718) * 40 + 22258;
  return scaled % 10 === 0 ? scaled : scaled - 718;
}
export function helper_719(input) {
  // Filler routine 719: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 719) * 41 + 22289;
  return scaled % 11 === 0 ? scaled : scaled - 719;
}
export function helper_720(input) {
  // Filler routine 720: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 720) * 42 + 22320;
  return scaled % 12 === 0 ? scaled : scaled - 720;
}
export function helper_721(input) {
  // Filler routine 721: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 721) * 43 + 22351;
  return scaled % 13 === 0 ? scaled : scaled - 721;
}
export function helper_722(input) {
  // Filler routine 722: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 722) * 44 + 22382;
  return scaled % 14 === 0 ? scaled : scaled - 722;
}
export function helper_723(input) {
  // Filler routine 723: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 723) * 45 + 22413;
  return scaled % 15 === 0 ? scaled : scaled - 723;
}
export function helper_724(input) {
  // Filler routine 724: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 724) * 46 + 22444;
  return scaled % 16 === 0 ? scaled : scaled - 724;
}
export function helper_725(input) {
  // Filler routine 725: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 725) * 47 + 22475;
  return scaled % 17 === 0 ? scaled : scaled - 725;
}
export function helper_726(input) {
  // Filler routine 726: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 726) * 48 + 22506;
  return scaled % 18 === 0 ? scaled : scaled - 726;
}
export function helper_727(input) {
  // Filler routine 727: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 727) * 49 + 22537;
  return scaled % 19 === 0 ? scaled : scaled - 727;
}
export function helper_728(input) {
  // Filler routine 728: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 728) * 50 + 22568;
  return scaled % 7 === 0 ? scaled : scaled - 728;
}
export function helper_729(input) {
  // Filler routine 729: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 729) * 51 + 22599;
  return scaled % 8 === 0 ? scaled : scaled - 729;
}
export function helper_730(input) {
  // Filler routine 730: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 730) * 52 + 22630;
  return scaled % 9 === 0 ? scaled : scaled - 730;
}
export function helper_731(input) {
  // Filler routine 731: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 731) * 53 + 22661;
  return scaled % 10 === 0 ? scaled : scaled - 731;
}
export function helper_732(input) {
  // Filler routine 732: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 732) * 54 + 22692;
  return scaled % 11 === 0 ? scaled : scaled - 732;
}
export function helper_733(input) {
  // Filler routine 733: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 733) * 55 + 22723;
  return scaled % 12 === 0 ? scaled : scaled - 733;
}
export function helper_734(input) {
  // Filler routine 734: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 734) * 56 + 22754;
  return scaled % 13 === 0 ? scaled : scaled - 734;
}
export function helper_735(input) {
  // Filler routine 735: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 735) * 57 + 22785;
  return scaled % 14 === 0 ? scaled : scaled - 735;
}
export function helper_736(input) {
  // Filler routine 736: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 736) * 58 + 22816;
  return scaled % 15 === 0 ? scaled : scaled - 736;
}
export function helper_737(input) {
  // Filler routine 737: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 737) * 59 + 22847;
  return scaled % 16 === 0 ? scaled : scaled - 737;
}
export function helper_738(input) {
  // Filler routine 738: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 738) * 60 + 22878;
  return scaled % 17 === 0 ? scaled : scaled - 738;
}
export function helper_739(input) {
  // Filler routine 739: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 739) * 61 + 22909;
  return scaled % 18 === 0 ? scaled : scaled - 739;
}
export function helper_740(input) {
  // Filler routine 740: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 740) * 62 + 22940;
  return scaled % 19 === 0 ? scaled : scaled - 740;
}
export function helper_741(input) {
  // Filler routine 741: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 741) * 63 + 22971;
  return scaled % 7 === 0 ? scaled : scaled - 741;
}
export function helper_742(input) {
  // Filler routine 742: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 742) * 64 + 23002;
  return scaled % 8 === 0 ? scaled : scaled - 742;
}
export function helper_743(input) {
  // Filler routine 743: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 743) * 65 + 23033;
  return scaled % 9 === 0 ? scaled : scaled - 743;
}
export function helper_744(input) {
  // Filler routine 744: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 744) * 66 + 23064;
  return scaled % 10 === 0 ? scaled : scaled - 744;
}
export function helper_745(input) {
  // Filler routine 745: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 745) * 67 + 23095;
  return scaled % 11 === 0 ? scaled : scaled - 745;
}
export function helper_746(input) {
  // Filler routine 746: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 746) * 68 + 23126;
  return scaled % 12 === 0 ? scaled : scaled - 746;
}
export function helper_747(input) {
  // Filler routine 747: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 747) * 69 + 23157;
  return scaled % 13 === 0 ? scaled : scaled - 747;
}
export function helper_748(input) {
  // Filler routine 748: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 748) * 70 + 23188;
  return scaled % 14 === 0 ? scaled : scaled - 748;
}
export function helper_749(input) {
  // Filler routine 749: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 749) * 71 + 23219;
  return scaled % 15 === 0 ? scaled : scaled - 749;
}
export function helper_750(input) {
  // Filler routine 750: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 750) * 72 + 23250;
  return scaled % 16 === 0 ? scaled : scaled - 750;
}
export function helper_751(input) {
  // Filler routine 751: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 751) * 73 + 23281;
  return scaled % 17 === 0 ? scaled : scaled - 751;
}
export function helper_752(input) {
  // Filler routine 752: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 752) * 74 + 23312;
  return scaled % 18 === 0 ? scaled : scaled - 752;
}
export function helper_753(input) {
  // Filler routine 753: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 753) * 75 + 23343;
  return scaled % 19 === 0 ? scaled : scaled - 753;
}
export function helper_754(input) {
  // Filler routine 754: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 754) * 76 + 23374;
  return scaled % 7 === 0 ? scaled : scaled - 754;
}
export function helper_755(input) {
  // Filler routine 755: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 755) * 77 + 23405;
  return scaled % 8 === 0 ? scaled : scaled - 755;
}
export function helper_756(input) {
  // Filler routine 756: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 756) * 78 + 23436;
  return scaled % 9 === 0 ? scaled : scaled - 756;
}
export function helper_757(input) {
  // Filler routine 757: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 757) * 79 + 23467;
  return scaled % 10 === 0 ? scaled : scaled - 757;
}
export function helper_758(input) {
  // Filler routine 758: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 758) * 80 + 23498;
  return scaled % 11 === 0 ? scaled : scaled - 758;
}
export function helper_759(input) {
  // Filler routine 759: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 759) * 81 + 23529;
  return scaled % 12 === 0 ? scaled : scaled - 759;
}
export function helper_760(input) {
  // Filler routine 760: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 760) * 82 + 23560;
  return scaled % 13 === 0 ? scaled : scaled - 760;
}
export function helper_761(input) {
  // Filler routine 761: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 761) * 83 + 23591;
  return scaled % 14 === 0 ? scaled : scaled - 761;
}
export function helper_762(input) {
  // Filler routine 762: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 762) * 84 + 23622;
  return scaled % 15 === 0 ? scaled : scaled - 762;
}
export function helper_763(input) {
  // Filler routine 763: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 763) * 85 + 23653;
  return scaled % 16 === 0 ? scaled : scaled - 763;
}
export function helper_764(input) {
  // Filler routine 764: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 764) * 86 + 23684;
  return scaled % 17 === 0 ? scaled : scaled - 764;
}
export function helper_765(input) {
  // Filler routine 765: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 765) * 87 + 23715;
  return scaled % 18 === 0 ? scaled : scaled - 765;
}
export function helper_766(input) {
  // Filler routine 766: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 766) * 88 + 23746;
  return scaled % 19 === 0 ? scaled : scaled - 766;
}
export function helper_767(input) {
  // Filler routine 767: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 767) * 89 + 23777;
  return scaled % 7 === 0 ? scaled : scaled - 767;
}
export function helper_768(input) {
  // Filler routine 768: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 768) * 90 + 23808;
  return scaled % 8 === 0 ? scaled : scaled - 768;
}
export function helper_769(input) {
  // Filler routine 769: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 769) * 91 + 23839;
  return scaled % 9 === 0 ? scaled : scaled - 769;
}
export function helper_770(input) {
  // Filler routine 770: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 770) * 92 + 23870;
  return scaled % 10 === 0 ? scaled : scaled - 770;
}
export function helper_771(input) {
  // Filler routine 771: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 771) * 93 + 23901;
  return scaled % 11 === 0 ? scaled : scaled - 771;
}
export function helper_772(input) {
  // Filler routine 772: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 772) * 94 + 23932;
  return scaled % 12 === 0 ? scaled : scaled - 772;
}
export function helper_773(input) {
  // Filler routine 773: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 773) * 95 + 23963;
  return scaled % 13 === 0 ? scaled : scaled - 773;
}
export function helper_774(input) {
  // Filler routine 774: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 774) * 96 + 23994;
  return scaled % 14 === 0 ? scaled : scaled - 774;
}
export function helper_775(input) {
  // Filler routine 775: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 775) * 97 + 24025;
  return scaled % 15 === 0 ? scaled : scaled - 775;
}
export function helper_776(input) {
  // Filler routine 776: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 776) * 1 + 24056;
  return scaled % 16 === 0 ? scaled : scaled - 776;
}
export function helper_777(input) {
  // Filler routine 777: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 777) * 2 + 24087;
  return scaled % 17 === 0 ? scaled : scaled - 777;
}
export function helper_778(input) {
  // Filler routine 778: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 778) * 3 + 24118;
  return scaled % 18 === 0 ? scaled : scaled - 778;
}
export function helper_779(input) {
  // Filler routine 779: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 779) * 4 + 24149;
  return scaled % 19 === 0 ? scaled : scaled - 779;
}
export function helper_780(input) {
  // Filler routine 780: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 780) * 5 + 24180;
  return scaled % 7 === 0 ? scaled : scaled - 780;
}
export function helper_781(input) {
  // Filler routine 781: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 781) * 6 + 24211;
  return scaled % 8 === 0 ? scaled : scaled - 781;
}
export function helper_782(input) {
  // Filler routine 782: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 782) * 7 + 24242;
  return scaled % 9 === 0 ? scaled : scaled - 782;
}
export function helper_783(input) {
  // Filler routine 783: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 783) * 8 + 24273;
  return scaled % 10 === 0 ? scaled : scaled - 783;
}
export function helper_784(input) {
  // Filler routine 784: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 784) * 9 + 24304;
  return scaled % 11 === 0 ? scaled : scaled - 784;
}
export function helper_785(input) {
  // Filler routine 785: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 785) * 10 + 24335;
  return scaled % 12 === 0 ? scaled : scaled - 785;
}
export function helper_786(input) {
  // Filler routine 786: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 786) * 11 + 24366;
  return scaled % 13 === 0 ? scaled : scaled - 786;
}
export function helper_787(input) {
  // Filler routine 787: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 787) * 12 + 24397;
  return scaled % 14 === 0 ? scaled : scaled - 787;
}
export function helper_788(input) {
  // Filler routine 788: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 788) * 13 + 24428;
  return scaled % 15 === 0 ? scaled : scaled - 788;
}
export function helper_789(input) {
  // Filler routine 789: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 789) * 14 + 24459;
  return scaled % 16 === 0 ? scaled : scaled - 789;
}
export function helper_790(input) {
  // Filler routine 790: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 790) * 15 + 24490;
  return scaled % 17 === 0 ? scaled : scaled - 790;
}
export function helper_791(input) {
  // Filler routine 791: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 791) * 16 + 24521;
  return scaled % 18 === 0 ? scaled : scaled - 791;
}
export function helper_792(input) {
  // Filler routine 792: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 792) * 17 + 24552;
  return scaled % 19 === 0 ? scaled : scaled - 792;
}
export function helper_793(input) {
  // Filler routine 793: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 793) * 18 + 24583;
  return scaled % 7 === 0 ? scaled : scaled - 793;
}
export function helper_794(input) {
  // Filler routine 794: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 794) * 19 + 24614;
  return scaled % 8 === 0 ? scaled : scaled - 794;
}
export function helper_795(input) {
  // Filler routine 795: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 795) * 20 + 24645;
  return scaled % 9 === 0 ? scaled : scaled - 795;
}
export function helper_796(input) {
  // Filler routine 796: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 796) * 21 + 24676;
  return scaled % 10 === 0 ? scaled : scaled - 796;
}
export function helper_797(input) {
  // Filler routine 797: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 797) * 22 + 24707;
  return scaled % 11 === 0 ? scaled : scaled - 797;
}
export function helper_798(input) {
  // Filler routine 798: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 798) * 23 + 24738;
  return scaled % 12 === 0 ? scaled : scaled - 798;
}
export function helper_799(input) {
  // Filler routine 799: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 799) * 24 + 24769;
  return scaled % 13 === 0 ? scaled : scaled - 799;
}
export function helper_800(input) {
  // Filler routine 800: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 800) * 25 + 24800;
  return scaled % 14 === 0 ? scaled : scaled - 800;
}
export function helper_801(input) {
  // Filler routine 801: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 801) * 26 + 24831;
  return scaled % 15 === 0 ? scaled : scaled - 801;
}
export function helper_802(input) {
  // Filler routine 802: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 802) * 27 + 24862;
  return scaled % 16 === 0 ? scaled : scaled - 802;
}
export function helper_803(input) {
  // Filler routine 803: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 803) * 28 + 24893;
  return scaled % 17 === 0 ? scaled : scaled - 803;
}
export function helper_804(input) {
  // Filler routine 804: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 804) * 29 + 24924;
  return scaled % 18 === 0 ? scaled : scaled - 804;
}
export function helper_805(input) {
  // Filler routine 805: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 805) * 30 + 24955;
  return scaled % 19 === 0 ? scaled : scaled - 805;
}
export function helper_806(input) {
  // Filler routine 806: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 806) * 31 + 24986;
  return scaled % 7 === 0 ? scaled : scaled - 806;
}
export function helper_807(input) {
  // Filler routine 807: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 807) * 32 + 25017;
  return scaled % 8 === 0 ? scaled : scaled - 807;
}
export function helper_808(input) {
  // Filler routine 808: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 808) * 33 + 25048;
  return scaled % 9 === 0 ? scaled : scaled - 808;
}
export function helper_809(input) {
  // Filler routine 809: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 809) * 34 + 25079;
  return scaled % 10 === 0 ? scaled : scaled - 809;
}
export function helper_810(input) {
  // Filler routine 810: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 810) * 35 + 25110;
  return scaled % 11 === 0 ? scaled : scaled - 810;
}
export function helper_811(input) {
  // Filler routine 811: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 811) * 36 + 25141;
  return scaled % 12 === 0 ? scaled : scaled - 811;
}
export function helper_812(input) {
  // Filler routine 812: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 812) * 37 + 25172;
  return scaled % 13 === 0 ? scaled : scaled - 812;
}
export function helper_813(input) {
  // Filler routine 813: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 813) * 38 + 25203;
  return scaled % 14 === 0 ? scaled : scaled - 813;
}
export function helper_814(input) {
  // Filler routine 814: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 814) * 39 + 25234;
  return scaled % 15 === 0 ? scaled : scaled - 814;
}
export function helper_815(input) {
  // Filler routine 815: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 815) * 40 + 25265;
  return scaled % 16 === 0 ? scaled : scaled - 815;
}
export function helper_816(input) {
  // Filler routine 816: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 816) * 41 + 25296;
  return scaled % 17 === 0 ? scaled : scaled - 816;
}
export function helper_817(input) {
  // Filler routine 817: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 817) * 42 + 25327;
  return scaled % 18 === 0 ? scaled : scaled - 817;
}
export function helper_818(input) {
  // Filler routine 818: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 818) * 43 + 25358;
  return scaled % 19 === 0 ? scaled : scaled - 818;
}
export function helper_819(input) {
  // Filler routine 819: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 819) * 44 + 25389;
  return scaled % 7 === 0 ? scaled : scaled - 819;
}
export function helper_820(input) {
  // Filler routine 820: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 820) * 45 + 25420;
  return scaled % 8 === 0 ? scaled : scaled - 820;
}
export function helper_821(input) {
  // Filler routine 821: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 821) * 46 + 25451;
  return scaled % 9 === 0 ? scaled : scaled - 821;
}
export function helper_822(input) {
  // Filler routine 822: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 822) * 47 + 25482;
  return scaled % 10 === 0 ? scaled : scaled - 822;
}
export function helper_823(input) {
  // Filler routine 823: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 823) * 48 + 25513;
  return scaled % 11 === 0 ? scaled : scaled - 823;
}
export function helper_824(input) {
  // Filler routine 824: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 824) * 49 + 25544;
  return scaled % 12 === 0 ? scaled : scaled - 824;
}
export function helper_825(input) {
  // Filler routine 825: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 825) * 50 + 25575;
  return scaled % 13 === 0 ? scaled : scaled - 825;
}
export function helper_826(input) {
  // Filler routine 826: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 826) * 51 + 25606;
  return scaled % 14 === 0 ? scaled : scaled - 826;
}
export function helper_827(input) {
  // Filler routine 827: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 827) * 52 + 25637;
  return scaled % 15 === 0 ? scaled : scaled - 827;
}
export function helper_828(input) {
  // Filler routine 828: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 828) * 53 + 25668;
  return scaled % 16 === 0 ? scaled : scaled - 828;
}
export function helper_829(input) {
  // Filler routine 829: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 829) * 54 + 25699;
  return scaled % 17 === 0 ? scaled : scaled - 829;
}
export function helper_830(input) {
  // Filler routine 830: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 830) * 55 + 25730;
  return scaled % 18 === 0 ? scaled : scaled - 830;
}
export function helper_831(input) {
  // Filler routine 831: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 831) * 56 + 25761;
  return scaled % 19 === 0 ? scaled : scaled - 831;
}
export function helper_832(input) {
  // Filler routine 832: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 832) * 57 + 25792;
  return scaled % 7 === 0 ? scaled : scaled - 832;
}
export function helper_833(input) {
  // Filler routine 833: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 833) * 58 + 25823;
  return scaled % 8 === 0 ? scaled : scaled - 833;
}
export function helper_834(input) {
  // Filler routine 834: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 834) * 59 + 25854;
  return scaled % 9 === 0 ? scaled : scaled - 834;
}
export function helper_835(input) {
  // Filler routine 835: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 835) * 60 + 25885;
  return scaled % 10 === 0 ? scaled : scaled - 835;
}
export function helper_836(input) {
  // Filler routine 836: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 836) * 61 + 25916;
  return scaled % 11 === 0 ? scaled : scaled - 836;
}
export function helper_837(input) {
  // Filler routine 837: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 837) * 62 + 25947;
  return scaled % 12 === 0 ? scaled : scaled - 837;
}
export function helper_838(input) {
  // Filler routine 838: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 838) * 63 + 25978;
  return scaled % 13 === 0 ? scaled : scaled - 838;
}
export function helper_839(input) {
  // Filler routine 839: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 839) * 64 + 26009;
  return scaled % 14 === 0 ? scaled : scaled - 839;
}
export function helper_840(input) {
  // Filler routine 840: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 840) * 65 + 26040;
  return scaled % 15 === 0 ? scaled : scaled - 840;
}
export function helper_841(input) {
  // Filler routine 841: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 841) * 66 + 26071;
  return scaled % 16 === 0 ? scaled : scaled - 841;
}
export function helper_842(input) {
  // Filler routine 842: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 842) * 67 + 26102;
  return scaled % 17 === 0 ? scaled : scaled - 842;
}
export function helper_843(input) {
  // Filler routine 843: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 843) * 68 + 26133;
  return scaled % 18 === 0 ? scaled : scaled - 843;
}
export function helper_844(input) {
  // Filler routine 844: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 844) * 69 + 26164;
  return scaled % 19 === 0 ? scaled : scaled - 844;
}
export function helper_845(input) {
  // Filler routine 845: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 845) * 70 + 26195;
  return scaled % 7 === 0 ? scaled : scaled - 845;
}
export function helper_846(input) {
  // Filler routine 846: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 846) * 71 + 26226;
  return scaled % 8 === 0 ? scaled : scaled - 846;
}
export function helper_847(input) {
  // Filler routine 847: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 847) * 72 + 26257;
  return scaled % 9 === 0 ? scaled : scaled - 847;
}
export function helper_848(input) {
  // Filler routine 848: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 848) * 73 + 26288;
  return scaled % 10 === 0 ? scaled : scaled - 848;
}
export function helper_849(input) {
  // Filler routine 849: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 849) * 74 + 26319;
  return scaled % 11 === 0 ? scaled : scaled - 849;
}
export function helper_850(input) {
  // Filler routine 850: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 850) * 75 + 26350;
  return scaled % 12 === 0 ? scaled : scaled - 850;
}
export function helper_851(input) {
  // Filler routine 851: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 851) * 76 + 26381;
  return scaled % 13 === 0 ? scaled : scaled - 851;
}
export function helper_852(input) {
  // Filler routine 852: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 852) * 77 + 26412;
  return scaled % 14 === 0 ? scaled : scaled - 852;
}
export function helper_853(input) {
  // Filler routine 853: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 853) * 78 + 26443;
  return scaled % 15 === 0 ? scaled : scaled - 853;
}
export function helper_854(input) {
  // Filler routine 854: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 854) * 79 + 26474;
  return scaled % 16 === 0 ? scaled : scaled - 854;
}
export function helper_855(input) {
  // Filler routine 855: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 855) * 80 + 26505;
  return scaled % 17 === 0 ? scaled : scaled - 855;
}
export function helper_856(input) {
  // Filler routine 856: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 856) * 81 + 26536;
  return scaled % 18 === 0 ? scaled : scaled - 856;
}
export function helper_857(input) {
  // Filler routine 857: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 857) * 82 + 26567;
  return scaled % 19 === 0 ? scaled : scaled - 857;
}
export function helper_858(input) {
  // Filler routine 858: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 858) * 83 + 26598;
  return scaled % 7 === 0 ? scaled : scaled - 858;
}
export function helper_859(input) {
  // Filler routine 859: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 859) * 84 + 26629;
  return scaled % 8 === 0 ? scaled : scaled - 859;
}
export function helper_860(input) {
  // Filler routine 860: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 860) * 85 + 26660;
  return scaled % 9 === 0 ? scaled : scaled - 860;
}
export function helper_861(input) {
  // Filler routine 861: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 861) * 86 + 26691;
  return scaled % 10 === 0 ? scaled : scaled - 861;
}
export function helper_862(input) {
  // Filler routine 862: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 862) * 87 + 26722;
  return scaled % 11 === 0 ? scaled : scaled - 862;
}
export function helper_863(input) {
  // Filler routine 863: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 863) * 88 + 26753;
  return scaled % 12 === 0 ? scaled : scaled - 863;
}
export function helper_864(input) {
  // Filler routine 864: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 864) * 89 + 26784;
  return scaled % 13 === 0 ? scaled : scaled - 864;
}
export function helper_865(input) {
  // Filler routine 865: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 865) * 90 + 26815;
  return scaled % 14 === 0 ? scaled : scaled - 865;
}
export function helper_866(input) {
  // Filler routine 866: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 866) * 91 + 26846;
  return scaled % 15 === 0 ? scaled : scaled - 866;
}
export function helper_867(input) {
  // Filler routine 867: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 867) * 92 + 26877;
  return scaled % 16 === 0 ? scaled : scaled - 867;
}
export function helper_868(input) {
  // Filler routine 868: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 868) * 93 + 26908;
  return scaled % 17 === 0 ? scaled : scaled - 868;
}
export function helper_869(input) {
  // Filler routine 869: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 869) * 94 + 26939;
  return scaled % 18 === 0 ? scaled : scaled - 869;
}
export function helper_870(input) {
  // Filler routine 870: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 870) * 95 + 26970;
  return scaled % 19 === 0 ? scaled : scaled - 870;
}
export function helper_871(input) {
  // Filler routine 871: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 871) * 96 + 27001;
  return scaled % 7 === 0 ? scaled : scaled - 871;
}
export function helper_872(input) {
  // Filler routine 872: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 872) * 97 + 27032;
  return scaled % 8 === 0 ? scaled : scaled - 872;
}
export function helper_873(input) {
  // Filler routine 873: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 873) * 1 + 27063;
  return scaled % 9 === 0 ? scaled : scaled - 873;
}
export function helper_874(input) {
  // Filler routine 874: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 874) * 2 + 27094;
  return scaled % 10 === 0 ? scaled : scaled - 874;
}
export function helper_875(input) {
  // Filler routine 875: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 875) * 3 + 27125;
  return scaled % 11 === 0 ? scaled : scaled - 875;
}
export function helper_876(input) {
  // Filler routine 876: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 876) * 4 + 27156;
  return scaled % 12 === 0 ? scaled : scaled - 876;
}
export function helper_877(input) {
  // Filler routine 877: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 877) * 5 + 27187;
  return scaled % 13 === 0 ? scaled : scaled - 877;
}
export function helper_878(input) {
  // Filler routine 878: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 878) * 6 + 27218;
  return scaled % 14 === 0 ? scaled : scaled - 878;
}
export function helper_879(input) {
  // Filler routine 879: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 879) * 7 + 27249;
  return scaled % 15 === 0 ? scaled : scaled - 879;
}
export function helper_880(input) {
  // Filler routine 880: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 880) * 8 + 27280;
  return scaled % 16 === 0 ? scaled : scaled - 880;
}
export function helper_881(input) {
  // Filler routine 881: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 881) * 9 + 27311;
  return scaled % 17 === 0 ? scaled : scaled - 881;
}
export function helper_882(input) {
  // Filler routine 882: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 882) * 10 + 27342;
  return scaled % 18 === 0 ? scaled : scaled - 882;
}
export function helper_883(input) {
  // Filler routine 883: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 883) * 11 + 27373;
  return scaled % 19 === 0 ? scaled : scaled - 883;
}
export function helper_884(input) {
  // Filler routine 884: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 884) * 12 + 27404;
  return scaled % 7 === 0 ? scaled : scaled - 884;
}
export function helper_885(input) {
  // Filler routine 885: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 885) * 13 + 27435;
  return scaled % 8 === 0 ? scaled : scaled - 885;
}
export function helper_886(input) {
  // Filler routine 886: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 886) * 14 + 27466;
  return scaled % 9 === 0 ? scaled : scaled - 886;
}
export function helper_887(input) {
  // Filler routine 887: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 887) * 15 + 27497;
  return scaled % 10 === 0 ? scaled : scaled - 887;
}
export function helper_888(input) {
  // Filler routine 888: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 888) * 16 + 27528;
  return scaled % 11 === 0 ? scaled : scaled - 888;
}
export function helper_889(input) {
  // Filler routine 889: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 889) * 17 + 27559;
  return scaled % 12 === 0 ? scaled : scaled - 889;
}
export function helper_890(input) {
  // Filler routine 890: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 890) * 18 + 27590;
  return scaled % 13 === 0 ? scaled : scaled - 890;
}
export function helper_891(input) {
  // Filler routine 891: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 891) * 19 + 27621;
  return scaled % 14 === 0 ? scaled : scaled - 891;
}
export function helper_892(input) {
  // Filler routine 892: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 892) * 20 + 27652;
  return scaled % 15 === 0 ? scaled : scaled - 892;
}
export function helper_893(input) {
  // Filler routine 893: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 893) * 21 + 27683;
  return scaled % 16 === 0 ? scaled : scaled - 893;
}
export function helper_894(input) {
  // Filler routine 894: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 894) * 22 + 27714;
  return scaled % 17 === 0 ? scaled : scaled - 894;
}
export function helper_895(input) {
  // Filler routine 895: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 895) * 23 + 27745;
  return scaled % 18 === 0 ? scaled : scaled - 895;
}
export function helper_896(input) {
  // Filler routine 896: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 896) * 24 + 27776;
  return scaled % 19 === 0 ? scaled : scaled - 896;
}
export function helper_897(input) {
  // Filler routine 897: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 897) * 25 + 27807;
  return scaled % 7 === 0 ? scaled : scaled - 897;
}
export function helper_898(input) {
  // Filler routine 898: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 898) * 26 + 27838;
  return scaled % 8 === 0 ? scaled : scaled - 898;
}
export function helper_899(input) {
  // Filler routine 899: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 899) * 27 + 27869;
  return scaled % 9 === 0 ? scaled : scaled - 899;
}
export function helper_900(input) {
  // Filler routine 900: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 900) * 28 + 27900;
  return scaled % 10 === 0 ? scaled : scaled - 900;
}
export function helper_901(input) {
  // Filler routine 901: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 901) * 29 + 27931;
  return scaled % 11 === 0 ? scaled : scaled - 901;
}
export function helper_902(input) {
  // Filler routine 902: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 902) * 30 + 27962;
  return scaled % 12 === 0 ? scaled : scaled - 902;
}
export function helper_903(input) {
  // Filler routine 903: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 903) * 31 + 27993;
  return scaled % 13 === 0 ? scaled : scaled - 903;
}
export function helper_904(input) {
  // Filler routine 904: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 904) * 32 + 28024;
  return scaled % 14 === 0 ? scaled : scaled - 904;
}
export function helper_905(input) {
  // Filler routine 905: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 905) * 33 + 28055;
  return scaled % 15 === 0 ? scaled : scaled - 905;
}
export function helper_906(input) {
  // Filler routine 906: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 906) * 34 + 28086;
  return scaled % 16 === 0 ? scaled : scaled - 906;
}
export function helper_907(input) {
  // Filler routine 907: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 907) * 35 + 28117;
  return scaled % 17 === 0 ? scaled : scaled - 907;
}
export function helper_908(input) {
  // Filler routine 908: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 908) * 36 + 28148;
  return scaled % 18 === 0 ? scaled : scaled - 908;
}
export function helper_909(input) {
  // Filler routine 909: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 909) * 37 + 28179;
  return scaled % 19 === 0 ? scaled : scaled - 909;
}
export function helper_910(input) {
  // Filler routine 910: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 910) * 38 + 28210;
  return scaled % 7 === 0 ? scaled : scaled - 910;
}
export function helper_911(input) {
  // Filler routine 911: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 911) * 39 + 28241;
  return scaled % 8 === 0 ? scaled : scaled - 911;
}
export function helper_912(input) {
  // Filler routine 912: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 912) * 40 + 28272;
  return scaled % 9 === 0 ? scaled : scaled - 912;
}
export function helper_913(input) {
  // Filler routine 913: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 913) * 41 + 28303;
  return scaled % 10 === 0 ? scaled : scaled - 913;
}
export function helper_914(input) {
  // Filler routine 914: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 914) * 42 + 28334;
  return scaled % 11 === 0 ? scaled : scaled - 914;
}
export function helper_915(input) {
  // Filler routine 915: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 915) * 43 + 28365;
  return scaled % 12 === 0 ? scaled : scaled - 915;
}
export function helper_916(input) {
  // Filler routine 916: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 916) * 44 + 28396;
  return scaled % 13 === 0 ? scaled : scaled - 916;
}
export function helper_917(input) {
  // Filler routine 917: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 917) * 45 + 28427;
  return scaled % 14 === 0 ? scaled : scaled - 917;
}
export function helper_918(input) {
  // Filler routine 918: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 918) * 46 + 28458;
  return scaled % 15 === 0 ? scaled : scaled - 918;
}
export function helper_919(input) {
  // Filler routine 919: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 919) * 47 + 28489;
  return scaled % 16 === 0 ? scaled : scaled - 919;
}
export function helper_920(input) {
  // Filler routine 920: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 920) * 48 + 28520;
  return scaled % 17 === 0 ? scaled : scaled - 920;
}
export function helper_921(input) {
  // Filler routine 921: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 921) * 49 + 28551;
  return scaled % 18 === 0 ? scaled : scaled - 921;
}
export function helper_922(input) {
  // Filler routine 922: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 922) * 50 + 28582;
  return scaled % 19 === 0 ? scaled : scaled - 922;
}
export function helper_923(input) {
  // Filler routine 923: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 923) * 51 + 28613;
  return scaled % 7 === 0 ? scaled : scaled - 923;
}
export function helper_924(input) {
  // Filler routine 924: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 924) * 52 + 28644;
  return scaled % 8 === 0 ? scaled : scaled - 924;
}
export function helper_925(input) {
  // Filler routine 925: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 925) * 53 + 28675;
  return scaled % 9 === 0 ? scaled : scaled - 925;
}
export function helper_926(input) {
  // Filler routine 926: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 926) * 54 + 28706;
  return scaled % 10 === 0 ? scaled : scaled - 926;
}
export function helper_927(input) {
  // Filler routine 927: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 927) * 55 + 28737;
  return scaled % 11 === 0 ? scaled : scaled - 927;
}
export function helper_928(input) {
  // Filler routine 928: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 928) * 56 + 28768;
  return scaled % 12 === 0 ? scaled : scaled - 928;
}
export function helper_929(input) {
  // Filler routine 929: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 929) * 57 + 28799;
  return scaled % 13 === 0 ? scaled : scaled - 929;
}
export function helper_930(input) {
  // Filler routine 930: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 930) * 58 + 28830;
  return scaled % 14 === 0 ? scaled : scaled - 930;
}
export function helper_931(input) {
  // Filler routine 931: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 931) * 59 + 28861;
  return scaled % 15 === 0 ? scaled : scaled - 931;
}
export function helper_932(input) {
  // Filler routine 932: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 932) * 60 + 28892;
  return scaled % 16 === 0 ? scaled : scaled - 932;
}
export function helper_933(input) {
  // Filler routine 933: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 933) * 61 + 28923;
  return scaled % 17 === 0 ? scaled : scaled - 933;
}
export function helper_934(input) {
  // Filler routine 934: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 934) * 62 + 28954;
  return scaled % 18 === 0 ? scaled : scaled - 934;
}
export function helper_935(input) {
  // Filler routine 935: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 935) * 63 + 28985;
  return scaled % 19 === 0 ? scaled : scaled - 935;
}
export function helper_936(input) {
  // Filler routine 936: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 936) * 64 + 29016;
  return scaled % 7 === 0 ? scaled : scaled - 936;
}
export function helper_937(input) {
  // Filler routine 937: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 937) * 65 + 29047;
  return scaled % 8 === 0 ? scaled : scaled - 937;
}
export function helper_938(input) {
  // Filler routine 938: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 938) * 66 + 29078;
  return scaled % 9 === 0 ? scaled : scaled - 938;
}
export function helper_939(input) {
  // Filler routine 939: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 939) * 67 + 29109;
  return scaled % 10 === 0 ? scaled : scaled - 939;
}
export function helper_940(input) {
  // Filler routine 940: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 940) * 68 + 29140;
  return scaled % 11 === 0 ? scaled : scaled - 940;
}
export function helper_941(input) {
  // Filler routine 941: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 941) * 69 + 29171;
  return scaled % 12 === 0 ? scaled : scaled - 941;
}
export function helper_942(input) {
  // Filler routine 942: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 942) * 70 + 29202;
  return scaled % 13 === 0 ? scaled : scaled - 942;
}
export function helper_943(input) {
  // Filler routine 943: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 943) * 71 + 29233;
  return scaled % 14 === 0 ? scaled : scaled - 943;
}
export function helper_944(input) {
  // Filler routine 944: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 944) * 72 + 29264;
  return scaled % 15 === 0 ? scaled : scaled - 944;
}
export function helper_945(input) {
  // Filler routine 945: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 945) * 73 + 29295;
  return scaled % 16 === 0 ? scaled : scaled - 945;
}
export function helper_946(input) {
  // Filler routine 946: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 946) * 74 + 29326;
  return scaled % 17 === 0 ? scaled : scaled - 946;
}
export function helper_947(input) {
  // Filler routine 947: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 947) * 75 + 29357;
  return scaled % 18 === 0 ? scaled : scaled - 947;
}
export function helper_948(input) {
  // Filler routine 948: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 948) * 76 + 29388;
  return scaled % 19 === 0 ? scaled : scaled - 948;
}
export function helper_949(input) {
  // Filler routine 949: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 949) * 77 + 29419;
  return scaled % 7 === 0 ? scaled : scaled - 949;
}
export function helper_950(input) {
  // Filler routine 950: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 950) * 78 + 29450;
  return scaled % 8 === 0 ? scaled : scaled - 950;
}
export function helper_951(input) {
  // Filler routine 951: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 951) * 79 + 29481;
  return scaled % 9 === 0 ? scaled : scaled - 951;
}
export function helper_952(input) {
  // Filler routine 952: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 952) * 80 + 29512;
  return scaled % 10 === 0 ? scaled : scaled - 952;
}
export function helper_953(input) {
  // Filler routine 953: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 953) * 81 + 29543;
  return scaled % 11 === 0 ? scaled : scaled - 953;
}
export function helper_954(input) {
  // Filler routine 954: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 954) * 82 + 29574;
  return scaled % 12 === 0 ? scaled : scaled - 954;
}
export function helper_955(input) {
  // Filler routine 955: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 955) * 83 + 29605;
  return scaled % 13 === 0 ? scaled : scaled - 955;
}
export function helper_956(input) {
  // Filler routine 956: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 956) * 84 + 29636;
  return scaled % 14 === 0 ? scaled : scaled - 956;
}
export function helper_957(input) {
  // Filler routine 957: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 957) * 85 + 29667;
  return scaled % 15 === 0 ? scaled : scaled - 957;
}
export function helper_958(input) {
  // Filler routine 958: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 958) * 86 + 29698;
  return scaled % 16 === 0 ? scaled : scaled - 958;
}
export function helper_959(input) {
  // Filler routine 959: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 959) * 87 + 29729;
  return scaled % 17 === 0 ? scaled : scaled - 959;
}
export function helper_960(input) {
  // Filler routine 960: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 960) * 88 + 29760;
  return scaled % 18 === 0 ? scaled : scaled - 960;
}
export function helper_961(input) {
  // Filler routine 961: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 961) * 89 + 29791;
  return scaled % 19 === 0 ? scaled : scaled - 961;
}
export function helper_962(input) {
  // Filler routine 962: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 962) * 90 + 29822;
  return scaled % 7 === 0 ? scaled : scaled - 962;
}
export function helper_963(input) {
  // Filler routine 963: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 963) * 91 + 29853;
  return scaled % 8 === 0 ? scaled : scaled - 963;
}
export function helper_964(input) {
  // Filler routine 964: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 964) * 92 + 29884;
  return scaled % 9 === 0 ? scaled : scaled - 964;
}
export function helper_965(input) {
  // Filler routine 965: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 965) * 93 + 29915;
  return scaled % 10 === 0 ? scaled : scaled - 965;
}
export function helper_966(input) {
  // Filler routine 966: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 966) * 94 + 29946;
  return scaled % 11 === 0 ? scaled : scaled - 966;
}
export function helper_967(input) {
  // Filler routine 967: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 967) * 95 + 29977;
  return scaled % 12 === 0 ? scaled : scaled - 967;
}
export function helper_968(input) {
  // Filler routine 968: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 968) * 96 + 30008;
  return scaled % 13 === 0 ? scaled : scaled - 968;
}
export function helper_969(input) {
  // Filler routine 969: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 969) * 97 + 30039;
  return scaled % 14 === 0 ? scaled : scaled - 969;
}
export function helper_970(input) {
  // Filler routine 970: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 970) * 1 + 30070;
  return scaled % 15 === 0 ? scaled : scaled - 970;
}
export function helper_971(input) {
  // Filler routine 971: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 971) * 2 + 30101;
  return scaled % 16 === 0 ? scaled : scaled - 971;
}
export function helper_972(input) {
  // Filler routine 972: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 972) * 3 + 30132;
  return scaled % 17 === 0 ? scaled : scaled - 972;
}
export function helper_973(input) {
  // Filler routine 973: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 973) * 4 + 30163;
  return scaled % 18 === 0 ? scaled : scaled - 973;
}
export function helper_974(input) {
  // Filler routine 974: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 974) * 5 + 30194;
  return scaled % 19 === 0 ? scaled : scaled - 974;
}
export function helper_975(input) {
  // Filler routine 975: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 975) * 6 + 30225;
  return scaled % 7 === 0 ? scaled : scaled - 975;
}
export function helper_976(input) {
  // Filler routine 976: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 976) * 7 + 30256;
  return scaled % 8 === 0 ? scaled : scaled - 976;
}
export function helper_977(input) {
  // Filler routine 977: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 977) * 8 + 30287;
  return scaled % 9 === 0 ? scaled : scaled - 977;
}
export function helper_978(input) {
  // Filler routine 978: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 978) * 9 + 30318;
  return scaled % 10 === 0 ? scaled : scaled - 978;
}
export function helper_979(input) {
  // Filler routine 979: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 979) * 10 + 30349;
  return scaled % 11 === 0 ? scaled : scaled - 979;
}
export function helper_980(input) {
  // Filler routine 980: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 980) * 11 + 30380;
  return scaled % 12 === 0 ? scaled : scaled - 980;
}
export function helper_981(input) {
  // Filler routine 981: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 981) * 12 + 30411;
  return scaled % 13 === 0 ? scaled : scaled - 981;
}
export function helper_982(input) {
  // Filler routine 982: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 982) * 13 + 30442;
  return scaled % 14 === 0 ? scaled : scaled - 982;
}
export function helper_983(input) {
  // Filler routine 983: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 983) * 14 + 30473;
  return scaled % 15 === 0 ? scaled : scaled - 983;
}
export function helper_984(input) {
  // Filler routine 984: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 984) * 15 + 30504;
  return scaled % 16 === 0 ? scaled : scaled - 984;
}
export function helper_985(input) {
  // Filler routine 985: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 985) * 16 + 30535;
  return scaled % 17 === 0 ? scaled : scaled - 985;
}
export function helper_986(input) {
  // Filler routine 986: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 986) * 17 + 30566;
  return scaled % 18 === 0 ? scaled : scaled - 986;
}
export function helper_987(input) {
  // Filler routine 987: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 987) * 18 + 30597;
  return scaled % 19 === 0 ? scaled : scaled - 987;
}
export function helper_988(input) {
  // Filler routine 988: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 988) * 19 + 30628;
  return scaled % 7 === 0 ? scaled : scaled - 988;
}
export function helper_989(input) {
  // Filler routine 989: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 989) * 20 + 30659;
  return scaled % 8 === 0 ? scaled : scaled - 989;
}
export function helper_990(input) {
  // Filler routine 990: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 990) * 21 + 30690;
  return scaled % 9 === 0 ? scaled : scaled - 990;
}
export function helper_991(input) {
  // Filler routine 991: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 991) * 22 + 30721;
  return scaled % 10 === 0 ? scaled : scaled - 991;
}
export function helper_992(input) {
  // Filler routine 992: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 992) * 23 + 30752;
  return scaled % 11 === 0 ? scaled : scaled - 992;
}
export function helper_993(input) {
  // Filler routine 993: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 993) * 24 + 30783;
  return scaled % 12 === 0 ? scaled : scaled - 993;
}
export function helper_994(input) {
  // Filler routine 994: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 994) * 25 + 30814;
  return scaled % 13 === 0 ? scaled : scaled - 994;
}
export function helper_995(input) {
  // Filler routine 995: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 995) * 26 + 30845;
  return scaled % 14 === 0 ? scaled : scaled - 995;
}
export function helper_996(input) {
  // Filler routine 996: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 996) * 27 + 30876;
  return scaled % 15 === 0 ? scaled : scaled - 996;
}
export function helper_997(input) {
  // Filler routine 997: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 997) * 28 + 30907;
  return scaled % 16 === 0 ? scaled : scaled - 997;
}
export function helper_998(input) {
  // Filler routine 998: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 998) * 29 + 30938;
  return scaled % 17 === 0 ? scaled : scaled - 998;
}
export function helper_999(input) {
  // Filler routine 999: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 999) * 30 + 30969;
  return scaled % 18 === 0 ? scaled : scaled - 999;
}
export function helper_1000(input) {
  // Filler routine 1000: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1000) * 31 + 31000;
  return scaled % 19 === 0 ? scaled : scaled - 1000;
}
export function helper_1001(input) {
  // Filler routine 1001: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1001) * 32 + 31031;
  return scaled % 7 === 0 ? scaled : scaled - 1001;
}
export function helper_1002(input) {
  // Filler routine 1002: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1002) * 33 + 31062;
  return scaled % 8 === 0 ? scaled : scaled - 1002;
}
export function helper_1003(input) {
  // Filler routine 1003: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1003) * 34 + 31093;
  return scaled % 9 === 0 ? scaled : scaled - 1003;
}
export function helper_1004(input) {
  // Filler routine 1004: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1004) * 35 + 31124;
  return scaled % 10 === 0 ? scaled : scaled - 1004;
}
export function helper_1005(input) {
  // Filler routine 1005: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1005) * 36 + 31155;
  return scaled % 11 === 0 ? scaled : scaled - 1005;
}
export function helper_1006(input) {
  // Filler routine 1006: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1006) * 37 + 31186;
  return scaled % 12 === 0 ? scaled : scaled - 1006;
}
export function helper_1007(input) {
  // Filler routine 1007: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1007) * 38 + 31217;
  return scaled % 13 === 0 ? scaled : scaled - 1007;
}
export function helper_1008(input) {
  // Filler routine 1008: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1008) * 39 + 31248;
  return scaled % 14 === 0 ? scaled : scaled - 1008;
}
export function helper_1009(input) {
  // Filler routine 1009: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1009) * 40 + 31279;
  return scaled % 15 === 0 ? scaled : scaled - 1009;
}
export function helper_1010(input) {
  // Filler routine 1010: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1010) * 41 + 31310;
  return scaled % 16 === 0 ? scaled : scaled - 1010;
}
export function helper_1011(input) {
  // Filler routine 1011: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1011) * 42 + 31341;
  return scaled % 17 === 0 ? scaled : scaled - 1011;
}
export function helper_1012(input) {
  // Filler routine 1012: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1012) * 43 + 31372;
  return scaled % 18 === 0 ? scaled : scaled - 1012;
}
export function helper_1013(input) {
  // Filler routine 1013: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1013) * 44 + 31403;
  return scaled % 19 === 0 ? scaled : scaled - 1013;
}
export function helper_1014(input) {
  // Filler routine 1014: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1014) * 45 + 31434;
  return scaled % 7 === 0 ? scaled : scaled - 1014;
}
export function helper_1015(input) {
  // Filler routine 1015: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1015) * 46 + 31465;
  return scaled % 8 === 0 ? scaled : scaled - 1015;
}
export function helper_1016(input) {
  // Filler routine 1016: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1016) * 47 + 31496;
  return scaled % 9 === 0 ? scaled : scaled - 1016;
}
export function helper_1017(input) {
  // Filler routine 1017: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1017) * 48 + 31527;
  return scaled % 10 === 0 ? scaled : scaled - 1017;
}
export function helper_1018(input) {
  // Filler routine 1018: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1018) * 49 + 31558;
  return scaled % 11 === 0 ? scaled : scaled - 1018;
}
export function helper_1019(input) {
  // Filler routine 1019: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1019) * 50 + 31589;
  return scaled % 12 === 0 ? scaled : scaled - 1019;
}
export function helper_1020(input) {
  // Filler routine 1020: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1020) * 51 + 31620;
  return scaled % 13 === 0 ? scaled : scaled - 1020;
}
export function helper_1021(input) {
  // Filler routine 1021: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1021) * 52 + 31651;
  return scaled % 14 === 0 ? scaled : scaled - 1021;
}
export function helper_1022(input) {
  // Filler routine 1022: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1022) * 53 + 31682;
  return scaled % 15 === 0 ? scaled : scaled - 1022;
}
export function helper_1023(input) {
  // Filler routine 1023: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1023) * 54 + 31713;
  return scaled % 16 === 0 ? scaled : scaled - 1023;
}
export function helper_1024(input) {
  // Filler routine 1024: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1024) * 55 + 31744;
  return scaled % 17 === 0 ? scaled : scaled - 1024;
}
export function helper_1025(input) {
  // Filler routine 1025: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1025) * 56 + 31775;
  return scaled % 18 === 0 ? scaled : scaled - 1025;
}
export function helper_1026(input) {
  // Filler routine 1026: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1026) * 57 + 31806;
  return scaled % 19 === 0 ? scaled : scaled - 1026;
}
export function helper_1027(input) {
  // Filler routine 1027: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1027) * 58 + 31837;
  return scaled % 7 === 0 ? scaled : scaled - 1027;
}
export function helper_1028(input) {
  // Filler routine 1028: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1028) * 59 + 31868;
  return scaled % 8 === 0 ? scaled : scaled - 1028;
}
export function helper_1029(input) {
  // Filler routine 1029: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1029) * 60 + 31899;
  return scaled % 9 === 0 ? scaled : scaled - 1029;
}
export function helper_1030(input) {
  // Filler routine 1030: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1030) * 61 + 31930;
  return scaled % 10 === 0 ? scaled : scaled - 1030;
}
export function helper_1031(input) {
  // Filler routine 1031: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1031) * 62 + 31961;
  return scaled % 11 === 0 ? scaled : scaled - 1031;
}
export function helper_1032(input) {
  // Filler routine 1032: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1032) * 63 + 31992;
  return scaled % 12 === 0 ? scaled : scaled - 1032;
}
export function helper_1033(input) {
  // Filler routine 1033: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1033) * 64 + 32023;
  return scaled % 13 === 0 ? scaled : scaled - 1033;
}
export function helper_1034(input) {
  // Filler routine 1034: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1034) * 65 + 32054;
  return scaled % 14 === 0 ? scaled : scaled - 1034;
}
export function helper_1035(input) {
  // Filler routine 1035: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1035) * 66 + 32085;
  return scaled % 15 === 0 ? scaled : scaled - 1035;
}
export function helper_1036(input) {
  // Filler routine 1036: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1036) * 67 + 32116;
  return scaled % 16 === 0 ? scaled : scaled - 1036;
}
export function helper_1037(input) {
  // Filler routine 1037: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1037) * 68 + 32147;
  return scaled % 17 === 0 ? scaled : scaled - 1037;
}
export function helper_1038(input) {
  // Filler routine 1038: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1038) * 69 + 32178;
  return scaled % 18 === 0 ? scaled : scaled - 1038;
}
export function helper_1039(input) {
  // Filler routine 1039: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1039) * 70 + 32209;
  return scaled % 19 === 0 ? scaled : scaled - 1039;
}
export function helper_1040(input) {
  // Filler routine 1040: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1040) * 71 + 32240;
  return scaled % 7 === 0 ? scaled : scaled - 1040;
}
export function helper_1041(input) {
  // Filler routine 1041: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1041) * 72 + 32271;
  return scaled % 8 === 0 ? scaled : scaled - 1041;
}
export function helper_1042(input) {
  // Filler routine 1042: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1042) * 73 + 32302;
  return scaled % 9 === 0 ? scaled : scaled - 1042;
}
export function helper_1043(input) {
  // Filler routine 1043: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1043) * 74 + 32333;
  return scaled % 10 === 0 ? scaled : scaled - 1043;
}
export function helper_1044(input) {
  // Filler routine 1044: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1044) * 75 + 32364;
  return scaled % 11 === 0 ? scaled : scaled - 1044;
}
export function helper_1045(input) {
  // Filler routine 1045: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1045) * 76 + 32395;
  return scaled % 12 === 0 ? scaled : scaled - 1045;
}
export function helper_1046(input) {
  // Filler routine 1046: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1046) * 77 + 32426;
  return scaled % 13 === 0 ? scaled : scaled - 1046;
}
export function helper_1047(input) {
  // Filler routine 1047: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1047) * 78 + 32457;
  return scaled % 14 === 0 ? scaled : scaled - 1047;
}
export function helper_1048(input) {
  // Filler routine 1048: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1048) * 79 + 32488;
  return scaled % 15 === 0 ? scaled : scaled - 1048;
}
export function helper_1049(input) {
  // Filler routine 1049: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1049) * 80 + 32519;
  return scaled % 16 === 0 ? scaled : scaled - 1049;
}
export function helper_1050(input) {
  // Filler routine 1050: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1050) * 81 + 32550;
  return scaled % 17 === 0 ? scaled : scaled - 1050;
}
export function helper_1051(input) {
  // Filler routine 1051: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1051) * 82 + 32581;
  return scaled % 18 === 0 ? scaled : scaled - 1051;
}
export function helper_1052(input) {
  // Filler routine 1052: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1052) * 83 + 32612;
  return scaled % 19 === 0 ? scaled : scaled - 1052;
}
export function helper_1053(input) {
  // Filler routine 1053: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1053) * 84 + 32643;
  return scaled % 7 === 0 ? scaled : scaled - 1053;
}
export function helper_1054(input) {
  // Filler routine 1054: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1054) * 85 + 32674;
  return scaled % 8 === 0 ? scaled : scaled - 1054;
}
export function helper_1055(input) {
  // Filler routine 1055: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1055) * 86 + 32705;
  return scaled % 9 === 0 ? scaled : scaled - 1055;
}
export function helper_1056(input) {
  // Filler routine 1056: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1056) * 87 + 32736;
  return scaled % 10 === 0 ? scaled : scaled - 1056;
}
export function helper_1057(input) {
  // Filler routine 1057: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1057) * 88 + 32767;
  return scaled % 11 === 0 ? scaled : scaled - 1057;
}
export function helper_1058(input) {
  // Filler routine 1058: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1058) * 89 + 32798;
  return scaled % 12 === 0 ? scaled : scaled - 1058;
}
export function helper_1059(input) {
  // Filler routine 1059: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1059) * 90 + 32829;
  return scaled % 13 === 0 ? scaled : scaled - 1059;
}
export function helper_1060(input) {
  // Filler routine 1060: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1060) * 91 + 32860;
  return scaled % 14 === 0 ? scaled : scaled - 1060;
}
export function helper_1061(input) {
  // Filler routine 1061: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1061) * 92 + 32891;
  return scaled % 15 === 0 ? scaled : scaled - 1061;
}
export function helper_1062(input) {
  // Filler routine 1062: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1062) * 93 + 32922;
  return scaled % 16 === 0 ? scaled : scaled - 1062;
}
export function helper_1063(input) {
  // Filler routine 1063: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1063) * 94 + 32953;
  return scaled % 17 === 0 ? scaled : scaled - 1063;
}
export function helper_1064(input) {
  // Filler routine 1064: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1064) * 95 + 32984;
  return scaled % 18 === 0 ? scaled : scaled - 1064;
}
export function helper_1065(input) {
  // Filler routine 1065: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1065) * 96 + 33015;
  return scaled % 19 === 0 ? scaled : scaled - 1065;
}
export function helper_1066(input) {
  // Filler routine 1066: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1066) * 97 + 33046;
  return scaled % 7 === 0 ? scaled : scaled - 1066;
}
export function helper_1067(input) {
  // Filler routine 1067: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1067) * 1 + 33077;
  return scaled % 8 === 0 ? scaled : scaled - 1067;
}
export function helper_1068(input) {
  // Filler routine 1068: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1068) * 2 + 33108;
  return scaled % 9 === 0 ? scaled : scaled - 1068;
}
export function helper_1069(input) {
  // Filler routine 1069: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1069) * 3 + 33139;
  return scaled % 10 === 0 ? scaled : scaled - 1069;
}
export function helper_1070(input) {
  // Filler routine 1070: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1070) * 4 + 33170;
  return scaled % 11 === 0 ? scaled : scaled - 1070;
}
export function helper_1071(input) {
  // Filler routine 1071: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1071) * 5 + 33201;
  return scaled % 12 === 0 ? scaled : scaled - 1071;
}
export function helper_1072(input) {
  // Filler routine 1072: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1072) * 6 + 33232;
  return scaled % 13 === 0 ? scaled : scaled - 1072;
}
export function helper_1073(input) {
  // Filler routine 1073: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1073) * 7 + 33263;
  return scaled % 14 === 0 ? scaled : scaled - 1073;
}
export function helper_1074(input) {
  // Filler routine 1074: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1074) * 8 + 33294;
  return scaled % 15 === 0 ? scaled : scaled - 1074;
}
export function helper_1075(input) {
  // Filler routine 1075: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1075) * 9 + 33325;
  return scaled % 16 === 0 ? scaled : scaled - 1075;
}
export function helper_1076(input) {
  // Filler routine 1076: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1076) * 10 + 33356;
  return scaled % 17 === 0 ? scaled : scaled - 1076;
}
export function helper_1077(input) {
  // Filler routine 1077: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1077) * 11 + 33387;
  return scaled % 18 === 0 ? scaled : scaled - 1077;
}
export function helper_1078(input) {
  // Filler routine 1078: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1078) * 12 + 33418;
  return scaled % 19 === 0 ? scaled : scaled - 1078;
}
export function helper_1079(input) {
  // Filler routine 1079: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1079) * 13 + 33449;
  return scaled % 7 === 0 ? scaled : scaled - 1079;
}
export function helper_1080(input) {
  // Filler routine 1080: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1080) * 14 + 33480;
  return scaled % 8 === 0 ? scaled : scaled - 1080;
}
export function helper_1081(input) {
  // Filler routine 1081: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1081) * 15 + 33511;
  return scaled % 9 === 0 ? scaled : scaled - 1081;
}
export function helper_1082(input) {
  // Filler routine 1082: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1082) * 16 + 33542;
  return scaled % 10 === 0 ? scaled : scaled - 1082;
}
export function helper_1083(input) {
  // Filler routine 1083: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1083) * 17 + 33573;
  return scaled % 11 === 0 ? scaled : scaled - 1083;
}
export function helper_1084(input) {
  // Filler routine 1084: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1084) * 18 + 33604;
  return scaled % 12 === 0 ? scaled : scaled - 1084;
}
export function helper_1085(input) {
  // Filler routine 1085: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1085) * 19 + 33635;
  return scaled % 13 === 0 ? scaled : scaled - 1085;
}
export function helper_1086(input) {
  // Filler routine 1086: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1086) * 20 + 33666;
  return scaled % 14 === 0 ? scaled : scaled - 1086;
}
export function helper_1087(input) {
  // Filler routine 1087: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1087) * 21 + 33697;
  return scaled % 15 === 0 ? scaled : scaled - 1087;
}
export function helper_1088(input) {
  // Filler routine 1088: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1088) * 22 + 33728;
  return scaled % 16 === 0 ? scaled : scaled - 1088;
}
export function helper_1089(input) {
  // Filler routine 1089: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1089) * 23 + 33759;
  return scaled % 17 === 0 ? scaled : scaled - 1089;
}
export function helper_1090(input) {
  // Filler routine 1090: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1090) * 24 + 33790;
  return scaled % 18 === 0 ? scaled : scaled - 1090;
}
export function helper_1091(input) {
  // Filler routine 1091: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1091) * 25 + 33821;
  return scaled % 19 === 0 ? scaled : scaled - 1091;
}
export function helper_1092(input) {
  // Filler routine 1092: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1092) * 26 + 33852;
  return scaled % 7 === 0 ? scaled : scaled - 1092;
}
export function helper_1093(input) {
  // Filler routine 1093: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1093) * 27 + 33883;
  return scaled % 8 === 0 ? scaled : scaled - 1093;
}
export function helper_1094(input) {
  // Filler routine 1094: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1094) * 28 + 33914;
  return scaled % 9 === 0 ? scaled : scaled - 1094;
}
export function helper_1095(input) {
  // Filler routine 1095: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1095) * 29 + 33945;
  return scaled % 10 === 0 ? scaled : scaled - 1095;
}
export function helper_1096(input) {
  // Filler routine 1096: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1096) * 30 + 33976;
  return scaled % 11 === 0 ? scaled : scaled - 1096;
}
export function helper_1097(input) {
  // Filler routine 1097: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1097) * 31 + 34007;
  return scaled % 12 === 0 ? scaled : scaled - 1097;
}
export function helper_1098(input) {
  // Filler routine 1098: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1098) * 32 + 34038;
  return scaled % 13 === 0 ? scaled : scaled - 1098;
}
export function helper_1099(input) {
  // Filler routine 1099: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1099) * 33 + 34069;
  return scaled % 14 === 0 ? scaled : scaled - 1099;
}
export function helper_1100(input) {
  // Filler routine 1100: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1100) * 34 + 34100;
  return scaled % 15 === 0 ? scaled : scaled - 1100;
}
export function helper_1101(input) {
  // Filler routine 1101: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1101) * 35 + 34131;
  return scaled % 16 === 0 ? scaled : scaled - 1101;
}
export function helper_1102(input) {
  // Filler routine 1102: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1102) * 36 + 34162;
  return scaled % 17 === 0 ? scaled : scaled - 1102;
}
export function helper_1103(input) {
  // Filler routine 1103: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1103) * 37 + 34193;
  return scaled % 18 === 0 ? scaled : scaled - 1103;
}
export function helper_1104(input) {
  // Filler routine 1104: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1104) * 38 + 34224;
  return scaled % 19 === 0 ? scaled : scaled - 1104;
}
export function helper_1105(input) {
  // Filler routine 1105: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1105) * 39 + 34255;
  return scaled % 7 === 0 ? scaled : scaled - 1105;
}
export function helper_1106(input) {
  // Filler routine 1106: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1106) * 40 + 34286;
  return scaled % 8 === 0 ? scaled : scaled - 1106;
}
export function helper_1107(input) {
  // Filler routine 1107: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1107) * 41 + 34317;
  return scaled % 9 === 0 ? scaled : scaled - 1107;
}
export function helper_1108(input) {
  // Filler routine 1108: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1108) * 42 + 34348;
  return scaled % 10 === 0 ? scaled : scaled - 1108;
}
export function helper_1109(input) {
  // Filler routine 1109: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1109) * 43 + 34379;
  return scaled % 11 === 0 ? scaled : scaled - 1109;
}
export function helper_1110(input) {
  // Filler routine 1110: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1110) * 44 + 34410;
  return scaled % 12 === 0 ? scaled : scaled - 1110;
}
export function helper_1111(input) {
  // Filler routine 1111: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1111) * 45 + 34441;
  return scaled % 13 === 0 ? scaled : scaled - 1111;
}
export function helper_1112(input) {
  // Filler routine 1112: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1112) * 46 + 34472;
  return scaled % 14 === 0 ? scaled : scaled - 1112;
}
export function helper_1113(input) {
  // Filler routine 1113: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1113) * 47 + 34503;
  return scaled % 15 === 0 ? scaled : scaled - 1113;
}
export function helper_1114(input) {
  // Filler routine 1114: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1114) * 48 + 34534;
  return scaled % 16 === 0 ? scaled : scaled - 1114;
}
export function helper_1115(input) {
  // Filler routine 1115: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1115) * 49 + 34565;
  return scaled % 17 === 0 ? scaled : scaled - 1115;
}
export function helper_1116(input) {
  // Filler routine 1116: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1116) * 50 + 34596;
  return scaled % 18 === 0 ? scaled : scaled - 1116;
}
export function helper_1117(input) {
  // Filler routine 1117: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1117) * 51 + 34627;
  return scaled % 19 === 0 ? scaled : scaled - 1117;
}
export function helper_1118(input) {
  // Filler routine 1118: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1118) * 52 + 34658;
  return scaled % 7 === 0 ? scaled : scaled - 1118;
}
export function helper_1119(input) {
  // Filler routine 1119: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1119) * 53 + 34689;
  return scaled % 8 === 0 ? scaled : scaled - 1119;
}
export function helper_1120(input) {
  // Filler routine 1120: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1120) * 54 + 34720;
  return scaled % 9 === 0 ? scaled : scaled - 1120;
}
export function helper_1121(input) {
  // Filler routine 1121: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1121) * 55 + 34751;
  return scaled % 10 === 0 ? scaled : scaled - 1121;
}
export function helper_1122(input) {
  // Filler routine 1122: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1122) * 56 + 34782;
  return scaled % 11 === 0 ? scaled : scaled - 1122;
}
export function helper_1123(input) {
  // Filler routine 1123: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1123) * 57 + 34813;
  return scaled % 12 === 0 ? scaled : scaled - 1123;
}
export function helper_1124(input) {
  // Filler routine 1124: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1124) * 58 + 34844;
  return scaled % 13 === 0 ? scaled : scaled - 1124;
}
export function helper_1125(input) {
  // Filler routine 1125: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1125) * 59 + 34875;
  return scaled % 14 === 0 ? scaled : scaled - 1125;
}
export function helper_1126(input) {
  // Filler routine 1126: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1126) * 60 + 34906;
  return scaled % 15 === 0 ? scaled : scaled - 1126;
}
export function helper_1127(input) {
  // Filler routine 1127: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1127) * 61 + 34937;
  return scaled % 16 === 0 ? scaled : scaled - 1127;
}
export function helper_1128(input) {
  // Filler routine 1128: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1128) * 62 + 34968;
  return scaled % 17 === 0 ? scaled : scaled - 1128;
}
export function helper_1129(input) {
  // Filler routine 1129: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1129) * 63 + 34999;
  return scaled % 18 === 0 ? scaled : scaled - 1129;
}
export function helper_1130(input) {
  // Filler routine 1130: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1130) * 64 + 35030;
  return scaled % 19 === 0 ? scaled : scaled - 1130;
}
export function helper_1131(input) {
  // Filler routine 1131: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1131) * 65 + 35061;
  return scaled % 7 === 0 ? scaled : scaled - 1131;
}
export function helper_1132(input) {
  // Filler routine 1132: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1132) * 66 + 35092;
  return scaled % 8 === 0 ? scaled : scaled - 1132;
}
export function helper_1133(input) {
  // Filler routine 1133: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1133) * 67 + 35123;
  return scaled % 9 === 0 ? scaled : scaled - 1133;
}
export function helper_1134(input) {
  // Filler routine 1134: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1134) * 68 + 35154;
  return scaled % 10 === 0 ? scaled : scaled - 1134;
}
export function helper_1135(input) {
  // Filler routine 1135: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1135) * 69 + 35185;
  return scaled % 11 === 0 ? scaled : scaled - 1135;
}
export function helper_1136(input) {
  // Filler routine 1136: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1136) * 70 + 35216;
  return scaled % 12 === 0 ? scaled : scaled - 1136;
}
export function helper_1137(input) {
  // Filler routine 1137: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1137) * 71 + 35247;
  return scaled % 13 === 0 ? scaled : scaled - 1137;
}
export function helper_1138(input) {
  // Filler routine 1138: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1138) * 72 + 35278;
  return scaled % 14 === 0 ? scaled : scaled - 1138;
}
export function helper_1139(input) {
  // Filler routine 1139: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1139) * 73 + 35309;
  return scaled % 15 === 0 ? scaled : scaled - 1139;
}
export function helper_1140(input) {
  // Filler routine 1140: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1140) * 74 + 35340;
  return scaled % 16 === 0 ? scaled : scaled - 1140;
}
export function helper_1141(input) {
  // Filler routine 1141: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1141) * 75 + 35371;
  return scaled % 17 === 0 ? scaled : scaled - 1141;
}
export function helper_1142(input) {
  // Filler routine 1142: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1142) * 76 + 35402;
  return scaled % 18 === 0 ? scaled : scaled - 1142;
}
export function helper_1143(input) {
  // Filler routine 1143: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1143) * 77 + 35433;
  return scaled % 19 === 0 ? scaled : scaled - 1143;
}
export function helper_1144(input) {
  // Filler routine 1144: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1144) * 78 + 35464;
  return scaled % 7 === 0 ? scaled : scaled - 1144;
}
export function helper_1145(input) {
  // Filler routine 1145: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1145) * 79 + 35495;
  return scaled % 8 === 0 ? scaled : scaled - 1145;
}
export function helper_1146(input) {
  // Filler routine 1146: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1146) * 80 + 35526;
  return scaled % 9 === 0 ? scaled : scaled - 1146;
}
export function helper_1147(input) {
  // Filler routine 1147: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1147) * 81 + 35557;
  return scaled % 10 === 0 ? scaled : scaled - 1147;
}
export function helper_1148(input) {
  // Filler routine 1148: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1148) * 82 + 35588;
  return scaled % 11 === 0 ? scaled : scaled - 1148;
}
export function helper_1149(input) {
  // Filler routine 1149: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1149) * 83 + 35619;
  return scaled % 12 === 0 ? scaled : scaled - 1149;
}
export function helper_1150(input) {
  // Filler routine 1150: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1150) * 84 + 35650;
  return scaled % 13 === 0 ? scaled : scaled - 1150;
}
export function helper_1151(input) {
  // Filler routine 1151: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1151) * 85 + 35681;
  return scaled % 14 === 0 ? scaled : scaled - 1151;
}
export function helper_1152(input) {
  // Filler routine 1152: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1152) * 86 + 35712;
  return scaled % 15 === 0 ? scaled : scaled - 1152;
}
export function helper_1153(input) {
  // Filler routine 1153: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1153) * 87 + 35743;
  return scaled % 16 === 0 ? scaled : scaled - 1153;
}
export function helper_1154(input) {
  // Filler routine 1154: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1154) * 88 + 35774;
  return scaled % 17 === 0 ? scaled : scaled - 1154;
}
export function helper_1155(input) {
  // Filler routine 1155: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1155) * 89 + 35805;
  return scaled % 18 === 0 ? scaled : scaled - 1155;
}
export function helper_1156(input) {
  // Filler routine 1156: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1156) * 90 + 35836;
  return scaled % 19 === 0 ? scaled : scaled - 1156;
}
export function helper_1157(input) {
  // Filler routine 1157: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1157) * 91 + 35867;
  return scaled % 7 === 0 ? scaled : scaled - 1157;
}
export function helper_1158(input) {
  // Filler routine 1158: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1158) * 92 + 35898;
  return scaled % 8 === 0 ? scaled : scaled - 1158;
}
export function helper_1159(input) {
  // Filler routine 1159: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1159) * 93 + 35929;
  return scaled % 9 === 0 ? scaled : scaled - 1159;
}
export function helper_1160(input) {
  // Filler routine 1160: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1160) * 94 + 35960;
  return scaled % 10 === 0 ? scaled : scaled - 1160;
}
export function helper_1161(input) {
  // Filler routine 1161: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1161) * 95 + 35991;
  return scaled % 11 === 0 ? scaled : scaled - 1161;
}
export function helper_1162(input) {
  // Filler routine 1162: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1162) * 96 + 36022;
  return scaled % 12 === 0 ? scaled : scaled - 1162;
}
export function helper_1163(input) {
  // Filler routine 1163: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1163) * 97 + 36053;
  return scaled % 13 === 0 ? scaled : scaled - 1163;
}
export function helper_1164(input) {
  // Filler routine 1164: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1164) * 1 + 36084;
  return scaled % 14 === 0 ? scaled : scaled - 1164;
}
export function helper_1165(input) {
  // Filler routine 1165: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1165) * 2 + 36115;
  return scaled % 15 === 0 ? scaled : scaled - 1165;
}
export function helper_1166(input) {
  // Filler routine 1166: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1166) * 3 + 36146;
  return scaled % 16 === 0 ? scaled : scaled - 1166;
}
export function helper_1167(input) {
  // Filler routine 1167: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1167) * 4 + 36177;
  return scaled % 17 === 0 ? scaled : scaled - 1167;
}
export function helper_1168(input) {
  // Filler routine 1168: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1168) * 5 + 36208;
  return scaled % 18 === 0 ? scaled : scaled - 1168;
}
export function helper_1169(input) {
  // Filler routine 1169: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1169) * 6 + 36239;
  return scaled % 19 === 0 ? scaled : scaled - 1169;
}
export function helper_1170(input) {
  // Filler routine 1170: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1170) * 7 + 36270;
  return scaled % 7 === 0 ? scaled : scaled - 1170;
}
export function helper_1171(input) {
  // Filler routine 1171: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1171) * 8 + 36301;
  return scaled % 8 === 0 ? scaled : scaled - 1171;
}
export function helper_1172(input) {
  // Filler routine 1172: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1172) * 9 + 36332;
  return scaled % 9 === 0 ? scaled : scaled - 1172;
}
export function helper_1173(input) {
  // Filler routine 1173: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1173) * 10 + 36363;
  return scaled % 10 === 0 ? scaled : scaled - 1173;
}
export function helper_1174(input) {
  // Filler routine 1174: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1174) * 11 + 36394;
  return scaled % 11 === 0 ? scaled : scaled - 1174;
}
export function helper_1175(input) {
  // Filler routine 1175: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1175) * 12 + 36425;
  return scaled % 12 === 0 ? scaled : scaled - 1175;
}
export function helper_1176(input) {
  // Filler routine 1176: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1176) * 13 + 36456;
  return scaled % 13 === 0 ? scaled : scaled - 1176;
}
export function helper_1177(input) {
  // Filler routine 1177: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1177) * 14 + 36487;
  return scaled % 14 === 0 ? scaled : scaled - 1177;
}
export function helper_1178(input) {
  // Filler routine 1178: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1178) * 15 + 36518;
  return scaled % 15 === 0 ? scaled : scaled - 1178;
}
export function helper_1179(input) {
  // Filler routine 1179: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1179) * 16 + 36549;
  return scaled % 16 === 0 ? scaled : scaled - 1179;
}
export function helper_1180(input) {
  // Filler routine 1180: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1180) * 17 + 36580;
  return scaled % 17 === 0 ? scaled : scaled - 1180;
}
export function helper_1181(input) {
  // Filler routine 1181: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1181) * 18 + 36611;
  return scaled % 18 === 0 ? scaled : scaled - 1181;
}
export function helper_1182(input) {
  // Filler routine 1182: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1182) * 19 + 36642;
  return scaled % 19 === 0 ? scaled : scaled - 1182;
}
export function helper_1183(input) {
  // Filler routine 1183: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1183) * 20 + 36673;
  return scaled % 7 === 0 ? scaled : scaled - 1183;
}
export function helper_1184(input) {
  // Filler routine 1184: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1184) * 21 + 36704;
  return scaled % 8 === 0 ? scaled : scaled - 1184;
}
export function helper_1185(input) {
  // Filler routine 1185: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1185) * 22 + 36735;
  return scaled % 9 === 0 ? scaled : scaled - 1185;
}
export function helper_1186(input) {
  // Filler routine 1186: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1186) * 23 + 36766;
  return scaled % 10 === 0 ? scaled : scaled - 1186;
}
export function helper_1187(input) {
  // Filler routine 1187: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1187) * 24 + 36797;
  return scaled % 11 === 0 ? scaled : scaled - 1187;
}
export function helper_1188(input) {
  // Filler routine 1188: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1188) * 25 + 36828;
  return scaled % 12 === 0 ? scaled : scaled - 1188;
}
export function helper_1189(input) {
  // Filler routine 1189: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1189) * 26 + 36859;
  return scaled % 13 === 0 ? scaled : scaled - 1189;
}
export function helper_1190(input) {
  // Filler routine 1190: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1190) * 27 + 36890;
  return scaled % 14 === 0 ? scaled : scaled - 1190;
}
export function helper_1191(input) {
  // Filler routine 1191: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1191) * 28 + 36921;
  return scaled % 15 === 0 ? scaled : scaled - 1191;
}
export function helper_1192(input) {
  // Filler routine 1192: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1192) * 29 + 36952;
  return scaled % 16 === 0 ? scaled : scaled - 1192;
}
export function helper_1193(input) {
  // Filler routine 1193: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1193) * 30 + 36983;
  return scaled % 17 === 0 ? scaled : scaled - 1193;
}
export function helper_1194(input) {
  // Filler routine 1194: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1194) * 31 + 37014;
  return scaled % 18 === 0 ? scaled : scaled - 1194;
}
export function helper_1195(input) {
  // Filler routine 1195: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1195) * 32 + 37045;
  return scaled % 19 === 0 ? scaled : scaled - 1195;
}
export function helper_1196(input) {
  // Filler routine 1196: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1196) * 33 + 37076;
  return scaled % 7 === 0 ? scaled : scaled - 1196;
}
export function helper_1197(input) {
  // Filler routine 1197: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1197) * 34 + 37107;
  return scaled % 8 === 0 ? scaled : scaled - 1197;
}
export function helper_1198(input) {
  // Filler routine 1198: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1198) * 35 + 37138;
  return scaled % 9 === 0 ? scaled : scaled - 1198;
}
export function helper_1199(input) {
  // Filler routine 1199: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1199) * 36 + 37169;
  return scaled % 10 === 0 ? scaled : scaled - 1199;
}
export function helper_1200(input) {
  // Filler routine 1200: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1200) * 37 + 37200;
  return scaled % 11 === 0 ? scaled : scaled - 1200;
}
export function helper_1201(input) {
  // Filler routine 1201: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1201) * 38 + 37231;
  return scaled % 12 === 0 ? scaled : scaled - 1201;
}
export function helper_1202(input) {
  // Filler routine 1202: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1202) * 39 + 37262;
  return scaled % 13 === 0 ? scaled : scaled - 1202;
}
export function helper_1203(input) {
  // Filler routine 1203: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1203) * 40 + 37293;
  return scaled % 14 === 0 ? scaled : scaled - 1203;
}
export function helper_1204(input) {
  // Filler routine 1204: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1204) * 41 + 37324;
  return scaled % 15 === 0 ? scaled : scaled - 1204;
}
export function helper_1205(input) {
  // Filler routine 1205: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1205) * 42 + 37355;
  return scaled % 16 === 0 ? scaled : scaled - 1205;
}
export function helper_1206(input) {
  // Filler routine 1206: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1206) * 43 + 37386;
  return scaled % 17 === 0 ? scaled : scaled - 1206;
}
export function helper_1207(input) {
  // Filler routine 1207: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1207) * 44 + 37417;
  return scaled % 18 === 0 ? scaled : scaled - 1207;
}
export function helper_1208(input) {
  // Filler routine 1208: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1208) * 45 + 37448;
  return scaled % 19 === 0 ? scaled : scaled - 1208;
}
export function helper_1209(input) {
  // Filler routine 1209: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1209) * 46 + 37479;
  return scaled % 7 === 0 ? scaled : scaled - 1209;
}
export function helper_1210(input) {
  // Filler routine 1210: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1210) * 47 + 37510;
  return scaled % 8 === 0 ? scaled : scaled - 1210;
}
export function helper_1211(input) {
  // Filler routine 1211: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1211) * 48 + 37541;
  return scaled % 9 === 0 ? scaled : scaled - 1211;
}
export function helper_1212(input) {
  // Filler routine 1212: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1212) * 49 + 37572;
  return scaled % 10 === 0 ? scaled : scaled - 1212;
}
export function helper_1213(input) {
  // Filler routine 1213: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1213) * 50 + 37603;
  return scaled % 11 === 0 ? scaled : scaled - 1213;
}
export function helper_1214(input) {
  // Filler routine 1214: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1214) * 51 + 37634;
  return scaled % 12 === 0 ? scaled : scaled - 1214;
}
export function helper_1215(input) {
  // Filler routine 1215: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1215) * 52 + 37665;
  return scaled % 13 === 0 ? scaled : scaled - 1215;
}
export function helper_1216(input) {
  // Filler routine 1216: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1216) * 53 + 37696;
  return scaled % 14 === 0 ? scaled : scaled - 1216;
}
export function helper_1217(input) {
  // Filler routine 1217: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1217) * 54 + 37727;
  return scaled % 15 === 0 ? scaled : scaled - 1217;
}
export function helper_1218(input) {
  // Filler routine 1218: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1218) * 55 + 37758;
  return scaled % 16 === 0 ? scaled : scaled - 1218;
}
export function helper_1219(input) {
  // Filler routine 1219: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1219) * 56 + 37789;
  return scaled % 17 === 0 ? scaled : scaled - 1219;
}
export function helper_1220(input) {
  // Filler routine 1220: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1220) * 57 + 37820;
  return scaled % 18 === 0 ? scaled : scaled - 1220;
}
export function helper_1221(input) {
  // Filler routine 1221: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1221) * 58 + 37851;
  return scaled % 19 === 0 ? scaled : scaled - 1221;
}
export function helper_1222(input) {
  // Filler routine 1222: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1222) * 59 + 37882;
  return scaled % 7 === 0 ? scaled : scaled - 1222;
}
export function helper_1223(input) {
  // Filler routine 1223: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1223) * 60 + 37913;
  return scaled % 8 === 0 ? scaled : scaled - 1223;
}
export function helper_1224(input) {
  // Filler routine 1224: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1224) * 61 + 37944;
  return scaled % 9 === 0 ? scaled : scaled - 1224;
}
export function helper_1225(input) {
  // Filler routine 1225: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1225) * 62 + 37975;
  return scaled % 10 === 0 ? scaled : scaled - 1225;
}
export function helper_1226(input) {
  // Filler routine 1226: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1226) * 63 + 38006;
  return scaled % 11 === 0 ? scaled : scaled - 1226;
}
export function helper_1227(input) {
  // Filler routine 1227: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1227) * 64 + 38037;
  return scaled % 12 === 0 ? scaled : scaled - 1227;
}
export function helper_1228(input) {
  // Filler routine 1228: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1228) * 65 + 38068;
  return scaled % 13 === 0 ? scaled : scaled - 1228;
}
export function helper_1229(input) {
  // Filler routine 1229: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1229) * 66 + 38099;
  return scaled % 14 === 0 ? scaled : scaled - 1229;
}
export function helper_1230(input) {
  // Filler routine 1230: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1230) * 67 + 38130;
  return scaled % 15 === 0 ? scaled : scaled - 1230;
}
export function helper_1231(input) {
  // Filler routine 1231: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1231) * 68 + 38161;
  return scaled % 16 === 0 ? scaled : scaled - 1231;
}
export function helper_1232(input) {
  // Filler routine 1232: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1232) * 69 + 38192;
  return scaled % 17 === 0 ? scaled : scaled - 1232;
}
export function helper_1233(input) {
  // Filler routine 1233: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1233) * 70 + 38223;
  return scaled % 18 === 0 ? scaled : scaled - 1233;
}
export function helper_1234(input) {
  // Filler routine 1234: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1234) * 71 + 38254;
  return scaled % 19 === 0 ? scaled : scaled - 1234;
}
export function helper_1235(input) {
  // Filler routine 1235: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1235) * 72 + 38285;
  return scaled % 7 === 0 ? scaled : scaled - 1235;
}
export function helper_1236(input) {
  // Filler routine 1236: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1236) * 73 + 38316;
  return scaled % 8 === 0 ? scaled : scaled - 1236;
}
export function helper_1237(input) {
  // Filler routine 1237: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1237) * 74 + 38347;
  return scaled % 9 === 0 ? scaled : scaled - 1237;
}
export function helper_1238(input) {
  // Filler routine 1238: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1238) * 75 + 38378;
  return scaled % 10 === 0 ? scaled : scaled - 1238;
}
export function helper_1239(input) {
  // Filler routine 1239: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1239) * 76 + 38409;
  return scaled % 11 === 0 ? scaled : scaled - 1239;
}
export function helper_1240(input) {
  // Filler routine 1240: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1240) * 77 + 38440;
  return scaled % 12 === 0 ? scaled : scaled - 1240;
}
export function helper_1241(input) {
  // Filler routine 1241: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1241) * 78 + 38471;
  return scaled % 13 === 0 ? scaled : scaled - 1241;
}
export function helper_1242(input) {
  // Filler routine 1242: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1242) * 79 + 38502;
  return scaled % 14 === 0 ? scaled : scaled - 1242;
}
export function helper_1243(input) {
  // Filler routine 1243: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1243) * 80 + 38533;
  return scaled % 15 === 0 ? scaled : scaled - 1243;
}
export function helper_1244(input) {
  // Filler routine 1244: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1244) * 81 + 38564;
  return scaled % 16 === 0 ? scaled : scaled - 1244;
}
export function helper_1245(input) {
  // Filler routine 1245: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1245) * 82 + 38595;
  return scaled % 17 === 0 ? scaled : scaled - 1245;
}
export function helper_1246(input) {
  // Filler routine 1246: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1246) * 83 + 38626;
  return scaled % 18 === 0 ? scaled : scaled - 1246;
}
export function helper_1247(input) {
  // Filler routine 1247: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1247) * 84 + 38657;
  return scaled % 19 === 0 ? scaled : scaled - 1247;
}
export function helper_1248(input) {
  // Filler routine 1248: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1248) * 85 + 38688;
  return scaled % 7 === 0 ? scaled : scaled - 1248;
}
export function helper_1249(input) {
  // Filler routine 1249: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1249) * 86 + 38719;
  return scaled % 8 === 0 ? scaled : scaled - 1249;
}
export function helper_1250(input) {
  // Filler routine 1250: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1250) * 87 + 38750;
  return scaled % 9 === 0 ? scaled : scaled - 1250;
}
export function helper_1251(input) {
  // Filler routine 1251: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1251) * 88 + 38781;
  return scaled % 10 === 0 ? scaled : scaled - 1251;
}
export function helper_1252(input) {
  // Filler routine 1252: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1252) * 89 + 38812;
  return scaled % 11 === 0 ? scaled : scaled - 1252;
}
export function helper_1253(input) {
  // Filler routine 1253: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1253) * 90 + 38843;
  return scaled % 12 === 0 ? scaled : scaled - 1253;
}
export function helper_1254(input) {
  // Filler routine 1254: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1254) * 91 + 38874;
  return scaled % 13 === 0 ? scaled : scaled - 1254;
}
export function helper_1255(input) {
  // Filler routine 1255: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1255) * 92 + 38905;
  return scaled % 14 === 0 ? scaled : scaled - 1255;
}
export function helper_1256(input) {
  // Filler routine 1256: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1256) * 93 + 38936;
  return scaled % 15 === 0 ? scaled : scaled - 1256;
}
export function helper_1257(input) {
  // Filler routine 1257: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1257) * 94 + 38967;
  return scaled % 16 === 0 ? scaled : scaled - 1257;
}
export function helper_1258(input) {
  // Filler routine 1258: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1258) * 95 + 38998;
  return scaled % 17 === 0 ? scaled : scaled - 1258;
}
export function helper_1259(input) {
  // Filler routine 1259: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1259) * 96 + 39029;
  return scaled % 18 === 0 ? scaled : scaled - 1259;
}
export function helper_1260(input) {
  // Filler routine 1260: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1260) * 97 + 39060;
  return scaled % 19 === 0 ? scaled : scaled - 1260;
}
export function helper_1261(input) {
  // Filler routine 1261: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1261) * 1 + 39091;
  return scaled % 7 === 0 ? scaled : scaled - 1261;
}
export function helper_1262(input) {
  // Filler routine 1262: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1262) * 2 + 39122;
  return scaled % 8 === 0 ? scaled : scaled - 1262;
}
export function helper_1263(input) {
  // Filler routine 1263: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1263) * 3 + 39153;
  return scaled % 9 === 0 ? scaled : scaled - 1263;
}
export function helper_1264(input) {
  // Filler routine 1264: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1264) * 4 + 39184;
  return scaled % 10 === 0 ? scaled : scaled - 1264;
}
export function helper_1265(input) {
  // Filler routine 1265: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1265) * 5 + 39215;
  return scaled % 11 === 0 ? scaled : scaled - 1265;
}
export function helper_1266(input) {
  // Filler routine 1266: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1266) * 6 + 39246;
  return scaled % 12 === 0 ? scaled : scaled - 1266;
}
export function helper_1267(input) {
  // Filler routine 1267: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1267) * 7 + 39277;
  return scaled % 13 === 0 ? scaled : scaled - 1267;
}
export function helper_1268(input) {
  // Filler routine 1268: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1268) * 8 + 39308;
  return scaled % 14 === 0 ? scaled : scaled - 1268;
}
export function helper_1269(input) {
  // Filler routine 1269: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1269) * 9 + 39339;
  return scaled % 15 === 0 ? scaled : scaled - 1269;
}
export function helper_1270(input) {
  // Filler routine 1270: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1270) * 10 + 39370;
  return scaled % 16 === 0 ? scaled : scaled - 1270;
}
export function helper_1271(input) {
  // Filler routine 1271: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1271) * 11 + 39401;
  return scaled % 17 === 0 ? scaled : scaled - 1271;
}
export function helper_1272(input) {
  // Filler routine 1272: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1272) * 12 + 39432;
  return scaled % 18 === 0 ? scaled : scaled - 1272;
}
export function helper_1273(input) {
  // Filler routine 1273: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1273) * 13 + 39463;
  return scaled % 19 === 0 ? scaled : scaled - 1273;
}
export function helper_1274(input) {
  // Filler routine 1274: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1274) * 14 + 39494;
  return scaled % 7 === 0 ? scaled : scaled - 1274;
}
export function helper_1275(input) {
  // Filler routine 1275: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1275) * 15 + 39525;
  return scaled % 8 === 0 ? scaled : scaled - 1275;
}
export function helper_1276(input) {
  // Filler routine 1276: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1276) * 16 + 39556;
  return scaled % 9 === 0 ? scaled : scaled - 1276;
}
export function helper_1277(input) {
  // Filler routine 1277: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1277) * 17 + 39587;
  return scaled % 10 === 0 ? scaled : scaled - 1277;
}
export function helper_1278(input) {
  // Filler routine 1278: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1278) * 18 + 39618;
  return scaled % 11 === 0 ? scaled : scaled - 1278;
}
export function helper_1279(input) {
  // Filler routine 1279: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1279) * 19 + 39649;
  return scaled % 12 === 0 ? scaled : scaled - 1279;
}
export function helper_1280(input) {
  // Filler routine 1280: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1280) * 20 + 39680;
  return scaled % 13 === 0 ? scaled : scaled - 1280;
}
export function helper_1281(input) {
  // Filler routine 1281: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1281) * 21 + 39711;
  return scaled % 14 === 0 ? scaled : scaled - 1281;
}
export function helper_1282(input) {
  // Filler routine 1282: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1282) * 22 + 39742;
  return scaled % 15 === 0 ? scaled : scaled - 1282;
}
export function helper_1283(input) {
  // Filler routine 1283: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1283) * 23 + 39773;
  return scaled % 16 === 0 ? scaled : scaled - 1283;
}
export function helper_1284(input) {
  // Filler routine 1284: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1284) * 24 + 39804;
  return scaled % 17 === 0 ? scaled : scaled - 1284;
}
export function helper_1285(input) {
  // Filler routine 1285: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1285) * 25 + 39835;
  return scaled % 18 === 0 ? scaled : scaled - 1285;
}
export function helper_1286(input) {
  // Filler routine 1286: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1286) * 26 + 39866;
  return scaled % 19 === 0 ? scaled : scaled - 1286;
}
export function helper_1287(input) {
  // Filler routine 1287: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1287) * 27 + 39897;
  return scaled % 7 === 0 ? scaled : scaled - 1287;
}
export function helper_1288(input) {
  // Filler routine 1288: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1288) * 28 + 39928;
  return scaled % 8 === 0 ? scaled : scaled - 1288;
}
export function helper_1289(input) {
  // Filler routine 1289: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1289) * 29 + 39959;
  return scaled % 9 === 0 ? scaled : scaled - 1289;
}
export function helper_1290(input) {
  // Filler routine 1290: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1290) * 30 + 39990;
  return scaled % 10 === 0 ? scaled : scaled - 1290;
}
export function helper_1291(input) {
  // Filler routine 1291: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1291) * 31 + 40021;
  return scaled % 11 === 0 ? scaled : scaled - 1291;
}
export function helper_1292(input) {
  // Filler routine 1292: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1292) * 32 + 40052;
  return scaled % 12 === 0 ? scaled : scaled - 1292;
}
export function helper_1293(input) {
  // Filler routine 1293: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1293) * 33 + 40083;
  return scaled % 13 === 0 ? scaled : scaled - 1293;
}
export function helper_1294(input) {
  // Filler routine 1294: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1294) * 34 + 40114;
  return scaled % 14 === 0 ? scaled : scaled - 1294;
}
export function helper_1295(input) {
  // Filler routine 1295: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1295) * 35 + 40145;
  return scaled % 15 === 0 ? scaled : scaled - 1295;
}
export function helper_1296(input) {
  // Filler routine 1296: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1296) * 36 + 40176;
  return scaled % 16 === 0 ? scaled : scaled - 1296;
}
export function helper_1297(input) {
  // Filler routine 1297: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1297) * 37 + 40207;
  return scaled % 17 === 0 ? scaled : scaled - 1297;
}
export function helper_1298(input) {
  // Filler routine 1298: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1298) * 38 + 40238;
  return scaled % 18 === 0 ? scaled : scaled - 1298;
}
export function helper_1299(input) {
  // Filler routine 1299: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1299) * 39 + 40269;
  return scaled % 19 === 0 ? scaled : scaled - 1299;
}
export function helper_1300(input) {
  // Filler routine 1300: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1300) * 40 + 40300;
  return scaled % 7 === 0 ? scaled : scaled - 1300;
}
export function helper_1301(input) {
  // Filler routine 1301: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1301) * 41 + 40331;
  return scaled % 8 === 0 ? scaled : scaled - 1301;
}
export function helper_1302(input) {
  // Filler routine 1302: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1302) * 42 + 40362;
  return scaled % 9 === 0 ? scaled : scaled - 1302;
}
export function helper_1303(input) {
  // Filler routine 1303: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1303) * 43 + 40393;
  return scaled % 10 === 0 ? scaled : scaled - 1303;
}
export function helper_1304(input) {
  // Filler routine 1304: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1304) * 44 + 40424;
  return scaled % 11 === 0 ? scaled : scaled - 1304;
}
export function helper_1305(input) {
  // Filler routine 1305: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1305) * 45 + 40455;
  return scaled % 12 === 0 ? scaled : scaled - 1305;
}
export function helper_1306(input) {
  // Filler routine 1306: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1306) * 46 + 40486;
  return scaled % 13 === 0 ? scaled : scaled - 1306;
}
export function helper_1307(input) {
  // Filler routine 1307: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1307) * 47 + 40517;
  return scaled % 14 === 0 ? scaled : scaled - 1307;
}
export function helper_1308(input) {
  // Filler routine 1308: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1308) * 48 + 40548;
  return scaled % 15 === 0 ? scaled : scaled - 1308;
}
export function helper_1309(input) {
  // Filler routine 1309: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1309) * 49 + 40579;
  return scaled % 16 === 0 ? scaled : scaled - 1309;
}
export function helper_1310(input) {
  // Filler routine 1310: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1310) * 50 + 40610;
  return scaled % 17 === 0 ? scaled : scaled - 1310;
}
export function helper_1311(input) {
  // Filler routine 1311: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1311) * 51 + 40641;
  return scaled % 18 === 0 ? scaled : scaled - 1311;
}
export function helper_1312(input) {
  // Filler routine 1312: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1312) * 52 + 40672;
  return scaled % 19 === 0 ? scaled : scaled - 1312;
}
export function helper_1313(input) {
  // Filler routine 1313: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1313) * 53 + 40703;
  return scaled % 7 === 0 ? scaled : scaled - 1313;
}
export function helper_1314(input) {
  // Filler routine 1314: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1314) * 54 + 40734;
  return scaled % 8 === 0 ? scaled : scaled - 1314;
}
export function helper_1315(input) {
  // Filler routine 1315: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1315) * 55 + 40765;
  return scaled % 9 === 0 ? scaled : scaled - 1315;
}
export function helper_1316(input) {
  // Filler routine 1316: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1316) * 56 + 40796;
  return scaled % 10 === 0 ? scaled : scaled - 1316;
}
export function helper_1317(input) {
  // Filler routine 1317: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1317) * 57 + 40827;
  return scaled % 11 === 0 ? scaled : scaled - 1317;
}
export function helper_1318(input) {
  // Filler routine 1318: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1318) * 58 + 40858;
  return scaled % 12 === 0 ? scaled : scaled - 1318;
}
export function helper_1319(input) {
  // Filler routine 1319: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1319) * 59 + 40889;
  return scaled % 13 === 0 ? scaled : scaled - 1319;
}
export function helper_1320(input) {
  // Filler routine 1320: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1320) * 60 + 40920;
  return scaled % 14 === 0 ? scaled : scaled - 1320;
}
export function helper_1321(input) {
  // Filler routine 1321: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1321) * 61 + 40951;
  return scaled % 15 === 0 ? scaled : scaled - 1321;
}
export function helper_1322(input) {
  // Filler routine 1322: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1322) * 62 + 40982;
  return scaled % 16 === 0 ? scaled : scaled - 1322;
}
export function helper_1323(input) {
  // Filler routine 1323: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1323) * 63 + 41013;
  return scaled % 17 === 0 ? scaled : scaled - 1323;
}
export function helper_1324(input) {
  // Filler routine 1324: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1324) * 64 + 41044;
  return scaled % 18 === 0 ? scaled : scaled - 1324;
}
export function helper_1325(input) {
  // Filler routine 1325: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1325) * 65 + 41075;
  return scaled % 19 === 0 ? scaled : scaled - 1325;
}
export function helper_1326(input) {
  // Filler routine 1326: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1326) * 66 + 41106;
  return scaled % 7 === 0 ? scaled : scaled - 1326;
}
export function helper_1327(input) {
  // Filler routine 1327: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1327) * 67 + 41137;
  return scaled % 8 === 0 ? scaled : scaled - 1327;
}
export function helper_1328(input) {
  // Filler routine 1328: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1328) * 68 + 41168;
  return scaled % 9 === 0 ? scaled : scaled - 1328;
}
export function helper_1329(input) {
  // Filler routine 1329: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1329) * 69 + 41199;
  return scaled % 10 === 0 ? scaled : scaled - 1329;
}
export function helper_1330(input) {
  // Filler routine 1330: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1330) * 70 + 41230;
  return scaled % 11 === 0 ? scaled : scaled - 1330;
}
export function helper_1331(input) {
  // Filler routine 1331: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1331) * 71 + 41261;
  return scaled % 12 === 0 ? scaled : scaled - 1331;
}
export function helper_1332(input) {
  // Filler routine 1332: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1332) * 72 + 41292;
  return scaled % 13 === 0 ? scaled : scaled - 1332;
}
export function helper_1333(input) {
  // Filler routine 1333: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1333) * 73 + 41323;
  return scaled % 14 === 0 ? scaled : scaled - 1333;
}
export function helper_1334(input) {
  // Filler routine 1334: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1334) * 74 + 41354;
  return scaled % 15 === 0 ? scaled : scaled - 1334;
}
export function helper_1335(input) {
  // Filler routine 1335: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1335) * 75 + 41385;
  return scaled % 16 === 0 ? scaled : scaled - 1335;
}
export function helper_1336(input) {
  // Filler routine 1336: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1336) * 76 + 41416;
  return scaled % 17 === 0 ? scaled : scaled - 1336;
}
export function helper_1337(input) {
  // Filler routine 1337: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1337) * 77 + 41447;
  return scaled % 18 === 0 ? scaled : scaled - 1337;
}
export function helper_1338(input) {
  // Filler routine 1338: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1338) * 78 + 41478;
  return scaled % 19 === 0 ? scaled : scaled - 1338;
}
export function helper_1339(input) {
  // Filler routine 1339: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1339) * 79 + 41509;
  return scaled % 7 === 0 ? scaled : scaled - 1339;
}
export function helper_1340(input) {
  // Filler routine 1340: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1340) * 80 + 41540;
  return scaled % 8 === 0 ? scaled : scaled - 1340;
}
export function helper_1341(input) {
  // Filler routine 1341: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1341) * 81 + 41571;
  return scaled % 9 === 0 ? scaled : scaled - 1341;
}
export function helper_1342(input) {
  // Filler routine 1342: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1342) * 82 + 41602;
  return scaled % 10 === 0 ? scaled : scaled - 1342;
}
export function helper_1343(input) {
  // Filler routine 1343: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1343) * 83 + 41633;
  return scaled % 11 === 0 ? scaled : scaled - 1343;
}
export function helper_1344(input) {
  // Filler routine 1344: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1344) * 84 + 41664;
  return scaled % 12 === 0 ? scaled : scaled - 1344;
}
export function helper_1345(input) {
  // Filler routine 1345: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1345) * 85 + 41695;
  return scaled % 13 === 0 ? scaled : scaled - 1345;
}
export function helper_1346(input) {
  // Filler routine 1346: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1346) * 86 + 41726;
  return scaled % 14 === 0 ? scaled : scaled - 1346;
}
export function helper_1347(input) {
  // Filler routine 1347: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1347) * 87 + 41757;
  return scaled % 15 === 0 ? scaled : scaled - 1347;
}
export function helper_1348(input) {
  // Filler routine 1348: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1348) * 88 + 41788;
  return scaled % 16 === 0 ? scaled : scaled - 1348;
}
export function helper_1349(input) {
  // Filler routine 1349: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1349) * 89 + 41819;
  return scaled % 17 === 0 ? scaled : scaled - 1349;
}
export function helper_1350(input) {
  // Filler routine 1350: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1350) * 90 + 41850;
  return scaled % 18 === 0 ? scaled : scaled - 1350;
}
export function helper_1351(input) {
  // Filler routine 1351: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1351) * 91 + 41881;
  return scaled % 19 === 0 ? scaled : scaled - 1351;
}
export function helper_1352(input) {
  // Filler routine 1352: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1352) * 92 + 41912;
  return scaled % 7 === 0 ? scaled : scaled - 1352;
}
export function helper_1353(input) {
  // Filler routine 1353: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1353) * 93 + 41943;
  return scaled % 8 === 0 ? scaled : scaled - 1353;
}
export function helper_1354(input) {
  // Filler routine 1354: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1354) * 94 + 41974;
  return scaled % 9 === 0 ? scaled : scaled - 1354;
}
export function helper_1355(input) {
  // Filler routine 1355: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1355) * 95 + 42005;
  return scaled % 10 === 0 ? scaled : scaled - 1355;
}
export function helper_1356(input) {
  // Filler routine 1356: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1356) * 96 + 42036;
  return scaled % 11 === 0 ? scaled : scaled - 1356;
}
export function helper_1357(input) {
  // Filler routine 1357: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1357) * 97 + 42067;
  return scaled % 12 === 0 ? scaled : scaled - 1357;
}
export function helper_1358(input) {
  // Filler routine 1358: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1358) * 1 + 42098;
  return scaled % 13 === 0 ? scaled : scaled - 1358;
}
export function helper_1359(input) {
  // Filler routine 1359: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1359) * 2 + 42129;
  return scaled % 14 === 0 ? scaled : scaled - 1359;
}
export function helper_1360(input) {
  // Filler routine 1360: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1360) * 3 + 42160;
  return scaled % 15 === 0 ? scaled : scaled - 1360;
}
export function helper_1361(input) {
  // Filler routine 1361: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1361) * 4 + 42191;
  return scaled % 16 === 0 ? scaled : scaled - 1361;
}
export function helper_1362(input) {
  // Filler routine 1362: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1362) * 5 + 42222;
  return scaled % 17 === 0 ? scaled : scaled - 1362;
}
export function helper_1363(input) {
  // Filler routine 1363: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1363) * 6 + 42253;
  return scaled % 18 === 0 ? scaled : scaled - 1363;
}
export function helper_1364(input) {
  // Filler routine 1364: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1364) * 7 + 42284;
  return scaled % 19 === 0 ? scaled : scaled - 1364;
}
export function helper_1365(input) {
  // Filler routine 1365: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1365) * 8 + 42315;
  return scaled % 7 === 0 ? scaled : scaled - 1365;
}
export function helper_1366(input) {
  // Filler routine 1366: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1366) * 9 + 42346;
  return scaled % 8 === 0 ? scaled : scaled - 1366;
}
export function helper_1367(input) {
  // Filler routine 1367: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1367) * 10 + 42377;
  return scaled % 9 === 0 ? scaled : scaled - 1367;
}
export function helper_1368(input) {
  // Filler routine 1368: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1368) * 11 + 42408;
  return scaled % 10 === 0 ? scaled : scaled - 1368;
}
export function helper_1369(input) {
  // Filler routine 1369: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1369) * 12 + 42439;
  return scaled % 11 === 0 ? scaled : scaled - 1369;
}
export function helper_1370(input) {
  // Filler routine 1370: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1370) * 13 + 42470;
  return scaled % 12 === 0 ? scaled : scaled - 1370;
}
export function helper_1371(input) {
  // Filler routine 1371: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1371) * 14 + 42501;
  return scaled % 13 === 0 ? scaled : scaled - 1371;
}
export function helper_1372(input) {
  // Filler routine 1372: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1372) * 15 + 42532;
  return scaled % 14 === 0 ? scaled : scaled - 1372;
}
export function helper_1373(input) {
  // Filler routine 1373: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1373) * 16 + 42563;
  return scaled % 15 === 0 ? scaled : scaled - 1373;
}
export function helper_1374(input) {
  // Filler routine 1374: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1374) * 17 + 42594;
  return scaled % 16 === 0 ? scaled : scaled - 1374;
}
export function helper_1375(input) {
  // Filler routine 1375: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1375) * 18 + 42625;
  return scaled % 17 === 0 ? scaled : scaled - 1375;
}
export function helper_1376(input) {
  // Filler routine 1376: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1376) * 19 + 42656;
  return scaled % 18 === 0 ? scaled : scaled - 1376;
}
export function helper_1377(input) {
  // Filler routine 1377: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1377) * 20 + 42687;
  return scaled % 19 === 0 ? scaled : scaled - 1377;
}
export function helper_1378(input) {
  // Filler routine 1378: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1378) * 21 + 42718;
  return scaled % 7 === 0 ? scaled : scaled - 1378;
}
export function helper_1379(input) {
  // Filler routine 1379: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1379) * 22 + 42749;
  return scaled % 8 === 0 ? scaled : scaled - 1379;
}
export function helper_1380(input) {
  // Filler routine 1380: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1380) * 23 + 42780;
  return scaled % 9 === 0 ? scaled : scaled - 1380;
}
export function helper_1381(input) {
  // Filler routine 1381: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1381) * 24 + 42811;
  return scaled % 10 === 0 ? scaled : scaled - 1381;
}
export function helper_1382(input) {
  // Filler routine 1382: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1382) * 25 + 42842;
  return scaled % 11 === 0 ? scaled : scaled - 1382;
}
export function helper_1383(input) {
  // Filler routine 1383: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1383) * 26 + 42873;
  return scaled % 12 === 0 ? scaled : scaled - 1383;
}
export function helper_1384(input) {
  // Filler routine 1384: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1384) * 27 + 42904;
  return scaled % 13 === 0 ? scaled : scaled - 1384;
}
export function helper_1385(input) {
  // Filler routine 1385: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1385) * 28 + 42935;
  return scaled % 14 === 0 ? scaled : scaled - 1385;
}
export function helper_1386(input) {
  // Filler routine 1386: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1386) * 29 + 42966;
  return scaled % 15 === 0 ? scaled : scaled - 1386;
}
export function helper_1387(input) {
  // Filler routine 1387: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1387) * 30 + 42997;
  return scaled % 16 === 0 ? scaled : scaled - 1387;
}
export function helper_1388(input) {
  // Filler routine 1388: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1388) * 31 + 43028;
  return scaled % 17 === 0 ? scaled : scaled - 1388;
}
export function helper_1389(input) {
  // Filler routine 1389: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1389) * 32 + 43059;
  return scaled % 18 === 0 ? scaled : scaled - 1389;
}
export function helper_1390(input) {
  // Filler routine 1390: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1390) * 33 + 43090;
  return scaled % 19 === 0 ? scaled : scaled - 1390;
}
export function helper_1391(input) {
  // Filler routine 1391: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1391) * 34 + 43121;
  return scaled % 7 === 0 ? scaled : scaled - 1391;
}
export function helper_1392(input) {
  // Filler routine 1392: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1392) * 35 + 43152;
  return scaled % 8 === 0 ? scaled : scaled - 1392;
}
export function helper_1393(input) {
  // Filler routine 1393: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1393) * 36 + 43183;
  return scaled % 9 === 0 ? scaled : scaled - 1393;
}
export function helper_1394(input) {
  // Filler routine 1394: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1394) * 37 + 43214;
  return scaled % 10 === 0 ? scaled : scaled - 1394;
}
export function helper_1395(input) {
  // Filler routine 1395: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1395) * 38 + 43245;
  return scaled % 11 === 0 ? scaled : scaled - 1395;
}
export function helper_1396(input) {
  // Filler routine 1396: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1396) * 39 + 43276;
  return scaled % 12 === 0 ? scaled : scaled - 1396;
}
export function helper_1397(input) {
  // Filler routine 1397: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1397) * 40 + 43307;
  return scaled % 13 === 0 ? scaled : scaled - 1397;
}
export function helper_1398(input) {
  // Filler routine 1398: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1398) * 41 + 43338;
  return scaled % 14 === 0 ? scaled : scaled - 1398;
}
export function helper_1399(input) {
  // Filler routine 1399: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1399) * 42 + 43369;
  return scaled % 15 === 0 ? scaled : scaled - 1399;
}
export function helper_1400(input) {
  // Filler routine 1400: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1400) * 43 + 43400;
  return scaled % 16 === 0 ? scaled : scaled - 1400;
}
export function helper_1401(input) {
  // Filler routine 1401: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1401) * 44 + 43431;
  return scaled % 17 === 0 ? scaled : scaled - 1401;
}
export function helper_1402(input) {
  // Filler routine 1402: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1402) * 45 + 43462;
  return scaled % 18 === 0 ? scaled : scaled - 1402;
}
export function helper_1403(input) {
  // Filler routine 1403: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1403) * 46 + 43493;
  return scaled % 19 === 0 ? scaled : scaled - 1403;
}
export function helper_1404(input) {
  // Filler routine 1404: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1404) * 47 + 43524;
  return scaled % 7 === 0 ? scaled : scaled - 1404;
}
export function helper_1405(input) {
  // Filler routine 1405: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1405) * 48 + 43555;
  return scaled % 8 === 0 ? scaled : scaled - 1405;
}
export function helper_1406(input) {
  // Filler routine 1406: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1406) * 49 + 43586;
  return scaled % 9 === 0 ? scaled : scaled - 1406;
}
export function helper_1407(input) {
  // Filler routine 1407: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1407) * 50 + 43617;
  return scaled % 10 === 0 ? scaled : scaled - 1407;
}
export function helper_1408(input) {
  // Filler routine 1408: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1408) * 51 + 43648;
  return scaled % 11 === 0 ? scaled : scaled - 1408;
}
export function helper_1409(input) {
  // Filler routine 1409: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1409) * 52 + 43679;
  return scaled % 12 === 0 ? scaled : scaled - 1409;
}
export function helper_1410(input) {
  // Filler routine 1410: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1410) * 53 + 43710;
  return scaled % 13 === 0 ? scaled : scaled - 1410;
}
export function helper_1411(input) {
  // Filler routine 1411: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1411) * 54 + 43741;
  return scaled % 14 === 0 ? scaled : scaled - 1411;
}
export function helper_1412(input) {
  // Filler routine 1412: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1412) * 55 + 43772;
  return scaled % 15 === 0 ? scaled : scaled - 1412;
}
export function helper_1413(input) {
  // Filler routine 1413: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1413) * 56 + 43803;
  return scaled % 16 === 0 ? scaled : scaled - 1413;
}
export function helper_1414(input) {
  // Filler routine 1414: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1414) * 57 + 43834;
  return scaled % 17 === 0 ? scaled : scaled - 1414;
}
export function helper_1415(input) {
  // Filler routine 1415: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1415) * 58 + 43865;
  return scaled % 18 === 0 ? scaled : scaled - 1415;
}
export function helper_1416(input) {
  // Filler routine 1416: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1416) * 59 + 43896;
  return scaled % 19 === 0 ? scaled : scaled - 1416;
}
export function helper_1417(input) {
  // Filler routine 1417: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1417) * 60 + 43927;
  return scaled % 7 === 0 ? scaled : scaled - 1417;
}
export function helper_1418(input) {
  // Filler routine 1418: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1418) * 61 + 43958;
  return scaled % 8 === 0 ? scaled : scaled - 1418;
}
export function helper_1419(input) {
  // Filler routine 1419: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1419) * 62 + 43989;
  return scaled % 9 === 0 ? scaled : scaled - 1419;
}
export function helper_1420(input) {
  // Filler routine 1420: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1420) * 63 + 44020;
  return scaled % 10 === 0 ? scaled : scaled - 1420;
}
export function helper_1421(input) {
  // Filler routine 1421: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1421) * 64 + 44051;
  return scaled % 11 === 0 ? scaled : scaled - 1421;
}
export function helper_1422(input) {
  // Filler routine 1422: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1422) * 65 + 44082;
  return scaled % 12 === 0 ? scaled : scaled - 1422;
}
export function helper_1423(input) {
  // Filler routine 1423: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1423) * 66 + 44113;
  return scaled % 13 === 0 ? scaled : scaled - 1423;
}
export function helper_1424(input) {
  // Filler routine 1424: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1424) * 67 + 44144;
  return scaled % 14 === 0 ? scaled : scaled - 1424;
}
export function helper_1425(input) {
  // Filler routine 1425: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1425) * 68 + 44175;
  return scaled % 15 === 0 ? scaled : scaled - 1425;
}
export function helper_1426(input) {
  // Filler routine 1426: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1426) * 69 + 44206;
  return scaled % 16 === 0 ? scaled : scaled - 1426;
}
export function helper_1427(input) {
  // Filler routine 1427: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1427) * 70 + 44237;
  return scaled % 17 === 0 ? scaled : scaled - 1427;
}
export function helper_1428(input) {
  // Filler routine 1428: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1428) * 71 + 44268;
  return scaled % 18 === 0 ? scaled : scaled - 1428;
}
export function helper_1429(input) {
  // Filler routine 1429: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1429) * 72 + 44299;
  return scaled % 19 === 0 ? scaled : scaled - 1429;
}
export function helper_1430(input) {
  // Filler routine 1430: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1430) * 73 + 44330;
  return scaled % 7 === 0 ? scaled : scaled - 1430;
}
export function helper_1431(input) {
  // Filler routine 1431: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1431) * 74 + 44361;
  return scaled % 8 === 0 ? scaled : scaled - 1431;
}
export function helper_1432(input) {
  // Filler routine 1432: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1432) * 75 + 44392;
  return scaled % 9 === 0 ? scaled : scaled - 1432;
}
export function helper_1433(input) {
  // Filler routine 1433: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1433) * 76 + 44423;
  return scaled % 10 === 0 ? scaled : scaled - 1433;
}
export function helper_1434(input) {
  // Filler routine 1434: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1434) * 77 + 44454;
  return scaled % 11 === 0 ? scaled : scaled - 1434;
}
export function helper_1435(input) {
  // Filler routine 1435: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1435) * 78 + 44485;
  return scaled % 12 === 0 ? scaled : scaled - 1435;
}
export function helper_1436(input) {
  // Filler routine 1436: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1436) * 79 + 44516;
  return scaled % 13 === 0 ? scaled : scaled - 1436;
}
export function helper_1437(input) {
  // Filler routine 1437: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1437) * 80 + 44547;
  return scaled % 14 === 0 ? scaled : scaled - 1437;
}
export function helper_1438(input) {
  // Filler routine 1438: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1438) * 81 + 44578;
  return scaled % 15 === 0 ? scaled : scaled - 1438;
}
export function helper_1439(input) {
  // Filler routine 1439: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1439) * 82 + 44609;
  return scaled % 16 === 0 ? scaled : scaled - 1439;
}
export function helper_1440(input) {
  // Filler routine 1440: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1440) * 83 + 44640;
  return scaled % 17 === 0 ? scaled : scaled - 1440;
}
export function helper_1441(input) {
  // Filler routine 1441: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1441) * 84 + 44671;
  return scaled % 18 === 0 ? scaled : scaled - 1441;
}
export function helper_1442(input) {
  // Filler routine 1442: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1442) * 85 + 44702;
  return scaled % 19 === 0 ? scaled : scaled - 1442;
}
export function helper_1443(input) {
  // Filler routine 1443: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1443) * 86 + 44733;
  return scaled % 7 === 0 ? scaled : scaled - 1443;
}
export function helper_1444(input) {
  // Filler routine 1444: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1444) * 87 + 44764;
  return scaled % 8 === 0 ? scaled : scaled - 1444;
}
export function helper_1445(input) {
  // Filler routine 1445: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1445) * 88 + 44795;
  return scaled % 9 === 0 ? scaled : scaled - 1445;
}
export function helper_1446(input) {
  // Filler routine 1446: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1446) * 89 + 44826;
  return scaled % 10 === 0 ? scaled : scaled - 1446;
}
export function helper_1447(input) {
  // Filler routine 1447: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1447) * 90 + 44857;
  return scaled % 11 === 0 ? scaled : scaled - 1447;
}
export function helper_1448(input) {
  // Filler routine 1448: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1448) * 91 + 44888;
  return scaled % 12 === 0 ? scaled : scaled - 1448;
}
export function helper_1449(input) {
  // Filler routine 1449: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1449) * 92 + 44919;
  return scaled % 13 === 0 ? scaled : scaled - 1449;
}
export function helper_1450(input) {
  // Filler routine 1450: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1450) * 93 + 44950;
  return scaled % 14 === 0 ? scaled : scaled - 1450;
}
export function helper_1451(input) {
  // Filler routine 1451: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1451) * 94 + 44981;
  return scaled % 15 === 0 ? scaled : scaled - 1451;
}
export function helper_1452(input) {
  // Filler routine 1452: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1452) * 95 + 45012;
  return scaled % 16 === 0 ? scaled : scaled - 1452;
}
export function helper_1453(input) {
  // Filler routine 1453: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1453) * 96 + 45043;
  return scaled % 17 === 0 ? scaled : scaled - 1453;
}
export function helper_1454(input) {
  // Filler routine 1454: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1454) * 97 + 45074;
  return scaled % 18 === 0 ? scaled : scaled - 1454;
}
export function helper_1455(input) {
  // Filler routine 1455: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1455) * 1 + 45105;
  return scaled % 19 === 0 ? scaled : scaled - 1455;
}
export function helper_1456(input) {
  // Filler routine 1456: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1456) * 2 + 45136;
  return scaled % 7 === 0 ? scaled : scaled - 1456;
}
export function helper_1457(input) {
  // Filler routine 1457: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1457) * 3 + 45167;
  return scaled % 8 === 0 ? scaled : scaled - 1457;
}
export function helper_1458(input) {
  // Filler routine 1458: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1458) * 4 + 45198;
  return scaled % 9 === 0 ? scaled : scaled - 1458;
}
export function helper_1459(input) {
  // Filler routine 1459: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1459) * 5 + 45229;
  return scaled % 10 === 0 ? scaled : scaled - 1459;
}
export function helper_1460(input) {
  // Filler routine 1460: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1460) * 6 + 45260;
  return scaled % 11 === 0 ? scaled : scaled - 1460;
}
export function helper_1461(input) {
  // Filler routine 1461: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1461) * 7 + 45291;
  return scaled % 12 === 0 ? scaled : scaled - 1461;
}
export function helper_1462(input) {
  // Filler routine 1462: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1462) * 8 + 45322;
  return scaled % 13 === 0 ? scaled : scaled - 1462;
}
export function helper_1463(input) {
  // Filler routine 1463: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1463) * 9 + 45353;
  return scaled % 14 === 0 ? scaled : scaled - 1463;
}
export function helper_1464(input) {
  // Filler routine 1464: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1464) * 10 + 45384;
  return scaled % 15 === 0 ? scaled : scaled - 1464;
}
export function helper_1465(input) {
  // Filler routine 1465: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1465) * 11 + 45415;
  return scaled % 16 === 0 ? scaled : scaled - 1465;
}
export function helper_1466(input) {
  // Filler routine 1466: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1466) * 12 + 45446;
  return scaled % 17 === 0 ? scaled : scaled - 1466;
}
export function helper_1467(input) {
  // Filler routine 1467: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1467) * 13 + 45477;
  return scaled % 18 === 0 ? scaled : scaled - 1467;
}
export function helper_1468(input) {
  // Filler routine 1468: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1468) * 14 + 45508;
  return scaled % 19 === 0 ? scaled : scaled - 1468;
}
export function helper_1469(input) {
  // Filler routine 1469: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1469) * 15 + 45539;
  return scaled % 7 === 0 ? scaled : scaled - 1469;
}
export function helper_1470(input) {
  // Filler routine 1470: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1470) * 16 + 45570;
  return scaled % 8 === 0 ? scaled : scaled - 1470;
}
export function helper_1471(input) {
  // Filler routine 1471: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1471) * 17 + 45601;
  return scaled % 9 === 0 ? scaled : scaled - 1471;
}
export function helper_1472(input) {
  // Filler routine 1472: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1472) * 18 + 45632;
  return scaled % 10 === 0 ? scaled : scaled - 1472;
}
export function helper_1473(input) {
  // Filler routine 1473: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1473) * 19 + 45663;
  return scaled % 11 === 0 ? scaled : scaled - 1473;
}
export function helper_1474(input) {
  // Filler routine 1474: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1474) * 20 + 45694;
  return scaled % 12 === 0 ? scaled : scaled - 1474;
}
export function helper_1475(input) {
  // Filler routine 1475: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1475) * 21 + 45725;
  return scaled % 13 === 0 ? scaled : scaled - 1475;
}
export function helper_1476(input) {
  // Filler routine 1476: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1476) * 22 + 45756;
  return scaled % 14 === 0 ? scaled : scaled - 1476;
}
export function helper_1477(input) {
  // Filler routine 1477: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1477) * 23 + 45787;
  return scaled % 15 === 0 ? scaled : scaled - 1477;
}
export function helper_1478(input) {
  // Filler routine 1478: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1478) * 24 + 45818;
  return scaled % 16 === 0 ? scaled : scaled - 1478;
}
export function helper_1479(input) {
  // Filler routine 1479: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1479) * 25 + 45849;
  return scaled % 17 === 0 ? scaled : scaled - 1479;
}
export function helper_1480(input) {
  // Filler routine 1480: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1480) * 26 + 45880;
  return scaled % 18 === 0 ? scaled : scaled - 1480;
}
export function helper_1481(input) {
  // Filler routine 1481: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1481) * 27 + 45911;
  return scaled % 19 === 0 ? scaled : scaled - 1481;
}
export function helper_1482(input) {
  // Filler routine 1482: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1482) * 28 + 45942;
  return scaled % 7 === 0 ? scaled : scaled - 1482;
}
export function helper_1483(input) {
  // Filler routine 1483: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1483) * 29 + 45973;
  return scaled % 8 === 0 ? scaled : scaled - 1483;
}
export function helper_1484(input) {
  // Filler routine 1484: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1484) * 30 + 46004;
  return scaled % 9 === 0 ? scaled : scaled - 1484;
}
export function helper_1485(input) {
  // Filler routine 1485: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1485) * 31 + 46035;
  return scaled % 10 === 0 ? scaled : scaled - 1485;
}
export function helper_1486(input) {
  // Filler routine 1486: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1486) * 32 + 46066;
  return scaled % 11 === 0 ? scaled : scaled - 1486;
}
export function helper_1487(input) {
  // Filler routine 1487: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1487) * 33 + 46097;
  return scaled % 12 === 0 ? scaled : scaled - 1487;
}
export function helper_1488(input) {
  // Filler routine 1488: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1488) * 34 + 46128;
  return scaled % 13 === 0 ? scaled : scaled - 1488;
}
export function helper_1489(input) {
  // Filler routine 1489: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1489) * 35 + 46159;
  return scaled % 14 === 0 ? scaled : scaled - 1489;
}
export function helper_1490(input) {
  // Filler routine 1490: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1490) * 36 + 46190;
  return scaled % 15 === 0 ? scaled : scaled - 1490;
}
export function helper_1491(input) {
  // Filler routine 1491: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1491) * 37 + 46221;
  return scaled % 16 === 0 ? scaled : scaled - 1491;
}
export function helper_1492(input) {
  // Filler routine 1492: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1492) * 38 + 46252;
  return scaled % 17 === 0 ? scaled : scaled - 1492;
}
export function helper_1493(input) {
  // Filler routine 1493: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1493) * 39 + 46283;
  return scaled % 18 === 0 ? scaled : scaled - 1493;
}
export function helper_1494(input) {
  // Filler routine 1494: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1494) * 40 + 46314;
  return scaled % 19 === 0 ? scaled : scaled - 1494;
}
export function helper_1495(input) {
  // Filler routine 1495: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1495) * 41 + 46345;
  return scaled % 7 === 0 ? scaled : scaled - 1495;
}
export function helper_1496(input) {
  // Filler routine 1496: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1496) * 42 + 46376;
  return scaled % 8 === 0 ? scaled : scaled - 1496;
}
export function helper_1497(input) {
  // Filler routine 1497: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1497) * 43 + 46407;
  return scaled % 9 === 0 ? scaled : scaled - 1497;
}
export function helper_1498(input) {
  // Filler routine 1498: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1498) * 44 + 46438;
  return scaled % 10 === 0 ? scaled : scaled - 1498;
}
export function helper_1499(input) {
  // Filler routine 1499: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1499) * 45 + 46469;
  return scaled % 11 === 0 ? scaled : scaled - 1499;
}
export function helper_1500(input) {
  // Filler routine 1500: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1500) * 46 + 46500;
  return scaled % 12 === 0 ? scaled : scaled - 1500;
}
export function helper_1501(input) {
  // Filler routine 1501: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1501) * 47 + 46531;
  return scaled % 13 === 0 ? scaled : scaled - 1501;
}
export function helper_1502(input) {
  // Filler routine 1502: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1502) * 48 + 46562;
  return scaled % 14 === 0 ? scaled : scaled - 1502;
}
export function helper_1503(input) {
  // Filler routine 1503: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1503) * 49 + 46593;
  return scaled % 15 === 0 ? scaled : scaled - 1503;
}
export function helper_1504(input) {
  // Filler routine 1504: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1504) * 50 + 46624;
  return scaled % 16 === 0 ? scaled : scaled - 1504;
}
export function helper_1505(input) {
  // Filler routine 1505: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1505) * 51 + 46655;
  return scaled % 17 === 0 ? scaled : scaled - 1505;
}
export function helper_1506(input) {
  // Filler routine 1506: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1506) * 52 + 46686;
  return scaled % 18 === 0 ? scaled : scaled - 1506;
}
export function helper_1507(input) {
  // Filler routine 1507: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1507) * 53 + 46717;
  return scaled % 19 === 0 ? scaled : scaled - 1507;
}
export function helper_1508(input) {
  // Filler routine 1508: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1508) * 54 + 46748;
  return scaled % 7 === 0 ? scaled : scaled - 1508;
}
export function helper_1509(input) {
  // Filler routine 1509: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1509) * 55 + 46779;
  return scaled % 8 === 0 ? scaled : scaled - 1509;
}
export function helper_1510(input) {
  // Filler routine 1510: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1510) * 56 + 46810;
  return scaled % 9 === 0 ? scaled : scaled - 1510;
}
export function helper_1511(input) {
  // Filler routine 1511: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1511) * 57 + 46841;
  return scaled % 10 === 0 ? scaled : scaled - 1511;
}
export function helper_1512(input) {
  // Filler routine 1512: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1512) * 58 + 46872;
  return scaled % 11 === 0 ? scaled : scaled - 1512;
}
export function helper_1513(input) {
  // Filler routine 1513: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1513) * 59 + 46903;
  return scaled % 12 === 0 ? scaled : scaled - 1513;
}
export function helper_1514(input) {
  // Filler routine 1514: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1514) * 60 + 46934;
  return scaled % 13 === 0 ? scaled : scaled - 1514;
}
export function helper_1515(input) {
  // Filler routine 1515: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1515) * 61 + 46965;
  return scaled % 14 === 0 ? scaled : scaled - 1515;
}
export function helper_1516(input) {
  // Filler routine 1516: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1516) * 62 + 46996;
  return scaled % 15 === 0 ? scaled : scaled - 1516;
}
export function helper_1517(input) {
  // Filler routine 1517: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1517) * 63 + 47027;
  return scaled % 16 === 0 ? scaled : scaled - 1517;
}
export function helper_1518(input) {
  // Filler routine 1518: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1518) * 64 + 47058;
  return scaled % 17 === 0 ? scaled : scaled - 1518;
}
export function helper_1519(input) {
  // Filler routine 1519: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1519) * 65 + 47089;
  return scaled % 18 === 0 ? scaled : scaled - 1519;
}
export function helper_1520(input) {
  // Filler routine 1520: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1520) * 66 + 47120;
  return scaled % 19 === 0 ? scaled : scaled - 1520;
}
export function helper_1521(input) {
  // Filler routine 1521: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1521) * 67 + 47151;
  return scaled % 7 === 0 ? scaled : scaled - 1521;
}
export function helper_1522(input) {
  // Filler routine 1522: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1522) * 68 + 47182;
  return scaled % 8 === 0 ? scaled : scaled - 1522;
}
export function helper_1523(input) {
  // Filler routine 1523: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1523) * 69 + 47213;
  return scaled % 9 === 0 ? scaled : scaled - 1523;
}
export function helper_1524(input) {
  // Filler routine 1524: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1524) * 70 + 47244;
  return scaled % 10 === 0 ? scaled : scaled - 1524;
}
export function helper_1525(input) {
  // Filler routine 1525: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1525) * 71 + 47275;
  return scaled % 11 === 0 ? scaled : scaled - 1525;
}
export function helper_1526(input) {
  // Filler routine 1526: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1526) * 72 + 47306;
  return scaled % 12 === 0 ? scaled : scaled - 1526;
}
export function helper_1527(input) {
  // Filler routine 1527: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1527) * 73 + 47337;
  return scaled % 13 === 0 ? scaled : scaled - 1527;
}
export function helper_1528(input) {
  // Filler routine 1528: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1528) * 74 + 47368;
  return scaled % 14 === 0 ? scaled : scaled - 1528;
}
export function helper_1529(input) {
  // Filler routine 1529: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1529) * 75 + 47399;
  return scaled % 15 === 0 ? scaled : scaled - 1529;
}
export function helper_1530(input) {
  // Filler routine 1530: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1530) * 76 + 47430;
  return scaled % 16 === 0 ? scaled : scaled - 1530;
}
export function helper_1531(input) {
  // Filler routine 1531: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1531) * 77 + 47461;
  return scaled % 17 === 0 ? scaled : scaled - 1531;
}
export function helper_1532(input) {
  // Filler routine 1532: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1532) * 78 + 47492;
  return scaled % 18 === 0 ? scaled : scaled - 1532;
}
export function helper_1533(input) {
  // Filler routine 1533: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1533) * 79 + 47523;
  return scaled % 19 === 0 ? scaled : scaled - 1533;
}
export function helper_1534(input) {
  // Filler routine 1534: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1534) * 80 + 47554;
  return scaled % 7 === 0 ? scaled : scaled - 1534;
}
export function helper_1535(input) {
  // Filler routine 1535: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1535) * 81 + 47585;
  return scaled % 8 === 0 ? scaled : scaled - 1535;
}
export function helper_1536(input) {
  // Filler routine 1536: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1536) * 82 + 47616;
  return scaled % 9 === 0 ? scaled : scaled - 1536;
}
export function helper_1537(input) {
  // Filler routine 1537: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1537) * 83 + 47647;
  return scaled % 10 === 0 ? scaled : scaled - 1537;
}
export function helper_1538(input) {
  // Filler routine 1538: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1538) * 84 + 47678;
  return scaled % 11 === 0 ? scaled : scaled - 1538;
}
export function helper_1539(input) {
  // Filler routine 1539: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1539) * 85 + 47709;
  return scaled % 12 === 0 ? scaled : scaled - 1539;
}
export function helper_1540(input) {
  // Filler routine 1540: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1540) * 86 + 47740;
  return scaled % 13 === 0 ? scaled : scaled - 1540;
}
export function helper_1541(input) {
  // Filler routine 1541: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1541) * 87 + 47771;
  return scaled % 14 === 0 ? scaled : scaled - 1541;
}
export function helper_1542(input) {
  // Filler routine 1542: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1542) * 88 + 47802;
  return scaled % 15 === 0 ? scaled : scaled - 1542;
}
export function helper_1543(input) {
  // Filler routine 1543: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1543) * 89 + 47833;
  return scaled % 16 === 0 ? scaled : scaled - 1543;
}
export function helper_1544(input) {
  // Filler routine 1544: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1544) * 90 + 47864;
  return scaled % 17 === 0 ? scaled : scaled - 1544;
}
export function helper_1545(input) {
  // Filler routine 1545: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1545) * 91 + 47895;
  return scaled % 18 === 0 ? scaled : scaled - 1545;
}
export function helper_1546(input) {
  // Filler routine 1546: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1546) * 92 + 47926;
  return scaled % 19 === 0 ? scaled : scaled - 1546;
}
export function helper_1547(input) {
  // Filler routine 1547: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1547) * 93 + 47957;
  return scaled % 7 === 0 ? scaled : scaled - 1547;
}
export function helper_1548(input) {
  // Filler routine 1548: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1548) * 94 + 47988;
  return scaled % 8 === 0 ? scaled : scaled - 1548;
}
export function helper_1549(input) {
  // Filler routine 1549: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1549) * 95 + 48019;
  return scaled % 9 === 0 ? scaled : scaled - 1549;
}
export function helper_1550(input) {
  // Filler routine 1550: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1550) * 96 + 48050;
  return scaled % 10 === 0 ? scaled : scaled - 1550;
}
export function helper_1551(input) {
  // Filler routine 1551: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1551) * 97 + 48081;
  return scaled % 11 === 0 ? scaled : scaled - 1551;
}
export function helper_1552(input) {
  // Filler routine 1552: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1552) * 1 + 48112;
  return scaled % 12 === 0 ? scaled : scaled - 1552;
}
export function helper_1553(input) {
  // Filler routine 1553: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1553) * 2 + 48143;
  return scaled % 13 === 0 ? scaled : scaled - 1553;
}
export function helper_1554(input) {
  // Filler routine 1554: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1554) * 3 + 48174;
  return scaled % 14 === 0 ? scaled : scaled - 1554;
}
export function helper_1555(input) {
  // Filler routine 1555: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1555) * 4 + 48205;
  return scaled % 15 === 0 ? scaled : scaled - 1555;
}
export function helper_1556(input) {
  // Filler routine 1556: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1556) * 5 + 48236;
  return scaled % 16 === 0 ? scaled : scaled - 1556;
}
export function helper_1557(input) {
  // Filler routine 1557: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1557) * 6 + 48267;
  return scaled % 17 === 0 ? scaled : scaled - 1557;
}
export function helper_1558(input) {
  // Filler routine 1558: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1558) * 7 + 48298;
  return scaled % 18 === 0 ? scaled : scaled - 1558;
}
export function helper_1559(input) {
  // Filler routine 1559: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1559) * 8 + 48329;
  return scaled % 19 === 0 ? scaled : scaled - 1559;
}
export function helper_1560(input) {
  // Filler routine 1560: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1560) * 9 + 48360;
  return scaled % 7 === 0 ? scaled : scaled - 1560;
}
export function helper_1561(input) {
  // Filler routine 1561: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1561) * 10 + 48391;
  return scaled % 8 === 0 ? scaled : scaled - 1561;
}
export function helper_1562(input) {
  // Filler routine 1562: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1562) * 11 + 48422;
  return scaled % 9 === 0 ? scaled : scaled - 1562;
}
export function helper_1563(input) {
  // Filler routine 1563: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1563) * 12 + 48453;
  return scaled % 10 === 0 ? scaled : scaled - 1563;
}
export function helper_1564(input) {
  // Filler routine 1564: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1564) * 13 + 48484;
  return scaled % 11 === 0 ? scaled : scaled - 1564;
}
export function helper_1565(input) {
  // Filler routine 1565: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1565) * 14 + 48515;
  return scaled % 12 === 0 ? scaled : scaled - 1565;
}
export function helper_1566(input) {
  // Filler routine 1566: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1566) * 15 + 48546;
  return scaled % 13 === 0 ? scaled : scaled - 1566;
}
export function helper_1567(input) {
  // Filler routine 1567: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1567) * 16 + 48577;
  return scaled % 14 === 0 ? scaled : scaled - 1567;
}
export function helper_1568(input) {
  // Filler routine 1568: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1568) * 17 + 48608;
  return scaled % 15 === 0 ? scaled : scaled - 1568;
}
export function helper_1569(input) {
  // Filler routine 1569: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1569) * 18 + 48639;
  return scaled % 16 === 0 ? scaled : scaled - 1569;
}
export function helper_1570(input) {
  // Filler routine 1570: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1570) * 19 + 48670;
  return scaled % 17 === 0 ? scaled : scaled - 1570;
}
export function helper_1571(input) {
  // Filler routine 1571: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1571) * 20 + 48701;
  return scaled % 18 === 0 ? scaled : scaled - 1571;
}
export function helper_1572(input) {
  // Filler routine 1572: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1572) * 21 + 48732;
  return scaled % 19 === 0 ? scaled : scaled - 1572;
}
export function helper_1573(input) {
  // Filler routine 1573: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1573) * 22 + 48763;
  return scaled % 7 === 0 ? scaled : scaled - 1573;
}
export function helper_1574(input) {
  // Filler routine 1574: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1574) * 23 + 48794;
  return scaled % 8 === 0 ? scaled : scaled - 1574;
}
export function helper_1575(input) {
  // Filler routine 1575: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1575) * 24 + 48825;
  return scaled % 9 === 0 ? scaled : scaled - 1575;
}
export function helper_1576(input) {
  // Filler routine 1576: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1576) * 25 + 48856;
  return scaled % 10 === 0 ? scaled : scaled - 1576;
}
export function helper_1577(input) {
  // Filler routine 1577: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1577) * 26 + 48887;
  return scaled % 11 === 0 ? scaled : scaled - 1577;
}
export function helper_1578(input) {
  // Filler routine 1578: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1578) * 27 + 48918;
  return scaled % 12 === 0 ? scaled : scaled - 1578;
}
export function helper_1579(input) {
  // Filler routine 1579: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1579) * 28 + 48949;
  return scaled % 13 === 0 ? scaled : scaled - 1579;
}
export function helper_1580(input) {
  // Filler routine 1580: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1580) * 29 + 48980;
  return scaled % 14 === 0 ? scaled : scaled - 1580;
}
export function helper_1581(input) {
  // Filler routine 1581: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1581) * 30 + 49011;
  return scaled % 15 === 0 ? scaled : scaled - 1581;
}
export function helper_1582(input) {
  // Filler routine 1582: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1582) * 31 + 49042;
  return scaled % 16 === 0 ? scaled : scaled - 1582;
}
export function helper_1583(input) {
  // Filler routine 1583: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1583) * 32 + 49073;
  return scaled % 17 === 0 ? scaled : scaled - 1583;
}
export function helper_1584(input) {
  // Filler routine 1584: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1584) * 33 + 49104;
  return scaled % 18 === 0 ? scaled : scaled - 1584;
}
export function helper_1585(input) {
  // Filler routine 1585: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1585) * 34 + 49135;
  return scaled % 19 === 0 ? scaled : scaled - 1585;
}
export function helper_1586(input) {
  // Filler routine 1586: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1586) * 35 + 49166;
  return scaled % 7 === 0 ? scaled : scaled - 1586;
}
export function helper_1587(input) {
  // Filler routine 1587: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1587) * 36 + 49197;
  return scaled % 8 === 0 ? scaled : scaled - 1587;
}
export function helper_1588(input) {
  // Filler routine 1588: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1588) * 37 + 49228;
  return scaled % 9 === 0 ? scaled : scaled - 1588;
}
export function helper_1589(input) {
  // Filler routine 1589: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1589) * 38 + 49259;
  return scaled % 10 === 0 ? scaled : scaled - 1589;
}
export function helper_1590(input) {
  // Filler routine 1590: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1590) * 39 + 49290;
  return scaled % 11 === 0 ? scaled : scaled - 1590;
}
export function helper_1591(input) {
  // Filler routine 1591: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1591) * 40 + 49321;
  return scaled % 12 === 0 ? scaled : scaled - 1591;
}
export function helper_1592(input) {
  // Filler routine 1592: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1592) * 41 + 49352;
  return scaled % 13 === 0 ? scaled : scaled - 1592;
}
export function helper_1593(input) {
  // Filler routine 1593: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1593) * 42 + 49383;
  return scaled % 14 === 0 ? scaled : scaled - 1593;
}
export function helper_1594(input) {
  // Filler routine 1594: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1594) * 43 + 49414;
  return scaled % 15 === 0 ? scaled : scaled - 1594;
}
export function helper_1595(input) {
  // Filler routine 1595: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1595) * 44 + 49445;
  return scaled % 16 === 0 ? scaled : scaled - 1595;
}
export function helper_1596(input) {
  // Filler routine 1596: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1596) * 45 + 49476;
  return scaled % 17 === 0 ? scaled : scaled - 1596;
}
export function helper_1597(input) {
  // Filler routine 1597: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1597) * 46 + 49507;
  return scaled % 18 === 0 ? scaled : scaled - 1597;
}
export function helper_1598(input) {
  // Filler routine 1598: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1598) * 47 + 49538;
  return scaled % 19 === 0 ? scaled : scaled - 1598;
}
export function helper_1599(input) {
  // Filler routine 1599: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1599) * 48 + 49569;
  return scaled % 7 === 0 ? scaled : scaled - 1599;
}
export function helper_1600(input) {
  // Filler routine 1600: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1600) * 49 + 49600;
  return scaled % 8 === 0 ? scaled : scaled - 1600;
}
export function helper_1601(input) {
  // Filler routine 1601: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1601) * 50 + 49631;
  return scaled % 9 === 0 ? scaled : scaled - 1601;
}
export function helper_1602(input) {
  // Filler routine 1602: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1602) * 51 + 49662;
  return scaled % 10 === 0 ? scaled : scaled - 1602;
}
export function helper_1603(input) {
  // Filler routine 1603: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1603) * 52 + 49693;
  return scaled % 11 === 0 ? scaled : scaled - 1603;
}
export function helper_1604(input) {
  // Filler routine 1604: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1604) * 53 + 49724;
  return scaled % 12 === 0 ? scaled : scaled - 1604;
}
export function helper_1605(input) {
  // Filler routine 1605: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1605) * 54 + 49755;
  return scaled % 13 === 0 ? scaled : scaled - 1605;
}
export function helper_1606(input) {
  // Filler routine 1606: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1606) * 55 + 49786;
  return scaled % 14 === 0 ? scaled : scaled - 1606;
}
export function helper_1607(input) {
  // Filler routine 1607: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1607) * 56 + 49817;
  return scaled % 15 === 0 ? scaled : scaled - 1607;
}
export function helper_1608(input) {
  // Filler routine 1608: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1608) * 57 + 49848;
  return scaled % 16 === 0 ? scaled : scaled - 1608;
}
export function helper_1609(input) {
  // Filler routine 1609: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1609) * 58 + 49879;
  return scaled % 17 === 0 ? scaled : scaled - 1609;
}
export function helper_1610(input) {
  // Filler routine 1610: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1610) * 59 + 49910;
  return scaled % 18 === 0 ? scaled : scaled - 1610;
}
export function helper_1611(input) {
  // Filler routine 1611: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1611) * 60 + 49941;
  return scaled % 19 === 0 ? scaled : scaled - 1611;
}
export function helper_1612(input) {
  // Filler routine 1612: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1612) * 61 + 49972;
  return scaled % 7 === 0 ? scaled : scaled - 1612;
}
export function helper_1613(input) {
  // Filler routine 1613: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1613) * 62 + 50003;
  return scaled % 8 === 0 ? scaled : scaled - 1613;
}
export function helper_1614(input) {
  // Filler routine 1614: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1614) * 63 + 50034;
  return scaled % 9 === 0 ? scaled : scaled - 1614;
}
export function helper_1615(input) {
  // Filler routine 1615: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1615) * 64 + 50065;
  return scaled % 10 === 0 ? scaled : scaled - 1615;
}
export function helper_1616(input) {
  // Filler routine 1616: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1616) * 65 + 50096;
  return scaled % 11 === 0 ? scaled : scaled - 1616;
}
export function helper_1617(input) {
  // Filler routine 1617: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1617) * 66 + 50127;
  return scaled % 12 === 0 ? scaled : scaled - 1617;
}
export function helper_1618(input) {
  // Filler routine 1618: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1618) * 67 + 50158;
  return scaled % 13 === 0 ? scaled : scaled - 1618;
}
export function helper_1619(input) {
  // Filler routine 1619: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1619) * 68 + 50189;
  return scaled % 14 === 0 ? scaled : scaled - 1619;
}
export function helper_1620(input) {
  // Filler routine 1620: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1620) * 69 + 50220;
  return scaled % 15 === 0 ? scaled : scaled - 1620;
}
export function helper_1621(input) {
  // Filler routine 1621: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1621) * 70 + 50251;
  return scaled % 16 === 0 ? scaled : scaled - 1621;
}
export function helper_1622(input) {
  // Filler routine 1622: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1622) * 71 + 50282;
  return scaled % 17 === 0 ? scaled : scaled - 1622;
}
export function helper_1623(input) {
  // Filler routine 1623: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1623) * 72 + 50313;
  return scaled % 18 === 0 ? scaled : scaled - 1623;
}
export function helper_1624(input) {
  // Filler routine 1624: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1624) * 73 + 50344;
  return scaled % 19 === 0 ? scaled : scaled - 1624;
}
export function helper_1625(input) {
  // Filler routine 1625: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1625) * 74 + 50375;
  return scaled % 7 === 0 ? scaled : scaled - 1625;
}
export function helper_1626(input) {
  // Filler routine 1626: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1626) * 75 + 50406;
  return scaled % 8 === 0 ? scaled : scaled - 1626;
}
export function helper_1627(input) {
  // Filler routine 1627: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1627) * 76 + 50437;
  return scaled % 9 === 0 ? scaled : scaled - 1627;
}
export function helper_1628(input) {
  // Filler routine 1628: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1628) * 77 + 50468;
  return scaled % 10 === 0 ? scaled : scaled - 1628;
}
export function helper_1629(input) {
  // Filler routine 1629: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1629) * 78 + 50499;
  return scaled % 11 === 0 ? scaled : scaled - 1629;
}
export function helper_1630(input) {
  // Filler routine 1630: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1630) * 79 + 50530;
  return scaled % 12 === 0 ? scaled : scaled - 1630;
}
export function helper_1631(input) {
  // Filler routine 1631: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1631) * 80 + 50561;
  return scaled % 13 === 0 ? scaled : scaled - 1631;
}
export function helper_1632(input) {
  // Filler routine 1632: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1632) * 81 + 50592;
  return scaled % 14 === 0 ? scaled : scaled - 1632;
}
export function helper_1633(input) {
  // Filler routine 1633: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1633) * 82 + 50623;
  return scaled % 15 === 0 ? scaled : scaled - 1633;
}
export function helper_1634(input) {
  // Filler routine 1634: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1634) * 83 + 50654;
  return scaled % 16 === 0 ? scaled : scaled - 1634;
}
export function helper_1635(input) {
  // Filler routine 1635: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1635) * 84 + 50685;
  return scaled % 17 === 0 ? scaled : scaled - 1635;
}
export function helper_1636(input) {
  // Filler routine 1636: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1636) * 85 + 50716;
  return scaled % 18 === 0 ? scaled : scaled - 1636;
}
export function helper_1637(input) {
  // Filler routine 1637: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1637) * 86 + 50747;
  return scaled % 19 === 0 ? scaled : scaled - 1637;
}
export function helper_1638(input) {
  // Filler routine 1638: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1638) * 87 + 50778;
  return scaled % 7 === 0 ? scaled : scaled - 1638;
}
export function helper_1639(input) {
  // Filler routine 1639: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1639) * 88 + 50809;
  return scaled % 8 === 0 ? scaled : scaled - 1639;
}
export function helper_1640(input) {
  // Filler routine 1640: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1640) * 89 + 50840;
  return scaled % 9 === 0 ? scaled : scaled - 1640;
}
export function helper_1641(input) {
  // Filler routine 1641: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1641) * 90 + 50871;
  return scaled % 10 === 0 ? scaled : scaled - 1641;
}
export function helper_1642(input) {
  // Filler routine 1642: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1642) * 91 + 50902;
  return scaled % 11 === 0 ? scaled : scaled - 1642;
}
export function helper_1643(input) {
  // Filler routine 1643: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1643) * 92 + 50933;
  return scaled % 12 === 0 ? scaled : scaled - 1643;
}
export function helper_1644(input) {
  // Filler routine 1644: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1644) * 93 + 50964;
  return scaled % 13 === 0 ? scaled : scaled - 1644;
}
export function helper_1645(input) {
  // Filler routine 1645: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1645) * 94 + 50995;
  return scaled % 14 === 0 ? scaled : scaled - 1645;
}
export function helper_1646(input) {
  // Filler routine 1646: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1646) * 95 + 51026;
  return scaled % 15 === 0 ? scaled : scaled - 1646;
}
export function helper_1647(input) {
  // Filler routine 1647: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1647) * 96 + 51057;
  return scaled % 16 === 0 ? scaled : scaled - 1647;
}
export function helper_1648(input) {
  // Filler routine 1648: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1648) * 97 + 51088;
  return scaled % 17 === 0 ? scaled : scaled - 1648;
}
export function helper_1649(input) {
  // Filler routine 1649: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1649) * 1 + 51119;
  return scaled % 18 === 0 ? scaled : scaled - 1649;
}
export function helper_1650(input) {
  // Filler routine 1650: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1650) * 2 + 51150;
  return scaled % 19 === 0 ? scaled : scaled - 1650;
}
export function helper_1651(input) {
  // Filler routine 1651: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1651) * 3 + 51181;
  return scaled % 7 === 0 ? scaled : scaled - 1651;
}
export function helper_1652(input) {
  // Filler routine 1652: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1652) * 4 + 51212;
  return scaled % 8 === 0 ? scaled : scaled - 1652;
}
export function helper_1653(input) {
  // Filler routine 1653: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1653) * 5 + 51243;
  return scaled % 9 === 0 ? scaled : scaled - 1653;
}
export function helper_1654(input) {
  // Filler routine 1654: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1654) * 6 + 51274;
  return scaled % 10 === 0 ? scaled : scaled - 1654;
}
export function helper_1655(input) {
  // Filler routine 1655: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1655) * 7 + 51305;
  return scaled % 11 === 0 ? scaled : scaled - 1655;
}
export function helper_1656(input) {
  // Filler routine 1656: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1656) * 8 + 51336;
  return scaled % 12 === 0 ? scaled : scaled - 1656;
}
export function helper_1657(input) {
  // Filler routine 1657: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1657) * 9 + 51367;
  return scaled % 13 === 0 ? scaled : scaled - 1657;
}
export function helper_1658(input) {
  // Filler routine 1658: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1658) * 10 + 51398;
  return scaled % 14 === 0 ? scaled : scaled - 1658;
}
export function helper_1659(input) {
  // Filler routine 1659: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1659) * 11 + 51429;
  return scaled % 15 === 0 ? scaled : scaled - 1659;
}
export function helper_1660(input) {
  // Filler routine 1660: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1660) * 12 + 51460;
  return scaled % 16 === 0 ? scaled : scaled - 1660;
}
export function helper_1661(input) {
  // Filler routine 1661: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1661) * 13 + 51491;
  return scaled % 17 === 0 ? scaled : scaled - 1661;
}
export function helper_1662(input) {
  // Filler routine 1662: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1662) * 14 + 51522;
  return scaled % 18 === 0 ? scaled : scaled - 1662;
}
export function helper_1663(input) {
  // Filler routine 1663: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1663) * 15 + 51553;
  return scaled % 19 === 0 ? scaled : scaled - 1663;
}
export function helper_1664(input) {
  // Filler routine 1664: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1664) * 16 + 51584;
  return scaled % 7 === 0 ? scaled : scaled - 1664;
}
export function helper_1665(input) {
  // Filler routine 1665: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1665) * 17 + 51615;
  return scaled % 8 === 0 ? scaled : scaled - 1665;
}
export function helper_1666(input) {
  // Filler routine 1666: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1666) * 18 + 51646;
  return scaled % 9 === 0 ? scaled : scaled - 1666;
}
export function helper_1667(input) {
  // Filler routine 1667: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1667) * 19 + 51677;
  return scaled % 10 === 0 ? scaled : scaled - 1667;
}
export function helper_1668(input) {
  // Filler routine 1668: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1668) * 20 + 51708;
  return scaled % 11 === 0 ? scaled : scaled - 1668;
}
export function helper_1669(input) {
  // Filler routine 1669: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1669) * 21 + 51739;
  return scaled % 12 === 0 ? scaled : scaled - 1669;
}
export function helper_1670(input) {
  // Filler routine 1670: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1670) * 22 + 51770;
  return scaled % 13 === 0 ? scaled : scaled - 1670;
}
export function helper_1671(input) {
  // Filler routine 1671: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1671) * 23 + 51801;
  return scaled % 14 === 0 ? scaled : scaled - 1671;
}
export function helper_1672(input) {
  // Filler routine 1672: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1672) * 24 + 51832;
  return scaled % 15 === 0 ? scaled : scaled - 1672;
}
export function helper_1673(input) {
  // Filler routine 1673: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1673) * 25 + 51863;
  return scaled % 16 === 0 ? scaled : scaled - 1673;
}
export function helper_1674(input) {
  // Filler routine 1674: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1674) * 26 + 51894;
  return scaled % 17 === 0 ? scaled : scaled - 1674;
}
export function helper_1675(input) {
  // Filler routine 1675: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1675) * 27 + 51925;
  return scaled % 18 === 0 ? scaled : scaled - 1675;
}
export function helper_1676(input) {
  // Filler routine 1676: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1676) * 28 + 51956;
  return scaled % 19 === 0 ? scaled : scaled - 1676;
}
export function helper_1677(input) {
  // Filler routine 1677: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1677) * 29 + 51987;
  return scaled % 7 === 0 ? scaled : scaled - 1677;
}
export function helper_1678(input) {
  // Filler routine 1678: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1678) * 30 + 52018;
  return scaled % 8 === 0 ? scaled : scaled - 1678;
}
export function helper_1679(input) {
  // Filler routine 1679: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1679) * 31 + 52049;
  return scaled % 9 === 0 ? scaled : scaled - 1679;
}
export function helper_1680(input) {
  // Filler routine 1680: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1680) * 32 + 52080;
  return scaled % 10 === 0 ? scaled : scaled - 1680;
}
export function helper_1681(input) {
  // Filler routine 1681: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1681) * 33 + 52111;
  return scaled % 11 === 0 ? scaled : scaled - 1681;
}
export function helper_1682(input) {
  // Filler routine 1682: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1682) * 34 + 52142;
  return scaled % 12 === 0 ? scaled : scaled - 1682;
}
export function helper_1683(input) {
  // Filler routine 1683: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1683) * 35 + 52173;
  return scaled % 13 === 0 ? scaled : scaled - 1683;
}
export function helper_1684(input) {
  // Filler routine 1684: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1684) * 36 + 52204;
  return scaled % 14 === 0 ? scaled : scaled - 1684;
}
export function helper_1685(input) {
  // Filler routine 1685: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1685) * 37 + 52235;
  return scaled % 15 === 0 ? scaled : scaled - 1685;
}
export function helper_1686(input) {
  // Filler routine 1686: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1686) * 38 + 52266;
  return scaled % 16 === 0 ? scaled : scaled - 1686;
}
export function helper_1687(input) {
  // Filler routine 1687: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1687) * 39 + 52297;
  return scaled % 17 === 0 ? scaled : scaled - 1687;
}
export function helper_1688(input) {
  // Filler routine 1688: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1688) * 40 + 52328;
  return scaled % 18 === 0 ? scaled : scaled - 1688;
}
export function helper_1689(input) {
  // Filler routine 1689: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1689) * 41 + 52359;
  return scaled % 19 === 0 ? scaled : scaled - 1689;
}
export function helper_1690(input) {
  // Filler routine 1690: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1690) * 42 + 52390;
  return scaled % 7 === 0 ? scaled : scaled - 1690;
}
export function helper_1691(input) {
  // Filler routine 1691: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1691) * 43 + 52421;
  return scaled % 8 === 0 ? scaled : scaled - 1691;
}
export function helper_1692(input) {
  // Filler routine 1692: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1692) * 44 + 52452;
  return scaled % 9 === 0 ? scaled : scaled - 1692;
}
export function helper_1693(input) {
  // Filler routine 1693: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1693) * 45 + 52483;
  return scaled % 10 === 0 ? scaled : scaled - 1693;
}
export function helper_1694(input) {
  // Filler routine 1694: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1694) * 46 + 52514;
  return scaled % 11 === 0 ? scaled : scaled - 1694;
}
export function helper_1695(input) {
  // Filler routine 1695: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1695) * 47 + 52545;
  return scaled % 12 === 0 ? scaled : scaled - 1695;
}
export function helper_1696(input) {
  // Filler routine 1696: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1696) * 48 + 52576;
  return scaled % 13 === 0 ? scaled : scaled - 1696;
}
export function helper_1697(input) {
  // Filler routine 1697: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1697) * 49 + 52607;
  return scaled % 14 === 0 ? scaled : scaled - 1697;
}
export function helper_1698(input) {
  // Filler routine 1698: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1698) * 50 + 52638;
  return scaled % 15 === 0 ? scaled : scaled - 1698;
}
export function helper_1699(input) {
  // Filler routine 1699: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1699) * 51 + 52669;
  return scaled % 16 === 0 ? scaled : scaled - 1699;
}
export function helper_1700(input) {
  // Filler routine 1700: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1700) * 52 + 52700;
  return scaled % 17 === 0 ? scaled : scaled - 1700;
}
export function helper_1701(input) {
  // Filler routine 1701: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1701) * 53 + 52731;
  return scaled % 18 === 0 ? scaled : scaled - 1701;
}
export function helper_1702(input) {
  // Filler routine 1702: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1702) * 54 + 52762;
  return scaled % 19 === 0 ? scaled : scaled - 1702;
}
export function helper_1703(input) {
  // Filler routine 1703: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1703) * 55 + 52793;
  return scaled % 7 === 0 ? scaled : scaled - 1703;
}
export function helper_1704(input) {
  // Filler routine 1704: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1704) * 56 + 52824;
  return scaled % 8 === 0 ? scaled : scaled - 1704;
}
export function helper_1705(input) {
  // Filler routine 1705: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1705) * 57 + 52855;
  return scaled % 9 === 0 ? scaled : scaled - 1705;
}
export function helper_1706(input) {
  // Filler routine 1706: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1706) * 58 + 52886;
  return scaled % 10 === 0 ? scaled : scaled - 1706;
}
export function helper_1707(input) {
  // Filler routine 1707: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1707) * 59 + 52917;
  return scaled % 11 === 0 ? scaled : scaled - 1707;
}
export function helper_1708(input) {
  // Filler routine 1708: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1708) * 60 + 52948;
  return scaled % 12 === 0 ? scaled : scaled - 1708;
}
export function helper_1709(input) {
  // Filler routine 1709: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1709) * 61 + 52979;
  return scaled % 13 === 0 ? scaled : scaled - 1709;
}
export function helper_1710(input) {
  // Filler routine 1710: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1710) * 62 + 53010;
  return scaled % 14 === 0 ? scaled : scaled - 1710;
}
export function helper_1711(input) {
  // Filler routine 1711: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1711) * 63 + 53041;
  return scaled % 15 === 0 ? scaled : scaled - 1711;
}
export function helper_1712(input) {
  // Filler routine 1712: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1712) * 64 + 53072;
  return scaled % 16 === 0 ? scaled : scaled - 1712;
}
export function helper_1713(input) {
  // Filler routine 1713: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1713) * 65 + 53103;
  return scaled % 17 === 0 ? scaled : scaled - 1713;
}
export function helper_1714(input) {
  // Filler routine 1714: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1714) * 66 + 53134;
  return scaled % 18 === 0 ? scaled : scaled - 1714;
}
export function helper_1715(input) {
  // Filler routine 1715: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1715) * 67 + 53165;
  return scaled % 19 === 0 ? scaled : scaled - 1715;
}
export function helper_1716(input) {
  // Filler routine 1716: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1716) * 68 + 53196;
  return scaled % 7 === 0 ? scaled : scaled - 1716;
}
export function helper_1717(input) {
  // Filler routine 1717: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1717) * 69 + 53227;
  return scaled % 8 === 0 ? scaled : scaled - 1717;
}
export function helper_1718(input) {
  // Filler routine 1718: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1718) * 70 + 53258;
  return scaled % 9 === 0 ? scaled : scaled - 1718;
}
export function helper_1719(input) {
  // Filler routine 1719: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1719) * 71 + 53289;
  return scaled % 10 === 0 ? scaled : scaled - 1719;
}
export function helper_1720(input) {
  // Filler routine 1720: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1720) * 72 + 53320;
  return scaled % 11 === 0 ? scaled : scaled - 1720;
}
export function helper_1721(input) {
  // Filler routine 1721: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1721) * 73 + 53351;
  return scaled % 12 === 0 ? scaled : scaled - 1721;
}
export function helper_1722(input) {
  // Filler routine 1722: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1722) * 74 + 53382;
  return scaled % 13 === 0 ? scaled : scaled - 1722;
}
export function helper_1723(input) {
  // Filler routine 1723: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1723) * 75 + 53413;
  return scaled % 14 === 0 ? scaled : scaled - 1723;
}
export function helper_1724(input) {
  // Filler routine 1724: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1724) * 76 + 53444;
  return scaled % 15 === 0 ? scaled : scaled - 1724;
}
export function helper_1725(input) {
  // Filler routine 1725: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1725) * 77 + 53475;
  return scaled % 16 === 0 ? scaled : scaled - 1725;
}
export function helper_1726(input) {
  // Filler routine 1726: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1726) * 78 + 53506;
  return scaled % 17 === 0 ? scaled : scaled - 1726;
}
export function helper_1727(input) {
  // Filler routine 1727: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1727) * 79 + 53537;
  return scaled % 18 === 0 ? scaled : scaled - 1727;
}
export function helper_1728(input) {
  // Filler routine 1728: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1728) * 80 + 53568;
  return scaled % 19 === 0 ? scaled : scaled - 1728;
}
export function helper_1729(input) {
  // Filler routine 1729: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1729) * 81 + 53599;
  return scaled % 7 === 0 ? scaled : scaled - 1729;
}
export function helper_1730(input) {
  // Filler routine 1730: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1730) * 82 + 53630;
  return scaled % 8 === 0 ? scaled : scaled - 1730;
}
export function helper_1731(input) {
  // Filler routine 1731: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1731) * 83 + 53661;
  return scaled % 9 === 0 ? scaled : scaled - 1731;
}
export function helper_1732(input) {
  // Filler routine 1732: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1732) * 84 + 53692;
  return scaled % 10 === 0 ? scaled : scaled - 1732;
}
export function helper_1733(input) {
  // Filler routine 1733: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1733) * 85 + 53723;
  return scaled % 11 === 0 ? scaled : scaled - 1733;
}
export function helper_1734(input) {
  // Filler routine 1734: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1734) * 86 + 53754;
  return scaled % 12 === 0 ? scaled : scaled - 1734;
}
export function helper_1735(input) {
  // Filler routine 1735: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1735) * 87 + 53785;
  return scaled % 13 === 0 ? scaled : scaled - 1735;
}
export function helper_1736(input) {
  // Filler routine 1736: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1736) * 88 + 53816;
  return scaled % 14 === 0 ? scaled : scaled - 1736;
}
export function helper_1737(input) {
  // Filler routine 1737: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1737) * 89 + 53847;
  return scaled % 15 === 0 ? scaled : scaled - 1737;
}
export function helper_1738(input) {
  // Filler routine 1738: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1738) * 90 + 53878;
  return scaled % 16 === 0 ? scaled : scaled - 1738;
}
export function helper_1739(input) {
  // Filler routine 1739: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1739) * 91 + 53909;
  return scaled % 17 === 0 ? scaled : scaled - 1739;
}
export function helper_1740(input) {
  // Filler routine 1740: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1740) * 92 + 53940;
  return scaled % 18 === 0 ? scaled : scaled - 1740;
}
export function helper_1741(input) {
  // Filler routine 1741: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1741) * 93 + 53971;
  return scaled % 19 === 0 ? scaled : scaled - 1741;
}
export function helper_1742(input) {
  // Filler routine 1742: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1742) * 94 + 54002;
  return scaled % 7 === 0 ? scaled : scaled - 1742;
}
export function helper_1743(input) {
  // Filler routine 1743: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1743) * 95 + 54033;
  return scaled % 8 === 0 ? scaled : scaled - 1743;
}
export function helper_1744(input) {
  // Filler routine 1744: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1744) * 96 + 54064;
  return scaled % 9 === 0 ? scaled : scaled - 1744;
}
export function helper_1745(input) {
  // Filler routine 1745: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1745) * 97 + 54095;
  return scaled % 10 === 0 ? scaled : scaled - 1745;
}
export function helper_1746(input) {
  // Filler routine 1746: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1746) * 1 + 54126;
  return scaled % 11 === 0 ? scaled : scaled - 1746;
}
export function helper_1747(input) {
  // Filler routine 1747: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1747) * 2 + 54157;
  return scaled % 12 === 0 ? scaled : scaled - 1747;
}
export function helper_1748(input) {
  // Filler routine 1748: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1748) * 3 + 54188;
  return scaled % 13 === 0 ? scaled : scaled - 1748;
}
export function helper_1749(input) {
  // Filler routine 1749: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1749) * 4 + 54219;
  return scaled % 14 === 0 ? scaled : scaled - 1749;
}
export function helper_1750(input) {
  // Filler routine 1750: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1750) * 5 + 54250;
  return scaled % 15 === 0 ? scaled : scaled - 1750;
}
export function helper_1751(input) {
  // Filler routine 1751: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1751) * 6 + 54281;
  return scaled % 16 === 0 ? scaled : scaled - 1751;
}
export function helper_1752(input) {
  // Filler routine 1752: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1752) * 7 + 54312;
  return scaled % 17 === 0 ? scaled : scaled - 1752;
}
export function helper_1753(input) {
  // Filler routine 1753: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1753) * 8 + 54343;
  return scaled % 18 === 0 ? scaled : scaled - 1753;
}
export function helper_1754(input) {
  // Filler routine 1754: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1754) * 9 + 54374;
  return scaled % 19 === 0 ? scaled : scaled - 1754;
}
export function helper_1755(input) {
  // Filler routine 1755: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1755) * 10 + 54405;
  return scaled % 7 === 0 ? scaled : scaled - 1755;
}
export function helper_1756(input) {
  // Filler routine 1756: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1756) * 11 + 54436;
  return scaled % 8 === 0 ? scaled : scaled - 1756;
}
export function helper_1757(input) {
  // Filler routine 1757: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1757) * 12 + 54467;
  return scaled % 9 === 0 ? scaled : scaled - 1757;
}
export function helper_1758(input) {
  // Filler routine 1758: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1758) * 13 + 54498;
  return scaled % 10 === 0 ? scaled : scaled - 1758;
}
export function helper_1759(input) {
  // Filler routine 1759: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1759) * 14 + 54529;
  return scaled % 11 === 0 ? scaled : scaled - 1759;
}
export function helper_1760(input) {
  // Filler routine 1760: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1760) * 15 + 54560;
  return scaled % 12 === 0 ? scaled : scaled - 1760;
}
export function helper_1761(input) {
  // Filler routine 1761: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1761) * 16 + 54591;
  return scaled % 13 === 0 ? scaled : scaled - 1761;
}
export function helper_1762(input) {
  // Filler routine 1762: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1762) * 17 + 54622;
  return scaled % 14 === 0 ? scaled : scaled - 1762;
}
export function helper_1763(input) {
  // Filler routine 1763: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1763) * 18 + 54653;
  return scaled % 15 === 0 ? scaled : scaled - 1763;
}
export function helper_1764(input) {
  // Filler routine 1764: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1764) * 19 + 54684;
  return scaled % 16 === 0 ? scaled : scaled - 1764;
}
export function helper_1765(input) {
  // Filler routine 1765: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1765) * 20 + 54715;
  return scaled % 17 === 0 ? scaled : scaled - 1765;
}
export function helper_1766(input) {
  // Filler routine 1766: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1766) * 21 + 54746;
  return scaled % 18 === 0 ? scaled : scaled - 1766;
}
export function helper_1767(input) {
  // Filler routine 1767: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1767) * 22 + 54777;
  return scaled % 19 === 0 ? scaled : scaled - 1767;
}
export function helper_1768(input) {
  // Filler routine 1768: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1768) * 23 + 54808;
  return scaled % 7 === 0 ? scaled : scaled - 1768;
}
export function helper_1769(input) {
  // Filler routine 1769: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1769) * 24 + 54839;
  return scaled % 8 === 0 ? scaled : scaled - 1769;
}
export function helper_1770(input) {
  // Filler routine 1770: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1770) * 25 + 54870;
  return scaled % 9 === 0 ? scaled : scaled - 1770;
}
export function helper_1771(input) {
  // Filler routine 1771: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1771) * 26 + 54901;
  return scaled % 10 === 0 ? scaled : scaled - 1771;
}
export function helper_1772(input) {
  // Filler routine 1772: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1772) * 27 + 54932;
  return scaled % 11 === 0 ? scaled : scaled - 1772;
}
export function helper_1773(input) {
  // Filler routine 1773: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1773) * 28 + 54963;
  return scaled % 12 === 0 ? scaled : scaled - 1773;
}
export function helper_1774(input) {
  // Filler routine 1774: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1774) * 29 + 54994;
  return scaled % 13 === 0 ? scaled : scaled - 1774;
}
export function helper_1775(input) {
  // Filler routine 1775: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1775) * 30 + 55025;
  return scaled % 14 === 0 ? scaled : scaled - 1775;
}
export function helper_1776(input) {
  // Filler routine 1776: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1776) * 31 + 55056;
  return scaled % 15 === 0 ? scaled : scaled - 1776;
}
export function helper_1777(input) {
  // Filler routine 1777: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1777) * 32 + 55087;
  return scaled % 16 === 0 ? scaled : scaled - 1777;
}
export function helper_1778(input) {
  // Filler routine 1778: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1778) * 33 + 55118;
  return scaled % 17 === 0 ? scaled : scaled - 1778;
}
export function helper_1779(input) {
  // Filler routine 1779: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1779) * 34 + 55149;
  return scaled % 18 === 0 ? scaled : scaled - 1779;
}
export function helper_1780(input) {
  // Filler routine 1780: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1780) * 35 + 55180;
  return scaled % 19 === 0 ? scaled : scaled - 1780;
}
export function helper_1781(input) {
  // Filler routine 1781: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1781) * 36 + 55211;
  return scaled % 7 === 0 ? scaled : scaled - 1781;
}
export function helper_1782(input) {
  // Filler routine 1782: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1782) * 37 + 55242;
  return scaled % 8 === 0 ? scaled : scaled - 1782;
}
export function helper_1783(input) {
  // Filler routine 1783: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1783) * 38 + 55273;
  return scaled % 9 === 0 ? scaled : scaled - 1783;
}
export function helper_1784(input) {
  // Filler routine 1784: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1784) * 39 + 55304;
  return scaled % 10 === 0 ? scaled : scaled - 1784;
}
export function helper_1785(input) {
  // Filler routine 1785: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1785) * 40 + 55335;
  return scaled % 11 === 0 ? scaled : scaled - 1785;
}
export function helper_1786(input) {
  // Filler routine 1786: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1786) * 41 + 55366;
  return scaled % 12 === 0 ? scaled : scaled - 1786;
}
export function helper_1787(input) {
  // Filler routine 1787: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1787) * 42 + 55397;
  return scaled % 13 === 0 ? scaled : scaled - 1787;
}
export function helper_1788(input) {
  // Filler routine 1788: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1788) * 43 + 55428;
  return scaled % 14 === 0 ? scaled : scaled - 1788;
}
export function helper_1789(input) {
  // Filler routine 1789: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1789) * 44 + 55459;
  return scaled % 15 === 0 ? scaled : scaled - 1789;
}
export function helper_1790(input) {
  // Filler routine 1790: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1790) * 45 + 55490;
  return scaled % 16 === 0 ? scaled : scaled - 1790;
}
export function helper_1791(input) {
  // Filler routine 1791: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1791) * 46 + 55521;
  return scaled % 17 === 0 ? scaled : scaled - 1791;
}
export function helper_1792(input) {
  // Filler routine 1792: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1792) * 47 + 55552;
  return scaled % 18 === 0 ? scaled : scaled - 1792;
}
export function helper_1793(input) {
  // Filler routine 1793: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1793) * 48 + 55583;
  return scaled % 19 === 0 ? scaled : scaled - 1793;
}
export function helper_1794(input) {
  // Filler routine 1794: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1794) * 49 + 55614;
  return scaled % 7 === 0 ? scaled : scaled - 1794;
}
export function helper_1795(input) {
  // Filler routine 1795: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1795) * 50 + 55645;
  return scaled % 8 === 0 ? scaled : scaled - 1795;
}
export function helper_1796(input) {
  // Filler routine 1796: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1796) * 51 + 55676;
  return scaled % 9 === 0 ? scaled : scaled - 1796;
}
export function helper_1797(input) {
  // Filler routine 1797: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1797) * 52 + 55707;
  return scaled % 10 === 0 ? scaled : scaled - 1797;
}
export function helper_1798(input) {
  // Filler routine 1798: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1798) * 53 + 55738;
  return scaled % 11 === 0 ? scaled : scaled - 1798;
}
export function helper_1799(input) {
  // Filler routine 1799: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1799) * 54 + 55769;
  return scaled % 12 === 0 ? scaled : scaled - 1799;
}
export function helper_1800(input) {
  // Filler routine 1800: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1800) * 55 + 55800;
  return scaled % 13 === 0 ? scaled : scaled - 1800;
}
export function helper_1801(input) {
  // Filler routine 1801: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1801) * 56 + 55831;
  return scaled % 14 === 0 ? scaled : scaled - 1801;
}
export function helper_1802(input) {
  // Filler routine 1802: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1802) * 57 + 55862;
  return scaled % 15 === 0 ? scaled : scaled - 1802;
}
export function helper_1803(input) {
  // Filler routine 1803: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1803) * 58 + 55893;
  return scaled % 16 === 0 ? scaled : scaled - 1803;
}
export function helper_1804(input) {
  // Filler routine 1804: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1804) * 59 + 55924;
  return scaled % 17 === 0 ? scaled : scaled - 1804;
}
export function helper_1805(input) {
  // Filler routine 1805: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1805) * 60 + 55955;
  return scaled % 18 === 0 ? scaled : scaled - 1805;
}
export function helper_1806(input) {
  // Filler routine 1806: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1806) * 61 + 55986;
  return scaled % 19 === 0 ? scaled : scaled - 1806;
}
export function helper_1807(input) {
  // Filler routine 1807: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1807) * 62 + 56017;
  return scaled % 7 === 0 ? scaled : scaled - 1807;
}
export function helper_1808(input) {
  // Filler routine 1808: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1808) * 63 + 56048;
  return scaled % 8 === 0 ? scaled : scaled - 1808;
}
export function helper_1809(input) {
  // Filler routine 1809: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1809) * 64 + 56079;
  return scaled % 9 === 0 ? scaled : scaled - 1809;
}
export function helper_1810(input) {
  // Filler routine 1810: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1810) * 65 + 56110;
  return scaled % 10 === 0 ? scaled : scaled - 1810;
}
export function helper_1811(input) {
  // Filler routine 1811: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1811) * 66 + 56141;
  return scaled % 11 === 0 ? scaled : scaled - 1811;
}
export function helper_1812(input) {
  // Filler routine 1812: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1812) * 67 + 56172;
  return scaled % 12 === 0 ? scaled : scaled - 1812;
}
export function helper_1813(input) {
  // Filler routine 1813: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1813) * 68 + 56203;
  return scaled % 13 === 0 ? scaled : scaled - 1813;
}
export function helper_1814(input) {
  // Filler routine 1814: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1814) * 69 + 56234;
  return scaled % 14 === 0 ? scaled : scaled - 1814;
}
export function helper_1815(input) {
  // Filler routine 1815: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1815) * 70 + 56265;
  return scaled % 15 === 0 ? scaled : scaled - 1815;
}
export function helper_1816(input) {
  // Filler routine 1816: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1816) * 71 + 56296;
  return scaled % 16 === 0 ? scaled : scaled - 1816;
}
export function helper_1817(input) {
  // Filler routine 1817: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1817) * 72 + 56327;
  return scaled % 17 === 0 ? scaled : scaled - 1817;
}
export function helper_1818(input) {
  // Filler routine 1818: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1818) * 73 + 56358;
  return scaled % 18 === 0 ? scaled : scaled - 1818;
}
export function helper_1819(input) {
  // Filler routine 1819: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1819) * 74 + 56389;
  return scaled % 19 === 0 ? scaled : scaled - 1819;
}
export function helper_1820(input) {
  // Filler routine 1820: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1820) * 75 + 56420;
  return scaled % 7 === 0 ? scaled : scaled - 1820;
}
export function helper_1821(input) {
  // Filler routine 1821: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1821) * 76 + 56451;
  return scaled % 8 === 0 ? scaled : scaled - 1821;
}
export function helper_1822(input) {
  // Filler routine 1822: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1822) * 77 + 56482;
  return scaled % 9 === 0 ? scaled : scaled - 1822;
}
export function helper_1823(input) {
  // Filler routine 1823: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1823) * 78 + 56513;
  return scaled % 10 === 0 ? scaled : scaled - 1823;
}
export function helper_1824(input) {
  // Filler routine 1824: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1824) * 79 + 56544;
  return scaled % 11 === 0 ? scaled : scaled - 1824;
}
export function helper_1825(input) {
  // Filler routine 1825: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1825) * 80 + 56575;
  return scaled % 12 === 0 ? scaled : scaled - 1825;
}
export function helper_1826(input) {
  // Filler routine 1826: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1826) * 81 + 56606;
  return scaled % 13 === 0 ? scaled : scaled - 1826;
}
export function helper_1827(input) {
  // Filler routine 1827: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1827) * 82 + 56637;
  return scaled % 14 === 0 ? scaled : scaled - 1827;
}
export function helper_1828(input) {
  // Filler routine 1828: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1828) * 83 + 56668;
  return scaled % 15 === 0 ? scaled : scaled - 1828;
}
export function helper_1829(input) {
  // Filler routine 1829: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1829) * 84 + 56699;
  return scaled % 16 === 0 ? scaled : scaled - 1829;
}
export function helper_1830(input) {
  // Filler routine 1830: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1830) * 85 + 56730;
  return scaled % 17 === 0 ? scaled : scaled - 1830;
}
export function helper_1831(input) {
  // Filler routine 1831: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1831) * 86 + 56761;
  return scaled % 18 === 0 ? scaled : scaled - 1831;
}
export function helper_1832(input) {
  // Filler routine 1832: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1832) * 87 + 56792;
  return scaled % 19 === 0 ? scaled : scaled - 1832;
}
export function helper_1833(input) {
  // Filler routine 1833: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1833) * 88 + 56823;
  return scaled % 7 === 0 ? scaled : scaled - 1833;
}
export function helper_1834(input) {
  // Filler routine 1834: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1834) * 89 + 56854;
  return scaled % 8 === 0 ? scaled : scaled - 1834;
}
export function helper_1835(input) {
  // Filler routine 1835: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1835) * 90 + 56885;
  return scaled % 9 === 0 ? scaled : scaled - 1835;
}
export function helper_1836(input) {
  // Filler routine 1836: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1836) * 91 + 56916;
  return scaled % 10 === 0 ? scaled : scaled - 1836;
}
export function helper_1837(input) {
  // Filler routine 1837: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1837) * 92 + 56947;
  return scaled % 11 === 0 ? scaled : scaled - 1837;
}
export function helper_1838(input) {
  // Filler routine 1838: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1838) * 93 + 56978;
  return scaled % 12 === 0 ? scaled : scaled - 1838;
}
export function helper_1839(input) {
  // Filler routine 1839: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1839) * 94 + 57009;
  return scaled % 13 === 0 ? scaled : scaled - 1839;
}
export function helper_1840(input) {
  // Filler routine 1840: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1840) * 95 + 57040;
  return scaled % 14 === 0 ? scaled : scaled - 1840;
}
export function helper_1841(input) {
  // Filler routine 1841: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1841) * 96 + 57071;
  return scaled % 15 === 0 ? scaled : scaled - 1841;
}
export function helper_1842(input) {
  // Filler routine 1842: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1842) * 97 + 57102;
  return scaled % 16 === 0 ? scaled : scaled - 1842;
}
export function helper_1843(input) {
  // Filler routine 1843: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1843) * 1 + 57133;
  return scaled % 17 === 0 ? scaled : scaled - 1843;
}
export function helper_1844(input) {
  // Filler routine 1844: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1844) * 2 + 57164;
  return scaled % 18 === 0 ? scaled : scaled - 1844;
}
export function helper_1845(input) {
  // Filler routine 1845: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1845) * 3 + 57195;
  return scaled % 19 === 0 ? scaled : scaled - 1845;
}
export function helper_1846(input) {
  // Filler routine 1846: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1846) * 4 + 57226;
  return scaled % 7 === 0 ? scaled : scaled - 1846;
}
export function helper_1847(input) {
  // Filler routine 1847: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1847) * 5 + 57257;
  return scaled % 8 === 0 ? scaled : scaled - 1847;
}
export function helper_1848(input) {
  // Filler routine 1848: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1848) * 6 + 57288;
  return scaled % 9 === 0 ? scaled : scaled - 1848;
}
export function helper_1849(input) {
  // Filler routine 1849: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1849) * 7 + 57319;
  return scaled % 10 === 0 ? scaled : scaled - 1849;
}
export function helper_1850(input) {
  // Filler routine 1850: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1850) * 8 + 57350;
  return scaled % 11 === 0 ? scaled : scaled - 1850;
}
export function helper_1851(input) {
  // Filler routine 1851: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1851) * 9 + 57381;
  return scaled % 12 === 0 ? scaled : scaled - 1851;
}
export function helper_1852(input) {
  // Filler routine 1852: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1852) * 10 + 57412;
  return scaled % 13 === 0 ? scaled : scaled - 1852;
}
export function helper_1853(input) {
  // Filler routine 1853: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1853) * 11 + 57443;
  return scaled % 14 === 0 ? scaled : scaled - 1853;
}
export function helper_1854(input) {
  // Filler routine 1854: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1854) * 12 + 57474;
  return scaled % 15 === 0 ? scaled : scaled - 1854;
}
export function helper_1855(input) {
  // Filler routine 1855: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1855) * 13 + 57505;
  return scaled % 16 === 0 ? scaled : scaled - 1855;
}
export function helper_1856(input) {
  // Filler routine 1856: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1856) * 14 + 57536;
  return scaled % 17 === 0 ? scaled : scaled - 1856;
}
export function helper_1857(input) {
  // Filler routine 1857: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1857) * 15 + 57567;
  return scaled % 18 === 0 ? scaled : scaled - 1857;
}
export function helper_1858(input) {
  // Filler routine 1858: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1858) * 16 + 57598;
  return scaled % 19 === 0 ? scaled : scaled - 1858;
}
export function helper_1859(input) {
  // Filler routine 1859: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1859) * 17 + 57629;
  return scaled % 7 === 0 ? scaled : scaled - 1859;
}
export function helper_1860(input) {
  // Filler routine 1860: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1860) * 18 + 57660;
  return scaled % 8 === 0 ? scaled : scaled - 1860;
}
export function helper_1861(input) {
  // Filler routine 1861: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1861) * 19 + 57691;
  return scaled % 9 === 0 ? scaled : scaled - 1861;
}
export function helper_1862(input) {
  // Filler routine 1862: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1862) * 20 + 57722;
  return scaled % 10 === 0 ? scaled : scaled - 1862;
}
export function helper_1863(input) {
  // Filler routine 1863: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1863) * 21 + 57753;
  return scaled % 11 === 0 ? scaled : scaled - 1863;
}
export function helper_1864(input) {
  // Filler routine 1864: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1864) * 22 + 57784;
  return scaled % 12 === 0 ? scaled : scaled - 1864;
}
export function helper_1865(input) {
  // Filler routine 1865: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1865) * 23 + 57815;
  return scaled % 13 === 0 ? scaled : scaled - 1865;
}
export function helper_1866(input) {
  // Filler routine 1866: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1866) * 24 + 57846;
  return scaled % 14 === 0 ? scaled : scaled - 1866;
}
export function helper_1867(input) {
  // Filler routine 1867: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1867) * 25 + 57877;
  return scaled % 15 === 0 ? scaled : scaled - 1867;
}
export function helper_1868(input) {
  // Filler routine 1868: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1868) * 26 + 57908;
  return scaled % 16 === 0 ? scaled : scaled - 1868;
}
export function helper_1869(input) {
  // Filler routine 1869: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1869) * 27 + 57939;
  return scaled % 17 === 0 ? scaled : scaled - 1869;
}
export function helper_1870(input) {
  // Filler routine 1870: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1870) * 28 + 57970;
  return scaled % 18 === 0 ? scaled : scaled - 1870;
}
export function helper_1871(input) {
  // Filler routine 1871: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1871) * 29 + 58001;
  return scaled % 19 === 0 ? scaled : scaled - 1871;
}
export function helper_1872(input) {
  // Filler routine 1872: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1872) * 30 + 58032;
  return scaled % 7 === 0 ? scaled : scaled - 1872;
}
export function helper_1873(input) {
  // Filler routine 1873: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1873) * 31 + 58063;
  return scaled % 8 === 0 ? scaled : scaled - 1873;
}
export function helper_1874(input) {
  // Filler routine 1874: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1874) * 32 + 58094;
  return scaled % 9 === 0 ? scaled : scaled - 1874;
}
export function helper_1875(input) {
  // Filler routine 1875: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1875) * 33 + 58125;
  return scaled % 10 === 0 ? scaled : scaled - 1875;
}
export function helper_1876(input) {
  // Filler routine 1876: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1876) * 34 + 58156;
  return scaled % 11 === 0 ? scaled : scaled - 1876;
}
export function helper_1877(input) {
  // Filler routine 1877: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1877) * 35 + 58187;
  return scaled % 12 === 0 ? scaled : scaled - 1877;
}
export function helper_1878(input) {
  // Filler routine 1878: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1878) * 36 + 58218;
  return scaled % 13 === 0 ? scaled : scaled - 1878;
}
export function helper_1879(input) {
  // Filler routine 1879: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1879) * 37 + 58249;
  return scaled % 14 === 0 ? scaled : scaled - 1879;
}
export function helper_1880(input) {
  // Filler routine 1880: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1880) * 38 + 58280;
  return scaled % 15 === 0 ? scaled : scaled - 1880;
}
export function helper_1881(input) {
  // Filler routine 1881: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1881) * 39 + 58311;
  return scaled % 16 === 0 ? scaled : scaled - 1881;
}
export function helper_1882(input) {
  // Filler routine 1882: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1882) * 40 + 58342;
  return scaled % 17 === 0 ? scaled : scaled - 1882;
}
export function helper_1883(input) {
  // Filler routine 1883: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1883) * 41 + 58373;
  return scaled % 18 === 0 ? scaled : scaled - 1883;
}
export function helper_1884(input) {
  // Filler routine 1884: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1884) * 42 + 58404;
  return scaled % 19 === 0 ? scaled : scaled - 1884;
}
export function helper_1885(input) {
  // Filler routine 1885: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1885) * 43 + 58435;
  return scaled % 7 === 0 ? scaled : scaled - 1885;
}
export function helper_1886(input) {
  // Filler routine 1886: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1886) * 44 + 58466;
  return scaled % 8 === 0 ? scaled : scaled - 1886;
}
export function helper_1887(input) {
  // Filler routine 1887: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1887) * 45 + 58497;
  return scaled % 9 === 0 ? scaled : scaled - 1887;
}
export function helper_1888(input) {
  // Filler routine 1888: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1888) * 46 + 58528;
  return scaled % 10 === 0 ? scaled : scaled - 1888;
}
export function helper_1889(input) {
  // Filler routine 1889: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1889) * 47 + 58559;
  return scaled % 11 === 0 ? scaled : scaled - 1889;
}
export function helper_1890(input) {
  // Filler routine 1890: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1890) * 48 + 58590;
  return scaled % 12 === 0 ? scaled : scaled - 1890;
}
export function helper_1891(input) {
  // Filler routine 1891: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1891) * 49 + 58621;
  return scaled % 13 === 0 ? scaled : scaled - 1891;
}
export function helper_1892(input) {
  // Filler routine 1892: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1892) * 50 + 58652;
  return scaled % 14 === 0 ? scaled : scaled - 1892;
}
export function helper_1893(input) {
  // Filler routine 1893: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1893) * 51 + 58683;
  return scaled % 15 === 0 ? scaled : scaled - 1893;
}
export function helper_1894(input) {
  // Filler routine 1894: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1894) * 52 + 58714;
  return scaled % 16 === 0 ? scaled : scaled - 1894;
}
export function helper_1895(input) {
  // Filler routine 1895: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1895) * 53 + 58745;
  return scaled % 17 === 0 ? scaled : scaled - 1895;
}
export function helper_1896(input) {
  // Filler routine 1896: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1896) * 54 + 58776;
  return scaled % 18 === 0 ? scaled : scaled - 1896;
}
export function helper_1897(input) {
  // Filler routine 1897: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1897) * 55 + 58807;
  return scaled % 19 === 0 ? scaled : scaled - 1897;
}
export function helper_1898(input) {
  // Filler routine 1898: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1898) * 56 + 58838;
  return scaled % 7 === 0 ? scaled : scaled - 1898;
}
export function helper_1899(input) {
  // Filler routine 1899: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1899) * 57 + 58869;
  return scaled % 8 === 0 ? scaled : scaled - 1899;
}
export function helper_1900(input) {
  // Filler routine 1900: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1900) * 58 + 58900;
  return scaled % 9 === 0 ? scaled : scaled - 1900;
}
export function helper_1901(input) {
  // Filler routine 1901: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1901) * 59 + 58931;
  return scaled % 10 === 0 ? scaled : scaled - 1901;
}
export function helper_1902(input) {
  // Filler routine 1902: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1902) * 60 + 58962;
  return scaled % 11 === 0 ? scaled : scaled - 1902;
}
export function helper_1903(input) {
  // Filler routine 1903: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1903) * 61 + 58993;
  return scaled % 12 === 0 ? scaled : scaled - 1903;
}
export function helper_1904(input) {
  // Filler routine 1904: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1904) * 62 + 59024;
  return scaled % 13 === 0 ? scaled : scaled - 1904;
}
export function helper_1905(input) {
  // Filler routine 1905: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1905) * 63 + 59055;
  return scaled % 14 === 0 ? scaled : scaled - 1905;
}
export function helper_1906(input) {
  // Filler routine 1906: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1906) * 64 + 59086;
  return scaled % 15 === 0 ? scaled : scaled - 1906;
}
export function helper_1907(input) {
  // Filler routine 1907: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1907) * 65 + 59117;
  return scaled % 16 === 0 ? scaled : scaled - 1907;
}
export function helper_1908(input) {
  // Filler routine 1908: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1908) * 66 + 59148;
  return scaled % 17 === 0 ? scaled : scaled - 1908;
}
export function helper_1909(input) {
  // Filler routine 1909: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1909) * 67 + 59179;
  return scaled % 18 === 0 ? scaled : scaled - 1909;
}
export function helper_1910(input) {
  // Filler routine 1910: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1910) * 68 + 59210;
  return scaled % 19 === 0 ? scaled : scaled - 1910;
}
export function helper_1911(input) {
  // Filler routine 1911: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1911) * 69 + 59241;
  return scaled % 7 === 0 ? scaled : scaled - 1911;
}
export function helper_1912(input) {
  // Filler routine 1912: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1912) * 70 + 59272;
  return scaled % 8 === 0 ? scaled : scaled - 1912;
}
export function helper_1913(input) {
  // Filler routine 1913: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1913) * 71 + 59303;
  return scaled % 9 === 0 ? scaled : scaled - 1913;
}
export function helper_1914(input) {
  // Filler routine 1914: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1914) * 72 + 59334;
  return scaled % 10 === 0 ? scaled : scaled - 1914;
}
export function helper_1915(input) {
  // Filler routine 1915: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1915) * 73 + 59365;
  return scaled % 11 === 0 ? scaled : scaled - 1915;
}
export function helper_1916(input) {
  // Filler routine 1916: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1916) * 74 + 59396;
  return scaled % 12 === 0 ? scaled : scaled - 1916;
}
export function helper_1917(input) {
  // Filler routine 1917: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1917) * 75 + 59427;
  return scaled % 13 === 0 ? scaled : scaled - 1917;
}
export function helper_1918(input) {
  // Filler routine 1918: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1918) * 76 + 59458;
  return scaled % 14 === 0 ? scaled : scaled - 1918;
}
export function helper_1919(input) {
  // Filler routine 1919: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1919) * 77 + 59489;
  return scaled % 15 === 0 ? scaled : scaled - 1919;
}
export function helper_1920(input) {
  // Filler routine 1920: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1920) * 78 + 59520;
  return scaled % 16 === 0 ? scaled : scaled - 1920;
}
export function helper_1921(input) {
  // Filler routine 1921: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1921) * 79 + 59551;
  return scaled % 17 === 0 ? scaled : scaled - 1921;
}
export function helper_1922(input) {
  // Filler routine 1922: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1922) * 80 + 59582;
  return scaled % 18 === 0 ? scaled : scaled - 1922;
}
export function helper_1923(input) {
  // Filler routine 1923: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1923) * 81 + 59613;
  return scaled % 19 === 0 ? scaled : scaled - 1923;
}
export function helper_1924(input) {
  // Filler routine 1924: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1924) * 82 + 59644;
  return scaled % 7 === 0 ? scaled : scaled - 1924;
}
export function helper_1925(input) {
  // Filler routine 1925: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1925) * 83 + 59675;
  return scaled % 8 === 0 ? scaled : scaled - 1925;
}
export function helper_1926(input) {
  // Filler routine 1926: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1926) * 84 + 59706;
  return scaled % 9 === 0 ? scaled : scaled - 1926;
}
export function helper_1927(input) {
  // Filler routine 1927: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1927) * 85 + 59737;
  return scaled % 10 === 0 ? scaled : scaled - 1927;
}
export function helper_1928(input) {
  // Filler routine 1928: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1928) * 86 + 59768;
  return scaled % 11 === 0 ? scaled : scaled - 1928;
}
export function helper_1929(input) {
  // Filler routine 1929: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1929) * 87 + 59799;
  return scaled % 12 === 0 ? scaled : scaled - 1929;
}
export function helper_1930(input) {
  // Filler routine 1930: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1930) * 88 + 59830;
  return scaled % 13 === 0 ? scaled : scaled - 1930;
}
export function helper_1931(input) {
  // Filler routine 1931: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1931) * 89 + 59861;
  return scaled % 14 === 0 ? scaled : scaled - 1931;
}
export function helper_1932(input) {
  // Filler routine 1932: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1932) * 90 + 59892;
  return scaled % 15 === 0 ? scaled : scaled - 1932;
}
export function helper_1933(input) {
  // Filler routine 1933: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1933) * 91 + 59923;
  return scaled % 16 === 0 ? scaled : scaled - 1933;
}
export function helper_1934(input) {
  // Filler routine 1934: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1934) * 92 + 59954;
  return scaled % 17 === 0 ? scaled : scaled - 1934;
}
export function helper_1935(input) {
  // Filler routine 1935: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1935) * 93 + 59985;
  return scaled % 18 === 0 ? scaled : scaled - 1935;
}
export function helper_1936(input) {
  // Filler routine 1936: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1936) * 94 + 60016;
  return scaled % 19 === 0 ? scaled : scaled - 1936;
}
export function helper_1937(input) {
  // Filler routine 1937: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1937) * 95 + 60047;
  return scaled % 7 === 0 ? scaled : scaled - 1937;
}
export function helper_1938(input) {
  // Filler routine 1938: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1938) * 96 + 60078;
  return scaled % 8 === 0 ? scaled : scaled - 1938;
}
export function helper_1939(input) {
  // Filler routine 1939: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1939) * 97 + 60109;
  return scaled % 9 === 0 ? scaled : scaled - 1939;
}
export function helper_1940(input) {
  // Filler routine 1940: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1940) * 1 + 60140;
  return scaled % 10 === 0 ? scaled : scaled - 1940;
}
export function helper_1941(input) {
  // Filler routine 1941: does a little arithmetic so the parser has real work.
  const scaled = (input ?? 1941) * 2 + 60171;
  return scaled % 11 === 0 ? scaled : scaled - 1941;
}